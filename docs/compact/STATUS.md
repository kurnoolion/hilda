# Status

**Active phase**: development
**Last updated**: 2026-06-11
**Last drift-check**: 2026-06-10 — mode: design (focused on today's deltas) — 4 drifts resolved [DRIFT-1/2 option (a) impl notes appended; DRIFT-3 option (c) focus-aware refresh reframe + new SP UI strategy Flag; DRIFT-4 option (a) sharepoint_integration MODULE.md FR-87 invariant rewrite correcting A→B→C semantic + per-step path per FR-84/D-047/D-064], 0 skipped, 2 deferred surfaced (template_schema/enums.py implementation drift; Excel Choice-values manual verification). Prior 2026-06-05 requirements run: 5 drifts resolved.
**Last design review**: 2026-05-23 → 2026-05-26 — HILDA_Design.md + PROJECT.md + SYSTEM.md + structure-conventions.md + phases/*.md + DECISIONS.md + sharepoint/REQUIREMENTS.md (new) + STATUS.md user-led section-by-section review. structure-conventions.md updates: §intro Temporal → Celery + K8s → Docker Compose Ph-1/Ph-2 / MicroK8s Ph-3+; top-level layout expanded with full `customizations/` YAML sub-structure (`sharepoint_config/`, `template_schemas/`, `rules/`, `test_report_parsers/`, etc.), `config/global.json` added with env-var override pattern documented, full `deploy/` directory tree (compose, secrets, scripts, charts, grafana, prometheus, Dockerfile); new "SharePoint UI boundary" subsection documenting SP UI engineer vs HILDA team ownership split and the shared-contract flow; SharePointBackend bullet dropped stale "document library paths" + added SP-alert config; CredentialBackend bullet sops Ph-1/Ph-2 + Vault Ph-3+ (Sealed Secrets removed); sync-API wrapping section added two-stage IO paragraph for corp_messenger_gateway / corp_plm_gateway model; Temporal → Celery in UI-blocking section; IssueTracker/Messenger Protocol bullets updated to reflect gateway-PC routing; Config format section rewritten with global.json + env-var override pattern + runtime-data clarification. HILDA_Design.md: 32 action items applied (§1, §3.3, §3.4 incl. new §3.5, §4.1, §4.2, §5 Stage 4, §6.1, §6.2, §6.3, §7.1, §7.4, §10, §11, §12, §13). PROJECT.md: In-scope Ph-1 blob split into Ph-1/Ph-2/Ph-3+ (CI/CD pipeline, K8s, HashiCorp Vault all placed in Ph-3+); out-of-scope DEF list reconciled with `requirements.md ## Deferred`; per-PM credentials constraint phase-scoped; runtime LLM K8s reference corrected; success-criteria NFR-9 reference removed; Users line + Contributors table phase-scoped. SYSTEM.md: §1 three-pillar topology (SP entity rows + NSD documents + containerized services) with YAML config in boundary (Deliverables removed); §2 four-workload split with §2.1 module roster (now 20 modules — added `corp_messenger_gateway` and `corp_plm_gateway` as corp-side HILDA-team-owned modules); §3 fully rewritten with corrected network boundary model (corp/lab firewall: HILDA-PC outbound to corp allowed; corp inbound to HILDA-PC blocked unconditionally) and two corp-side intake PCs (reverse-proxy PC + PLM gateway PC); §3.1 replaced wrong on-prem-vs-outside-premises framing with corrected unified model — **SP → HILDA channel is SP alerts → email → HILDA mailbox via SP's built-in alert feature on the deliverable list; `sp_alert_parser` sub-module added to `email_service` (module 11); IMAP IDLE primary / short-interval polling fallback / FR-23 deadline-tiered as third-tier fallback only**; SP alert email format documented from sample (subject `Alert_<List>_<Suffix> - <ItemTitle>`, action verb in sub-header, key:value body, routing key `(ProjectID, MinorMilestone, ItemNumber)`); topology Mermaid diagram redrawn to show corp/lab zones + intake PCs; Conflicts table updated (C3/C5 broader deployment surface; new C6 for SP-alert channel); §5 deployment topology now shows 3 HILDA-owned hosts (HILDA PC + reverse-proxy PC + PLM gateway PC); §4 heading TBD cleaned; §5/§7/§8/§9 Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+ rename; hilda-llm-gateway role clarified (runtime inference + code-gen); hilda-api NSD download corrected (was "SP-mediated"); §8 phase-split CI-only (Ph-1/Ph-2) vs full CI/CD (Ph-3+); Open Questions updated: #10 resolved (SP-alert channel), new #11 (D-021/D-026 revision for 3-host surface), #12 (IMAP IDLE Exchange admin check), #13 (MilestoneGating schema gap), #14 (ItemNumber stability), #15 (SP alert "Anything changes" setting), #16 (D-024 reconciliation). 3 new ADRs (D-044 doc boundary; D-045 schema/content boundary; D-046 canonical schema source). FR-47 promoted resolution_path to formal enum. **Follow-up D-XXX needed** (architecture phase): (a) `[D-024]` CI/CD shape reconciliation pinning Helm to Ph-3+; (b) new ADR for SP-alert email channel + `sp_alert_parser` sub-module; (c) `[D-021]` / `[D-026]` revision documenting 3-host HILDA-owned deployment surface; (d) IMAP IDLE / EWS streaming confirmation from Exchange admin; (e) MilestoneGating + ItemNumber-stability schema decisions; (f) `corp_messenger_gateway` and `corp_plm_gateway` MODULE.md drafts. **2026-05-26 extension**: sharepoint/REQUIREMENTS.md created (commit `f8e9345`) and amended (commit `1c102a9` — added TGGroups SP list §2.8 closing TG-group fields propagation gap); `core/src/storage/MODULE.md` initial draft committed (`442400e`) with partial section-by-section review (Purpose narrowed to FR-31 authoritative; hilda-svc defined as AD service account; Ph-1/Ph-3+ → Ph-aware in Deferred); `phases/development.md` review completed; **DECISIONS.md bidirectional audit closed** — backward sweep (21 impl notes appended across 21 ADRs, commit `a6d66b1`) + forward sweep (4 new ADRs D-048/D-049/D-050/D-051 captured, commit `4ae4f71`); follow-ups (a) (b) (c) above all resolved via the 2026-05-24 impl notes; (d) (e) (f) remain pending. Plus propagation discipline added (STATUS.md Next + Flags) to prevent future derivative-doc gaps.

## Done

- 2026-04-30 Project initialized via `/project-init` (greenfield with design-input preflight; `HILDA_Design.md` imported into `docs/compact/design-inputs/`)
- 2026-04-30 D-001 captured (three-tier code organization: `core/` + `customizations/` + `config/`); `structure-conventions.md` updated to mirror `the reference implementation`'s layout (refs: D-001)
- 2026-04-30 D-002 captured (chat-mediated collaboration: stable error codes + compact RPT/MET/FIX/QC reports; no-proprietary-content hard invariant); `structure-conventions.md` Chat-mediated collaboration section added; PROJECT.md Constraints + all three phase prompts wired (refs: D-002)
- 2026-04-30 D-003 captured (adapter pattern for proprietary systems: intermediate primitives + on-prem code-generated adapters; dev LLM never reads proprietary specs); Ph-1 issue tracking = Jira via IssueTracker Protocol; structure-conventions.md Protocol boundaries refined; PROJECT.md three-tier LLM access model added; phases/architecture.md + development.md wired with hard invariants (refs: D-003)
- 2026-04-30 D-004 captured (SharePoint integration split: standard API mechanics in core, deployment-specific config in customizations); structure-conventions.md + phases updated (refs: D-004)
- 2026-04-30 D-005 captured (every module independently testable through an appropriate interface: CLI for functional, mock web harness for UI; --mock/--dry-run for side-effect modules; --diagnostic emits compact reports per D-002); structure-conventions.md Module definition + Module doc schema updated; PROJECT.md Constraints + phases/architecture.md + development.md + requirements.md TODO comment wired (refs: D-005)
- 2026-04-30 D-006 captured (SharePoint REST API + on-prem AD auth, NTLM/Kerberos; supersedes Microsoft Graph references in HILDA_Design.md); resolves PROJECT.md Open question #1 (refs: D-006)
- 2026-04-30 D-007 captured (all LLM hosting on-premises — both runtime LLM and API Spec Ingestor LLM); resolves PROJECT.md Open question #2 (refs: D-007)
- 2026-04-30 D-008 captured (IssueTracker intermediate-primitive Protocol: async-native over sync proprietary APIs; ITR- error code prefix; full method surface + Pydantic data classes); structure-conventions.md sync-API wrapping convention added; resolves PROJECT.md Intermediate-primitive Protocol design Open question (refs: D-008)
- 2026-04-30 D-009 captured (Messenger intermediate-primitive Protocol: async-native over sync proprietary APIs; MSG- error code prefix; full method surface + Pydantic data classes); shares AttachmentInput with IssueTracker (refs: D-009)
- 2026-04-30 D-010 captured (Excel/Template Schema Ingestor — proprietary customer-template schemas processed on-prem; parallel pattern to D-003 API Spec Ingestor; dev LLM never reads proprietary template schemas); PROJECT.md three-tier LLM access model + Constraints updated; partially resolves customer-template-authoring Open question (refs: D-010)
- 2026-04-30 5 unresolved Open questions backlogged from PROJECT.md to STATUS.md Flags (eval-data channel; customer template authoring path normalization; browser-automation versioning; API Spec Ingestor input format; Ph-1 messenger choice). New backlog entry added: Template Schema Ingestor input format (refs: D-010).
- 2026-05-01 `requirements.md` initial Ph-1 draft populated from `HILDA_Design.md` + decisions D-001..D-010: 45 FR, 15 NFR, 13 Deferred items grouped by subsystem.
- 2026-05-01 Requirements redlines applied per session feedback: FR-9 (BATCH-id email consolidation), FR-12 (three-path inbound routing — structured block / `mailto:` tap-links / PM triage; idempotency on `(BATCH-id, item-index, status)`), FR-13 (shared network drive `\\share\hilda\...` supersedes SharePoint Document Libraries), FR-16/17 (parser + classifier driven by D-011), FR-18 (submission package read from NW drive), NFR-8 (SP scope = Lists + classic web parts only), NFR-12/13 (extended to D-011); new FR-46 (final|interim classification rule), FR-47 (failed-without-waiver dashboard surface), FR-48 (auto-create Waiver DeliveryItem; TPM is not waiver-path authority), NFR-16 (NW drive read/write boundary — `hilda-svc` writes only, HILDA-mediated reads only).
- 2026-05-01 D-011 captured (Test Report Document Profiler — proprietary historical test reports across Excel/Word/PDF processed on-prem; parallel pattern to D-003 + D-010; canonical classification rule owned in `core/src/test_report/`); structure-conventions.md Ingestor section generalized to "Ingestor / Profiler pattern — three modules"; PROJECT.md three-tier LLM access model updated; PMs / TPMs (Technical Project Managers) terminology clarified in Contributors table (refs: D-011).
- 2026-05-01 Reclassified runtime vs dev/test invariants per session feedback: FR-43 → NFR-17 (compact-reports invariant); FR-44 → NFR-18 (error-codes invariant); FR-45 split into FR-49 (runtime `--diagnostic` for ops + RPT emission) and NFR-19 (dev/test `--mock` / `--dry-run` / mock web harness); section "Audit & chat-mediated collaboration" renamed "Audit & runtime diagnostics".
- 2026-05-01 D-012 captured (Multi-item email status updates — three-path design: structured reply block / per-item `mailto:` tap-links / PM manual triage; idempotent on `(BATCH-id, item-index, status)`; outbound multipart/alternative, ASCII-only structured block; authority for FR-9, FR-12).
- 2026-05-01 D-013 captured (Shared network drive — single `hilda-svc` AD service account writes, HILDA-mediated reads via `https://hilda.corp/dl/<token>`, no per-customer AD groups in Ph-1; path convention `\\share\hilda\<customer>\<device>\<milestone>\<deliverable>\<item>\` with `inbound/`/`outbound/`/`revisions/`; supersedes SharePoint Document Libraries; authority for FR-13, FR-17, FR-18, NFR-8, NFR-16).
- 2026-05-04 D-014 captured (customer template authoring: two separately maintained paths — SharePoint UI + Excel upload — TPM-selectable; no normalization in Ph-1; both produce identical internal data model representations); FR-39 updated to anchor `[D-014]`; resolves backlogged Flag "Customer template authoring path normalization" (refs: D-014).
- 2026-05-04 D-015 captured (API Spec Ingestor input format: OpenAPI 3.x canonical + on-prem LLM preprocessing pass converts other formats first; `spec_normalizer.py` sub-module within the Ingestor); resolves backlogged Flag "API Spec Ingestor input format" (refs: D-015).
- 2026-05-04 D-016 captured (Ph-1 messenger targets: Slack + proprietary internal messenger; both wired through `Messenger` Protocol `[D-009]`; Slack chosen over Teams for setup/unit-test simplicity; proprietary adapter exercises API Spec Ingestor end-to-end in Ph-1); FR-50 added (Messenger adapter Ph-1 targets); DEF-5 + DEF-6 revisit triggers updated; resolves backlogged Flag "Ph-1 messenger choice" (refs: D-016).
- 2026-05-04 Flags triage: browser-automation confirmed out of Ph-1 (already DEF-10, closed); eval-data flag refined — multiple eval/correction surfaces during dev time (test report review, tech report review, email parsing, etc.), details deferred to architecture.
- 2026-05-04 PROJECT.md TODOs filled from `HILDA_Design.md` §1 §2 §13 §15: one-line, Problem, Users, In scope Ph-1 (10 bullet items), Out of scope (9 items, all mapped to DEF-N), Success criteria (7 metrics). Requirements phase exit criteria met.
- 2026-05-04 Architecture phase started. Module list approved: 18 core modules across 6 dependency layers + 4 customization families. Sequenced by dependency order + TPM feedback priority.
- 2026-05-04 D-017 captured (central diagnostics module at `core/src/diagnostics/` — standalone leaf node, owns error_codes.py registry + compact report schemas + QC template base class; 18 module prefixes pre-registered). `core/src/diagnostics/MODULE.md` drafted (refs: D-017).
- 2026-05-04 D-018 captured (Template Schema Ingestor three-mode input format: schema-file YAML / row-offset + LLM column resolution / full LLM infer; recommended workflow: infer-once → commit schema.yaml → schema-file for production; resolves backlogged Flag "Template Schema Ingestor input format"). `core/src/template_schema/MODULE.md` drafted — Layer 0 complete (2/18) (refs: D-018).
- 2026-05-04 D-019 captured (credential_service Ph-1 = K8s Secrets / ops-provisioned; stable `get_credential` interface preserved for Ph-3+ Vault backend swap; FR-32–FR-38 deferred → DEF-14; FR-51 added for Ph-1; PROJECT.md In scope Ph-1 updated). (refs: D-019)
- 2026-05-04 D-020 captured (sharepoint_integration: SpClient / SharePointListProvider Protocol separation; FileBasedListProvider boilerplate in core/; list_crud.py sole compositor; the reference implementation 3-tier config for operational values; customizations/sharepoint_config/ for business config). `core/src/sharepoint_integration/MODULE.md` drafted — Layer 1: 1/3 (3/18 overall) (refs: D-020).
- 2026-05-05 Project plumbing landed: requirements.txt (pydantic, httpx, fastapi, uvicorn, pyyaml, pytest, pytest-asyncio), pyproject.toml with pytest config, .gitignore, package `__init__.py` skeleton for `core/`, `core/src/`, `core/tests/`, `customizations/`, `customizations/sharepoint_config/`, `config/sharepoint_integration.json` default.
- 2026-05-05 `diagnostics` module implemented + 27 unit tests passing: ErrorCode + 18-prefix PREFIX_REGISTRY + PipelineError (context as explicit dict to avoid kwarg collision), ReportRecord/ReportWriter for RPT/MET/FIX/QC compact reports with bool/int/float/str field support and round-trip parser, QCField + QCTemplate enforcing no-free-text invariant (str only via enum), CLI with `--diagnostic` / `--validate`. NFR-17, NFR-18, [D-002] enforced by construction.
- 2026-05-05 `template_schema` module implemented + 24 unit tests passing: enums (DeliveryState/ItemType/TrackingModality/CustomerDeliveryModality/MilestoneStatus/RuleScope/RuleActionType/TestReportItemStatus/TestReportClassification), extensibility registries (mutable sets seeded from enums for FR-7/NFR-14), slug helpers (validate_slug + make_slug per [D-013]), Pydantic entity bases (Device/Milestone/Deliverable/DeliveryItem/CustomerTemplate/AutomationRule), CustomerSchema with YAML round-trip per [D-018], CLI with `--diagnostic` / `--validate`. TSC-E001..TSC-W002 error codes registered.
- 2026-05-05 `sharepoint_integration` core implemented + 28 unit tests passing: GlobalSharePointConfig (3-tier: CLI > env > file; password redacted in repr/str), ListScope dataclass, _AuthHandler protocol with NoAuth/NTLM/Kerberos handlers (NoAuth active for Ph-1 dev; NTLM/Kerberos surface SHP-E004 with detail until corp AD lab is wired), SpClient (async SP REST with retry on 429/503, 401→SHP-E004, 4xx→SHP-E001, list-name single-quote escaping, OData $select/$filter/$top + pagination), SharePointListProvider Protocol + FileBasedListProvider (3-tier scope precedence: device override → customer config → SHP-E002), SpCrud compositor (canonical→SP column translation on write, SP→canonical on read, $filter translation). Tests use httpx.MockTransport.
- 2026-05-05 Mock SharePoint server + CLI implemented + 22 new tests passing (full suite 101): FastAPI app with two surfaces over shared InMemoryStore — SP 2017 REST endpoints (GET/POST/PATCH/DELETE; tiny OData eq evaluator) and HTML browser UI (/, /lists/&lt;name&gt;, /audit) — designed extensibly so dashboard mock can plug onto the same app object later (TPM use case). CLI: `--diagnostic` / `--mock` (in-process via httpx.ASGITransport) / `--dry-run --customer` / `--serve --port` (uvicorn for TPM browser). Sample example.yaml customer config seeded.
- 2026-05-06 System-architecture phase opened: drafted `docs/compact/SYSTEM.md` (peer of PROJECT.md / structure-conventions.md) covering process topology, inter-component comms, persistence, deployment, observability, secrets flow, CI/CD, networking. Logged 4 conflicts with HILDA_Design.md (Graph→REST `[D-006]`, Vault→K8s-Secrets `[D-019]`, microservices→TBD, Temporal→TBD); first two pre-resolved.
- 2026-05-06 D-021 captured (process granularity Ph-1 = modular monolith, one image, three K8s Deployments: `hilda-api` / `hilda-worker` / `hilda-llm-gateway`; all 18 modules in-process; per-customer adapter pods deferred to customer 2; HILDA_Design.md §11's 12-pod microservices inventory preserved as Ph-3++ target). Resolves SYSTEM.md §2 + conflict C3 (refs: D-021).
- 2026-05-06 D-022 captured (workflow engine Ph-1 = Celery + Redis broker + Postgres result backend; `hilda-beat` singleton Deployment loads schedule from SP `AutomationRules`; `core/src/workflow_engine/` owns Celery app + dispatcher, `core/src/rule_engine/` stays a pure evaluator; HILDA_Design.md's Temporal StatefulSet removed from Ph-1, deferred to Ph-3+ if multi-step durable orchestration emerges). Resolves SYSTEM.md §4 + conflict C4 (refs: D-022).
- 2026-05-06 D-023 captured (observability Ph-1 = light stack, zero HILDA-owned pods; structured JSON stdout logs + `/metrics` Prometheus endpoint per pod + compact reports per [D-002]; dashboards/alerts as code in `deploy/grafana/dashboards/` and `deploy/prometheus/alerts/`; no HILDA-owned Grafana/Loki/Tempo; distributed tracing deferred). Resolves SYSTEM.md §6 + Open Question #3 (refs: D-023).
- 2026-05-06 D-024 captured (CI/CD shape Ph-1: tool-agnostic pipeline (PR → tests + build + scan; merge → tag SHA + push + deploy test + smoke; manual prod promotion with semver re-tag); single umbrella Helm chart at `deploy/charts/hilda/` with per-env values files; mock SP runs as separate test-env Deployment; specific corp-tool selections — CI runner / image registry / GitOps / env topology — remain backlogged Flag). Resolves SYSTEM.md §8 shape + Open Question #5 (refs: D-024).

- 2026-05-08 Teacher/student LLM scaffold ported from `the reference implementation`: `.clinerules/` (4 files — project context, role, content-safety with 11 HILDA redaction categories, output discipline with 9 report types) + `cline-playbooks/` (8 playbooks: orient, mapping, sp-connect, ingest-template, profile-test-report, ingest-api-spec, debug-pipeline, share-back). Cline (on-prem student) runs HILDA CLIs against real SP and proprietary data; Teacher LLM (Claude) designs + codes; redacted compact reports bridge the air-gap.
- 2026-05-08 Ph-1 deployment platform pivot: bare-metal Linux PC + Docker Compose (not K8s). D-025 captured (customer YAML mount: Ph-1 bind-mount, Ph-3+ ConfigMap). D-026 captured (deployment platform Ph-1 = Docker Compose; same process boundaries, container image, task architecture; K8s is Ph-3++ target). SYSTEM.md fully updated (C5 added to conflicts table; §2, §5, §7, §8, §9, open questions all revised). PROJECT.md Infrastructure + Credential Service bullets updated.
- 2026-05-08 Cline git workflow clarified: one-way bridge — origin (github.com) is read-only from work PC; Cline pushes only to `company`; Teacher sees Cline's work via compact reports (hand-typed) only, never via git. `.clinerules/01-role.md` updated; `utils/git-sync/sync-work.sh` corrected (reverted erroneous push-to-origin step). D-027 captured.
- 2026-05-08 `issue_tracker` module designed + implemented end-to-end
- 2026-05-12 Data model review session (round 2) — `HILDA_Design.md` updated with: `tg_name` marked registry-controlled (`TGNameRegistry`); `plm_id` added to DeliveryItems (PLM system ID, permanent source of truth, dynamic); `actual_item_info` description updated to HILDA-mediated download URL per `[D-013]`; form factor flags added to CustomerTemplates (`handset`, `tablet`, `wearable`, `mr`, `hmr_smr`, Boolean). `template_schema/MODULE.md` updated (TGNameRegistry, DeliveryItemBase.plm_id, CustomerTemplateBase form factors). Pending code sync extended to include these fields.
- 2026-05-12 Data model review session (round 1) — `HILDA_Design.md` updated with: (a) D-028 captured: Deliverable level removed from hierarchy; DeliveryItems now parent directly to Milestone, grouped by `tg_name`; (b) `ItemType.Binary` → `Confirmation (Yes/No)` (owner-reply-closes, no artifact); (c) new DeliveryItem fields: `item_no`, `tg_name`, `owner_status_note`, `actual_completion_date`; (d) new Milestone field: `email_cc_list` per-milestone CC distribution list; (e) `Deliverables` SP List removed from §3.4. `requirements.md` updated (FR-2, FR-5, FR-6, FR-9, FR-13, FR-22, FR-40). `core/src/template_schema/MODULE.md` updated (Purpose, enums, MilestoneBase, DeliverableBase removed, DeliveryItemBase, EntitySchemaConfig). **Pending**: `core/src/template_schema/enums.py` and `models.py` code sync — required before next development trip.
- 2026-05-12 Requirements phase session — switched active phase to requirements. FR-4 deferred → DEF-15 (Excel import schema validation, phase TBD). Clarified terminology: "Stage" = workflow stages 1–6 (§5 HILDA_Design.md, already used in requirements.md section headers); "Phase" = implementation rollout phases 1–4 (§13 HILDA_Design.md). Further requirements review in progress.
- 2026-05-12 Requirements phase session (continued) — completed inline `[Ph-1]`/`[Ph-2]` tagging on all active FRs; FR-1 path (b) and FR-39 path (b) Excel flows deferred to Ph-2 (Template Schema Ingestor is Ph-2, consistent with DEF-15); FR-9/FR-12 per-item `mailto:` tap-links deferred to Ph-2 (D-012 implementation note updated); FR-52 added (Ph-1: runtime LLM attachment-to-item routing via first-page content inspection); FR-53 added (Ph-1: runtime LLM initial quality review for test reports, tech reports, and waivers); DEF-2 partially promoted to FR-53 (PM feedback workflow remains deferred); runtime LLM Ph-1 scope established (FR-52 + FR-53 functions only — DEF-1, DEF-3, DEF-4 remain deferred).
- 2026-05-13 Requirements phase session — DEF-1 (free-text message classification) promoted to Ph-1: FR-12 path (c) upgraded from direct-PM-triage to rule-based-first → runtime LLM fallback (Option B); FR-52 attachment routing upgraded from LLM-first to two-tier (fuzzy match → LLM fallback, scale context: ~100 reports per milestone); fusion design: when path (c) fires with attachments, single fused LLM call covers both message classification and attachment routing; DEF-1 struck through (promoted); D-029 implementation note added; D-033 captured (two-tier attachment routing); D-034 captured (fused LLM call for path (c) + attachments); sender email attribution added to FR-12 and FR-24.
- 2026-05-13 Requirements phase session (continued) — FR-13 clarifications: 4 inbound tracking modalities confirmed (Email: status+attachments; Corp Messenger: status only; Corp PLM: documents only; API: status/closure only); corp PLM = source of truth for documents; FR-2 updated (PLM issue created per owner×milestone at tracker creation); FR-5 updated (plm_id invariant); FR-10 updated (cross-channel messenger escalation when email goes unanswered); FR-13 major update (drop folder, polling, PLM auto-upload); FR-25 updated (IssueTracker dual role); FR-26 rewritten (PLM document-only, one issue per owner×milestone, InternalJira status path separated); FR-54 added (inbound messenger processing — Ph-1 PM triage / Ph-2 LLM classify); FR-55 added (direct drop-folder monitoring); D-035 captured (PLM source of truth).
- 2026-05-13 Requirements phase session (continued 3) — tracking_modality confirmed multi-value (5 values: Email, CorporateMessenger, CorporatePLM, NetworkSharedDrive, CustomerJIRA); FR-7 updated (all values + valid combinations enumerated); FR-9 updated (per-modality outreach behaviour); FR-13 updated (PLM pull vs push direction; carrier/device/milestone/item path confirmed; revisions/ handling explicit); FR-25/FR-26 corrected (internal Jira removed entirely; Jira = customer-facing CustomerJIRA closure polling only); FR-55 scoped to NetworkSharedDrive modality; D-035 implementation note updated.
- 2026-05-13 DEF-1 promoted to Ph-1: FR-12 path (c) rule-based + LLM fallback; fused with FR-52. D-033 (two-tier attachment routing), D-034 (fused LLM call for path c + attachments), D-035 (PLM source of truth), D-036 (sender attribution on inbound emails), D-037 (multi-value tracking_modality) captured.
- 2026-05-13 drift-check requirements (5 drifts resolved): FR-46 promoted Ph-2 → Ph-1 (classifier needed by FR-16 in Ph-1); FR-50 promoted Ph-2 → Ph-1; FR-54 demoted to Ph-2 entirely; NFR-15 rewritten (Temporal Workers removed; hilda-worker substituted; HA replicas scoped to Ph-3+); NFR-4 rewritten (sops/age AES-256-GCM at rest; mTLS deferred to Ph-3+); FR-51 updated (K8s Secrets → sops-encrypted .env). D-038 captured (sops/age Ph-1/Ph-2 secrets encryption at rest). D-033 implementation note: scope extended to PLM poll source.
- 2026-05-14 NSD path convention rewritten — `doc_type_slug/doc_id_slug/revN/` supersedes deliverable_slug + `revisions/` (D-013 implementation note added). PLM established as source of truth for documents. FR-13 rewritten end-to-end. drift-check requirements (3 resolved, 2 surfaced as Flags). DEF-6 promoted to Ph-1 (corp PLM IssueTracker adapter, generated by API Spec Ingestor alongside proprietary messenger adapter); D-003 impl note added. FR-2/FR-26 Ph-1 confirmed.
- 2026-05-14 SharePoint UI section added — FR-56 (milestone view + Start Collection action), FR-57 (HILDA document enumeration API), FR-58 (item_type discriminator), FR-59 (document rows grouped by doc_type), FR-60 (parser + LLM review results per revision), FR-61 (HILDA-mediated download endpoint), FR-62 (Ph-2 PM/TPM upload surface). Rule engine taxonomy expanded — FR-28 full trigger list (11 triggers); FR-29 actions including TriggerParser, NotifyPM, StartItemCollection.
- 2026-05-15 Major FR overhaul + D-039 captured (4-step classification pipeline: hash → slug → Tier-2 LLM → staged/ holding). delivery_state 10-state enum formalized in FR-7; doc_type enum (test_report / tech_report / waiver); document index 13-field schema. FR-63 Submit to Carrier, FR-64 Close All Items, FR-65 Send Reminder SP UI actions added. requirements.md Appendices A (rule engine triggers + state machine), B (FRs requiring LLM calls), C (phase scope index) added. Cleanup: ~/work/nora → "the reference implementation" across PROJECT.md, phases/, structure-conventions.md.
- 2026-05-17 NSD/PLM source-of-truth split formalized: D-040 (NSD classified path = in-progress source of truth; PLM = submitted-only); D-041 (submission assembly source = NSD classified path in both phases). FR-66 (corp messenger version-selection workflow), FR-67 (PLM stale attachment deletion), FR-68 (PLM-NSD sync verification post-dispatch), FR-69 (carrier `portal_structure.yaml`), FR-70 (item_completion_pct computed field) added. FR-7 OwnerClosed reduced from 3 to 2 guard conditions (doc_count + all reviews; owner-done is trigger, not third condition). PendingPMReview → UnderPMReview rename across all FRs. Multiple FR-7/13/55 corrections for clarity (Confirmation item shortcut, ingest channel funneling, PLM no-re-upload, original_filename vs doc_id_slug distinction, download_url field, ACL deferral to DEF-16).
- 2026-05-18 FR-48 waiver auto-create removed (waivers are owner-created; HILDA only tracks waiver_ref). FR-47 [Ph-2] resolution-path = PM annotation only; DEF-17 (per-path automation deferred to Ph-3+). D-039 Step 2 short-circuit added for NEW_DOCUMENT case (no LLM call when no prior doc_id_slug exists for the item × doc_type pair). FR-53 deferral clause [Ph-2] tag added.
- 2026-05-19 DEF-18 (per-DeliveryItem ACL on download endpoint, deferred to Ph-3+). FR-61 clarified as direct browser → NSD stream (distinct from FR-18 batch assembly download to HILDA PC). FR-13 ingest_source enum formalized (Email / CorporatePLM / NetworkSharedDrive / SharePointUI). FR-18/19/20/22/63/64 moved to Ph-1. State machine redesign: UnderPMReview placed AFTER OwnerClosed (transient fork). FR-21 / FR-22 / FR-64 deferred via DEF-19 (carrier feedback automation) + DEF-20 (automated Closed transition). FR-62/63 rewritten for delta re-submission. FR-69 schema adds file_overwrite_policy + revision_file_suffix_pattern. FR-71 [Ph-2] Owner Discovery Function added; tg_owner + email_cc_list (per-milestone + per-item override) fields added. Dynamic deadline-tiered `polling_schedule` AutomationRule introduced for FR-26 (PLM) and FR-55 (NSD).
- 2026-05-20 FR-72 [Ph-2] ZIP ingestion added — five-area NSD structure (`zip-store/` per item + TG-scoped `un-resolved-zip/` at milestone level). FR-13 five-area layout updated. FR-25 CustomerJIRA rewritten — per-device-model polling (milestone-agnostic), live open-ticket dashboard, owner email = close intent (advisory JIRA), TPM (= PM in this item type) self-approves. FR-26 corrected (PLM issue keyed on owner × milestone, not per-item; late-join ItemStartCollection logic).
- 2026-05-21 FR-13 rewritten as **two-tree NSD architecture**: Owner inbound tree (`\\share\hilda\inbound\<carrier>\<device>\<milestone>\<item>\`) + HILDA internal tree (`\\share\hilda\internal\<carrier>\<device>\<milestone>\<tg_name>\`). HILDA resolves internal path by looking up DeliveryItem record from `<item_slug>` to retrieve `tg_name`. Five HILDA-internal zones formalized. FR-72 rewritten with TG-scoped ZIP routing. FR-73 [Ph-1] async carrier-package assembly added. FR-74 [Ph-1] collection-phase closure added (threshold-based polling halt across PLM / NSD / JIRA). FR-75 promoted from DEF-21 (general fault routing — all faults to HildaOpsAlert in Ph-1/Ph-2). FR-64 Close All Items promoted to Ph-1. FR-21 / DEF-19 deferral aligned to Ph-3+. **D-042 captured (Open design question — customer template storage location and TPM authoring path)**.
- 2026-05-22 email_group_alias + corp_id_list TG-level fields added [Ph-1] (per-TG email distribution alias for outreach; per-TG corp-ID list for messenger escalation). FR-70 item_completion_pct computation formula rewritten. drift-check requirements mode (3 drifts resolved: D-022 beat loader; Appendix B TSI path; FR-28 MilestoneAllClosed inline trigger). **switch-phase: requirements → architecture**.
- 2026-05-23 FR-30 rule storage layout under `customizations/rules/` (Global / Customer / Device 3-tier YAML resolution); FR-31 expanded with SP web part UI surface and three sub-capabilities (pause/resume, customize parameters, manual trigger); FR-39/40/41 ops-owned YAML template authoring. **D-043 captured (Ph-3+ MicroK8s plan: RabbitMQ Quorum Queues, Rook/Ceph PVCs, MetalLB VIP, ConfigMap volumes for customizations).**
- 2026-05-23 → 2026-05-24 **Full architecture-review pass** on HILDA_Design.md (32 edits across §1, §3.3, §3.4, §3.5 new, §4.1, §4.2, §5 Stage 4, §6.1, §6.2, §6.3, §7.1, §7.4, §10, §11, §12, §13), PROJECT.md (Ph-1 → Ph-1/Ph-2/Ph-3+ split; DEF list reconciled; per-PM credentials phase-scoped), SYSTEM.md (three-pillar topology; 20-module roster including new `corp_messenger_gateway` + `corp_plm_gateway`; corrected corp/lab firewall model with two intake PCs; SP-alert email channel formalized with `sp_alert_parser` sub-module; topology Mermaid redrawn; Conflicts C3/C5/C6 updated; Open Questions #10 resolved, #11–#16 added), structure-conventions.md (Temporal → Celery; K8s → Docker Compose Ph-1/Ph-2 → MicroK8s Ph-3+; `customizations/` YAML sub-structure expanded; `config/global.json` added; `deploy/` tree added; new "SharePoint UI boundary" subsection; CredentialBackend sops + Vault; sync-API wrapping two-stage IO addition). **D-044** (documentation boundary policy), **D-045** (schema/content boundary invariant), **D-046** (canonical schema source — Pydantic models generating SP provisioning + Postgres DDL + YAML schema) captured. FR-47 `resolution_path` promoted to formal enum. 6 architecture-phase follow-up ADRs flagged in Open Questions.
- 2026-05-24 STATUS.md back-filled: 12 Done entries appended covering the 2026-05-13 → 2026-05-24 gap; Next section rewritten (16 modules remaining + 2 new gateway modules; 6 follow-up ADRs surfaced; SP-engineer + Exchange-admin confirmation items added; stale items struck); Flags section refreshed (broadened MAP.md staleness; new D-042 flag; 2026-05-24 follow-up ADRs block; SP "Anything changes" config note; STATUS.md upkeep policy — close-session daily going forward).
- 2026-05-24 phases/requirements.md review: PROJECT.md field list updated (added Constraints, "In scope Ph-1" → "In scope by phase Ph-1/Ph-2/Ph-3+"); requirements-shaped HILDA_Design.md sections list expanded (§3 data model, §4 templates, §7 adapters, §8 orchestration, §10.1–§10.6 credentials, §12 configurability, §14 risks); "no access" constraint list expanded (proprietary specs `[D-003]`/`[D-010]`/`[D-011]`, corp messenger / PLM / JIRA / NSD content, R&D reply prose, lab-network runtime env); remote-collaboration paragraph expanded to cover Teacher/Student LLM scaffold per `[D-027]` (`.clinerules/` + `cline-playbooks/`); SYSTEM.md and structure-conventions.md added to "Load when entering" as conditional background context with `[D-NNN]`-anchor guardrail; "Ph-1" verbiage removed throughout; new Don't rule added (don't redesign requirements around SYSTEM.md's platform shape — surface conflicts as follow-up ADRs).
- 2026-05-24 DECISIONS.md back-fill review: 5 existing ADRs received 2026-05-24 implementation notes — **D-019** (K8s Secrets superseded by D-038 sops; Ph-1/Ph-2 = shared ops-team creds not per-PM; Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+), **D-021** (4-workload split not 3; 20 modules not 18; 3-host deployment surface not 1 PC; Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+), **D-022** (Ph-3+ broker migration to RabbitMQ Quorum Queues per D-043; Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+), **D-024** (Helm pipeline pinned to Ph-3+; Ph-1/Ph-2 = basic CI + manual deploy via sops + deploy.sh), **D-026** (3-host deployment surface; K8s → MicroK8s per D-043; Ph-1/Ph-2 credentials = sops not plaintext .env per D-038). **New ADR captured: D-047** (SP → HILDA notification channel via SP alert email + `sp_alert_parser` sub-module + IMAP IDLE primary / short-poll fallback / FR-23 third-tier; locks down the unconditional corp/lab firewall block; routing key `(ProjectID, MinorMilestone, ItemNumber)`; SP "Anything changes" config requirement; resolves SYSTEM.md Conflicts C6).
- 2026-05-24 **D-027 clarified, NOT superseded**: per user-provided git-sync README + clarification (Cline runs only on Work PC; corp git repo not accessible from Personal PC). Apparent contradiction in git history (kurnoolion-authored commits in origin/main) resolved — those commits were authored by **Claude on Personal PC** using the kurnoolion git identity in use at the time, not by Cline on Work PC. Identity transitioned to ai-math-01 from 2026-05-22 onward. Implementation note appended to D-027 documenting: (a) confirmed topology Personal PC → github.com (intermediary) → Work PC → company git; (b) git identity history kurnoolion → ai-math-01; (c) work split — Claude writes core/src/ + all docs + all MODULE.md + scaffolds for customizations/; Cline completes TODO scaffolds against proprietary inputs and pushes to company git only; (d) script tooling — setup-personal.sh / setup-work.sh / sync-personal.sh / sync-work.sh / git-debug.sh per README; (e) air-gap invariant unchanged. STATUS.md / Flags update: D-027 "needs user verification" flag now resolved. Personal PC normally **does not pull** from public github (Personal PC is the sole writer); pull is only needed for edge cases like multiple clones / worktrees.
- 2026-05-24 **Placeholder convention captured** for proprietary identifiers in `customizations/` scaffolds: new doc `cline-playbooks/placeholder-convention.md` defining `<SYS_N>` / `<CUST_N>` / `<DEV_N>` / `<TG_N>` / `<PERSON_N>` / `<URL_N>` placeholder categories, registry-comment header pattern at the top of each scaffold, slug-based env-var mapping, hand-off contract between Claude (writes placeholders + slugs) and Cline (fills TODOs with interface-shape facts; never substitutes placeholders into committed files). Referenced from `structure-conventions.md` Ingestor section. Anchored by `[D-002]` + `[D-027]`. Existing `<SYS0>` usage in `customizations/issue_tracker/defecttrack_adapter.py` retroactively validated as compliant.
- 2026-05-24 phases/architecture.md review: SYSTEM.md + DECISIONS.md added to Load list; module-list bullet refreshed with current architecture (Workflow Engine = Celery+Redis Ph-1/Ph-2 / RabbitMQ Ph-3+ per `[D-022]`/`[D-043]`; Credential Health Monitor marked Ph-3+ per DEF-14; `corp_messenger_gateway` + `corp_plm_gateway` added; `sp_alert_parser` sub-module per `[D-047]` referenced); tech-stack/stakeholder-answers source pinned to PROJECT.md Constraints + DECISIONS.md; stale Graph-API example replaced with current `[D-047]` SP-alert channel example; contribution surfaces list expanded from 4 to 10 items (SP web parts, SP alert email channel per `[D-047]`, Excel template upload, YAML rule/template ingestion, NSD owner inbound, owner email replies per FR-12, owner messenger replies per FR-54, owner CorporatePLM uploads, corp_messenger_gateway intake, corp_plm_gateway, eval-data correction); Observability bullet updated to anchor `[D-023]` light stack with zero HILDA-owned o11y services; Limited-access design patterns expanded with `[D-027]` Teacher/Student scaffold + placeholder-convention.md reference + two-host corp gateway architecture; Remote-collaboration contracts updated to reference `[D-017]` (central error_codes.py decided at `core/src/diagnostics/`) and placeholder convention; **Two Ingestor modules → Three Ingestor/Profiler modules** (added Test Report Document Profiler per `[D-011]`); vendor examples corrected (customer JIRA + corp PLM via Ingestor; Slack + proprietary messenger via Ingestor); meta-schema hierarchy updated to drop Deliverable per `[D-028]`; input-format TBDs resolved (`[D-018]` TSI three modes; `[D-015]` API Spec OpenAPI 3.x); SharePoint integration split clarified — document library uploads removed per `[D-013]`/`[D-041]`; mock-harness reference updated to mock-sharepoint Docker Compose service; Frozen SP 2017 section anchored to `[D-006]` + `[D-047]` with explicit corp/lab firewall asymmetry note; DECISIONS.md Artifacts entry refreshed with resolved-by anchors for all listed decision categories.
- 2026-05-24 phases/development.md review: Three-tier-layout persona line rewritten with `[D-027]` Claude/Cline authorship split (Claude = core/src + scaffolds with placeholders + docs; Cline = TODO completion against proprietary inputs on Work PC) + corp-side gateway PCs (corp_messenger_gateway + corp_plm_gateway as off-Docker-Compose modules); explicit "Do NOT load" sub-list added to Load list (HILDA_Design.md, SYSTEM.md, structure-conventions.md, phases/architecture.md, PROJECT.md — all architecture-phase scope by development time); DECISIONS.md added as Tier-2 on-demand. Six section subtexts updated: (a) Limited-access workflow — full proprietary-content list (specs, schemas, reports, corp messenger/PLM/JIRA, NSD, R&D prose, lab-network runtime) + `[D-027]` Teacher/Student + placeholder convention reference; (b) Compact reports are contracts — central error_codes.py anchored to `[D-017]` at `core/src/diagnostics/`; placeholder convention + 11 redaction classes referenced; (c) Honor human-in-the-loop — FR-63 + FR-28 PMApproval trigger + NFR-5 cited; delta-aware re-submission noted; attribution model phase-scoped (ops-team Ph-1/Ph-2 per `[D-019]` Ph-1 / `[D-038]`; per-PM Ph-3+ per DEF-14 / `[D-019]` Ph-3+); (d) Credential discipline — storage backends specified (sops Ph-1/Ph-2; Vault Ph-3+); never-cache rule anchored; CommunicationLog logging per NFR-6; (e) Don't engage with proprietary inputs — three Ingestor/Profiler modules (not two); list expanded to cover corp messenger/PLM/JIRA + NSD + R&D prose + customer feedback; placeholder convention as redirection mechanism; (f) Don't hard-code SP values — document library paths removed per `[D-013]`/`[D-041]`; SP-alert subscriber address + change-trigger settings per `[D-047]` added to deployment-config list. Observability bullet updated to `[D-023]` light stack with zero HILDA-owned o11y services; credential-expiry instrumentation marked Ph-3+ only. Test interface per `[D-005]` — mock-sharepoint Docker Compose service reference made concrete.
- 2026-05-26 `sharepoint/REQUIREMENTS.md` **created as new hand-off doc** for the SP UI engineer (commit `f8e9345`) — self-contained 12-section spec for the SharePoint 2017 web parts side of HILDA, strips HILDA-Python-side complexity (hilda-api / hilda-worker / Celery / Postgres / NSD internals / sops / MicroK8s). Contents: (1) Big picture + SP-2017-frozen stack constraint; (2) 6 SP Lists structure with full column definitions (Customers, Devices, Milestones, DeliveryItems, Users, PMCredentials, CommunicationLog) — CustomerTemplates and AutomationRules explicitly noted as NOT in SharePoint (YAML files per FR-30 / FR-39/40/41); (3) Views (Milestone primary, Device Tracker, PM Dashboard); (4) 8 buttons / actions with specs (Start Collection, Approve, Submit to Carrier delta-confirm, Close All Items, Refresh, Send Reminder, Upload Document Ph-2, Close Item Ph-2) — every button writes a SP field, triggers SP alert email per `[D-047]`; (5) Document section (HILDA enumeration API at `https://hilda.corp/docs/<delivery_item_id>`; per-revision rendering; HILDA-mediated download); (6) PM Review section for UnderPMReview items; (7) SP Alert configuration (critical: "Anything changes" trigger on all lists); (8) Live updates (SP REST polling 5-10s, NOT HILDA); (9) Permissions; (10) Phase scoping (Ph-1 build first / Ph-2 / Ph-3+); (11) Out of scope (Document Libraries, workflows, Power Apps, SPFx); (12) Contract + 6 open items for HILDA-team confirmation. Written as a black-box-friendly spec for the SP UI engineer.
- 2026-05-26 `sharepoint/REQUIREMENTS.md` **amended (commit `1c102a9`)** to add **TGGroups SP list (§2.8)** — the missing TG-group fields (`tg_owner_name`, `tg_owner_email`, `email_group_alias`, `corp_id_list`, `default_cc_list`) per FR-2 + FR-71 that were absent from the initial commit (`f8e9345`). Normalized as a separate list per `[D-045]` schema/content boundary — one row per `(milestone_id, tg_name)`; DeliveryItems lookup on `tg_name`. Cross-references updated in §3.1 (Milestone View — TG header rendering above each tg_name grouping), §7 (SP Alert subscription — now 8 lists including TGGroups), §10 Phase scoping (Phase 1 builds TGGroups list + auto-population; Phase 2 adds TPM inline edit per FR-71 ODF), §11/§12 Contract + Open Items (new open item #7 — TGGroups auto-population mechanism, JSON shapes, unique constraint enforcement to confirm with HILDA team). **Root cause: STATUS.md / DECISIONS.md sweep gap** flagged earlier — TG-group fields added 2026-05-19 (FR-71) + 2026-05-22 (email_group_alias + corp_id_list) propagated to HILDA_Design.md §3.4 + SYSTEM.md during 2026-05-24 architecture review but did NOT propagate to derivative docs. This confirms the need for the deferred STATUS.md + DECISIONS.md sweep we noted.
- 2026-05-26 `core/src/storage/MODULE.md` initial draft committed (`442400e`) — first Layer 1 MODULE.md per the per-layer design strategy. Partial section-by-section user review (Purpose narrowed to FR-31 authoritative + Contributes-to others; hilda-svc defined as AD service account not container; Ph-1/Ph-3+ → Ph-aware in Deferred). Remaining sections to be revisited during implementation per the fix-on-test-failure approach.
- 2026-05-26 **DECISIONS.md comprehensive cross-doc sweep (backward drift; commit `a6d66b1`)** — pattern-scanned all 47 ADRs (D-001 through D-047) against current state of requirements.md / HILDA_Design.md / SYSTEM.md / PROJECT.md / structure-conventions.md / phases/*.md. Categorized: 🔴 substantive staleness (14 ADRs), 🟡 terminology cleanup (7 ADRs), ✅ already current (18), 🚫 OPEN (D-042), 🔒 already covered by 2026-05-24 batch (D-019/D-021/D-022/D-024/D-026/D-027). **Net 21 new implementation notes appended (2026-05-26 dated)** per the immutability convention (append-only; never edit Decision bodies). The 21: D-002 (registry path + redaction categories + Teacher/Student boundary), D-003 (terminology + gateway routing), D-004 (document libraries removed; Open Q#1 resolved), D-005 (mock-sharepoint Docker service concrete), D-006 (credential service path resolved; SP→HILDA firewall asymmetry), D-007 (K8s → Docker Compose; Temporal → Celery), D-008 (Ph-1 + Celery + registry path + corp_plm_gateway routing + delete_attachment), D-009 (D-016 messenger choice resolved + corp_messenger_gateway routing + FR-54 Ph-2), D-010 (Deliverable removed + Excel Ph-2 + modes resolved + TG-group fields), D-012 (DEF-1 promoted + Redis idempotency cache concrete), D-013 (D-040/D-041 cross-ref + hilda-svc clarification), D-014 (Excel Ph-2 + D-042 open + TG-group fields), D-016 (corp_messenger_gateway routing + DEF-6 promotion context), D-017 (20 modules; +CMG- and CPG- prefixes needed), D-023 (Docker Compose terminology; ServiceMonitor → direct scrape; credential-expiry Ph-3+ only), D-025 (scope expanded to all customizations/ sub-directories), D-028 (TG-group fields + milestone email_cc_list superseded by per-item), D-030 (superseded by D-033 two-tier + path convention update), D-035 (D-040 supersession explicit + delete_attachment + Ph-2 deferred upload + gateway routing), D-037 (FR-21 → DEF-19 + FR-25 rewritten + FR-54 Ph-2 + tracking_modality storage resolved), D-038 (Ph terminology + mTLS Ph-3+). DECISIONS.md is now aligned with current state of all other docs through 2026-05-26.
- 2026-05-26 **4 new ADRs captured (D-048 through D-051; commit `4ae4f71`)** — forward-drift scan (architectural choices made elsewhere lacking `[D-XXX]` anchors). **D-048**: FR-66 multi-revision version-selection workflow — owner-mediated via corp messenger; anchors the transient `OwnerClosed` fork; alternatives (auto-pick latest / TPM picks / auto + revert) rejected with rationale. **D-049**: FR-71 Owner Discovery Function (ODF) + per-TG-group field model — TG-group metadata is TG-group-level not per-DeliveryItem; ODF is per-TG corp messenger outreach at tracker creation; anchors the TG-group field model affecting FR-2/FR-9/FR-10/FR-14. **D-050**: FR-72 ZIP ingestion + 5-area NSD structure — per-item area for NSD-sourced ZIPs; TG-scoped area for Email/PLM ZIPs; classified area for extracted files; staged area for ambiguous; outbound area for HILDA-generated; ZIP-as-temporary-container model. **D-051**: TGGroups SP list normalized as separate list (Option A) — one row per `(milestone_id, tg_name)`; rejected Option B (denormalize onto every DeliveryItem) for 20× duplication and Option C (YAML-only) for breaking FR-71. Two medium-priority candidates (D-052 async carrier-package assembly + D-053 SharePoint UI boundary) deferred to natural landing during respective MODULE.md drafts. **Plus propagation discipline added to STATUS.md** Next (FR-change checklist) + Flags (derivative-doc drift risk; mitigation includes drift-check expansion to derivative docs as Ph-1 architecture-phase task).
- 2026-05-26 **drift-check design mode** — 11 drifts resolved across 4 MODULE.md files; 15 deferred (unbuild-modules expected gaps tracked in Next per-layer plan). **template_schema/MODULE.md**: Purpose extended to cite FR-66/FR-70/FR-71 + D-028/D-046/D-049/D-051; DeliveryState enum 4→10 values per FR-7; TrackingModality 3→5 values per `[D-037]` (InternalIssueTracker removed); IngestSource + DocType enums added per FR-13/`[D-039]`; RuleActionType expanded to 13 values per FR-28/29 (added TriggerVersionSelection per `[D-048]`, TriggerODF per `[D-049]`, etc.); DeliveryItemBase gained `doc_count`, `review_required`, `review_status`, `item_completion_pct`, `email_cc_list`, `milestone_gating` per FR-2/FR-7/FR-53/FR-70; tracking_modality on DeliveryItemBase changed to multi-value `list[str]` per `[D-037]`; new `TGGroupBase` Pydantic model added per `[D-049]` + `[D-051]` (one row per `(milestone_id, tg_name)`). **issue_tracker/MODULE.md**: Purpose rewritten for two-adapter scope (corp PLM via `corp_plm_gateway` per `[D-021]` impl note + customer JIRA per FR-25 rewrite); InternalIssueTracker reference removed per `[D-037]`; FR-42 removed (reclassified); NSD/PLM source-of-truth split per `[D-035]`/`[D-040]`/`[D-041]` documented; Ph-1 immediate vs Ph-2 deferred PLM upload noted; corp_plm_gateway inbound routing noted; `delete_attachment` method added to Protocol per `[D-040]`/FR-67; Ph-3+ → Ph-3+ terminology fix. **sharepoint_integration/MODULE.md**: minimal addition noting 8 SP lists in scope per `sharepoint/REQUIREMENTS.md §2` (Customers, Devices, Milestones, DeliveryItems, Users, PMCredentials, CommunicationLog, TGGroups per `[D-051]`); list-agnostic architecture confirmed. **customizations/issue_tracker/MODULE.md**: Purpose updated with corp_plm_gateway routing + Ph-1 terminology; new paragraph documenting placeholder convention per `[D-002]`/`[D-027]`/`cline-playbooks/placeholder-convention.md` with `<SYS_N>`/`<URL_N>`/`<CUST_N>` placeholders + registry-comment header pattern. Drift-check marker updated.
- 2026-05-28 **`core/src/email_service/MODULE.md` section-by-section review closed** (commit `714e65b`): Purpose + Workload assignment + Inbound reception (3-tier with IDLE field-reality caveat) + Source-of-truth + Sub-modules layout + Public surface (`protocol.py` + 5 active inbound `.py` files for test scenario + Ph-1 production surface) + Configuration + Invariants (incl. new "no standalone Postgres table for inbound email bodies") + Error codes + Key choices + Non-goals + Depends on + Depended on by + Deferred + Test interface — all reviewed. **5 review-round refinements applied**: poll_interval_s=60 + IDLE field-reality caveat; marked-read mechanism clarified (\\Seen + MOVE to INBOX/processed); test-scenario subset rewritten (FR-16 unavailable / FR-53 config-gated / D-039 Step 2+ skipped / FR-13 PLM upload config-gated); `classification_source` split into `doc_type_source` + `item_routing_source`; new no-Postgres-bodies Invariant. **Staged-document fallback ladder** queued as requirements-phase Flag per user item-6 (folder-only routing → owner outreach → TPM escalation; expected: FR-52 amendment + new ADR + `attachment_router.py` surface).
- 2026-05-28 **`core/src/email_service/MODULE.md` drafted** (Layer 2 entry point; anchors `[D-016]`, `[D-034]`, `[D-047]`; serves FR-9, FR-10, FR-12, FR-23, FR-24, FR-52, FR-54, NFR-1, NFR-2). Sub-module layout: inbound/ (receiver + classifier + subject_parser + body_parser_structured + body_parser_freetext + attachment_router); sp_alert_parser/; outbound/ (sender + composers + templates). **Test-scenario subset (per 2026-05-28 user use case — 80+ test reports, doc_type classification + attachment routing + structured response, no LLM review, no revisions)**: inbound/receiver.py (one-shot mode), classifier, subject_parser, body_parser_structured, attachment_router (with `plm_upload_enabled=false` + `review_required_enabled=false` config gates). Skipped for test: body_parser_freetext (FR-12 path c fused LLM), sp_alert_parser/, outbound/. `--once` CLI mode added as the test-scenario entry point. Initial commit pending.
- 2026-06-05 **Requirements.md user-led review pass — extended/continued** — second-pass + audit work after the 267d41b close-session:
  - **FR-19 selenium amendment** + `GoogleDriveBaseAdapter` base-class in `core/` (mirrors IssueTracker/Messenger pattern); `D-054` impl note 2026-06-05 documenting Google Drive REST API unavailability + browser-automation pivot. New STATUS.md flag for customer adapter browser-automation architecture-phase items (selector versioning, Chromium ops dep, PM session mgmt, capability flags).
  - **v1/v2 terminology cleanup**: 17 substitutions in requirements.md; bulk replace_all in STATUS.md (47 substitutions); global terminology mapping note added at top of DECISIONS.md (preserves [D-002] ADR-body immutability).
  - **FR-73 Download Package two-click async flow** made explicit: 7 sub-steps (i-vii); per-click regeneration; zip deleted on TPM download (FR-76 backstop).
  - **FR-28 + FR-29 audit-driven amendments**: 6 new triggers (incl. restored MilestoneAllClosed Ph-1 cleanup-only) + 8 new actions; user-driven simplification dropped `TemplateModified` in favor of `ItemModified.TagsModified` propagation. FR-29 reformatted to bulleted Ph-1/Ph-2 subsections.
  - **FR-77–FR-84 audit + 6 amendments**: default work-item excluded from FR-74 threshold + reaches Closed via TPM-Mark-Closed (FR-78); FR-82 restructured (YAML for new trackers + SP-UI propagation for active trackers); FR-77 EML-W007 for missing target_folder mapping; FR-79 PLM fan-out across distinct (owner × milestone) issues (per-association plm_attachment_id); FR-78 default_work_item template structure + fallback defaults; FR-84 sentinel routing key for milestone-scoped actions.
- 2026-06-05 **Requirements.md user-led review pass closed** — covers FR-2, FR-7, FR-12 (status applies wording reviewed, kept as-is), FR-13, FR-14, FR-17, FR-18, FR-19, FR-23, FR-26, FR-52, FR-55, FR-56, FR-57, FR-61, FR-62, FR-68, FR-72, FR-73, FR-76, FR-79. Major changes: (1) **Amendment-pattern abandoned — converted to in-place updates**: 11 FR amendments from 2026-05-28 batch + 3 clarification amendments from 2026-06-05 folded inline; both `### 2026-05-28 amendments` and `### 2026-06-05 clarification amendments` subsections removed; FR bodies now reflect current spec without "see amendment below" indirection. (2) **Drift-check requirements mode run** (5 drifts resolved: Appendix B FR-52 row + CLASSIFY_DOC row; Appendix C entries for FR-77–83; FR-73 + FR-76 amendments folded inline). (3) **PLM upload timing corrected** (FR-13/FR-17/FR-52/FR-55/FR-62): silent override of `[D-040]` Ph-2 deferral in 2026-05-28 batch restored to phase-conditional (Ph-1 immediate; Ph-2 deferred until OwnerClosed + TPM approval; only is_final revision uploaded). (4) **Multi-item document association** clarified across FR-57 (primary anchor in natural key) + FR-79 (primary in both DocumentIndexRow and DocumentItemAssociation M:M table). (5) **New FR-84** — SP→HILDA email notification channel per `[D-047]` (firewall-blocked SP→HILDA HTTP; sp_alert_parser sub-module; three-tier inbound IMAP IDLE → short-poll → deadline-tiered). (6) **FR-14 + FR-23 amendments** referencing FR-84 channel. (7) **FR-61 download disposition** — content-type-conditional (PDFs inline; Office/binaries attachment). **DECISIONS.md audit**: all 54 ADRs reviewed against requirements.md (24 well-captured; 24 architectural-only correctly covered elsewhere; 1 open D-042; 2 impl notes added — D-029 confirms three-function framing under 2026-05-28 evolution; D-030 supersession chain extended through FR-52 amendment). No new ADRs; no MODULE.md changes; no code changes.
- 2026-05-28 **Layer 1 complete — `storage/MODULE.md` section-by-section review closed**: Purpose + Pydantic row models (`DocumentIndexRow`, `CommunicationLogRow`, `BatchIdempotencyKey`) + document index API (`add_document_index_row`, `get_document_index_row`, `find_doc_id_slugs_for_item`, `list_revisions`, `update_review_findings`, `set_is_final`) + communication log API (`log_communication`, `query_communications`) + rule overrides API (`get_active_overrides`, `set_override`, `clear_override`) + Redis client section + NSD client section + Invariants + Key choices + Non-goals + Dependencies + Deferred — all reviewed. **`llm/MODULE.md` and `credential_service/MODULE.md` section reviews also closed 2026-05-28.** Layer 1 now 100% done (7 modules fully reviewed: `diagnostics`, `template_schema`, `sharepoint_integration`, `issue_tracker`, `storage`, `credential_service`, `llm`).
- 2026-05-28 **Layer 1 MODULE.md drafts + D-052 + LLM TaskKind refinement** — `core/src/credential_service/MODULE.md` drafted (Layer 1; anchors `[D-019]`, `[D-038]`; commit `63b394e`). `core/src/llm/MODULE.md` drafted (Layer 1; anchors `[D-007]`, `[D-029]`, `[D-033]`, `[D-034]`, `[D-039]`, `[D-052]`; initial commit `a7106ed`). `[D-052]` captured (dual-backend runtime LLM: local Ollama + corp on-prem LLM; empirical per-TaskKind routing; no automatic spillover; both backends within `[D-007]` corporate network boundary; commit `a7106ed`). LLM TaskKind enum refined: added `CLASSIFY_DOC_TYPE` (FR-52 / FR-55 filename-opaque → doc_type ∈ {test_report, tech_report, waiver}); fixed mislabeled `CLASSIFY_DOC` schema to D-039 Step 2 semantics (REVISION-of-existing vs NEW_DOCUMENT). Ph-1 LLM scope per `[D-029]` remains 3 functions; TaskKind partition expanded to 5 via `[D-052]` impl note 2026-05-28. Hardware context: dev box NVIDIA RTX A4000 16 GB (Option B routing with swap); production HILDA PC NVIDIA DGX Spark 128 GB unified memory (in setup, ~2026-06-04 ETA, removes VRAM ceiling and enables multi-model hot routing).
- 2026-05-26 **STATUS.md comprehensive audit** — Header date refreshed (2026-05-24 → 2026-05-26) + Last design review extended to cover 2026-05-26 work; Done section: missing initial sharepoint/REQUIREMENTS.md commit (`f8e9345`) added; chronological order of DECISIONS.md backward sweep + forward sweep entries swapped (impl notes commit `a6d66b1` first, new ADRs commit `4ae4f71` second); In progress section: storage MODULE.md continued review added (Public surface / Key choices / Non-goals / Depends-on sections remain unreviewed); Next section: module count refreshed (4 done + 1 in-progress draft + 15 remaining; 3 of 6 ADR follow-ups marked resolved; sub-conventions ratification status + code-sync TGGroups addition + D-052/D-053 deferred candidates noted); Flags section: MAP.md date refreshed + storage addition noted; CI/CD tool-selection flag cross-linked to D-024 reconciliation resolution; 2026-05-24 ADR follow-ups flag updated (3 resolved, 3 remaining); new D-048..D-051 first-touch caveat added. Net ~12 edits across 5 sections; no content removed.

- 2026-06-07 Phase B Module rollback — `template_schema/MODULE.md` committed `93e0aec`: ItemType.DEFAULT + DocType.DEFAULT enums; RuleActionType expanded with 13 new actions for FR-29 revised + TRIGGER_LLM_REVIEW → TRIGGER_AI_REVIEW rename; new RuleTriggerType + RuleSubTriggerType enums; TGGroupBase/MilestoneBase/DeliveryItemBase new fields (ingress_nsd, folder_routing_enabled, default_work_item_config on MilestoneBase, no_customer_upload, ingress_folder + target_folder inbound/outbound naming discipline, item_description tag-list); 4 new models (DefaultWorkItemConfig, FolderRoutingEntry, TGFolderRouting, TagCatalogEntry); TSC-W003 error code. Note: ItemType.DEFAULT remains valid; the "1:1 derivation from item.item_type → doc_type" docstring framing is in scope for the 2026-06-07 [D-053] reversal (see Flags).

- 2026-06-07 Phase B Module rollback — `storage/MODULE.md` file-centric refactor (commits `93e0aec` + `c02fff4`): DocumentIndexRow PK=`file_hash` (secondary unique constraint `(milestone_id, doc_id_slug, rev_number)` preserves FR-57 lookup); DocumentItemAssociation = symmetric M:M with composite PK `(file_hash, delivery_item_id)` carrying per-(file, item) data (local_classified_path, plm_id, plm_attachment_id, owner_email, upload_timestamp). Dropped: `download_url` (replaced by `make_download_token(file_hash, delivery_item_id, ttl_seconds)` computed at page-render time per FR-61); `is_primary_association` / `is_primary` (symmetric M:M — no primary/secondary semantic); `landing_item_id`; synthetic `association_id`. `inferred_tg_name` + `routing_resolution` (per-ingest) moved to DocumentIndexRow. New `PLMFanOutTarget` typed model. `AutomationRuleOverride.rule_id` clarified as soft FK to YAML rules per FR-30/FR-31; added `list_all_override_rule_ids()` for rule_engine startup orphan-audit. NSDPath: removed stale `to_download_token`; renamed `internal_un_resolved_zip` → `internal_unrouted_zip`; added `internal_default_workitem` + `ingress_folder`; clarified `internal_staged` ([D-039] Tier-2 NEW-vs-REVISION) vs `internal_default_workitem` (FR-78 which-work-item) as distinct ambiguity classes. New error codes: STR-E005..E008, STR-W002..W006. 10-item API surface review: cache_set TTL guard with MAX_CACHE_TTL_SECONDS; 9 convenience helpers (get_documents_for_item, list_documents_for_milestone, delete_document_item_association, cache_delete, list_tag_catalog_entries, reactivate_tag, list_active_overrides); query_communications gains file_hash/action_type/pm_id/until/offset filters + pagination cap; reassign_document_to_workitem syncs `DocumentIndexRow.inferred_tg_name` to target item's tg_name; set_override/clear_override emit CommunicationLog audit entries with credential_id=pm_id.

- 2026-06-07 Phase B Module rollback — `llm/MODULE.md` rollback + tri-backend amendment + clarifications (commit pending in this session): `CLASSIFY_DOC_TYPE` TaskKind removed per [D-053] impl note 2026-05-28b (5 → 4 TaskKinds; **scheduled for re-reversal after requirements-phase redesign of doc_type classification — see In progress / Flags**); FR-52 framing updated from `[D-033]` 2-tier to `[D-053]` 5-step pipeline; ROUTE_ATTACHMENT contextualized as step 4 of 5; REVIEW_DOCUMENT DEFAULT-skip note added (FR-7 amendment + FR-29 TRIGGER_AI_REVIEW gate). **Tri-backend expansion** ([D-052] dual → tri): BackendConfig.name Literal expanded `["ollama_a4000", "vllm_dgx", "corp_llm"]`; new Hardware-topology section with per-backend memory/bandwidth/profile table + capacity-vs-bandwidth note (DGX Spark 128 GB unified RAM ≠ throughput advantage for small models — A4000 GDDR6 ~448 GB/s > DGX Spark LPDDR5X ~273 GB/s for autoregressive inference of fits-on-A4000 models); tentative-assignments table expanded with `vllm_dgx` A/B candidate per task + model catalog policy (same Ollama models on both local backends for direct comparability; larger models added to vllm_dgx only if small-model A/B unsatisfactory). Host topology fix: HILDA PC distinct from A4000 box + DGX Spark box; all three model-serving hosts are separate Linux machines on the lab subnet; hilda-llm-gateway proxies outbound. Docstring clarifications: ReviewDocumentInput.checklist (build-time-generated YAML at `customizations/checklists/<slug>/<doc_type>.yaml` by Test Report Profiler in same run as parser); new "Relationship to parser_result" Non-goals section with comparison table + 4-quadrant coexistence matrix; expanded "Not a test report parser" Non-goal with build-time/runtime split.

- 2026-06-07 STATUS.md flags maintained: storage debt (DocumentIndexRow.inferred_tg_name field) added and resolved inline; requirements.md naming sweep flag (`target_folder` → `ingress_folder` for FR-77 inbound side); A/B-test gate flag rewritten for tri-backend with capacity-vs-bandwidth caveat; new [D-052] ADR-body impl note flag (dual→tri expansion needs append per [D-002] convention); new [D-053] reversal flag (HIGH priority — blocks email_service + customer_adapter Module rollbacks).

- 2026-06-08 Requirements-phase doc_type classification redesign session (commit pending this close-session) — addressed the `[D-053]` "1:1 derivation" reversal flagged at 2026-06-07. **13+ turn iterative refinement** locked the corrected model: **4 ItemTypes** (`Confirmation`, `TEST_TECH_WAIVER_REPORT`, `COMPLIANCE_CERTIFICATION_RELEASE_NOTES`, `Default` — collapsed from previous multi-valued enum); **5 DocTypes** (`test_report`, `tech_report`, `waiver`, `compliance_certification_release_notes`, `unresolved`); **alignment invariant** (per-ItemType allowed doc_type sets); **4 NSD storage paths** (classified / unrouted / staged-not-revision-determined / staged-not-classified); **2-step classification ladder** (filename regex Step 1 covers all 4 actionable doc_types; LLM CLASSIFY_DOC_TYPE Step 2 restricted to {test/tech/waiver}; UNRESOLVED on low confidence); **`[D-039]` skip optimization** at ingest when routing lands on Default or doc_type=UNRESOLVED (deferred to FR-83 reassignment / FR-87 resolution); **SP UI strict-order resolution** A → B → C (work-item reassign / doc_type resolve / revision resolve) routed via FR-84 SP-alert email channel. **3 new FRs landed**: FR-85 (doc_type classification pipeline), FR-86 (storage matrix + alignment invariant + `[D-039]` skip rule + DocumentIndexRow combinatorics + `<inferred_tg_name>` in unrouted path per `[D-060]`), FR-87 (SP UI strict-order A→B→C + explicit FR-84 SP-alert end-to-end flow). **6 existing FRs amended**: FR-7 (ItemType/DocType enums + alignment + corrected doc_count=0 guidance for waiver-only deliverables), FR-16 (classification per FR-85), FR-52 (strike 1:1 derivation + item-homogeneity invariant + EML-W005; reference FR-85/FR-86; "concurrent" wording removed), FR-53 (review_required conditioned on `TEST_TECH_WAIVER_REPORT` items; doc_type per FR-85), FR-78 (Default accepts all 5 doc_types; `[D-039]` skipped at ingest for Default), FR-83 (strict A→B→C per FR-87; no auto-override; explicit scope = unrouted path only). Header "Last updated" advanced to 2026-06-08. **18 design issues** identified and resolved through iterative refinement (slash naming → TEST_TECH_WAIVER_REPORT style; doc_count semantic dead-state → accepted with corrected guidance; `[D-039]` skip optimization; SP UI mechanism explicit invocation of FR-84 channel; `inferred_tg_name` path organization for unrouted).

- 2026-06-09 Phase B Module cascade against the corrected `[D-053]` model — Groups 2-4 of 4 applied (Group 1 `template_schema/MODULE.md` already in `aefd8b7` 2026-06-08). **storage/MODULE.md cascade** (uncommitted): DocumentIndexRow.doc_type 5-value enum + FR-86 storage matrix paragraph; parser_result + llm_review_findings docstrings updated for 5-value DocType; `NSDPath.internal_default_workitem()` signature added `inferred_tg_name_slug` param per `[D-060]` impl note 2026-06-08; renamed `internal_staged` → `internal_staged_revision`; added `internal_staged_classification`; new STR-W007 (stale staged document). User-review correction: `local_classified_path` → `local_nsd_path` rename + new `NSDPathType` enum (4 values) on DocumentItemAssociation for indexed state tracking. **llm/MODULE.md cascade** (uncommitted): `CLASSIFY_DOC_TYPE` TaskKind RESTORED (un-revert of 2026-05-28b removal); TaskKind count 4→5; restricted candidate set `{test/tech/waiver}` per FR-85 Step 2; ClassifyDocTypeInput/Output schemas restored; Non-goal flipped "Not a doc_type classifier" → "IS a doc_type classifier (Step 2 of FR-85 ladder)"; tentative-assignments table 4→5 rows; templates_loaded=5; coexistence matrix updated. User-review correction: `RouteAttachmentOutput` → `list[RouteAttachmentMatch]` per FR-79 multi-item; new `RouteAttachmentMatch` model; new `LLG-W008` (over-routing); `[D-029]` Ph-1 narrow surface bullet updated 4→5 TaskKinds. **email_service/MODULE.md cascade** (uncommitted): substantial — FR-52 pipeline 2-tier → 5-step (with FR-77 Type-2 folder routing as new step B3); doc_type classification → separate FR-85 2-step ladder; FR-86 4-path NSD storage dispatch via new NSDPath helpers; `RoutedAttachment` → multi-match per FR-79 with `AttachmentItemMatch` model + new `ClassificationResolution` enum; `tg_resolver.py` sub-module added per `[D-060]`; EML-W005 retired; EML-W006 (misalignment-staged) + EML-W007 (over-routing) added; Fr52Config field additions; sp_alert_parser gained FR-87 A→B→C action handlers (`tpm_reassign_to_workitem`, `tpm_resolve_doc_type`, `tpm_resolve_revision`); QC template revised for new fields. **Plus same-day template_schema/MODULE.md post-cascade incremental**: CustomerDeliveryModality + `GOOGLE_DRIVE` (− `FILE_STORAGE`); `MilestoneBase.default_work_item_config` type `dict | None` → `DefaultWorkItemConfig | None`. Cascade chain entire (Groups 1-4) complete against the corrected requirements model locked 2026-06-08.

- 2026-06-10 Session 4 (Architecture phase — Batch 1 closure) — **`customizations/rules/MODULE.md` Ph-1 initial draft** (~250 lines; NEW at `customizations/rules/`). Data drop-zone documenting per-deployment AutomationRules + polling-schedule YAML consumed by `rule_engine.RuleSet.load` per `[D-030]` / `[D-031]`. Mirrors `customizations/sharepoint_config/` pattern: 3-tier scope ladder (Global `global/defaults.yaml` / Customer `<customer_slug>/customer_rules.yaml` / Device `<customer_slug>/<device_slug>/device_rules.yaml`), two top-level YAML keys (`rules:` TRIGGER_ACTION shape + `polling_schedules:` POLLING_SCHEDULE shape) per `Rule.kind` discriminator; condition DSL closed operator set (eq/neq/in/gt/gte/lt/lte/and/or — no Jinja, no Python, no jq); worked example shows Global+Customer multi-tier composition producing 2 independent Celery chains per `[D-066]`; bind-mount + SIGHUP `reload()` mechanism per `[D-025]`. Anchors `[D-001]`/`[D-022]`/`[D-025]`/`[D-030]`/`[D-031]`/`[D-062]`/`[D-066]` + FR-23/30/55. 12 invariants + 8 non-goals + 8 deferred Ph-2/Ph-3+ items. **Iterative cadence locked**: Architecture phase paused for **Batch 1 modules** (7 currently-designed: email_service, customer_adapter, rule_engine, workflow_engine, tracker, customizations/sharepoint_config, customizations/rules). Development phase begins on Batch 1. **Batch 2 architecture** resumes after Batch 1 development complete — Batch 2 list: messenger (Layer 2), test_report (Layer 2), test_report_profiler (Layer 3), api_spec_ingestor (Layer 3), dashboard (Layer 4), corp_messenger_gateway (Layer 5), corp_plm_gateway (Layer 5), customizations/issue_tracker Structure markers (small fix), customizations/checklists (drop-zone), customizations/test_report_parsers (drop-zone). **None of these are Ph-1-scope-cut** — all remain Ph-1 deliverables, just sequenced after Batch 1 dev for iteration efficiency. No ADRs warranted this session — iterative cadence is a project-workflow choice; customizations/rules content is idiomatic derivation from rule_engine + sister sharepoint_config pattern.

- 2026-06-10 Session 3 (Architecture phase continued) — **`workflow_engine/MODULE.md` Ph-1 initial draft** (~410 lines; NEW at `core/src/workflow_engine/`). Celery + Redis broker + Postgres result backend per `[D-022]`/`[D-043]`; 18 Ph-1 ActionKind task definitions mapped to ACTION_KIND_TO_TASK registry; TriggerDispatcher consuming `TriggerEvent` from 6 trigger sources (sp_alert / email_ingest / nsd_watch / plm_watch / scheduler_tick / state_change); Celery-beat schedule loader for periodic tickers (FR-23 / FR-26 / FR-55); hilda-beat singleton enforced by orchestrator; **4 queues by latency profile** (default / llm_calls / browser_automation / periodic); FR-31 sub-3 manual trigger handler bypassing rule_engine; FR-87 TPM-resolution round-trip worked example. Workload assignment clarified: workflow_engine code is entry point for **2 of the 4 [D-021] workloads** (`hilda-worker` + `hilda-beat`); `hilda-api` imports the Celery app to enqueue from corp-gateway POSTs but doesn't execute task bodies. Public surface: hilda_celery_app, TaskBinding, build_chain_from_rule_match, TriggerDispatcher.dispatch, BeatScheduleEntry, build_beat_schedule, reload_beat_schedule, handle_manual_trigger, WorkflowEngineConfig, polling/*.py (4 periodic tasks), trigger_sources/*.py (6 fire_from_* façades). 3 architect decisions captured inline as MODULE.md Key choices (no separate ADR): (1) 4 queues by latency profile; (2) hilda-beat singleton enforced by orchestrator; (3) 6 explicit trigger sources not generic event bus. **`tracker/MODULE.md` Ph-1 initial draft** (~510 lines; NEW at `core/src/tracker/`) heavily reviewed across ~12 iterations. 11-state delivery_state machine (Not Started + 8 active happy-path + Delayed + Blocked off-path); LEGAL_TRANSITIONS matrix with **23 legal transitions**; 8 transition guards per FR-7/FR-28 (transition_legal + OwnerClosed 2-condition + PMApproval gate via pm_approval_at field + carrier-upload + CLOSED requires TPM + Delayed/Blocked unconditional + OwnerClosed→UnderPMReview auto-advance + Rewind-from-Submitted requires TPM attribution); 4 cross-cutting orchestration actions consumed by workflow_engine task bodies: update_delivery_state, instantiate_default_workitem (FR-78), reassign_document_to_workitem (FR-83), propagate_tags_to_active_trackers (FR-82), apply_manual_tpm_override (FR-14); StateChangeDispatchSignal pattern breaks dependency cycle (tracker returns signal, workflow_engine task body fires StateChange trigger — keeps dependency one-way per `[D-022]`); 3 dependency-injection Protocols (StorageWriter / SpWriter / AuditWriter). **6 architect decisions captured inline as MODULE.md Key choices**: pure functional guards; dependency injection via Protocols; atomic state+SP+audit one-transaction; OWNER_CLOSED transient auto-advance; PM approval field on DeliveryItem (Option B; pm_approval_at + pm_approval_pm_id) with clearing discipline; rewind-from-SUBMITTED with TPM attribution (Option A for customer RFI). **2 architect decisions worth ADR capture** (selected at Step 3 triage): `[D-067]` customer RFI rewind path (SUBMITTED_TO_CUSTOMER → DOCUMENT_RECEIVED/OUTREACH_SENT with TPM attribution; TRK-E006); `[D-068]` PM approval recorded via DeliveryItem field (pm_approval_at; clearing discipline; rejected CommunicationLog query + implicit alternatives). Other tracker design refinements (matrix corrections + invariants): DELAYED/BLOCKED moved to reachable from OUTREACH_SENT onwards (not from OPEN); Confirmation items MUST have no_customer_upload=True; all Ph-1 CLOSED transitions are TPM-manual per FR-7+DEF-20 (per-item Mark-Closed + FR-64 batch); D-039 Steps 0+1 (Ph-1 deterministic hash dedup + slug match) fire on FR-83 reassignment with Ph-2 LLM Step 2/3 deferred (TRK-W007); apply_manual_tpm_override routing discipline (sub-trigger fields → rule_engine path; non-sub-trigger fields → FR-14 path); trigger_source 3-value semantic (automated / manual_tpm_override / tpm_button). 2 worked examples: OwnerStatusConfirmed "done" round-trip through OWNER_CLOSED transient + StateChange dispatch; FR-64 batch close milestone-level pseudocode + idempotency. Phase B Layer 2 cascade now: 5 modules done (email_service, customer_adapter, rule_engine, workflow_engine, tracker — tracker actually Layer 3 but drafted same session); 2 modules remain (messenger, test_report). Module count remaining drops 10 → 8 across all layers.

- 2026-06-10 Session 2 (Architecture phase resumed) — **`rule_engine/MODULE.md` initial Ph-1 draft** (~315 lines; NEW at `core/src/rule_engine/MODULE.md`). 8 sub-modules (`models.py` / `loader.py` / `resolver.py` / `evaluator.py` / `polling_schedule.py` / `orphan_audit.py` / `collision_audit.py` / `pause_state.py` + 2 CLIs). Public surface: TriggerKind (15 Ph-1 triggers), ActionKind (18 Ph-1 actions; Ph-2 deferred), RuleScope, RuleKind discriminator (TRIGGER_ACTION / POLLING_SCHEDULE), Rule (with kind discriminator + optional shape fields), PollingScheduleTier, EntityRef, RuleAction, TriggerEvent, RuleMatch, RuleSet, resolve_rules_for_entity, resolve_polling_schedule_for_item, RuleEngine.evaluate/explain, evaluate_polling_schedule, orphan + collision audits, PauseStateLookup Protocol, RuleEngineConfig. **4 architect decisions captured inline**: Q1 (Ph-1 action subset; Ph-2 → Deferred); Q2 (`[D-022]` pure evaluator boundary reaffirmed — no Celery / no SP write / no DB writes); Q3 (rule_engine owns Postgres `AutomationRuleOverride` orphan-audit per `[D-062]`); Q4 (**per-trigger ordered action lists; no priority / no score / no first-match — captured as `[D-066]`**). **2 model-shape decisions**: F1 absorbed via `Rule.kind` discriminator (TRIGGER_ACTION / POLLING_SCHEDULE) over splitting into two model classes (captured in MODULE.md Key choices, not separate ADR); F2 closed (sentinel `rule_id="__all_rules__"` for FR-31 sub-1 pause; ItemPauseRecord rejected — `AutomationRuleOverride` stays the single override table). **3 worked examples** grounding the abstract API: (1) `OwnerReassigned` single-rule-2-ordered-actions Global-only; (2) `LastContactThreshold` multi-rule additive composition (Customer overrides Global + adds new customer-specific rule); (3) `AttachmentReceived` + `polling_schedule` with FR-31 sub-2 Postgres tier override end-to-end. Each example walks YAML → resolve() → evaluate() → workflow_engine consumption + TPM SP UI options (FR-31 sub-1 / sub-2 / sub-3). Phase B Layer 2 cascade: rule_engine done; 3 modules remain in Layer 2 (workflow_engine, messenger, test_report). Module count remaining drops 11 → 10 across all layers.

- 2026-06-10 Session — customer_adapter/MODULE.md review pass (4 edits: FR-77 caller-resolves clarifier; SP UI FR-84 writeback invariant; selectors_version compatibility-pointer doc; selector-pack deploy-time refresh worked-example + CAD-W004) committed `b7d6edb` + pushed. **`[D-052]` ADR-body impl note 2026-06-07 appended** (dual→tri-backend: backend Literal expanded `["ollama_a4000","vllm_dgx","corp_llm"]`; hardware topology corrected — HILDA PC has no GPU; cross-refs `[D-068]` + `credential_service` SystemType split; chronology marker added to 2026-05-28b impl note) per `[D-002]` append-only. **regen-map run 2026-06-10** — 4 Structure sections populated with real code (`diagnostics`, `issue_tracker` core, `sharepoint_integration`, `template_schema`); 5 marked `[DRAFT]` (`credential_service`, `customer_adapter`, `email_service`, `llm`, `storage`); `MAP.md` wholly regenerated. Both committed `fd78e76` + pushed. Flagged: `customizations/issue_tracker/MODULE.md` missing Structure markers; `customizations/sharepoint_config/` NEW dir without MODULE.md. **`sharepoint_integration/MODULE.md` drift sweep** (uncommitted; 14 edits A1–A6 + B1–B4 + C1–C4): 8-list canonical entity set per `[D-051]`; `SpCrud.delete_item` + `SharePointListProvider.from_sp_fields` added to declared Public surface; `mock_server/` sub-module documented + `--serve --port` CLI mode; sample counts bumped 7→8; FR-84 outbound writeback invariant (SP→HILDA HTTP firewall-blocked; HILDA→SP REST sole writeback channel); FR-87 TPM resolution writeback path invariant (strict A→B→C ordering); column-map append-only invariant for 2026-06-08 cascade fields; SP Choice-field value sync added to Non-goals (SP UI engineer owns Choice values); SP-alert email channel added to Non-goals; Depended-on-by extended (`issue_tracker`, `customizations/issue_tracker`, indirect `customer_adapter`); two-halves-of-the-same-conversation positioning note for SP UI engineer collaboration; Rollback log block added. **HILDA_SP_Schema.xlsx Excel deliverable for SP UI engineer** (uncommitted, NEW at `docs/sp_ui_engineer/HILDA_SP_Schema.xlsx`) — 9 tabs (README + 8 SP lists per `[D-051]`), 112 rows covering Ph-1 + Ph-2 columns, 10-column-per-row layout (HILDA canonical → SP internal suggested → SP display → type → Choice values → Required → Phase → Default → Source FR/Decision → Notes); README documents the 3-tier name model (HILDA canonical / SP internal / SP display); 5 `???` cells flagged for HILDA-architect confirmation; covers Confirmation/TEST_TECH_WAIVER_REPORT/COMPLIANCE_CERTIFICATION_RELEASE_NOTES/Default ItemType + 11-value delivery_state (Not Started + 8 active + Delayed + Blocked) + PM/TPM/TeamLead/Admin roles. **Phase switch architecture → requirements** mid-session for FR-77 naming sweep evaluation; sweep dissolved into **no-op** under requirements-phase review (current variants "outermost source folder" / `<matched_source_folder>` / "outermost folder name relative to milestone root" / "outermost folder name inside the zip" are correctly specific per user — collapsing to generic `ingress_folder` would erase positional context).

- 2026-06-09 Same-day cascade follow-on review work (commits `50086b2`, `adf1759`, `bef99e0`, `65945e9`, `ad31919`, `65a53b7` — all pushed to origin/main): **email_service/MODULE.md** review pass (4 corrections + tg_resolver email-scope correction per `[D-060]` impl note 2026-06-09 + test-scenario rewrite to single-TG single-doc happy-path + over-routing threshold 10 per 5G/4G domain reality + Ph-1-first discipline shift + Test-scenario = initial validation NOT design scope clarifier). **diagnostics/MODULE.md** drift fixes (18→20 module prefix count; added CMG/CPG for corp-side gateway modules per SYSTEM.md §2.1). **credential_service/MODULE.md** — SystemType.LLM_GATEWAY split into 3 per `[D-052]` tri-backend (LLM_OLLAMA_A4000 / LLM_VLLM_DGX / LLM_CORP_LLM); per-backend .enc.env files; Purpose anchor list + Depended-on-by updated. **Locked conventions this session**: Ph-1-first discipline (active surface = Ph-1 only; Ph-2 → Deferred); Ph-2 gate enforcement (Claude flags Ph-2 dives going forward); Test-scenario = initial validation flow (full Ph-1 still in design scope). **No new ADRs this session** (all changes captured under existing `[D-053]` / `[D-055]` / `[D-060]` / `[D-052]` impl notes from earlier today).

## In progress

- ~~**SP UI requirements Ph-1 narrowing (NEW 2026-06-09; started, paused)**~~ *— CLOSED 2026-06-09: 11-group Ph-1 narrowing landed in `sharepoint/REQUIREMENTS.md` (commit `726da9d`). 3 new TPM-resolution buttons (§4.9 / §4.10 / §4.11) + new §7.4 SP-alert action-verb conventions + 5 new DeliveryItems fields + 3 new TGGroups fields + §12 open items refreshed (#7 RESOLVED; #8/#9/#10 NEW).*

- **Requirements.md user-led review continuation** — review pass closed for the FRs touched in 2026-06-05 session; future review passes may cover other FRs (FR-1 through FR-50 range; NFR section; Appendix A) when user resumes. No commitment on cadence; resume as needed.

- ~~**Architecture-phase Module rollbacks — partially complete (status 2026-06-07; updated 2026-06-09)**~~ *— CLOSED 2026-06-10: all 5 modules done. (1) `template_schema` ✓ `93e0aec` + `aefd8b7`; (2) `storage` ✓ `93e0aec` + `c02fff4` + cascade Group 2; (3) `llm` ✓ `054bcfa` + cascade Group 3; (4) `email_service` ✓ cascade Group 4; (5) `customer_adapter/MODULE.md` initial Ph-1 draft committed `f3804b8` 2026-06-09 + review-pass refinement `b7d6edb` 2026-06-10. Phase B cascade chain complete against the corrected `[D-053]` model locked 2026-06-08. Appendix refreshes in `requirements.md` (Appendix B FRs-requiring-LLM-calls + Appendix C phase scope index) remain queued — moved to Next.*

- **`[D-053]` reversal + doc_type classification redesign — CLOSED 2026-06-08.** All 3 design questions resolved through 13+ turn iterative refinement; corrected model captured in FR-7 + FR-85 + FR-86 + FR-87 + amendments to FR-16/FR-52/FR-53/FR-78/FR-83 (see Done 2026-06-08). Affected MODULE.md edits in `template_schema`, `storage`, `llm` move to a new In-progress entry below for the cascade work.

- ~~**MODULE.md cascade after `[D-053]` reversal close (NEW 2026-06-08)**~~ *— CLOSED 2026-06-09: cascade Groups 1 (template_schema in `aefd8b7`) + 2 (storage) + 3 (llm) + 4 (email_service) all complete and pending commit this close-session. `customer_adapter/MODULE.md` is NOT in scope of this cascade entry — it remains a NEW draft per the Module rollbacks entry below.*

- **MODULE.md cascade after `[D-053]` reversal close (NEW 2026-06-08; architecture phase when /switch-phase architecture resumes)** — 3 already-committed MODULE.md files need re-cascade against the corrected requirements model (locked 2026-06-08): **(1) `template_schema/MODULE.md`** — `ItemType` enum collapse from previous multi-value to 4 values (`Confirmation`, `TEST_TECH_WAIVER_REPORT`, `COMPLIANCE_CERTIFICATION_RELEASE_NOTES`, `Default`); `DocType` enum 4-value → 5-value (`compliance_certification_release_notes` rename of the former `default`; add `unresolved`); strike "1:1 derivation from item.item_type" docstring; add alignment invariant docstring per FR-86; restore `CLASSIFY_DOC_TYPE` TaskKind reference. **(2) `storage/MODULE.md`** — `NSDPath.internal_default_workitem()` signature add `inferred_tg_name` parameter (per FR-86 corrected path); add 2 new NSDPath helpers (`internal_staged_classification()`, `internal_staged_revision_for_classified()`); `DocumentIndexRow.doc_type` becomes `Optional[DocType]` allowing UNRESOLVED state (or sentinel value); document the FR-86 storage matrix in DocumentIndexRow docstring; add `STR-W007` (unrecognized doc_type) if needed. **(3) `llm/MODULE.md`** — restore `CLASSIFY_DOC_TYPE` TaskKind (un-revert the 2026-05-28b removal); add new ClassifyDocTypeInput/Output schemas with restricted candidate set `{test_report, tech_report, waiver}` per FR-85 Step 2; revert the "Not a doc_type classifier" Non-goal; tentative-assignments table 4 → 5 rows.

### 2026-05-28 requirements update — CLOSED (artifacts landed)

Closed at commit (this commit): all locked decisions from 8-item user-delivered requirements update materialized as durable artifacts. `requirements.md` gained 11 FR amendments (FR-7, FR-13, FR-17, FR-18, FR-19, FR-26, FR-52, FR-55, FR-62, FR-68, FR-72) + 7 new FRs (FR-77 through FR-83). `DECISIONS.md` gained 2 new ADRs (`[D-053]` routing model + default work-item + multi-item + ItemType.DEFAULT; `[D-054]` customer-portal destination + symmetric `no_customer_upload` + Google-Drive-only adapter) + 4 impl notes (on `[D-013]` dual-NSD, `[D-039]` item-routing-precedes-D-039, `[D-050]` simplified 4-area structure, `[D-052]` CLASSIFY_DOC_TYPE removed 5→4 TaskKinds). Phase switched back to architecture for Module rollbacks (see In progress).

### Requirements-phase working notes (2026-05-28; superseded by durable artifacts above)

> The detailed working notes below served as a recovery checkpoint during the section-by-section requirements review. All locked decisions are now durable in `requirements.md` (FR amendments + FR-77..83) and `DECISIONS.md` (D-053, D-054, impl notes on D-013/D-039/D-050/D-052). This subsection retained for audit trail; reference the durable artifacts for current spec.

**Section 1 — Doc types (locked):**
- `DocType` enum stays 4-valued: `{test_report, tech_report, waiver, default}`. Compliance docs, certification docs, release notes all fold into `default`.
- FR-16 (rule-based parser) → `test_report` only.
- FR-46 (final/interim classifier) → `test_report` only.
- FR-53 (LLM checklist review) → `{test_report, tech_report, waiver}` only.
- `default` doc_type triggers no downstream processing (no parser, no classifier, no review).
- `ItemType.DEFAULT` added (7 enum values now). ~~1:1 `item_type → expected_doc_type` derivation locks doc_type from item routing — no runtime doc_type LLM call needed.~~ **(2026-06-07 — REVERTED: `ItemType.DEFAULT` addition remains valid; the 1:1 derivation framing is incorrect — a single non-Confirmation, non-Default work item CAN receive any of `{test_report, tech_report, waiver}`, requiring per-document doc_type classification at runtime. See In progress "[D-053] reversal + doc_type classification redesign".)**
- ~~**`CLASSIFY_DOC_TYPE` TaskKind REMOVED from llm module** (5 → 4 Ph-1 TaskKinds). Requires `llm/MODULE.md` + `[D-052]` impl note rollback when returning to architecture phase.~~ **(2026-06-07 — SCHEDULED FOR RE-REVERSAL: `CLASSIFY_DOC_TYPE` (or equivalent classification mechanism) needs restoration; pending requirements-phase decision on mechanism. See In progress.)**

**Section 2 — Zip handling + PLM/Carrier upload (locked):**
- Zip files are archive-only — live in NSD `<tg>/_zip_audit/` (TG-level subdirectory); never uploaded to PLM or carrier portal.
- Version tree operates on individual files only — zip-level versioning irrelevant; D-039 + `[D-048]` apply per-individual-file.
- PLM uploads receive **only individual files** (Email/NSD-source: HILDA extracts + uploads; PLM-source: HILDA reads, no re-upload).
- Carrier portal receives **only individual files**, `is_final=true` after FR-18 dispatch; same for all ingress sources.
- PLM-source extracted files are written to HILDA NSD internal classified tree for audit consistency (`<tg>/<item>/<doc_type>/<doc_id_slug>/revN/`).
- `_zip_audit/<zip_filename>.zip` + `_zip_audit/<zip_filename>/<extracted-tree>/` co-located.
- New hash sync invariant: extends FR-68 — every file uploaded to carrier portal must hash-match a file present on PLM (top-level OR inside a PLM-resident zip). Non-match → `ITR-W003` PM-dashboard flag.

**Section 3 — Dual-NSD topology (locked):**
- NSD1 = bidirectional (HILDA-owned `internal/` tree + ingress); NSD2 = ingress-only.
- Both SMB-mounted on HILDA PC simultaneously; same `hilda-svc` AD service account.
- NSD2-sourced docs → NSD1's `internal/` tree (cross-drive write).
- NSD2 retention: original files stay on NSD2 forever; no archiving.
- Polling: both NSDs polled identically (same `polling_schedule`; no per-drive override).
- Resolution: FR-13 amendment + `[D-013]` impl note 2026-05-28; no new ADR.

**Section 6 partial — NSD ingestion structure overhaul (locked items):**
- No more per-item NSD ingress folders. Flat structure: `\\NSD_X\<carrier>\<device>\<milestone>\<Folder-1..n>\`.
- Folder name no longer authoritative for item routing.
- FR-52 routing pipeline (5-step ladder):
  1. **Strict substring** — `item_description` is semicolon-separated tag list; ALL tags must appear in filename.
  2. **Fuzzy match** — rapidfuzz(filename, item_name); top scorer above threshold wins. (`item_description` repurposed; no separate `routing_tags` field; SP column addition avoided.)
  3. **Source-folder → work-item** template (NSD-only; per-TG opt-in via `folder_routing_enabled = True`).
  4. **LLM `ROUTE_ATTACHMENT`** — first-page excerpt.
  5. **Staged → associated with milestone's default work-item** (FR-52 step 4); PM/TPM triage; parent NSD path tracked via new `DocumentIndexRow.unrouted_source_path: str | None`.
- TG-level routing strategy: TG-1 style (NSD1 ingress + no folder→work-item template) vs TG-2 style (NSD2 ingress + folder→work-item template); each TG declares its own ingress NSD + folder-routing opt-in.
- Default work-item: every milestone auto-includes one; `item_type = Default`; doesn't gate submission (per item 8).
- Multi-item routing (one doc → many items): adopt many-to-many join table `DocumentItemAssociation(document_index_row_id, delivery_item_id, association_kind: Literal["primary", "secondary"])` for clean per-association metadata.
- New `TGGroupBase` fields: `ingress_nsd: Literal["NSD1", "NSD2"]`, `folder_routing_enabled: bool`.
- Consolidated `folder_routing.yaml` per customer template, per TG: `<tg>/<source-folder-path>/{work_item, target_folder}` — both fields optional.

**Section 6 partial — Other (locked):**
- 6.2.4 fuzzy folder match: substring containment (case sensitivity TBD); longest-substring-wins on multi-match conflict.
- 6.2.5.5: source-folder template applies only to NSDx ingress (not Email/PLM).
- Item description format: semicolon-separated tag substrings; Pydantic validation regex; emit `TSC-W003` on non-tag format at template load; existing prose-description templates need cleanup migration.

**Section 7 partial — Email/PLM ingress (locked):**
- Zips extracted recursively; each extracted file → FR-52 routing.
- FR-52 step 3 (source-folder template) does NOT apply to zip-extracted files (no NSD source folder context); step 3 is NSD-direct only.
- 7.1.3.1 per-work-item → target-folder mapping: subsumed into consolidated `folder_routing.yaml` Option (b).
- 7.1.3.2: items in `staged/` block portal upload until TPM resolves.

**Section 8 — TPM manual handling (locked):**
- Default work-item does NOT gate `ReadyForSubmission` or carrier dispatch.
- TPM downloads via existing FR-57 download endpoint (`https://hilda.corp/dl/<scoped_token>`); manually uploads to carrier portal as needed (legacy/uncaptured deliverables path).

**Sections pending review:**
- Section 4: no-customer-upload property (granularity, state semantics, TPM download flow)
- Section 5: no-tracking TG + sub-item-only tracking (G-11/G-12/G-13 unresolved; FR-62 SP-UI upload extension question)

**ADR plan (revised after Section 3 disposition):**
- ~~`[D-053]` Dual-NSD topology~~ — superseded by `[D-013]` impl note 2026-05-28 (no new ADR).
- **`[D-053]` candidate**: Default work-item entity + multi-item routing + ItemType.DEFAULT + item homogeneity constraint (Section 6).
- **`[D-054]` candidate**: Per-TG `tracking_enabled` + `customer_upload_enabled` flags + customer-portal-as-carrier-submission-destination (Section 2+4+5).
- Disposition pending Section 4–5 close.

**FR amendments queued (drafts not yet written):**
- FR-13, FR-17, FR-18, FR-19, FR-52, FR-55, FR-68 amendments.
- FR-58 new (target-folder mapping).
- 3+ new FRs (default work-item, multi-item association, per-TG flags).

**Module rollbacks queued (apply after requirements-phase exit):**
- `llm/MODULE.md` — `CLASSIFY_DOC_TYPE` TaskKind removed; tentative-assignments table 5→4 rows; `[D-052]` impl note revised.
- `template_schema/MODULE.md` — `ItemType.DEFAULT` added; `item_description` semantic shift documented; `DocType` enum stays 4-valued (no compliance/cert/release_notes); new `TGGroupBase` fields.
- `email_service/MODULE.md` — FR-52 pipeline rewrite (5-step ladder); routing-tags semantic clarification.
- `storage/MODULE.md` — multi-item association table; `unrouted_source_path` field on `DocumentIndexRow`.
- `customer_adapter/MODULE.md` (not yet drafted) — Google Drive only Ph-1/Ph-2 in `customizations/`.
- `[D-050]` impl note — 5-area NSD structure simplified to `_zip_audit/` + classified + staged + outbound.

**Customer-adapter placement decided**: all flavors in `customizations/customer_adapter/<carrier_slug>_adapter.py`; Ph-1/Ph-2 = Google Drive only; Ph-3+ adds web portal + JIRA customer-portal. Protocol stays in `core/src/customer_adapter/protocol.py`.

## Next

- **Module architecture — Batch 1 complete; iterative dev↔arch cadence locked 2026-06-10 session 4**. Batch 1 design done (7 modules + 2 drop-zones; all Ph-1): `diagnostics`, `template_schema`, `storage`, `credential_service`, `sharepoint_integration`, `issue_tracker`, `llm` (Layer 0/1 fully done); `email_service`, `customer_adapter`, `rule_engine`, `workflow_engine` (Layer 2 designed); `tracker` (Layer 3 designed early); `customizations/sharepoint_config`, `customizations/rules` (drop-zone MODULE.md done). **Development of Batch 1 begins next.** **Batch 2 architecture revisit list** (will resume after Batch 1 development complete; none of these are Ph-1-scope-cut): `messenger` (Layer 2; Slack + proprietary adapter via api_spec_ingestor per `[D-016]`), `test_report` (Layer 2; FR-16/46; required when first customer with TEST_TECH_WAIVER_REPORT items onboards), `test_report_profiler` (Layer 3; producer of test_report's per-customer parsers per `[D-011]`; requires generic-vs-per-customer parser strategy flag resolution first), `api_spec_ingestor` (Layer 3; generates corp PLM adapter + proprietary messenger adapter per `[D-003]`/`[D-016]`), `dashboard` (Layer 4; PM/ops triage surfaces), `corp_messenger_gateway` (Layer 5 corp-side), `corp_plm_gateway` (Layer 5 corp-side), `customizations/issue_tracker` Structure markers (5-minute fix), `customizations/checklists` MODULE.md (drop-zone), `customizations/test_report_parsers` MODULE.md (drop-zone). `template_schema_ingestor` (Layer 3) stays Ph-2-only per DEF-15. Total HILDA-PC modules = 18; plus 2 corp-side gateway modules (`corp_messenger_gateway`, `corp_plm_gateway`) = 20 modules total. Next candidates by dependency order: Layer 2 (`workflow_engine`, `email_service` including new `sp_alert_parser` sub-module, `messenger`, `customer_adapter`, `rule_engine`, `test_report`) → Layer 3 (`tracker`, `template_schema_ingestor`, `api_spec_ingestor`, `test_report_profiler`) → Layer 4 (`dashboard`) → Layer 5 (corp-side gateways).
- **3 remaining follow-ups from the 2026-05-24 architecture review** (3 of the original 6 resolved via 2026-05-24 impl notes + D-047): (a) **IMAP IDLE / EWS streaming confirmation** from corp Exchange admin — covered as a separate Next item below; (b) **`MilestoneGating` + `ItemNumber`-stability schema decisions** with SP UI engineer — covered as a separate Next item below; (c) **`corp_messenger_gateway` + `corp_plm_gateway` MODULE.md drafts** — Layer 5 in the per-layer plan. *(Resolved on 2026-05-24: D-024 CI/CD reconciliation = impl note 2026-05-24 ✓; SP-alert email channel ADR = `[D-047]` captured 2026-05-24 ✓; `[D-021]` / `[D-026]` 3-host revision = impl notes 2026-05-24 ✓.)*
- **Resolve D-042 (Open design question)**: customer template storage location and TPM authoring path; currently customizations/template_schemas/ per FR-39/40/41 but full TPM authoring workflow + SP-side wiring still TBD.
- **Confirm with SP UI engineer** (cross-team architecture-phase action items): (1) `ItemNumber` stability — immutable for the life of a delivery item, or re-assignable when items added/removed mid-milestone? If unstable, SP-list needs an immutable `item_guid` column. (2) SP alert "Send Alerts for These Changes" setting must be configured to "Anything changes" (so all PM/TPM field edits fire alerts — required for FR-14 correctness). (3) Confirm SP-side list-field naming aligns with HILDA's canonical schema (or document the name-mapping translation layer needed in `sp_alert_parser`).
- **Confirm with corp Exchange admin**: IMAP IDLE (or EWS streaming notifications) is permitted from HILDA PC for the dedicated mailbox. If yes → IDLE primary, latency ~1–2 s. If no → short-interval polling fallback (5–10 s). Either way, FR-23 deadline-tiered schedule is the third-tier fallback only. (SYSTEM.md Open Q #12.)
- **Architecture-phase choice for Test Report Profiler (`[D-011]`)**: PDF text-extraction path (`pdfplumber` / `pypdf` / `pymupdf`) and legacy `doc` handler (`antiword` / LibreOffice headless). Decide before `test_report_profiler` MODULE.md.
- **Ratify or supersede the reference-imported sub-conventions in `structure-conventions.md`** during architecture: (a) cross-tier `core ↔ customizations` deps allowed in both directions — still pending explicit ratification ADR; (b) the two-tier `config/global.json` + `config/<module>.json` model + env-var override pattern added 2026-05-24 — anchored by `[D-025]` impl note 2026-05-26 (scope expanded from `sharepoint_config/` to all `customizations/` sub-directories); explicit ratification ADR still pending.
- **Wire NTLM/Kerberos auth handlers against a real SP 2017 instance** (deferred until corp AD lab access). Current handlers raise SHP-E004 with detail; Ph-1/Ph-2 dev path uses NoAuthHandler against the mock server.
- **Code sync before next development trip**: `core/src/template_schema/enums.py` (rename BINARY → CONFIRMATION, add TGNameRegistry) and `models.py` (remove DeliverableBase per D-028; update DeliveryItemBase, MilestoneBase, CustomerTemplateBase per D-028 + round-2 field additions + 2026-05-24 schema additions — TG-group fields, doc_count, review_required, etc.). **2026-05-26 update**: add `TGGroup` Pydantic model per `[D-051]` (one row per `(milestone_id, tg_name)` — `tg_owner_name`, `tg_owner_email`, `email_group_alias` nullable, `corp_id_list` JSON nullable, `default_cc_list` JSON nullable); update `tg_groups.yaml` schema per `[D-049]` (ODF source data).
- **Decide SP UI engineer feedback path**: original Next item (TPMs run `sharepoint_integration_cli --serve` against mock SP) is partially superseded — SP UI engineer is now prototyping on the actual corp SP (live email samples sent to HILDA team). Decide whether the mock-SP harness is still needed for TPM UX feedback, or only for HILDA-side automated tests.
- **Propagation checklist for FR/NFR changes** (new 2026-05-26): whenever `requirements.md` FRs or NFRs are added or modified, sweep derivative docs for stale references before close-session. Derivative docs currently in scope: `HILDA_Design.md` (data model + workflow stages + architecture sections), `SYSTEM.md` (communication channels + persistence + open questions), `sharepoint/REQUIREMENTS.md` (SP list columns + alert config + open items), `structure-conventions.md` (customizations/ layout), per-module `MODULE.md` files for any module affected by the FR. Pattern: open the FR/NFR text; grep derivative docs for related terms; update or flag. Apply as part of close-session daily.
- **Deferred ADR candidates from 2026-05-26 forward-drift scan** (renumbered 2026-05-28 — `D-052` was taken by dual-backend LLM ADR landed 2026-05-28): **D-053** async carrier-package assembly with progress feedback (FR-73) — will land naturally at `customer_adapter/MODULE.md` draft (Layer 3); **D-054** SharePoint UI boundary (SP UI engineer ownership vs HILDA team) — anchored implicitly in `structure-conventions.md` and `sharepoint/REQUIREMENTS.md`; standalone ADR deferred unless drift-check flags it later. Track here so the candidates don't get lost during MODULE.md drafting.

## Flags

- ~~**requirements.md naming sweep — `target_folder` → `ingress_folder` for inbound side (queued 2026-06-07; FR-77 / FR-52)**~~ *— CLOSED 2026-06-10 as no-op via requirements-phase evaluation. Premise misread on close inspection: `target_folder` is consistently used only for OUTBOUND (carrier-side) in `requirements.md`. The INBOUND side has multiple descriptive variants ("outermost source folder (NSD or zip-internal)", "outermost folder name relative to milestone root", "outermost folder name inside the zip", "outermost folder name relative to fully-extracted zip root", `<matched_source_folder>` in path templates) — each correctly specific to its narrative context per user feedback. Collapsing to generic `ingress_folder` would dilute positional context. `template_schema/MODULE.md` alignment unaffected (its `FolderRoutingEntry.ingress_folder` / `DeliveryItemBase.ingress_folder` / `DeliveryItemBase.target_folder` field names remain canonical — they're code-level field names, distinct from prose terminology in requirements.md).*

- **Storage module debt — `DocumentIndexRow.inferred_tg_name` (new 2026-06-07; FR-78 / FR-83 / [D-053])** — Default work-item is per-MILESTONE (not per-TG); decided during template_schema/MODULE.md rollback. TG of an inbound document IS knowable from the channel (NSD ingress folder via TGGroupBase.ingress_nsd; email sender via email_group_alias / owner_email lookup; PLM-id via DeliveryItemBase.plm_id reverse-lookup). When FR-52 routing pipeline cannot resolve a specific work-item, document lands on the milestone's default work-item BUT the document record must carry the resolved TG. **Action**: add `inferred_tg_name: str | None` to `DocumentIndexRow` (plus reverse-lookup helper) during `storage/MODULE.md` rollback. Consumed by FR-83 TPM-manual reassignment to shortlist candidate work-items within the resolved TG.

- **MAP.md broadly stale (2026-05-24; refreshed 2026-05-26)**: `template_schema` module Purpose line still reads "Device / Milestone / Deliverable / DeliveryItem" — Deliverable level removed by D-028. Many other modules have touched fields since the last regen — `sharepoint_integration`, `issue_tracker`, `diagnostics`, **`storage`** (new MODULE.md draft 2026-05-26 — adds new module-row to MAP.md). Run regen-map before next architecture-phase MODULE.md drafting batch.

- `structure-conventions.md` references `the reference implementation` as the live reference for layout details (cross-tier deps, config rules, Protocol-boundary pattern). If the reference implementation's conventions evolve, HILDA's `structure-conventions.md` does NOT auto-track — it's a one-time import, captured in D-001's "Why". Re-verify alignment when reviewing the conventions file.

- **[BACKLOGGED 2026-05-06 by `[D-024]`; D-024 reconciliation resolved 2026-05-24]** **CI/CD tool selection — pending corp-ops consultation.** Pipeline shape decided in `[D-024]`. Open: (a) CI runner — GitHub Actions self-hosted / GitLab CI / Jenkins / corp-specific; (b) image registry — Harbor / Artifactory / Nexus / corp-specific; (c) GitOps tool — ArgoCD / Flux / none (CI-driven `helm upgrade`); (d) environment topology. **Update 2026-05-26**: D-024 reconciliation completed via impl note 2026-05-24 (Helm pipeline pinned to Ph-3+; Ph-1/Ph-2 = basic CI + manual deploy via `deploy/scripts/deploy.sh` + sops per `[D-038]`). Tool-bound choices (a-d) **remain backlogged** pending corp-ops consultation — they apply to the Ph-3+ Helm-CD landing, not the Ph-1/Ph-2 manual deploy path which is operator-driven.

- ~~**[DRIFT-2 skipped 2026-05-14]**~~ *(resolved 2026-05-14: Ph-1 = corp PLM adapter via API Spec Ingestor; Ph-2 = CustomerJIRA adapter. DEF-6 promoted to Ph-1; D-003 impl note updated.)*

- **Per-TaskKind LLM backend A/B test gate (new 2026-05-28; updated 2026-06-07 for tri-backend per `[D-052]` Consequences)** — **4 Ph-1 TaskKinds** (was 5; `CLASSIFY_DOC_TYPE` removed 2026-05-28b per `[D-053]` — doc_type now derived 1:1 from `item.item_type`) require measured-quality A/B testing on representative real fixtures before `task_backend_map` / `task_model_map` are locked for production. TaskKinds: `ROUTE_ATTACHMENT` (FR-52 step 4), `CLASSIFY_DOC` (D-039 Step 2), `REVIEW_DOCUMENT` (FR-53), `CLASSIFY_MESSAGE` (FR-12 path c). **Three backends to test** (updated 2026-06-07): `ollama_a4000` (Ollama on RTX A4000, 16 GB GDDR6, ~448 GB/s — fastest single-stream tokens/sec for ≤14 GB models) vs `vllm_dgx` (vLLM on DGX Spark, 128 GB unified LPDDR5X, ~273 GB/s — supports models that don't fit on A4000 + continuous batching at concurrency, but per-token throughput LOWER than A4000 for fits-on-A4000 models due to bandwidth) vs `corp_llm` (corp on-prem chat + agentic APIs, rate-limited). **Model catalog**: same Ollama-format models (`qwen3:8b-q4_k_m`, `gemma3:12b`) served on both `ollama_a4000` (Ollama) and `vllm_dgx` (vLLM with HF weights) for direct comparability. If small-model A/B results unsatisfactory, larger models (`gemma3:27b`, `qwen2.5:32b`, `llama3.1:70b`) added to `vllm_dgx` catalog (DGX Spark capacity allows; A4000 cannot fit). Tentative defaults: all 4 on `ollama_a4000` until A/B run completes. **Priority**: `REVIEW_DOCUMENT` A/B run is highest-value (user observation: corp LLM empirically weak at test-case enumeration; FR-53 task shape is checklist-against-content, untested at HILDA scale). **Capacity-vs-bandwidth caveat (clarified 2026-06-07)**: DGX Spark's 128 GB unified RAM gives capacity for large models the A4000 cannot fit, but the LPDDR5X bandwidth (~273 GB/s) is LOWER than A4000's GDDR6 (~448 GB/s). For autoregressive inference of fits-on-A4000 models, A4000 typically wins per-token throughput; DGX Spark wins on capacity + vLLM continuous-batching concurrency. A/B results per TaskKind capture these tradeoffs empirically. **No automatic spillover** between backends per `[D-052]` — silent fallback would risk degraded quality without a gate; spillover surfaces as `LLG-W006`. Tracked owner: TBD.

- ~~**`[D-053]` impl note 2026-05-28b "1:1 derivation of doc_type" is incorrect (NEW 2026-06-07; HIGH priority)**~~ *— CLOSED 2026-06-08 via requirements-phase redesign session (Done 2026-06-08 entry). The corrected model is captured in FR-7 + FR-85 + FR-86 + FR-87 + amendments to FR-16/FR-52/FR-53/FR-78/FR-83. All 3 design questions resolved. MODULE.md cascade work moved to In-progress.*

- ~~**`[D-053]` ADR body needs impl note appended (NEW 2026-06-08)**~~ *— CLOSED 2026-06-08 at close-session triage. Impl note appended to `[D-053]` in DECISIONS.md capturing the full corrected model (4-value ItemType + 5-value DocType + 2-step ladder + 4-path matrix + alignment invariant + `[D-039]` skip rule + SP UI A→B→C). Per `[D-002]` append-only — original Decision/Why/Consequences bodies preserved verbatim.*

- ~~**`[D-060]` ADR body needs impl note appended (NEW 2026-06-08)**~~ *— CLOSED 2026-06-08 at close-session triage. Impl note appended to `[D-060]` in DECISIONS.md documenting the `inferred_tg_name`-in-unrouted-path extension. Original Decision/Why/Consequences preserved.*

- ~~**`[D-052]` ADR body needs impl note appended (new 2026-06-07)**~~ *— CLOSED 2026-06-10 via commit `fd78e76`. Impl note 2026-06-07 appended (dual→tri-backend; BackendConfig.name Literal expanded; hardware topology corrected; cross-refs `[D-068]` + `credential_service` SystemType split). Plus chronology marker added to 2026-05-28b impl note pointing to the 2026-06-08 `CLASSIFY_DOC_TYPE` restoration so 2026-05-28b doesn't read stale in isolation.*

- **5 `???` cells in `docs/sp_ui_engineer/HILDA_SP_Schema.xlsx` awaiting HILDA-architect confirmation (new 2026-06-10)** — (a) CommunicationLog: `milestone_id` / `tg_name` / `credential_id` / `source_system` columns requested but not explicit in `sharepoint/REQUIREMENTS.md` §2.7; marked Ph-2 pending confirmation. (b) DeliveryItems: `milestone_gating` field name (§12 #1). (c) Milestones: `close_all_triggered_at` field name (§12 #3). (d) TGGroups: `folder_routing_template_id` Ph-2 confirm. Resolve before sending Excel to SP UI engineer.

- **`customizations/issue_tracker/MODULE.md` missing Structure markers (new 2026-06-10)** — Flagged by regen-map 2026-06-10. File lacks `<!-- BEGIN:STRUCTURE -->` / `<!-- END:STRUCTURE -->` markers, so regen-map skipped Structure regeneration per skill spec. `defecttrack_adapter.py` has substantive public surface (`DefectTrackConfig`, `DefectTrackAdapter`, `make_adapter`) currently unreflected in MODULE.md. **Action**: add markers in a hand-edit so future regen-map passes include this module.

- **`customizations/sharepoint_config/` NEW dir without MODULE.md (new 2026-06-10)** — Flagged by regen-map 2026-06-10. Top-level customizations dir per structure-conventions but holds only `__init__.py` + `customers/example.yaml` (data drop-zone, no code). Two options: (a) add a stub MODULE.md documenting the per-customer SP-config drop-zone convention (data files only; no Python code); (b) carve out config-only dirs in `structure-conventions.md` so they're exempt from "every top-level customizations dir is a module." (a) is the cleaner long-term path since this is a contribution surface for ops + SP UI engineer.

- **template_schema TSC-W003 `item_description` tag-list field_validator not yet implemented (new 2026-06-10 — template-schema-v2-rewrite strand landed)** — Per template_schema/MODULE.md Invariants line 420: `item_description` should have a Pydantic `@field_validator` that splits on comma, strips whitespace, and checks each tag against a customer's tag catalog; unknown tags raise `TSC-W003` warning. The TSC-W003 error code IS registered in `core/src/template_schema/error_codes.py`, but the validator itself is NOT yet on `DeliveryItemBase`. **Why deferred**: (1) MODULE.md references "TagCatalogEntry registry" but no `TagCatalogRegistry: set[str]` exists in `core/src/template_schema/registry.py` — the registry mechanism may be forward-looking; (2) no current Python consumer of `item_description` tag-list validation — the consumer is `rule_engine`'s `PropagateTagsToActiveTrackers` action. **Trigger**: when `rule_engine` Phase 2 dev needs to consume tag lists, OR when `TagCatalogRegistry` architecture is finalized in MODULE.md. **Effort**: ~30 min once architecture clear.

- **`core/tests/test_mock_server.py` broken due to Starlette/httpx2 dep mismatch (new 2026-06-10)** — `pytest` against `test_mock_server.py` fails with `ModuleNotFoundError: No module named 'httpx2'` because Starlette's `testclient` deprecated `httpx` and requires `httpx2`. Not caused by template_schema work. **Fix**: `pip install httpx2` + update `requirements.txt` to include `httpx2` (or pin Starlette/FastAPI to older versions that still accept plain httpx). **Effort**: ~10 min. Surfaces in any pytest run across `core/tests/` without `--ignore=core/tests/test_mock_server.py`.

- **Batch 2 architecture-phase revisit after Batch 1 dev complete (new 2026-06-10 session 4)** — 10 modules in Batch 2 revisit list (see Next section). Trigger to resume: Batch 1 development phase completion. No urgency until then. Sub-flags:
  - **test_report module revisit trigger**: customer onboarding with TEST_TECH_WAIVER_REPORT items (when `review_required=True` becomes relevant). Currently `review_required=False` for all items per user deployment 2026-06-10 — workflow_engine `TRIGGER_PARSER` + `TRIGGER_AI_REVIEW` task bodies stub as no-op or NotImplementedError until test_report module lands.
  - **api_spec_ingestor module revisit trigger**: messenger OR corp PLM adapter generation needed. Until then, customer JIRA adapter (FR-25) hand-written remains the only IssueTracker adapter.
  - **`customizations/issue_tracker/MODULE.md` Structure markers** (5-min fix per regen-map 2026-06-10) — defer to Batch 2 revisit.

- **template_schema needs `pm_approval_at` + `pm_approval_pm_id` fields on DeliveryItemBase per `[D-068]` (new 2026-06-10 session 3)** — Option B PM approval storage decision requires 2 new fields on DeliveryItem. **Action**: add to `core/src/template_schema/MODULE.md` Public surface + enums + Pydantic model on next template_schema review pass.

- **HILDA_SP_Schema.xlsx + customizations/sharepoint_config need 2 new DeliveryItems columns per `[D-068]` (new 2026-06-10 session 3)** — `pm_approval_at` (DateTime) + `pm_approval_pm_id` (User type). Excel workbook needs row addition; customizations/sharepoint_config/MODULE.md example schema needs the mapping. Also need to discuss with SP UI engineer (SP column creation + alert config).

- **workflow_engine MODULE.md follow-up tasks (new 2026-06-10 session 3)** — Add `milestone.close_all_items` Celery task definition (FR-64 batch close path) + update `tasks/state.py` documentation: PMApproval action's UPDATE_STATE task body must set `pm_approval_at = now()` + `pm_approval_pm_id` BEFORE invoking tracker.update_delivery_state. Per `[D-068]` clearing discipline.

- **email_service sp_alert_parser routing-discipline table (new 2026-06-10 session 3)** — `email_service/MODULE.md` `sp_alert_parser` documentation should include explicit routing table: sub-trigger fields (owner_email/expected_completion_date/item_description) → rule_engine path via ItemModified.<sub_trigger>; all other TPM-edited fields → FR-14 manual override path via workflow_engine.apply_manual_tpm_override. Per tracker MODULE.md Invariant 2026-06-10.

- **template_schema Confirmation+no_customer_upload validator (new 2026-06-10 session 3)** — Confirmation items (`ItemType.Confirmation`) MUST have `no_customer_upload=True` per tracker invariant. Add TSC-W003 warning + Pydantic validator at template load time in `core/src/template_schema/`.

- **Excel workbook + customizations/sharepoint_config gap follow-up (new 2026-06-10 session 3)** — missing fields surfaced during SP UI engineer review: `owner_corp_id` on Users + `corp_id_list` on TGGroups + `tg_owner_name` / `tg_owner_email` / `default_cc_list` on TGGroups + `user_corp_messenger_handle` on Users. Plus SP system column reuse discipline (`Created` / `Modified` / `Author` / `Editor` not duplicated in custom columns). Defer until next sharepoint_config / Excel review pass.

- **SP UI in-focus refresh strategy (new 2026-06-10; FR-87 / `[D-064]` / sharepoint/REQUIREMENTS.md §8.1)** — SP UI engineer confirmed 2026-06-10 that no unconditional polling is needed while browser view is in focus; **focus-aware refresh** is the model. On tab/window focus-gain → re-fetch list deltas. In-focus refresh strategy (continuous interval / interaction-triggered / hybrid) is the SP UI engineer's choice. Track here in case the in-focus strategy choice has implications for FR-87 TPM-resolution UX (round-trip: SP-UI-button → SP-field-write → SP-alert email → HILDA processing → SP REST writeback → visible to TPM only on next in-focus refresh; total latency may matter for TPM UX). Revisit during SP UI engineer's prototype review.

- **`delivery_state` 11-value framing in MODULE.md test scenarios (optional, low-priority; new 2026-06-10)** — `customer_adapter/MODULE.md`, `email_service/MODULE.md`, `sharepoint_integration/MODULE.md` test-scenario sections describe an "8-state machine (Open → ... → Closed)" — this is the **happy-path traversal**, correct as far as it goes, but the canonical `delivery_state` enum is 11-value (`Not Started` + 8 active + `Delayed` + `Blocked`). `Not Started` = template-loaded by TPM in SP UI but tracking not active; `Open` = tracking started by HILDA. **Action**: consider a small follow-up pass to clarify "happy-path traversal of 8 active states within the 11-state `delivery_state` enum" in those test-scenario sections. Not strictly drift — the existing prose is technically accurate.

- **Generic-vs-per-customer test report parser strategy (new 2026-05-28; affects `[D-011]`, FR-16, FR-46)** — Current `[D-011]` scope assumes a per-customer rule-based parser generated by the Test Report Profiler from a historical corpus. User observation (2026-05-28): real-world test reports are heterogeneous across (a) file types (`xls`, `xlsx`, `pdf`, `doc`, `docx`), (b) content formats (tables, embedded images, multi-tab xlsx with or without summary tabs), and (c) structure adherence (~90% follow some template; ~10% are arbitrary feature reports, e.g. battery tests, with no standard `testcase_id` / `testcase_description` / `test_result` / `comment` columns). Building per-file-type rule-based parsers handles (a) and the structured 90% — but **fails on the unstructured 10%** and adds N parsers to maintain per file type per customer. Design principle to preserve: **HILDA must not require custom per-document logic — extraction should work generically across documents regardless of internal structure.** Likely path: LLM-augmented FR-16/FR-46 — rule-based parser for the structured majority, LLM fallback (a new `EXTRACT_TEST_CASES` TaskKind in `core/src/llm/`) for arbitrary-format reports. Revisit `[D-011]` during architecture phase before `test_report_profiler/MODULE.md` is drafted; may produce a new ADR superseding or extending `[D-011]`. Note in `llm/MODULE.md` Non-goals 2026-05-28 cross-references this Flag.
- **Staged-document fallback ladder — requirements-phase follow-up (raised 2026-05-28)**: Current FR-52 behaviour on Tier-2 LLM ambiguity is straight-to-`staged/` + PM dashboard flag. User proposed a 3-step fallback ladder before PM triage: **(a)** attempt routing to a target folder path (HILDA-PC local or customer-mapped) via a customizations-folder lookup — it's acceptable for a document to land in a folder without item-level routing being resolved; **(b)** if folder resolution also fails, reach out to the owner to determine the work item; **(c)** if owner does not respond after repeated attempts or nearing deadline, escalate visibility to TPM. This changes FR-52's failure-mode semantics + adds a new customizations surface (`customizations/folder_routing/`?) + introduces an owner-outreach branch that today lives only in FR-9/FR-10. **Action**: switch to requirements phase to walk through this carefully; expected outputs: FR-52 amendment + new `[D-XXX]` ADR + new MODULE.md surface on `email_service` `attachment_router.py` for the fallback ladder. Not blocking the 2026-05-28 80+-test-report use case (test scenario currently goes straight to `staged_pm` per current FR-52).

- **`D-052` ADR-number collision resolved 2026-05-28** — STATUS.md had reserved `D-052` and `D-053` as placeholders for deferred carrier-package-assembly (FR-73) and SP UI boundary ADRs from the 2026-05-26 forward-drift scan. Dual-backend LLM landed as `D-052` on 2026-05-28; placeholder candidates renumbered to `D-053` (carrier-package) and `D-054` (SP UI boundary) in this STATUS.md Next entry. No outstanding ADR-numbering conflicts.

- **Customer adapter browser-automation implementation (new 2026-06-05; FR-19 + `[D-054]` impl note)** — Google Drive REST API is unavailable per corp/carrier policy; FR-19 implementation uses `selenium`/`playwright` browser automation against headless Chromium. **Architecture-phase items**: (a) **selector-versioning strategy** — Google Drive UI changes will break selectors; need a deployment-time refresh mechanism (where do versioned selectors live? `customizations/customer_adapter/<carrier_slug>_adapter_config.yaml` per FR-19, but the refresh-on-UI-change procedure needs ops runbook); (b) **headless Chromium as hard ops dependency** — installed in `hilda-worker` container image OR pre-installed on HILDA PC; deploy procedure must include Chromium provisioning + version pinning; (c) **PM session management** — long-lived browser sessions per PM × carrier; session-cookie storage in `credential_service` (sops-encrypted); session-refresh / login-flow automation per carrier; (d) **Capability flags** — `getStatus` and `postComment` may be unavailable per carrier; the `CustomerAdapter` Protocol surface must accept `NotImplementedError` from these methods gracefully (e.g., dashboard surfaces "status not available for this carrier" rather than treating it as an error). To resolve when `customer_adapter/MODULE.md` is drafted (Phase B Module rollback queue).

- ~~**[DRIFT-3 skipped 2026-05-14]**~~ *(resolved 2026-05-14: D-035 impl note appended — inbound/ + revisions/ + timestamp/version-prefix convention superseded by doc_type_slug/doc_id_slug/revN/ sequential numbering per FR-13.)*

- **[BACKLOGGED 2026-05-13]** **Outbound notification format for CorporatePLM and NetworkSharedDrive modalities — TBD.** FR-9 notes "outbound format TBD architecture phase": when an owner's primary channel is PLM or shared drive, HILDA must inform them of the delivery path (PLM issue reference or shared drive path) at tracker creation and on reminders. Decide during architecture when PLM adapter and storage MODULE.md are drafted.

- **[BACKLOGGED 2026-04-30; refined 2026-05-04]** **Eval-data channel — multiple dev-time eval/correction surfaces** — PM corrections flow across multiple surfaces: test report review, tech report review, email parsing, message classification, customer-response drafts. How do these corrections feed back to improve checklists / prompts? No explicit pipeline in the design doc. Each surface will need its own correction schema and eval data path. Revisit when AI/LLM modules are designed in architecture phase.

- ~~**[BACKLOGGED 2026-04-30]** **Template Schema Ingestor input format**~~ — *resolved 2026-05-04 by `[D-018]`: three modes (schema-file YAML / row-offset / infer); `CustomerSchema` is the common output contract.*

- **[NEW 2026-05-21]** **D-042 (Open design question) — customer template storage location and TPM authoring path** is open. Captures the unresolved question of where customer templates live (currently `customizations/template_schemas/` per FR-39/40/41) and how TPMs author them end-to-end (Excel import path is deferred per DEF-15; SP UI authoring path TBD). Resolve before `template_schema_ingestor` MODULE.md and before any customer-onboarding runbook.

- **[NEW 2026-05-24; updated 2026-05-26] Architecture-phase follow-up ADRs surfaced during HILDA_Design.md / PROJECT.md / SYSTEM.md / structure-conventions.md review pass.** Status of the 6 items:
  - ~~(1) `[D-024]` reconciliation pinning Helm to Ph-3+~~ — **RESOLVED 2026-05-24** via impl note appended to D-024.
  - ~~(2) New ADR for SP-alert email channel + `sp_alert_parser` sub-module~~ — **RESOLVED 2026-05-24** as `[D-047]` captured.
  - ~~(3) `[D-021]` / `[D-026]` revision documenting actual 3-host HILDA-owned deployment surface~~ — **RESOLVED 2026-05-24** via impl notes appended to D-021 + D-026.
  - **(4) IMAP IDLE / EWS streaming confirmation from corp Exchange admin** — still pending; tracked as a separate Next item.
  - **(5) `MilestoneGating` + `ItemNumber`-stability schema decisions** — still pending; depend on SP UI engineer confirmation.
  - **(6) `corp_messenger_gateway` + `corp_plm_gateway` MODULE.md drafts** — still pending; will land at Layer 5 per the per-layer MODULE.md plan in Next.

- **[NEW 2026-05-24] SP-side configuration requirement** — SharePoint list alerts must be configured to "Send Alerts for These Changes: **Anything changes**" (not specific columns), otherwise TPM direct field edits do not fire alert emails to HILDA and FR-14 manual overrides will silently miss. To be captured in the deployment runbook.

- **[NEW 2026-05-24] STATUS.md upkeep policy** — confirmed: user will invoke `close-session` daily going forward to keep `Done` current. Until that cadence is established, manual `Done` appends will continue at the end of any working session where meaningful work landed.

- **[NEW 2026-05-26] Derivative-doc drift risk** — `drift-check` skill currently verifies `requirements.md` ↔ `DECISIONS.md` ↔ `MODULE.md` but does **not** check derivative docs like `sharepoint/REQUIREMENTS.md`, future ops runbooks, or per-module READMEs. Evidence: 2026-05-26 TG-group fields gap (`tg_owner_*`, `email_group_alias`, `corp_id_list` added to FR-2 + HILDA_Design.md §3.4 by 2026-05-24 but silently missed `sharepoint/REQUIREMENTS.md` in the 2026-05-26 initial commit `f8e9345`; surfaced by user review, fixed in commit `1c102a9`). **Mitigation**: propagation checklist added to Next section (above); apply as part of close-session daily. **Architecture-phase consideration**: expand `drift-check` to cover derivative docs as a Ph-1 task; treat each derivative doc as a "view" over `requirements.md` + `DECISIONS.md` with explicit cross-reference index (`docs/compact/derivative-docs.md` — new file, lists which derivative docs reference which FR/NFR/D-XXX). To be drafted during architecture phase.

- **[NEW 2026-05-26] D-048..D-051 are first-touch designs (forward-drift scan output)** — the 4 new ADRs captured 2026-05-26 (commit `4ae4f71`) were drafted cold from `requirements.md` FRs without an in-flight module design pass: **D-048** (FR-66 version-selection workflow), **D-049** (FR-71 ODF + TG-group field model), **D-050** (FR-72 ZIP ingestion + 5-area NSD structure), **D-051** (TGGroups SP list normalization). These are expected to be **revisited and possibly refined** when their corresponding MODULE.md drafts land (`messenger/MODULE.md` for D-048; `workflow_engine/MODULE.md` + ODF rule for D-049; `email_service/MODULE.md` + `storage/MODULE.md` extension for D-050; `tracker/MODULE.md` + SP list provisioning for D-051). Treat current versions as initial-design baselines subject to MODULE.md-driven refinement. The risk is the same as all forward-drift ADRs created without grounding in concrete module code — rationale may not survive contact with implementation reality. Mitigation: re-evaluate each as part of the corresponding MODULE.md drafting session.
