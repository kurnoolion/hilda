# Status

**Active phase**: development
**Last updated**: 2026-07-15 PM (16 commits 25bc746..9cf65ee: reconcile-sync-cascade impl + live-corp-box bugfixes (device_id scoping + natural-key dedup + params={} fix + cross-device shared-file router split) + Ph-1 UX polish (view-docs download button + friendly outreach headers) + 3-phase serialization cascade (import Not Started→Open + PM approval HILDA-driven RFS writeback + Confirmation 2-hop close + Default WI auto-close sweep + scheduled TPM DRR closure email with Excel attachment) + 2 SP onboarding tools (list_sp_columns.py + create_sp_rows_from_excel.py))
**Last drift-check**: 2026-07-02 — mode: dev-full — 8 drifts resolved (all 8 via [a] update-design-to-match-code: DRIFT-1 template_schema `template_lookup` module + public API added to Rollback log per D-141; DRIFT-2 workflow_engine 4 new task bindings + bootstrap changes added to Rollback log per D-139/D-140/D-141/D-142/D-143; DRIFT-3 rule_engine 4 new ActionKinds + RUL-W006 evaluator refinement added to Rollback log; DRIFT-4 credential_service JsonFileCredentialService added to new 2026-07-02 update section; DRIFT-5 storage sp_id column + helpers added to Rollback log per D-141; DRIFT-6 tracker Guard 4 trust-list + Pattern A boundary added to Rollback log per D-137/D-138/D-139/D-140; DRIFT-7 email_service parser Milestones-trigger promotion + D-143 stance added to Rollback log; DRIFT-8 customer_adapter customer_delivery_info denormalization + ADAPTER_FACTORY convention added to Rollback log per D-141), 0 skipped, 0 deferred surfaced. All drifts were documentation-lag flavor (docs behind code + just-ratified D-139..D-143); resolved by adding targeted 2026-07-02 rollback-log entries pointing at the ADRs + commit anchors. Full Public surface / Invariants / Sub-modules curated-section refresh deferred to a dedicated MODULE.md cascade session (est. 1-2 hours; tracked as STATUS Flag). Prior 2026-06-27 — mode: dev-full — 5 drifts resolved (all 5 via [a] update-design-to-match-code: DRIFT-1 MAP.md workflow_engine 10/18→16/18 per today's wire-up; DRIFT-2 template_schema/MODULE.md customer_delivery_modality relocation per D-126 Q2; DRIFT-3 template_schema/MODULE.md DeliveryState enum values PascalCase+space per D-124 α; DRIFT-4 email_service/MODULE.md state-comment values per D-124 α; DRIFT-5 tracker/MODULE.md DeliveryState enum values per D-124 α), 0 skipped, 1 deferred-honored (ops_alerts not yet in Depends-on lists of the 10 modules that will wire emit_alert call sites — tracked as next-session menu item A). Prior 2026-06-25 — mode: design — 7 drifts resolved (all 7 via [a] update-requirements-to-match-DECISIONS: DRIFT-1/2/3 D-104 Projects per-customer FR-2/FR-40/FR-77; DRIFT-4 D-116 FR-19 thin-wrapper Ph-1 framing; DRIFT-5 D-094 SUPERSEDED FR-58 item_type PascalCase cascade ~50 occurrences; DRIFT-6 D-098 FR-26 hash-match dropped; DRIFT-7 D-098 FR-25 IssueTracker.getAttachmentHash removed from Protocol surface), 0 skipped, 2 deferred-honored (D-115 dashboard-local auth + D-112 PauseStateLookup; neither referenced in requirements.md). Prior 2026-06-21 — mode: design — 0 drifts resolved, 1 deferred-on-confirm (D-094..D-107 ADR capture; rationale already in STATUS Done entries + commit messages — batch ratification deferred to dedicated triage session per close-session rule "never invent decision rationale"), 2 deferred-already-tracked (D5 FR-31 per-item scope for rule_engine Module #7; FR-52 5-step pipeline for email_service Module #12). Design layer is **remarkably clean** post-cascade — 10/12 candidates `[ALIGNED]`, no architectural drift. Prior 2026-06-20 — mode: requirements — 0 drifts resolved, 0 skipped, 2 deferred items surfaced (already in Flags: milestones xlsx corrections + D-DRAFT ADR candidates). Session was itself a 28-FR deep-scan with edits applied inline at architect's direction; spec text matches intent across the post-[D-083]/[D-085]/[D-088]/[D-091] cascade. Prior 2026-06-16 run: mode: requirements — 3 drifts resolved [DRIFT-1 option (a) Appendix A `owner × milestone` → `(device, milestone, owner)`; DRIFT-4 option (a) FR-7 NetworkSharedDrive `inbound/` → flat per-TG NSD ingress per FR-13; DRIFT-6 option (a) FR-13 summary table `inbound/, outbound/` → locked `_outbound/, _staged_revision/, _zip_audit/` paths + underscore-prefix convention], 3 skipped (DRIFT-2 FR-79 owner_email → owner_corp_id, DRIFT-3 FR-64 `Milestones.` prefix on closed_all_items_triggered_at, DRIFT-5 FR-74 inbound/ final-sweep wording — deferred per user to upcoming FR-79/FR-64/FR-74 comprehensive reviews). Prior 2026-06-10 run: mode: design — 4 resolved, 0 skipped.
**Last design review**: 2026-05-23 → 2026-05-26 — HILDA_Design.md + PROJECT.md + SYSTEM.md + structure-conventions.md + phases/*.md + DECISIONS.md + sharepoint/REQUIREMENTS.md (new) + STATUS.md user-led section-by-section review. structure-conventions.md updates: §intro Temporal → Celery + K8s → Docker Compose Ph-1/Ph-2 / MicroK8s Ph-3+; top-level layout expanded with full `customizations/` YAML sub-structure (`sharepoint_config/`, `template_schemas/`, `rules/`, `test_report_parsers/`, etc.), `config/global.json` added with env-var override pattern documented, full `deploy/` directory tree (compose, secrets, scripts, charts, grafana, prometheus, Dockerfile); new "SharePoint UI boundary" subsection documenting SP UI engineer vs HILDA team ownership split and the shared-contract flow; SharePointBackend bullet dropped stale "document library paths" + added SP-alert config; CredentialBackend bullet sops Ph-1/Ph-2 + Vault Ph-3+ (Sealed Secrets removed); sync-API wrapping section added two-stage IO paragraph for corp_messenger_gateway / corp_plm_gateway model; Temporal → Celery in UI-blocking section; IssueTracker/Messenger Protocol bullets updated to reflect gateway-PC routing; Config format section rewritten with global.json + env-var override pattern + runtime-data clarification. HILDA_Design.md: 32 action items applied (§1, §3.3, §3.4 incl. new §3.5, §4.1, §4.2, §5 Stage 4, §6.1, §6.2, §6.3, §7.1, §7.4, §10, §11, §12, §13). PROJECT.md: In-scope Ph-1 blob split into Ph-1/Ph-2/Ph-3+ (CI/CD pipeline, K8s, HashiCorp Vault all placed in Ph-3+); out-of-scope DEF list reconciled with `requirements.md ## Deferred`; per-PM credentials constraint phase-scoped; runtime LLM K8s reference corrected; success-criteria NFR-9 reference removed; Users line + Contributors table phase-scoped. SYSTEM.md: §1 three-pillar topology (SP entity rows + NSD documents + containerized services) with YAML config in boundary (Deliverables removed); §2 four-workload split with §2.1 module roster (now 20 modules — added `corp_messenger_gateway` and `corp_plm_gateway` as corp-side HILDA-team-owned modules); §3 fully rewritten with corrected network boundary model (corp/lab firewall: HILDA-PC outbound to corp allowed; corp inbound to HILDA-PC blocked unconditionally) and two corp-side intake PCs (reverse-proxy PC + PLM gateway PC); §3.1 replaced wrong on-prem-vs-outside-premises framing with corrected unified model — **SP → HILDA channel is SP alerts → email → HILDA mailbox via SP's built-in alert feature on the deliverable list; `sp_alert_parser` sub-module added to `email_service` (module 11); IMAP IDLE primary / short-interval polling fallback / FR-23 deadline-tiered as third-tier fallback only**; SP alert email format documented from sample (subject `Alert_<List>_<Suffix> - <ItemTitle>`, action verb in sub-header, key:value body, routing key `(ProjectID, MinorMilestone, ItemNumber)`); topology Mermaid diagram redrawn to show corp/lab zones + intake PCs; Conflicts table updated (C3/C5 broader deployment surface; new C6 for SP-alert channel); §5 deployment topology now shows 3 HILDA-owned hosts (HILDA PC + reverse-proxy PC + PLM gateway PC); §4 heading TBD cleaned; §5/§7/§8/§9 Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+ rename; hilda-llm-gateway role clarified (runtime inference + code-gen); hilda-api NSD download corrected (was "SP-mediated"); §8 phase-split CI-only (Ph-1/Ph-2) vs full CI/CD (Ph-3+); Open Questions updated: #10 resolved (SP-alert channel), new #11 (D-021/D-026 revision for 3-host surface), #12 (IMAP IDLE Exchange admin check), #13 (MilestoneGating schema gap), #14 (ItemNumber stability), #15 (SP alert "Anything changes" setting), #16 (D-024 reconciliation). 3 new ADRs (D-044 doc boundary; D-045 schema/content boundary; D-046 canonical schema source). FR-47 promoted resolution_path to formal enum. **Follow-up D-XXX needed** (architecture phase): (a) `[D-024]` CI/CD shape reconciliation pinning Helm to Ph-3+; (b) new ADR for SP-alert email channel + `sp_alert_parser` sub-module; (c) `[D-021]` / `[D-026]` revision documenting 3-host HILDA-owned deployment surface; (d) IMAP IDLE / EWS streaming confirmation from Exchange admin; (e) MilestoneGating + ItemNumber-stability schema decisions; (f) `corp_messenger_gateway` and `corp_plm_gateway` MODULE.md drafts. **2026-05-26 extension**: sharepoint/REQUIREMENTS.md created (commit `f8e9345`) and amended (commit `1c102a9` — added TGGroups SP list §2.8 closing TG-group fields propagation gap); `core/src/storage/MODULE.md` initial draft committed (`442400e`) with partial section-by-section review (Purpose narrowed to FR-31 authoritative; hilda-svc defined as AD service account; Ph-1/Ph-3+ → Ph-aware in Deferred); `phases/development.md` review completed; **DECISIONS.md bidirectional audit closed** — backward sweep (21 impl notes appended across 21 ADRs, commit `a6d66b1`) + forward sweep (4 new ADRs D-048/D-049/D-050/D-051 captured, commit `4ae4f71`); follow-ups (a) (b) (c) above all resolved via the 2026-05-24 impl notes; (d) (e) (f) remain pending. Plus propagation discipline added (STATUS.md Next + Flags) to prevent future derivative-doc gaps.

## Done

- 2026-04-30 Project initialized via `/project-init` (greenfield with design-input preflight; `HILDA_Design.md` imported into `docs/compact/design-inputs/`)
- 2026-04-30 D-001 captured (three-tier code organization: `core/` + `customizations/` + `config/`); `structure-conventions.md` updated to mirror `the reference implementation`'s layout (refs: D-001)
- 2026-04-30 D-002 captured (chat-mediated collaboration: stable error codes + compact RPT/MET/FIX/QC reports; no-proprietary-content hard invariant); `structure-conventions.md` Chat-mediated collaboration section added; PROJECT.md Constraints + all three phase prompts wired (refs: D-002)
- 2026-04-30 D-003 captured (adapter pattern for proprietary systems: intermediate primitives + on-prem code-generated adapters; dev LLM never reads proprietary specs); Ph-1 issue tracking = Jira via IssueTracker Protocol; structure-conventions.md Protocol boundaries refined; PROJECT.md three-tier LLM access model added; phases/architecture.md + development.md wired with hard invariants (refs: D-003)
- 2026-04-30 D-004 captured (SharePoint integration split: standard API mechanics in core, deployment-specific config in customizations); structure-conventions.md + phases updated (refs: D-004)
- 2026-04-30 D-005 captured (every module independently testable through an appropriate interface: CLI for functional, mock web harness for UI; --mock/--dry-run for side-effect modules; --diagnostic emits compact reports per D-002); structure-conventions.md Module definition + Module doc schema updated; PROJECT.md Constraints + phases/architecture.md + development.md + requirements.md TODO comment wired (refs: D-005)
- 2026-04-30 D-006 captured (SharePoint REST API + on-prem AD auth, NTLM/Kerberos; supersedes Microsoft Graph references in HILDA_Design.md); resolves PROJECT.md Open question #1 (refs: D-006)
- 2026-04-30 D-007 captured (all LLM hosting on-premises — both runtime LLM and API Spec Ingestor LLM); resolves PROJECT.md Open question #2 (refs: D-007)
- 2026-04-30 D-008 captured (IssueTracker intermediate-primitive Protocol: async-native over sync proprietary APIs; ITR- error code prefix; full method surface + Pydantic data classes); structure-conventions.md sync-API wrapping convention added; resolves PROJECT.md Intermediate-primitive Protocol design Open question (refs: D-008)
- 2026-04-30 D-009 captured (Messenger intermediate-primitive Protocol: async-native over sync proprietary APIs; MSG- error code prefix; full method surface + Pydantic data classes); shares AttachmentInput with IssueTracker (refs: D-009)
- 2026-04-30 D-010 captured (Excel/Template Schema Ingestor — proprietary customer-template schemas processed on-prem; parallel pattern to D-003 API Spec Ingestor; dev LLM never reads proprietary template schemas); PROJECT.md three-tier LLM access model + Constraints updated; partially resolves customer-template-authoring Open question (refs: D-010)
- 2026-04-30 5 unresolved Open questions backlogged from PROJECT.md to STATUS.md Flags (eval-data channel; customer template authoring path normalization; browser-automation versioning; API Spec Ingestor input format; Ph-1 messenger choice). New backlog entry added: Template Schema Ingestor input format (refs: D-010).
- 2026-05-01 `requirements.md` initial Ph-1 draft populated from `HILDA_Design.md` + decisions D-001..D-010: 45 FR, 15 NFR, 13 Deferred items grouped by subsystem.
- 2026-05-01 Requirements redlines applied per session feedback: FR-9 (BATCH-id email consolidation), FR-12 (three-path inbound routing — structured block / `mailto:` tap-links / PM triage; idempotency on `(BATCH-id, item-index, status)`), FR-13 (shared network drive `\\share\hilda\...` supersedes SharePoint Document Libraries), FR-16/17 (parser + classifier driven by D-011), FR-18 (submission package read from NW drive), NFR-8 (SP scope = Lists + classic web parts only), NFR-12/13 (extended to D-011); new FR-46 (final|interim classification rule), FR-47 (failed-without-waiver dashboard surface), FR-48 (auto-create Waiver DeliveryItem; TPM is not waiver-path authority), NFR-16 (NW drive read/write boundary — `hilda-svc` writes only, HILDA-mediated reads only).
- 2026-05-01 D-011 captured (Test Report Document Profiler — proprietary historical test reports across Excel/Word/PDF processed on-prem; parallel pattern to D-003 + D-010; canonical classification rule owned in `core/src/test_report/`); structure-conventions.md Ingestor section generalized to "Ingestor / Profiler pattern — three modules"; PROJECT.md three-tier LLM access model updated; PMs / TPMs (Technical Project Managers) terminology clarified in Contributors table (refs: D-011).
- 2026-05-01 Reclassified runtime vs dev/test invariants per session feedback: FR-43 → NFR-17 (compact-reports invariant); FR-44 → NFR-18 (error-codes invariant); FR-45 split into FR-49 (runtime `--diagnostic` for ops + RPT emission) and NFR-19 (dev/test `--mock` / `--dry-run` / mock web harness); section "Audit & chat-mediated collaboration" renamed "Audit & runtime diagnostics".
- 2026-05-01 D-012 captured (Multi-item email status updates — three-path design: structured reply block / per-item `mailto:` tap-links / PM manual triage; idempotent on `(BATCH-id, item-index, status)`; outbound multipart/alternative, ASCII-only structured block; authority for FR-9, FR-12).
- 2026-05-01 D-013 captured (Shared network drive — single `hilda-svc` AD service account writes, HILDA-mediated reads via `https://hilda.corp/dl/<token>`, no per-customer AD groups in Ph-1; path convention `\\share\hilda\<customer>\<device>\<milestone>\<deliverable>\<item>\` with `inbound/`/`outbound/`/`revisions/`; supersedes SharePoint Document Libraries; authority for FR-13, FR-17, FR-18, NFR-8, NFR-16).
- 2026-05-04 D-014 captured (customer template authoring: two separately maintained paths — SharePoint UI + Excel upload — TPM-selectable; no normalization in Ph-1; both produce identical internal data model representations); FR-39 updated to anchor `[D-014]`; resolves backlogged Flag "Customer template authoring path normalization" (refs: D-014).
- 2026-05-04 D-015 captured (API Spec Ingestor input format: OpenAPI 3.x canonical + on-prem LLM preprocessing pass converts other formats first; `spec_normalizer.py` sub-module within the Ingestor); resolves backlogged Flag "API Spec Ingestor input format" (refs: D-015).
- 2026-05-04 D-016 captured (Ph-1 messenger targets: Slack + proprietary internal messenger; both wired through `Messenger` Protocol `[D-009]`; Slack chosen over Teams for setup/unit-test simplicity; proprietary adapter exercises API Spec Ingestor end-to-end in Ph-1); FR-50 added (Messenger adapter Ph-1 targets); DEF-5 + DEF-6 revisit triggers updated; resolves backlogged Flag "Ph-1 messenger choice" (refs: D-016).
- 2026-05-04 Flags triage: browser-automation confirmed out of Ph-1 (already DEF-10, closed); eval-data flag refined — multiple eval/correction surfaces during dev time (test report review, tech report review, email parsing, etc.), details deferred to architecture.
- 2026-05-04 PROJECT.md TODOs filled from `HILDA_Design.md` §1 §2 §13 §15: one-line, Problem, Users, In scope Ph-1 (10 bullet items), Out of scope (9 items, all mapped to DEF-N), Success criteria (7 metrics). Requirements phase exit criteria met.
- 2026-05-04 Architecture phase started. Module list approved: 18 core modules across 6 dependency layers + 4 customization families. Sequenced by dependency order + TPM feedback priority.
- 2026-05-04 D-017 captured (central diagnostics module at `core/src/diagnostics/` — standalone leaf node, owns error_codes.py registry + compact report schemas + QC template base class; 18 module prefixes pre-registered). `core/src/diagnostics/MODULE.md` drafted (refs: D-017).
- 2026-05-04 D-018 captured (Template Schema Ingestor three-mode input format: schema-file YAML / row-offset + LLM column resolution / full LLM infer; recommended workflow: infer-once → commit schema.yaml → schema-file for production; resolves backlogged Flag "Template Schema Ingestor input format"). `core/src/template_schema/MODULE.md` drafted — Layer 0 complete (2/18) (refs: D-018).
- 2026-05-04 D-019 captured (credential_service Ph-1 = K8s Secrets / ops-provisioned; stable `get_credential` interface preserved for Ph-3+ Vault backend swap; FR-32–FR-38 deferred → DEF-14; FR-51 added for Ph-1; PROJECT.md In scope Ph-1 updated). (refs: D-019)
- 2026-05-04 D-020 captured (sharepoint_integration: SpClient / SharePointListProvider Protocol separation; FileBasedListProvider boilerplate in core/; list_crud.py sole compositor; the reference implementation 3-tier config for operational values; customizations/sharepoint_config/ for business config). `core/src/sharepoint_integration/MODULE.md` drafted — Layer 1: 1/3 (3/18 overall) (refs: D-020).
- 2026-05-05 Project plumbing landed: requirements.txt (pydantic, httpx, fastapi, uvicorn, pyyaml, pytest, pytest-asyncio), pyproject.toml with pytest config, .gitignore, package `__init__.py` skeleton for `core/`, `core/src/`, `core/tests/`, `customizations/`, `customizations/sharepoint_config/`, `config/sharepoint_integration.json` default.
- 2026-05-05 `diagnostics` module implemented + 27 unit tests passing: ErrorCode + 18-prefix PREFIX_REGISTRY + PipelineError (context as explicit dict to avoid kwarg collision), ReportRecord/ReportWriter for RPT/MET/FIX/QC compact reports with bool/int/float/str field support and round-trip parser, QCField + QCTemplate enforcing no-free-text invariant (str only via enum), CLI with `--diagnostic` / `--validate`. NFR-17, NFR-18, [D-002] enforced by construction.
- 2026-05-05 `template_schema` module implemented + 24 unit tests passing: enums (DeliveryState/ItemType/TrackingModality/CustomerDeliveryModality/MilestoneStatus/RuleScope/RuleActionType/TestReportItemStatus/TestReportClassification), extensibility registries (mutable sets seeded from enums for FR-7/NFR-14), slug helpers (validate_slug + make_slug per [D-013]), Pydantic entity bases (Device/Milestone/Deliverable/DeliveryItem/CustomerTemplate/AutomationRule), CustomerSchema with YAML round-trip per [D-018], CLI with `--diagnostic` / `--validate`. TSC-E001..TSC-W002 error codes registered.
- 2026-05-05 `sharepoint_integration` core implemented + 28 unit tests passing: GlobalSharePointConfig (3-tier: CLI > env > file; password redacted in repr/str), ListScope dataclass, _AuthHandler protocol with NoAuth/NTLM/Kerberos handlers (NoAuth active for Ph-1 dev; NTLM/Kerberos surface SHP-E004 with detail until corp AD lab is wired), SpClient (async SP REST with retry on 429/503, 401→SHP-E004, 4xx→SHP-E001, list-name single-quote escaping, OData $select/$filter/$top + pagination), SharePointListProvider Protocol + FileBasedListProvider (3-tier scope precedence: device override → customer config → SHP-E002), SpCrud compositor (canonical→SP column translation on write, SP→canonical on read, $filter translation). Tests use httpx.MockTransport.
- 2026-05-05 Mock SharePoint server + CLI implemented + 22 new tests passing (full suite 101): FastAPI app with two surfaces over shared InMemoryStore — SP 2017 REST endpoints (GET/POST/PATCH/DELETE; tiny OData eq evaluator) and HTML browser UI (/, /lists/&lt;name&gt;, /audit) — designed extensibly so dashboard mock can plug onto the same app object later (TPM use case). CLI: `--diagnostic` / `--mock` (in-process via httpx.ASGITransport) / `--dry-run --customer` / `--serve --port` (uvicorn for TPM browser). Sample example.yaml customer config seeded.
- 2026-05-06 System-architecture phase opened: drafted `docs/compact/SYSTEM.md` (peer of PROJECT.md / structure-conventions.md) covering process topology, inter-component comms, persistence, deployment, observability, secrets flow, CI/CD, networking. Logged 4 conflicts with HILDA_Design.md (Graph→REST `[D-006]`, Vault→K8s-Secrets `[D-019]`, microservices→TBD, Temporal→TBD); first two pre-resolved.
- 2026-05-06 D-021 captured (process granularity Ph-1 = modular monolith, one image, three K8s Deployments: `hilda-api` / `hilda-worker` / `hilda-llm-gateway`; all 18 modules in-process; per-customer adapter pods deferred to customer 2; HILDA_Design.md §11's 12-pod microservices inventory preserved as Ph-3++ target). Resolves SYSTEM.md §2 + conflict C3 (refs: D-021).
- 2026-05-06 D-022 captured (workflow engine Ph-1 = Celery + Redis broker + Postgres result backend; `hilda-beat` singleton Deployment loads schedule from SP `AutomationRules`; `core/src/workflow_engine/` owns Celery app + dispatcher, `core/src/rule_engine/` stays a pure evaluator; HILDA_Design.md's Temporal StatefulSet removed from Ph-1, deferred to Ph-3+ if multi-step durable orchestration emerges). Resolves SYSTEM.md §4 + conflict C4 (refs: D-022).
- 2026-05-06 D-023 captured (observability Ph-1 = light stack, zero HILDA-owned pods; structured JSON stdout logs + `/metrics` Prometheus endpoint per pod + compact reports per [D-002]; dashboards/alerts as code in `deploy/grafana/dashboards/` and `deploy/prometheus/alerts/`; no HILDA-owned Grafana/Loki/Tempo; distributed tracing deferred). Resolves SYSTEM.md §6 + Open Question #3 (refs: D-023).
- 2026-05-06 D-024 captured (CI/CD shape Ph-1: tool-agnostic pipeline (PR → tests + build + scan; merge → tag SHA + push + deploy test + smoke; manual prod promotion with semver re-tag); single umbrella Helm chart at `deploy/charts/hilda/` with per-env values files; mock SP runs as separate test-env Deployment; specific corp-tool selections — CI runner / image registry / GitOps / env topology — remain backlogged Flag). Resolves SYSTEM.md §8 shape + Open Question #5 (refs: D-024).

- 2026-05-08 Teacher/student LLM scaffold ported from `the reference implementation`: `.clinerules/` (4 files — project context, role, content-safety with 11 HILDA redaction categories, output discipline with 9 report types) + `cline-playbooks/` (8 playbooks: orient, mapping, sp-connect, ingest-template, profile-test-report, ingest-api-spec, debug-pipeline, share-back). Cline (on-prem student) runs HILDA CLIs against real SP and proprietary data; Teacher LLM (Claude) designs + codes; redacted compact reports bridge the air-gap.
- 2026-05-08 Ph-1 deployment platform pivot: bare-metal Linux PC + Docker Compose (not K8s). D-025 captured (customer YAML mount: Ph-1 bind-mount, Ph-3+ ConfigMap). D-026 captured (deployment platform Ph-1 = Docker Compose; same process boundaries, container image, task architecture; K8s is Ph-3++ target). SYSTEM.md fully updated (C5 added to conflicts table; §2, §5, §7, §8, §9, open questions all revised). PROJECT.md Infrastructure + Credential Service bullets updated.
- 2026-05-08 Cline git workflow clarified: one-way bridge — origin (github.com) is read-only from work PC; Cline pushes only to `company`; Teacher sees Cline's work via compact reports (hand-typed) only, never via git. `.clinerules/01-role.md` updated; `utils/git-sync/sync-work.sh` corrected (reverted erroneous push-to-origin step). D-027 captured.
- 2026-05-08 `issue_tracker` module designed + implemented end-to-end
- 2026-05-12 Data model review session (round 2) — `HILDA_Design.md` updated with: `tg_name` marked registry-controlled (`TGNameRegistry`); `plm_id` added to DeliveryItems (PLM system ID, permanent source of truth, dynamic); `actual_item_info` description updated to HILDA-mediated download URL per `[D-013]`; form factor flags added to CustomerTemplates (`handset`, `tablet`, `wearable`, `mr`, `hmr_smr`, Boolean). `template_schema/MODULE.md` updated (TGNameRegistry, DeliveryItemBase.plm_id, CustomerTemplateBase form factors). Pending code sync extended to include these fields.
- 2026-05-12 Data model review session (round 1) — `HILDA_Design.md` updated with: (a) D-028 captured: Deliverable level removed from hierarchy; DeliveryItems now parent directly to Milestone, grouped by `tg_name`; (b) `ItemType.Binary` → `Confirmation (Yes/No)` (owner-reply-closes, no artifact); (c) new DeliveryItem fields: `item_no`, `tg_name`, `owner_status_note`, `actual_completion_date`; (d) new Milestone field: `email_cc_list` per-milestone CC distribution list; (e) `Deliverables` SP List removed from §3.4. `requirements.md` updated (FR-2, FR-5, FR-6, FR-9, FR-13, FR-22, FR-40). `core/src/template_schema/MODULE.md` updated (Purpose, enums, MilestoneBase, DeliverableBase removed, DeliveryItemBase, EntitySchemaConfig). **Pending**: `core/src/template_schema/enums.py` and `models.py` code sync — required before next development trip.
- 2026-05-12 Requirements phase session — switched active phase to requirements. FR-4 deferred → DEF-15 (Excel import schema validation, phase TBD). Clarified terminology: "Stage" = workflow stages 1–6 (§5 HILDA_Design.md, already used in requirements.md section headers); "Phase" = implementation rollout phases 1–4 (§13 HILDA_Design.md). Further requirements review in progress.
- 2026-05-12 Requirements phase session (continued) — completed inline `[Ph-1]`/`[Ph-2]` tagging on all active FRs; FR-1 path (b) and FR-39 path (b) Excel flows deferred to Ph-2 (Template Schema Ingestor is Ph-2, consistent with DEF-15); FR-9/FR-12 per-item `mailto:` tap-links deferred to Ph-2 (D-012 implementation note updated); FR-52 added (Ph-1: runtime LLM attachment-to-item routing via first-page content inspection); FR-53 added (Ph-1: runtime LLM initial quality review for test reports, tech reports, and waivers); DEF-2 partially promoted to FR-53 (PM feedback workflow remains deferred); runtime LLM Ph-1 scope established (FR-52 + FR-53 functions only — DEF-1, DEF-3, DEF-4 remain deferred).
- 2026-05-13 Requirements phase session — DEF-1 (free-text message classification) promoted to Ph-1: FR-12 path (c) upgraded from direct-PM-triage to rule-based-first → runtime LLM fallback (Option B); FR-52 attachment routing upgraded from LLM-first to two-tier (fuzzy match → LLM fallback, scale context: ~100 reports per milestone); fusion design: when path (c) fires with attachments, single fused LLM call covers both message classification and attachment routing; DEF-1 struck through (promoted); D-029 implementation note added; D-033 captured (two-tier attachment routing); D-034 captured (fused LLM call for path (c) + attachments); sender email attribution added to FR-12 and FR-24.
- 2026-05-13 Requirements phase session (continued) — FR-13 clarifications: 4 inbound tracking modalities confirmed (Email: status+attachments; Corp Messenger: status only; Corp PLM: documents only; API: status/closure only); corp PLM = source of truth for documents; FR-2 updated (PLM issue created per owner×milestone at tracker creation); FR-5 updated (plm_id invariant); FR-10 updated (cross-channel messenger escalation when email goes unanswered); FR-13 major update (drop folder, polling, PLM auto-upload); FR-25 updated (IssueTracker dual role); FR-26 rewritten (PLM document-only, one issue per owner×milestone, InternalJira status path separated); FR-54 added (inbound messenger processing — Ph-1 PM triage / Ph-2 LLM classify); FR-55 added (direct drop-folder monitoring); D-035 captured (PLM source of truth).
- 2026-05-13 Requirements phase session (continued 3) — tracking_modality confirmed multi-value (5 values: Email, CorporateMessenger, CorporatePLM, NetworkSharedDrive, CustomerJIRA); FR-7 updated (all values + valid combinations enumerated); FR-9 updated (per-modality outreach behaviour); FR-13 updated (PLM pull vs push direction; carrier/device/milestone/item path confirmed; revisions/ handling explicit); FR-25/FR-26 corrected (internal Jira removed entirely; Jira = customer-facing CustomerJIRA closure polling only); FR-55 scoped to NetworkSharedDrive modality; D-035 implementation note updated.
- 2026-05-13 DEF-1 promoted to Ph-1: FR-12 path (c) rule-based + LLM fallback; fused with FR-52. D-033 (two-tier attachment routing), D-034 (fused LLM call for path c + attachments), D-035 (PLM source of truth), D-036 (sender attribution on inbound emails), D-037 (multi-value tracking_modality) captured.
- 2026-05-13 drift-check requirements (5 drifts resolved): FR-46 promoted Ph-2 → Ph-1 (classifier needed by FR-16 in Ph-1); FR-50 promoted Ph-2 → Ph-1; FR-54 demoted to Ph-2 entirely; NFR-15 rewritten (Temporal Workers removed; hilda-worker substituted; HA replicas scoped to Ph-3+); NFR-4 rewritten (sops/age AES-256-GCM at rest; mTLS deferred to Ph-3+); FR-51 updated (K8s Secrets → sops-encrypted .env). D-038 captured (sops/age Ph-1/Ph-2 secrets encryption at rest). D-033 implementation note: scope extended to PLM poll source.
- 2026-05-14 NSD path convention rewritten — `doc_type_slug/doc_id_slug/revN/` supersedes deliverable_slug + `revisions/` (D-013 implementation note added). PLM established as source of truth for documents. FR-13 rewritten end-to-end. drift-check requirements (3 resolved, 2 surfaced as Flags). DEF-6 promoted to Ph-1 (corp PLM IssueTracker adapter, generated by API Spec Ingestor alongside proprietary messenger adapter); D-003 impl note added. FR-2/FR-26 Ph-1 confirmed.
- 2026-05-14 SharePoint UI section added — FR-56 (milestone view + Start Collection action), FR-57 (HILDA document enumeration API), FR-58 (item_type discriminator), FR-59 (document rows grouped by doc_type), FR-60 (parser + LLM review results per revision), FR-61 (HILDA-mediated download endpoint), FR-62 (Ph-2 PM/TPM upload surface). Rule engine taxonomy expanded — FR-28 full trigger list (11 triggers); FR-29 actions including TriggerParser, NotifyPM, StartItemCollection.
- 2026-05-15 Major FR overhaul + D-039 captured (4-step classification pipeline: hash → slug → Tier-2 LLM → staged/ holding). delivery_state 10-state enum formalized in FR-7; doc_type enum (test_report / tech_report / waiver); document index 13-field schema. FR-63 Submit to Carrier, FR-64 Close All Items, FR-65 Send Reminder SP UI actions added. requirements.md Appendices A (rule engine triggers + state machine), B (FRs requiring LLM calls), C (phase scope index) added. Cleanup: ~/work/nora → "the reference implementation" across PROJECT.md, phases/, structure-conventions.md.
- 2026-05-17 NSD/PLM source-of-truth split formalized: D-040 (NSD classified path = in-progress source of truth; PLM = submitted-only); D-041 (submission assembly source = NSD classified path in both phases). FR-66 (corp messenger version-selection workflow), FR-67 (PLM stale attachment deletion), FR-68 (PLM-NSD sync verification post-dispatch), FR-69 (carrier `portal_structure.yaml`), FR-70 (item_completion_pct computed field) added. FR-7 OwnerClosed reduced from 3 to 2 guard conditions (doc_count + all reviews; owner-done is trigger, not third condition). PendingPMReview → UnderPMReview rename across all FRs. Multiple FR-7/13/55 corrections for clarity (Confirmation item shortcut, ingest channel funneling, PLM no-re-upload, original_filename vs doc_id_slug distinction, download_url field, ACL deferral to DEF-16).
- 2026-05-18 FR-48 waiver auto-create removed (waivers are owner-created; HILDA only tracks waiver_ref). FR-47 [Ph-2] resolution-path = PM annotation only; DEF-17 (per-path automation deferred to Ph-3+). D-039 Step 2 short-circuit added for NEW_DOCUMENT case (no LLM call when no prior doc_id_slug exists for the item × doc_type pair). FR-53 deferral clause [Ph-2] tag added.
- 2026-05-19 DEF-18 (per-DeliveryItem ACL on download endpoint, deferred to Ph-3+). FR-61 clarified as direct browser → NSD stream (distinct from FR-18 batch assembly download to HILDA PC). FR-13 ingest_source enum formalized (Email / CorporatePLM / NetworkSharedDrive / SharePointUI). FR-18/19/20/22/63/64 moved to Ph-1. State machine redesign: UnderPMReview placed AFTER OwnerClosed (transient fork). FR-21 / FR-22 / FR-64 deferred via DEF-19 (carrier feedback automation) + DEF-20 (automated Closed transition). FR-62/63 rewritten for delta re-submission. FR-69 schema adds file_overwrite_policy + revision_file_suffix_pattern. FR-71 [Ph-2] Owner Discovery Function added; tg_owner + email_cc_list (per-milestone + per-item override) fields added. Dynamic deadline-tiered `polling_schedule` AutomationRule introduced for FR-26 (PLM) and FR-55 (NSD).
- 2026-05-20 FR-72 [Ph-2] ZIP ingestion added — five-area NSD structure (`zip-store/` per item + TG-scoped `un-resolved-zip/` at milestone level). FR-13 five-area layout updated. FR-25 CustomerJIRA rewritten — per-device-model polling (milestone-agnostic), live open-ticket dashboard, owner email = close intent (advisory JIRA), TPM (= PM in this item type) self-approves. FR-26 corrected (PLM issue keyed on owner × milestone, not per-item; late-join ItemStartCollection logic).
- 2026-05-21 FR-13 rewritten as **two-tree NSD architecture**: Owner inbound tree (`\\share\hilda\inbound\<carrier>\<device>\<milestone>\<item>\`) + HILDA internal tree (`\\share\hilda\internal\<carrier>\<device>\<milestone>\<tg_name>\`). HILDA resolves internal path by looking up DeliveryItem record from `<item_slug>` to retrieve `tg_name`. Five HILDA-internal zones formalized. FR-72 rewritten with TG-scoped ZIP routing. FR-73 [Ph-1] async carrier-package assembly added. FR-74 [Ph-1] collection-phase closure added (threshold-based polling halt across PLM / NSD / JIRA). FR-75 promoted from DEF-21 (general fault routing — all faults to HildaOpsAlert in Ph-1/Ph-2). FR-64 Close All Items promoted to Ph-1. FR-21 / DEF-19 deferral aligned to Ph-3+. **D-042 captured (Open design question — customer template storage location and TPM authoring path)**.
- 2026-05-22 email_group_alias + corp_id_list TG-level fields added [Ph-1] (per-TG email distribution alias for outreach; per-TG corp-ID list for messenger escalation). FR-70 item_completion_pct computation formula rewritten. drift-check requirements mode (3 drifts resolved: D-022 beat loader; Appendix B TSI path; FR-28 MilestoneAllClosed inline trigger). **switch-phase: requirements → architecture**.
- 2026-05-23 FR-30 rule storage layout under `customizations/rules/` (Global / Customer / Device 3-tier YAML resolution); FR-31 expanded with SP web part UI surface and three sub-capabilities (pause/resume, customize parameters, manual trigger); FR-39/40/41 ops-owned YAML template authoring. **D-043 captured (Ph-3+ MicroK8s plan: RabbitMQ Quorum Queues, Rook/Ceph PVCs, MetalLB VIP, ConfigMap volumes for customizations).**
- 2026-05-23 → 2026-05-24 **Full architecture-review pass** on HILDA_Design.md (32 edits across §1, §3.3, §3.4, §3.5 new, §4.1, §4.2, §5 Stage 4, §6.1, §6.2, §6.3, §7.1, §7.4, §10, §11, §12, §13), PROJECT.md (Ph-1 → Ph-1/Ph-2/Ph-3+ split; DEF list reconciled; per-PM credentials phase-scoped), SYSTEM.md (three-pillar topology; 20-module roster including new `corp_messenger_gateway` + `corp_plm_gateway`; corrected corp/lab firewall model with two intake PCs; SP-alert email channel formalized with `sp_alert_parser` sub-module; topology Mermaid redrawn; Conflicts C3/C5/C6 updated; Open Questions #10 resolved, #11–#16 added), structure-conventions.md (Temporal → Celery; K8s → Docker Compose Ph-1/Ph-2 → MicroK8s Ph-3+; `customizations/` YAML sub-structure expanded; `config/global.json` added; `deploy/` tree added; new "SharePoint UI boundary" subsection; CredentialBackend sops + Vault; sync-API wrapping two-stage IO addition). **D-044** (documentation boundary policy), **D-045** (schema/content boundary invariant), **D-046** (canonical schema source — Pydantic models generating SP provisioning + Postgres DDL + YAML schema) captured. FR-47 `resolution_path` promoted to formal enum. 6 architecture-phase follow-up ADRs flagged in Open Questions.
- 2026-05-24 STATUS.md back-filled: 12 Done entries appended covering the 2026-05-13 → 2026-05-24 gap; Next section rewritten (16 modules remaining + 2 new gateway modules; 6 follow-up ADRs surfaced; SP-engineer + Exchange-admin confirmation items added; stale items struck); Flags section refreshed (broadened MAP.md staleness; new D-042 flag; 2026-05-24 follow-up ADRs block; SP "Anything changes" config note; STATUS.md upkeep policy — close-session daily going forward).
- 2026-05-24 phases/requirements.md review: PROJECT.md field list updated (added Constraints, "In scope Ph-1" → "In scope by phase Ph-1/Ph-2/Ph-3+"); requirements-shaped HILDA_Design.md sections list expanded (§3 data model, §4 templates, §7 adapters, §8 orchestration, §10.1–§10.6 credentials, §12 configurability, §14 risks); "no access" constraint list expanded (proprietary specs `[D-003]`/`[D-010]`/`[D-011]`, corp messenger / PLM / JIRA / NSD content, R&D reply prose, lab-network runtime env); remote-collaboration paragraph expanded to cover Teacher/Student LLM scaffold per `[D-027]` (`.clinerules/` + `cline-playbooks/`); SYSTEM.md and structure-conventions.md added to "Load when entering" as conditional background context with `[D-NNN]`-anchor guardrail; "Ph-1" verbiage removed throughout; new Don't rule added (don't redesign requirements around SYSTEM.md's platform shape — surface conflicts as follow-up ADRs).
- 2026-05-24 DECISIONS.md back-fill review: 5 existing ADRs received 2026-05-24 implementation notes — **D-019** (K8s Secrets superseded by D-038 sops; Ph-1/Ph-2 = shared ops-team creds not per-PM; Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+), **D-021** (4-workload split not 3; 20 modules not 18; 3-host deployment surface not 1 PC; Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+), **D-022** (Ph-3+ broker migration to RabbitMQ Quorum Queues per D-043; Ph-1/Ph-3+ → Ph-1/Ph-2/Ph-3+), **D-024** (Helm pipeline pinned to Ph-3+; Ph-1/Ph-2 = basic CI + manual deploy via sops + deploy.sh), **D-026** (3-host deployment surface; K8s → MicroK8s per D-043; Ph-1/Ph-2 credentials = sops not plaintext .env per D-038). **New ADR captured: D-047** (SP → HILDA notification channel via SP alert email + `sp_alert_parser` sub-module + IMAP IDLE primary / short-poll fallback / FR-23 third-tier; locks down the unconditional corp/lab firewall block; routing key `(ProjectID, MinorMilestone, ItemNumber)`; SP "Anything changes" config requirement; resolves SYSTEM.md Conflicts C6).
- 2026-05-24 **D-027 clarified, NOT superseded**: per user-provided git-sync README + clarification (Cline runs only on Work PC; corp git repo not accessible from Personal PC). Apparent contradiction in git history (kurnoolion-authored commits in origin/main) resolved — those commits were authored by **Claude on Personal PC** using the kurnoolion git identity in use at the time, not by Cline on Work PC. Identity transitioned to ai-math-01 from 2026-05-22 onward. Implementation note appended to D-027 documenting: (a) confirmed topology Personal PC → github.com (intermediary) → Work PC → company git; (b) git identity history kurnoolion → ai-math-01; (c) work split — Claude writes core/src/ + all docs + all MODULE.md + scaffolds for customizations/; Cline completes TODO scaffolds against proprietary inputs and pushes to company git only; (d) script tooling — setup-personal.sh / setup-work.sh / sync-personal.sh / sync-work.sh / git-debug.sh per README; (e) air-gap invariant unchanged. STATUS.md / Flags update: D-027 "needs user verification" flag now resolved. Personal PC normally **does not pull** from public github (Personal PC is the sole writer); pull is only needed for edge cases like multiple clones / worktrees.
- 2026-05-24 **Placeholder convention captured** for proprietary identifiers in `customizations/` scaffolds: new doc `cline-playbooks/placeholder-convention.md` defining `<SYS_N>` / `<CUST_N>` / `<DEV_N>` / `<TG_N>` / `<PERSON_N>` / `<URL_N>` placeholder categories, registry-comment header pattern at the top of each scaffold, slug-based env-var mapping, hand-off contract between Claude (writes placeholders + slugs) and Cline (fills TODOs with interface-shape facts; never substitutes placeholders into committed files). Referenced from `structure-conventions.md` Ingestor section. Anchored by `[D-002]` + `[D-027]`. Existing `<SYS0>` usage in `customizations/issue_tracker/defecttrack_adapter.py` retroactively validated as compliant.
- 2026-05-24 phases/architecture.md review: SYSTEM.md + DECISIONS.md added to Load list; module-list bullet refreshed with current architecture (Workflow Engine = Celery+Redis Ph-1/Ph-2 / RabbitMQ Ph-3+ per `[D-022]`/`[D-043]`; Credential Health Monitor marked Ph-3+ per DEF-14; `corp_messenger_gateway` + `corp_plm_gateway` added; `sp_alert_parser` sub-module per `[D-047]` referenced); tech-stack/stakeholder-answers source pinned to PROJECT.md Constraints + DECISIONS.md; stale Graph-API example replaced with current `[D-047]` SP-alert channel example; contribution surfaces list expanded from 4 to 10 items (SP web parts, SP alert email channel per `[D-047]`, Excel template upload, YAML rule/template ingestion, NSD owner inbound, owner email replies per FR-12, owner messenger replies per FR-54, owner CorporatePLM uploads, corp_messenger_gateway intake, corp_plm_gateway, eval-data correction); Observability bullet updated to anchor `[D-023]` light stack with zero HILDA-owned o11y services; Limited-access design patterns expanded with `[D-027]` Teacher/Student scaffold + placeholder-convention.md reference + two-host corp gateway architecture; Remote-collaboration contracts updated to reference `[D-017]` (central error_codes.py decided at `core/src/diagnostics/`) and placeholder convention; **Two Ingestor modules → Three Ingestor/Profiler modules** (added Test Report Document Profiler per `[D-011]`); vendor examples corrected (customer JIRA + corp PLM via Ingestor; Slack + proprietary messenger via Ingestor); meta-schema hierarchy updated to drop Deliverable per `[D-028]`; input-format TBDs resolved (`[D-018]` TSI three modes; `[D-015]` API Spec OpenAPI 3.x); SharePoint integration split clarified — document library uploads removed per `[D-013]`/`[D-041]`; mock-harness reference updated to mock-sharepoint Docker Compose service; Frozen SP 2017 section anchored to `[D-006]` + `[D-047]` with explicit corp/lab firewall asymmetry note; DECISIONS.md Artifacts entry refreshed with resolved-by anchors for all listed decision categories.
- 2026-05-24 phases/development.md review: Three-tier-layout persona line rewritten with `[D-027]` Claude/Cline authorship split (Claude = core/src + scaffolds with placeholders + docs; Cline = TODO completion against proprietary inputs on Work PC) + corp-side gateway PCs (corp_messenger_gateway + corp_plm_gateway as off-Docker-Compose modules); explicit "Do NOT load" sub-list added to Load list (HILDA_Design.md, SYSTEM.md, structure-conventions.md, phases/architecture.md, PROJECT.md — all architecture-phase scope by development time); DECISIONS.md added as Tier-2 on-demand. Six section subtexts updated: (a) Limited-access workflow — full proprietary-content list (specs, schemas, reports, corp messenger/PLM/JIRA, NSD, R&D prose, lab-network runtime) + `[D-027]` Teacher/Student + placeholder convention reference; (b) Compact reports are contracts — central error_codes.py anchored to `[D-017]` at `core/src/diagnostics/`; placeholder convention + 11 redaction classes referenced; (c) Honor human-in-the-loop — FR-63 + FR-28 PMApproval trigger + NFR-5 cited; delta-aware re-submission noted; attribution model phase-scoped (ops-team Ph-1/Ph-2 per `[D-019]` Ph-1 / `[D-038]`; per-PM Ph-3+ per DEF-14 / `[D-019]` Ph-3+); (d) Credential discipline — storage backends specified (sops Ph-1/Ph-2; Vault Ph-3+); never-cache rule anchored; CommunicationLog logging per NFR-6; (e) Don't engage with proprietary inputs — three Ingestor/Profiler modules (not two); list expanded to cover corp messenger/PLM/JIRA + NSD + R&D prose + customer feedback; placeholder convention as redirection mechanism; (f) Don't hard-code SP values — document library paths removed per `[D-013]`/`[D-041]`; SP-alert subscriber address + change-trigger settings per `[D-047]` added to deployment-config list. Observability bullet updated to `[D-023]` light stack with zero HILDA-owned o11y services; credential-expiry instrumentation marked Ph-3+ only. Test interface per `[D-005]` — mock-sharepoint Docker Compose service reference made concrete.
- 2026-05-26 `sharepoint/REQUIREMENTS.md` **created as new hand-off doc** for the SP UI engineer (commit `f8e9345`) — self-contained 12-section spec for the SharePoint 2017 web parts side of HILDA, strips HILDA-Python-side complexity (hilda-api / hilda-worker / Celery / Postgres / NSD internals / sops / MicroK8s). Contents: (1) Big picture + SP-2017-frozen stack constraint; (2) 6 SP Lists structure with full column definitions (Customers, Devices, Milestones, DeliveryItems, Users, PMCredentials, CommunicationLog) — CustomerTemplates and AutomationRules explicitly noted as NOT in SharePoint (YAML files per FR-30 / FR-39/40/41); (3) Views (Milestone primary, Device Tracker, PM Dashboard); (4) 8 buttons / actions with specs (Start Collection, Approve, Submit to Carrier delta-confirm, Close All Items, Refresh, Send Reminder, Upload Document Ph-2, Close Item Ph-2) — every button writes a SP field, triggers SP alert email per `[D-047]`; (5) Document section (HILDA enumeration API at `https://hilda.corp/docs/<delivery_item_id>`; per-revision rendering; HILDA-mediated download); (6) PM Review section for UnderPMReview items; (7) SP Alert configuration (critical: "Anything changes" trigger on all lists); (8) Live updates (SP REST polling 5-10s, NOT HILDA); (9) Permissions; (10) Phase scoping (Ph-1 build first / Ph-2 / Ph-3+); (11) Out of scope (Document Libraries, workflows, Power Apps, SPFx); (12) Contract + 6 open items for HILDA-team confirmation. Written as a black-box-friendly spec for the SP UI engineer.
- 2026-05-26 `sharepoint/REQUIREMENTS.md` **amended (commit `1c102a9`)** to add **TGGroups SP list (§2.8)** — the missing TG-group fields (`tg_owner_name`, `tg_owner_email`, `email_group_alias`, `corp_id_list`, `default_cc_list`) per FR-2 + FR-71 that were absent from the initial commit (`f8e9345`). Normalized as a separate list per `[D-045]` schema/content boundary — one row per `(milestone_id, tg_name)`; DeliveryItems lookup on `tg_name`. Cross-references updated in §3.1 (Milestone View — TG header rendering above each tg_name grouping), §7 (SP Alert subscription — now 8 lists including TGGroups), §10 Phase scoping (Phase 1 builds TGGroups list + auto-population; Phase 2 adds TPM inline edit per FR-71 ODF), §11/§12 Contract + Open Items (new open item #7 — TGGroups auto-population mechanism, JSON shapes, unique constraint enforcement to confirm with HILDA team). **Root cause: STATUS.md / DECISIONS.md sweep gap** flagged earlier — TG-group fields added 2026-05-19 (FR-71) + 2026-05-22 (email_group_alias + corp_id_list) propagated to HILDA_Design.md §3.4 + SYSTEM.md during 2026-05-24 architecture review but did NOT propagate to derivative docs. This confirms the need for the deferred STATUS.md + DECISIONS.md sweep we noted.
- 2026-05-26 `core/src/storage/MODULE.md` initial draft committed (`442400e`) — first Layer 1 MODULE.md per the per-layer design strategy. Partial section-by-section user review (Purpose narrowed to FR-31 authoritative + Contributes-to others; hilda-svc defined as AD service account not container; Ph-1/Ph-3+ → Ph-aware in Deferred). Remaining sections to be revisited during implementation per the fix-on-test-failure approach.
- 2026-05-26 **DECISIONS.md comprehensive cross-doc sweep (backward drift; commit `a6d66b1`)** — pattern-scanned all 47 ADRs (D-001 through D-047) against current state of requirements.md / HILDA_Design.md / SYSTEM.md / PROJECT.md / structure-conventions.md / phases/*.md. Categorized: 🔴 substantive staleness (14 ADRs), 🟡 terminology cleanup (7 ADRs), ✅ already current (18), 🚫 OPEN (D-042), 🔒 already covered by 2026-05-24 batch (D-019/D-021/D-022/D-024/D-026/D-027). **Net 21 new implementation notes appended (2026-05-26 dated)** per the immutability convention (append-only; never edit Decision bodies). The 21: D-002 (registry path + redaction categories + Teacher/Student boundary), D-003 (terminology + gateway routing), D-004 (document libraries removed; Open Q#1 resolved), D-005 (mock-sharepoint Docker service concrete), D-006 (credential service path resolved; SP→HILDA firewall asymmetry), D-007 (K8s → Docker Compose; Temporal → Celery), D-008 (Ph-1 + Celery + registry path + corp_plm_gateway routing + delete_attachment), D-009 (D-016 messenger choice resolved + corp_messenger_gateway routing + FR-54 Ph-2), D-010 (Deliverable removed + Excel Ph-2 + modes resolved + TG-group fields), D-012 (DEF-1 promoted + Redis idempotency cache concrete), D-013 (D-040/D-041 cross-ref + hilda-svc clarification), D-014 (Excel Ph-2 + D-042 open + TG-group fields), D-016 (corp_messenger_gateway routing + DEF-6 promotion context), D-017 (20 modules; +CMG- and CPG- prefixes needed), D-023 (Docker Compose terminology; ServiceMonitor → direct scrape; credential-expiry Ph-3+ only), D-025 (scope expanded to all customizations/ sub-directories), D-028 (TG-group fields + milestone email_cc_list superseded by per-item), D-030 (superseded by D-033 two-tier + path convention update), D-035 (D-040 supersession explicit + delete_attachment + Ph-2 deferred upload + gateway routing), D-037 (FR-21 → DEF-19 + FR-25 rewritten + FR-54 Ph-2 + tracking_modality storage resolved), D-038 (Ph terminology + mTLS Ph-3+). DECISIONS.md is now aligned with current state of all other docs through 2026-05-26.
- 2026-05-26 **4 new ADRs captured (D-048 through D-051; commit `4ae4f71`)** — forward-drift scan (architectural choices made elsewhere lacking `[D-XXX]` anchors). **D-048**: FR-66 multi-revision version-selection workflow — owner-mediated via corp messenger; anchors the transient `OwnerClosed` fork; alternatives (auto-pick latest / TPM picks / auto + revert) rejected with rationale. **D-049**: FR-71 Owner Discovery Function (ODF) + per-TG-group field model — TG-group metadata is TG-group-level not per-DeliveryItem; ODF is per-TG corp messenger outreach at tracker creation; anchors the TG-group field model affecting FR-2/FR-9/FR-10/FR-14. **D-050**: FR-72 ZIP ingestion + 5-area NSD structure — per-item area for NSD-sourced ZIPs; TG-scoped area for Email/PLM ZIPs; classified area for extracted files; staged area for ambiguous; outbound area for HILDA-generated; ZIP-as-temporary-container model. **D-051**: TGGroups SP list normalized as separate list (Option A) — one row per `(milestone_id, tg_name)`; rejected Option B (denormalize onto every DeliveryItem) for 20× duplication and Option C (YAML-only) for breaking FR-71. Two medium-priority candidates (D-052 async carrier-package assembly + D-053 SharePoint UI boundary) deferred to natural landing during respective MODULE.md drafts. **Plus propagation discipline added to STATUS.md** Next (FR-change checklist) + Flags (derivative-doc drift risk; mitigation includes drift-check expansion to derivative docs as Ph-1 architecture-phase task).
- 2026-05-26 **drift-check design mode** — 11 drifts resolved across 4 MODULE.md files; 15 deferred (unbuild-modules expected gaps tracked in Next per-layer plan). **template_schema/MODULE.md**: Purpose extended to cite FR-66/FR-70/FR-71 + D-028/D-046/D-049/D-051; DeliveryState enum 4→10 values per FR-7; TrackingModality 3→5 values per `[D-037]` (InternalIssueTracker removed); IngestSource + DocType enums added per FR-13/`[D-039]`; RuleActionType expanded to 13 values per FR-28/29 (added TriggerVersionSelection per `[D-048]`, TriggerODF per `[D-049]`, etc.); DeliveryItemBase gained `doc_count`, `review_required`, `review_status`, `item_completion_pct`, `email_cc_list`, `milestone_gating` per FR-2/FR-7/FR-53/FR-70; tracking_modality on DeliveryItemBase changed to multi-value `list[str]` per `[D-037]`; new `TGGroupBase` Pydantic model added per `[D-049]` + `[D-051]` (one row per `(milestone_id, tg_name)`). **issue_tracker/MODULE.md**: Purpose rewritten for two-adapter scope (corp PLM via `corp_plm_gateway` per `[D-021]` impl note + customer JIRA per FR-25 rewrite); InternalIssueTracker reference removed per `[D-037]`; FR-42 removed (reclassified); NSD/PLM source-of-truth split per `[D-035]`/`[D-040]`/`[D-041]` documented; Ph-1 immediate vs Ph-2 deferred PLM upload noted; corp_plm_gateway inbound routing noted; `delete_attachment` method added to Protocol per `[D-040]`/FR-67; Ph-3+ → Ph-3+ terminology fix. **sharepoint_integration/MODULE.md**: minimal addition noting 8 SP lists in scope per `sharepoint/REQUIREMENTS.md §2` (Customers, Devices, Milestones, DeliveryItems, Users, PMCredentials, CommunicationLog, TGGroups per `[D-051]`); list-agnostic architecture confirmed. **customizations/issue_tracker/MODULE.md**: Purpose updated with corp_plm_gateway routing + Ph-1 terminology; new paragraph documenting placeholder convention per `[D-002]`/`[D-027]`/`cline-playbooks/placeholder-convention.md` with `<SYS_N>`/`<URL_N>`/`<CUST_N>` placeholders + registry-comment header pattern. Drift-check marker updated.
- 2026-05-28 **`core/src/email_service/MODULE.md` section-by-section review closed** (commit `714e65b`): Purpose + Workload assignment + Inbound reception (3-tier with IDLE field-reality caveat) + Source-of-truth + Sub-modules layout + Public surface (`protocol.py` + 5 active inbound `.py` files for test scenario + Ph-1 production surface) + Configuration + Invariants (incl. new "no standalone Postgres table for inbound email bodies") + Error codes + Key choices + Non-goals + Depends on + Depended on by + Deferred + Test interface — all reviewed. **5 review-round refinements applied**: poll_interval_s=60 + IDLE field-reality caveat; marked-read mechanism clarified (\\Seen + MOVE to INBOX/processed); test-scenario subset rewritten (FR-16 unavailable / FR-53 config-gated / D-039 Step 2+ skipped / FR-13 PLM upload config-gated); `classification_source` split into `doc_type_source` + `item_routing_source`; new no-Postgres-bodies Invariant. **Staged-document fallback ladder** queued as requirements-phase Flag per user item-6 (folder-only routing → owner outreach → TPM escalation; expected: FR-52 amendment + new ADR + `attachment_router.py` surface).
- 2026-05-28 **`core/src/email_service/MODULE.md` drafted** (Layer 2 entry point; anchors `[D-016]`, `[D-034]`, `[D-047]`; serves FR-9, FR-10, FR-12, FR-23, FR-24, FR-52, FR-54, NFR-1, NFR-2). Sub-module layout: inbound/ (receiver + classifier + subject_parser + body_parser_structured + body_parser_freetext + attachment_router); sp_alert_parser/; outbound/ (sender + composers + templates). **Test-scenario subset (per 2026-05-28 user use case — 80+ test reports, doc_type classification + attachment routing + structured response, no LLM review, no revisions)**: inbound/receiver.py (one-shot mode), classifier, subject_parser, body_parser_structured, attachment_router (with `plm_upload_enabled=false` + `review_required_enabled=false` config gates). Skipped for test: body_parser_freetext (FR-12 path c fused LLM), sp_alert_parser/, outbound/. `--once` CLI mode added as the test-scenario entry point. Initial commit pending.
- 2026-06-05 **Requirements.md user-led review pass — extended/continued** — second-pass + audit work after the 267d41b close-session:
  - **FR-19 selenium amendment** + `GoogleDriveBaseAdapter` base-class in `core/` (mirrors IssueTracker/Messenger pattern); `D-054` impl note 2026-06-05 documenting Google Drive REST API unavailability + browser-automation pivot. New STATUS.md flag for customer adapter browser-automation architecture-phase items (selector versioning, Chromium ops dep, PM session mgmt, capability flags).
  - **v1/v2 terminology cleanup**: 17 substitutions in requirements.md; bulk replace_all in STATUS.md (47 substitutions); global terminology mapping note added at top of DECISIONS.md (preserves [D-002] ADR-body immutability).
  - **FR-73 Download Package two-click async flow** made explicit: 7 sub-steps (i-vii); per-click regeneration; zip deleted on TPM download (FR-76 backstop).
  - **FR-28 + FR-29 audit-driven amendments**: 6 new triggers (incl. restored MilestoneAllClosed Ph-1 cleanup-only) + 8 new actions; user-driven simplification dropped `TemplateModified` in favor of `ItemModified.TagsModified` propagation. FR-29 reformatted to bulleted Ph-1/Ph-2 subsections.
  - **FR-77–FR-84 audit + 6 amendments**: default work-item excluded from FR-74 threshold + reaches Closed via TPM-Mark-Closed (FR-78); FR-82 restructured (YAML for new trackers + SP-UI propagation for active trackers); FR-77 EML-W007 for missing target_folder mapping; FR-79 PLM fan-out across distinct (owner × milestone) issues (per-association plm_attachment_id); FR-78 default_work_item template structure + fallback defaults; FR-84 sentinel routing key for milestone-scoped actions.
- 2026-06-05 **Requirements.md user-led review pass closed** — covers FR-2, FR-7, FR-12 (status applies wording reviewed, kept as-is), FR-13, FR-14, FR-17, FR-18, FR-19, FR-23, FR-26, FR-52, FR-55, FR-56, FR-57, FR-61, FR-62, FR-68, FR-72, FR-73, FR-76, FR-79. Major changes: (1) **Amendment-pattern abandoned — converted to in-place updates**: 11 FR amendments from 2026-05-28 batch + 3 clarification amendments from 2026-06-05 folded inline; both `### 2026-05-28 amendments` and `### 2026-06-05 clarification amendments` subsections removed; FR bodies now reflect current spec without "see amendment below" indirection. (2) **Drift-check requirements mode run** (5 drifts resolved: Appendix B FR-52 row + CLASSIFY_DOC row; Appendix C entries for FR-77–83; FR-73 + FR-76 amendments folded inline). (3) **PLM upload timing corrected** (FR-13/FR-17/FR-52/FR-55/FR-62): silent override of `[D-040]` Ph-2 deferral in 2026-05-28 batch restored to phase-conditional (Ph-1 immediate; Ph-2 deferred until OwnerClosed + TPM approval; only is_final revision uploaded). (4) **Multi-item document association** clarified across FR-57 (primary anchor in natural key) + FR-79 (primary in both DocumentIndexRow and DocumentItemAssociation M:M table). (5) **New FR-84** — SP→HILDA email notification channel per `[D-047]` (firewall-blocked SP→HILDA HTTP; sp_alert_parser sub-module; three-tier inbound IMAP IDLE → short-poll → deadline-tiered). (6) **FR-14 + FR-23 amendments** referencing FR-84 channel. (7) **FR-61 download disposition** — content-type-conditional (PDFs inline; Office/binaries attachment). **DECISIONS.md audit**: all 54 ADRs reviewed against requirements.md (24 well-captured; 24 architectural-only correctly covered elsewhere; 1 open D-042; 2 impl notes added — D-029 confirms three-function framing under 2026-05-28 evolution; D-030 supersession chain extended through FR-52 amendment). No new ADRs; no MODULE.md changes; no code changes.
- 2026-05-28 **Layer 1 complete — `storage/MODULE.md` section-by-section review closed**: Purpose + Pydantic row models (`DocumentIndexRow`, `CommunicationLogRow`, `BatchIdempotencyKey`) + document index API (`add_document_index_row`, `get_document_index_row`, `find_doc_id_slugs_for_item`, `list_revisions`, `update_review_findings`, `set_is_final`) + communication log API (`log_communication`, `query_communications`) + rule overrides API (`get_active_overrides`, `set_override`, `clear_override`) + Redis client section + NSD client section + Invariants + Key choices + Non-goals + Dependencies + Deferred — all reviewed. **`llm/MODULE.md` and `credential_service/MODULE.md` section reviews also closed 2026-05-28.** Layer 1 now 100% done (7 modules fully reviewed: `diagnostics`, `template_schema`, `sharepoint_integration`, `issue_tracker`, `storage`, `credential_service`, `llm`).
- 2026-05-28 **Layer 1 MODULE.md drafts + D-052 + LLM TaskKind refinement** — `core/src/credential_service/MODULE.md` drafted (Layer 1; anchors `[D-019]`, `[D-038]`; commit `63b394e`). `core/src/llm/MODULE.md` drafted (Layer 1; anchors `[D-007]`, `[D-029]`, `[D-033]`, `[D-034]`, `[D-039]`, `[D-052]`; initial commit `a7106ed`). `[D-052]` captured (dual-backend runtime LLM: local Ollama + corp on-prem LLM; empirical per-TaskKind routing; no automatic spillover; both backends within `[D-007]` corporate network boundary; commit `a7106ed`). LLM TaskKind enum refined: added `CLASSIFY_DOC_TYPE` (FR-52 / FR-55 filename-opaque → doc_type ∈ {test_report, tech_report, waiver}); fixed mislabeled `CLASSIFY_DOC` schema to D-039 Step 2 semantics (REVISION-of-existing vs NEW_DOCUMENT). Ph-1 LLM scope per `[D-029]` remains 3 functions; TaskKind partition expanded to 5 via `[D-052]` impl note 2026-05-28. Hardware context: dev box NVIDIA RTX A4000 16 GB (Option B routing with swap); production HILDA PC NVIDIA DGX Spark 128 GB unified memory (in setup, ~2026-06-04 ETA, removes VRAM ceiling and enables multi-model hot routing).
- 2026-05-26 **STATUS.md comprehensive audit** — Header date refreshed (2026-05-24 → 2026-05-26) + Last design review extended to cover 2026-05-26 work; Done section: missing initial sharepoint/REQUIREMENTS.md commit (`f8e9345`) added; chronological order of DECISIONS.md backward sweep + forward sweep entries swapped (impl notes commit `a6d66b1` first, new ADRs commit `4ae4f71` second); In progress section: storage MODULE.md continued review added (Public surface / Key choices / Non-goals / Depends-on sections remain unreviewed); Next section: module count refreshed (4 done + 1 in-progress draft + 15 remaining; 3 of 6 ADR follow-ups marked resolved; sub-conventions ratification status + code-sync TGGroups addition + D-052/D-053 deferred candidates noted); Flags section: MAP.md date refreshed + storage addition noted; CI/CD tool-selection flag cross-linked to D-024 reconciliation resolution; 2026-05-24 ADR follow-ups flag updated (3 resolved, 3 remaining); new D-048..D-051 first-touch caveat added. Net ~12 edits across 5 sections; no content removed.

- 2026-06-07 Phase B Module rollback — `template_schema/MODULE.md` committed `93e0aec`: ItemType.DEFAULT + DocType.DEFAULT enums; RuleActionType expanded with 13 new actions for FR-29 revised + TRIGGER_LLM_REVIEW → TRIGGER_AI_REVIEW rename; new RuleTriggerType + RuleSubTriggerType enums; TGGroupBase/MilestoneBase/DeliveryItemBase new fields (ingress_nsd, folder_routing_enabled, default_work_item_config on MilestoneBase, no_customer_upload, ingress_folder + target_folder inbound/outbound naming discipline, item_description tag-list); 4 new models (DefaultWorkItemConfig, FolderRoutingEntry, TGFolderRouting, TagCatalogEntry); TSC-W003 error code. Note: ItemType.DEFAULT remains valid; the "1:1 derivation from item.item_type → doc_type" docstring framing is in scope for the 2026-06-07 [D-053] reversal (see Flags).

- 2026-06-07 Phase B Module rollback — `storage/MODULE.md` file-centric refactor (commits `93e0aec` + `c02fff4`): DocumentIndexRow PK=`file_hash` (secondary unique constraint `(milestone_id, doc_id_slug, rev_number)` preserves FR-57 lookup); DocumentItemAssociation = symmetric M:M with composite PK `(file_hash, delivery_item_id)` carrying per-(file, item) data (local_classified_path, plm_id, plm_attachment_id, owner_email, upload_timestamp). Dropped: `download_url` (replaced by `make_download_token(file_hash, delivery_item_id, ttl_seconds)` computed at page-render time per FR-61); `is_primary_association` / `is_primary` (symmetric M:M — no primary/secondary semantic); `landing_item_id`; synthetic `association_id`. `inferred_tg_name` + `routing_resolution` (per-ingest) moved to DocumentIndexRow. New `PLMFanOutTarget` typed model. `AutomationRuleOverride.rule_id` clarified as soft FK to YAML rules per FR-30/FR-31; added `list_all_override_rule_ids()` for rule_engine startup orphan-audit. NSDPath: removed stale `to_download_token`; renamed `internal_un_resolved_zip` → `internal_unrouted_zip`; added `internal_default_workitem` + `ingress_folder`; clarified `internal_staged` ([D-039] Tier-2 NEW-vs-REVISION) vs `internal_default_workitem` (FR-78 which-work-item) as distinct ambiguity classes. New error codes: STR-E005..E008, STR-W002..W006. 10-item API surface review: cache_set TTL guard with MAX_CACHE_TTL_SECONDS; 9 convenience helpers (get_documents_for_item, list_documents_for_milestone, delete_document_item_association, cache_delete, list_tag_catalog_entries, reactivate_tag, list_active_overrides); query_communications gains file_hash/action_type/pm_id/until/offset filters + pagination cap; reassign_document_to_workitem syncs `DocumentIndexRow.inferred_tg_name` to target item's tg_name; set_override/clear_override emit CommunicationLog audit entries with credential_id=pm_id.

- 2026-06-07 Phase B Module rollback — `llm/MODULE.md` rollback + tri-backend amendment + clarifications (commit pending in this session): `CLASSIFY_DOC_TYPE` TaskKind removed per [D-053] impl note 2026-05-28b (5 → 4 TaskKinds; **scheduled for re-reversal after requirements-phase redesign of doc_type classification — see In progress / Flags**); FR-52 framing updated from `[D-033]` 2-tier to `[D-053]` 5-step pipeline; ROUTE_ATTACHMENT contextualized as step 4 of 5; REVIEW_DOCUMENT DEFAULT-skip note added (FR-7 amendment + FR-29 TRIGGER_AI_REVIEW gate). **Tri-backend expansion** ([D-052] dual → tri): BackendConfig.name Literal expanded `["ollama_a4000", "vllm_dgx", "corp_llm"]`; new Hardware-topology section with per-backend memory/bandwidth/profile table + capacity-vs-bandwidth note (DGX Spark 128 GB unified RAM ≠ throughput advantage for small models — A4000 GDDR6 ~448 GB/s > DGX Spark LPDDR5X ~273 GB/s for autoregressive inference of fits-on-A4000 models); tentative-assignments table expanded with `vllm_dgx` A/B candidate per task + model catalog policy (same Ollama models on both local backends for direct comparability; larger models added to vllm_dgx only if small-model A/B unsatisfactory). Host topology fix: HILDA PC distinct from A4000 box + DGX Spark box; all three model-serving hosts are separate Linux machines on the lab subnet; hilda-llm-gateway proxies outbound. Docstring clarifications: ReviewDocumentInput.checklist (build-time-generated YAML at `customizations/checklists/<slug>/<doc_type>.yaml` by Test Report Profiler in same run as parser); new "Relationship to parser_result" Non-goals section with comparison table + 4-quadrant coexistence matrix; expanded "Not a test report parser" Non-goal with build-time/runtime split.

- 2026-06-07 STATUS.md flags maintained: storage debt (DocumentIndexRow.inferred_tg_name field) added and resolved inline; requirements.md naming sweep flag (`target_folder` → `ingress_folder` for FR-77 inbound side); A/B-test gate flag rewritten for tri-backend with capacity-vs-bandwidth caveat; new [D-052] ADR-body impl note flag (dual→tri expansion needs append per [D-002] convention); new [D-053] reversal flag (HIGH priority — blocks email_service + customer_adapter Module rollbacks).

- 2026-06-08 Requirements-phase doc_type classification redesign session (commit pending this close-session) — addressed the `[D-053]` "1:1 derivation" reversal flagged at 2026-06-07. **13+ turn iterative refinement** locked the corrected model: **4 ItemTypes** (`Confirmation`, `TEST_TECH_WAIVER_REPORT`, `COMPLIANCE_CERTIFICATION_RELEASE_NOTES`, `Default` — collapsed from previous multi-valued enum); **5 DocTypes** (`test_report`, `tech_report`, `waiver`, `compliance_certification_release_notes`, `unresolved`); **alignment invariant** (per-ItemType allowed doc_type sets); **4 NSD storage paths** (classified / unrouted / staged-not-revision-determined / staged-not-classified); **2-step classification ladder** (filename regex Step 1 covers all 4 actionable doc_types; LLM CLASSIFY_DOC_TYPE Step 2 restricted to {test/tech/waiver}; UNRESOLVED on low confidence); **`[D-039]` skip optimization** at ingest when routing lands on Default or doc_type=UNRESOLVED (deferred to FR-83 reassignment / FR-87 resolution); **SP UI strict-order resolution** A → B → C (work-item reassign / doc_type resolve / revision resolve) routed via FR-84 SP-alert email channel. **3 new FRs landed**: FR-85 (doc_type classification pipeline), FR-86 (storage matrix + alignment invariant + `[D-039]` skip rule + DocumentIndexRow combinatorics + `<inferred_tg_name>` in unrouted path per `[D-060]`), FR-87 (SP UI strict-order A→B→C + explicit FR-84 SP-alert end-to-end flow). **6 existing FRs amended**: FR-7 (ItemType/DocType enums + alignment + corrected doc_count=0 guidance for waiver-only deliverables), FR-16 (classification per FR-85), FR-52 (strike 1:1 derivation + item-homogeneity invariant + EML-W005; reference FR-85/FR-86; "concurrent" wording removed), FR-53 (review_required conditioned on `TEST_TECH_WAIVER_REPORT` items; doc_type per FR-85), FR-78 (Default accepts all 5 doc_types; `[D-039]` skipped at ingest for Default), FR-83 (strict A→B→C per FR-87; no auto-override; explicit scope = unrouted path only). Header "Last updated" advanced to 2026-06-08. **18 design issues** identified and resolved through iterative refinement (slash naming → TEST_TECH_WAIVER_REPORT style; doc_count semantic dead-state → accepted with corrected guidance; `[D-039]` skip optimization; SP UI mechanism explicit invocation of FR-84 channel; `inferred_tg_name` path organization for unrouted).

- 2026-06-09 Phase B Module cascade against the corrected `[D-053]` model — Groups 2-4 of 4 applied (Group 1 `template_schema/MODULE.md` already in `aefd8b7` 2026-06-08). **storage/MODULE.md cascade** (uncommitted): DocumentIndexRow.doc_type 5-value enum + FR-86 storage matrix paragraph; parser_result + llm_review_findings docstrings updated for 5-value DocType; `NSDPath.internal_default_workitem()` signature added `inferred_tg_name_slug` param per `[D-060]` impl note 2026-06-08; renamed `internal_staged` → `internal_staged_revision`; added `internal_staged_classification`; new STR-W007 (stale staged document). User-review correction: `local_classified_path` → `local_nsd_path` rename + new `NSDPathType` enum (4 values) on DocumentItemAssociation for indexed state tracking. **llm/MODULE.md cascade** (uncommitted): `CLASSIFY_DOC_TYPE` TaskKind RESTORED (un-revert of 2026-05-28b removal); TaskKind count 4→5; restricted candidate set `{test/tech/waiver}` per FR-85 Step 2; ClassifyDocTypeInput/Output schemas restored; Non-goal flipped "Not a doc_type classifier" → "IS a doc_type classifier (Step 2 of FR-85 ladder)"; tentative-assignments table 4→5 rows; templates_loaded=5; coexistence matrix updated. User-review correction: `RouteAttachmentOutput` → `list[RouteAttachmentMatch]` per FR-79 multi-item; new `RouteAttachmentMatch` model; new `LLG-W008` (over-routing); `[D-029]` Ph-1 narrow surface bullet updated 4→5 TaskKinds. **email_service/MODULE.md cascade** (uncommitted): substantial — FR-52 pipeline 2-tier → 5-step (with FR-77 Type-2 folder routing as new step B3); doc_type classification → separate FR-85 2-step ladder; FR-86 4-path NSD storage dispatch via new NSDPath helpers; `RoutedAttachment` → multi-match per FR-79 with `AttachmentItemMatch` model + new `ClassificationResolution` enum; `tg_resolver.py` sub-module added per `[D-060]`; EML-W005 retired; EML-W006 (misalignment-staged) + EML-W007 (over-routing) added; Fr52Config field additions; sp_alert_parser gained FR-87 A→B→C action handlers (`tpm_reassign_to_workitem`, `tpm_resolve_doc_type`, `tpm_resolve_revision`); QC template revised for new fields. **Plus same-day template_schema/MODULE.md post-cascade incremental**: CustomerDeliveryModality + `GOOGLE_DRIVE` (− `FILE_STORAGE`); `MilestoneBase.default_work_item_config` type `dict | None` → `DefaultWorkItemConfig | None`. Cascade chain entire (Groups 1-4) complete against the corrected requirements model locked 2026-06-08.

- 2026-06-13/14/15 Session — **strand `dashboard-v1` landed; 8 canonical decisions promoted + 4 impl notes appended; multi-FR cascade + SP UI engineer deliverables**. **Land-strand**: `dashboard-v1` strand archived to `_archive/`; promoted 8 D-DRAFTs to canonical **D-075** (FR-87 TPM-resolution UX → HILDA-tab POST), **D-076** (FR-87 sync-validate-and-enqueue + 3-channel async error UX), **D-077** (HILDA runtime SP coupling = 4 SP lists; supersedes earlier D-DRAFT-Z v1 customer.yaml model), **D-078** (`is_milestone_gating` semantic activated), **D-079** (ReadyForSubmission added to FR-62 with revert-to-UnderPMReview), **D-080** (owner_email split into corp_usa_email + corp_email per-item AND TG-level), **D-081** (form factor flag set 5→7; drr + ir_ffw_p1 added), **D-082** (`Milestone.target_date` sole TPM-editable + cascade dedup discipline). Plus **4 impl notes appended**: `[D-006]` NTLM-only confirmed; `[D-073]` Customers + Devices SP gain 4 HILDA-readable columns; `[D-077]` ×2 (joint-key + slug→id + single template.yaml; final R&R lock — SP UI engineer setup_milestone is sole SP row-creation path, HILDA never creates rows). **Multi-FR cascade in requirements.md (~30 FRs touched + 1 new FR + 1 new NFR)**: new **FR-88** (Owner identity model — extracted from FR-2 as dedicated FR), new **NFR-21** (HILDA-SP runtime coupling discipline — 4-list read, 2-list write, no row creation, SP canonical, no Postgres mirror). FR-2 trimmed from ~7000 → ~1100 words; runtime role of template.yaml clarified; setup_milestone granularity locked at per-(customer, device, milestone) triple with idempotency invariant + operational lifecycle. FR-3 explicit SP UI engineer web-part-creates-rows alignment. FR-5 SP-side uniqueness (Customers PK = Counter; Devices PK = ProjectID corp-assigned; Milestones PK = Counter). FR-6 Milestone.status 4-value enum locked. FR-7 fixed stale "Not Started" writer (SP UI engineer's setup_milestone, not HILDA). FR-8 rewritten with 6 explicit HILDA action steps (state transition, PLM issue + credentials, plm_id writeback, outreach, polling activation, Milestone.status writeback) + full re-trigger idempotency. FR-9 added sender attribution (assigned_pm_id), SPUI silent-outreach, failure handling per NFR-10, PLM/NSD CC list, FR-71 cross-reference. FR-13 source-of-truth reconciled with FR-2 Runtime role; FR-86 4-path cross-reference; default-work-item NSD path documented; directory creation responsibility documented; slug→id rename applied to NSD path placeholders (`<item_slug>` → `<item_path_id>`, `<tg_name_slug>` → `<tg_path_id>`); URL hostname fix (hilda.corp → hilda-proxy.corp consistent with `[D-074]`). FR-40 single template.yaml structure (milestones at customer level; devices as siblings; project_id NOT in template — corp-assigned at setup_milestone); 3-tier name model invariant (template.yaml uses HILDA-canonical; sharepoint_config maps canonical→SP-internal; SP_lists_authoritative.xlsx is sole authority); template_version deferred to Ph-3+. FR-56 Approve button text aligned with `[D-068]` SP-side write owner. FR-62 enablement extended for SPUI Open-state upload. FR-78 default work-item creation by setup_milestone (not HILDA). FR-84 5-field SP-alert routing key (`customer_id`, `Model`, `ProjectID`, `MinorMilestone`, `ItemNumber`); single-PK Device lookup by ProjectID + Model cross-validate. PROJECT.md PM ≡ TPM role nomenclature lock. **Naming + auth cleanups**: slug→id global rename (customer_slug → customer_id, device_slug → device_id, item_path_slug → item_path_id, tg_path_slug → tg_path_id); NTLM-only auth confirmed via Linux box inspection (`~/.smbcredentials` + cifs-utils `sec=ntlmssp`); Kerberos refs removed from FRs (browser→hilda-proxy auth scope separate per `[D-074]`). **SP UI engineer deliverables**: NEW `docs/sp_ui_engineer/SP_lists_authoritative.xlsx` (sole SP-side schema authority since 2026-06-15); NEW `docs/sp_ui_engineer/SP_UI_button_actions.md` (16 button specs with field-write contracts, SP-alert configuration requirements); deprecated `HILDA_SP_Schema.xlsx` + `DeliveryItem_visibility_review.xlsx` renamed `_DEPRECATED_2026-06-15`. **Sweep**: ~50 `D-DRAFT-*` references in requirements.md mechanically renamed to assigned `D-XXX` IDs post-land-strand. **Commits**: `689cc06` (in-strand SP UI engineer xlsx absorption + D-DRAFT-Z v2/14b/14c) + `0f40843` (land-strand promotion + multi-FR cascade + SP UI deliverables) — both pushed to origin/main 2026-06-15. **Strand discipline observed**: STATUS.md headers updated in-place during in-flight session; Done + Flags routed to strand journal; promoted to canonical STATUS.md at this close-session (this entry).

- 2026-06-12 Session (Architecture re-entry — SP UI engineer 2026-06-10 review absorption + dashboard Batch 1 promotion). **3 MODULE.md edits**: `template_schema` (`is_milestone_gating` rename + `item_no` immutability Invariant per `DeliveryItemBase`), `customizations/sharepoint_config` (7-list framing per D-DRAFT-Y; 9 denormalized TG columns on `delivery_items:`; `tg_groups:` dropped; milestone/users/delivery_items field-set updates; 2 new Invariants + 2 Non-goals + 3 Key choices), `storage` (`TGFolderRoutingRow.item_no` referential-integrity Invariant cross-referencing the new template_schema immutability). **2 decisions drafted** for ratification this session: **D-DRAFT-X → D-073** (SP UI engineer manually provisions SP lists per corp SP-2017 SP-alert + custom-task constraint; HILDA does not call REST to create lists); **D-DRAFT-Y → impl note appended to `[D-051]`** per `[D-002]` append-only (TGGroups removed as separate SP list → 7-list framing; denormalized onto DeliveryItems; SP-side TG columns are read-only display mirrors; YAML source-of-truth). **Architectural reviews**: FR-57/61 download_url contract walked line-by-line (token stable across FR-87 step B + step C; invalidated by FR-83 work-item reassignment + 300s TTL); FR-61 text reword queued (predates FR-86 4-path cascade); SP web-part-JS cross-origin XHR confirmed BLOCKED in user's corp environment (likely CSP / network ACL) → Variant A locked (HILDA server-renders document pages; SP renders link-out; no SP-side cross-origin fetch); dashboard module promoted from Batch 2 → Batch 1.5 architecture queue (next architecture session). **4 STATUS Flags closed** (lines 263 / 280 / 288 / 316 per pre-edit numbering — 5 `???` Excel cells, [D-068] PM-approval Excel addition, field-set gaps, MilestoneGating + ItemNumber-stability). **Sync**: fast-forwarded main 3 commits to origin/main (teammate's storage-v1 strand landing with D-071 + D-072 promoted); resolved one storage/MODULE.md rollback-log conflict (kept both today's 2026-06-12 entry + teammate's six 2026-06-11 entries; chronological-descending order preserved).

- 2026-06-10 Session 4 (Architecture phase — Batch 1 closure) — **`customizations/rules/MODULE.md` Ph-1 initial draft** (~250 lines; NEW at `customizations/rules/`). Data drop-zone documenting per-deployment AutomationRules + polling-schedule YAML consumed by `rule_engine.RuleSet.load` per `[D-030]` / `[D-031]`. Mirrors `customizations/sharepoint_config/` pattern: 3-tier scope ladder (Global `global/defaults.yaml` / Customer `<customer_slug>/customer_rules.yaml` / Device `<customer_slug>/<device_slug>/device_rules.yaml`), two top-level YAML keys (`rules:` TRIGGER_ACTION shape + `polling_schedules:` POLLING_SCHEDULE shape) per `Rule.kind` discriminator; condition DSL closed operator set (eq/neq/in/gt/gte/lt/lte/and/or — no Jinja, no Python, no jq); worked example shows Global+Customer multi-tier composition producing 2 independent Celery chains per `[D-066]`; bind-mount + SIGHUP `reload()` mechanism per `[D-025]`. Anchors `[D-001]`/`[D-022]`/`[D-025]`/`[D-030]`/`[D-031]`/`[D-062]`/`[D-066]` + FR-23/30/55. 12 invariants + 8 non-goals + 8 deferred Ph-2/Ph-3+ items. **Iterative cadence locked**: Architecture phase paused for **Batch 1 modules** (7 currently-designed: email_service, customer_adapter, rule_engine, workflow_engine, tracker, customizations/sharepoint_config, customizations/rules). Development phase begins on Batch 1. **Batch 2 architecture** resumes after Batch 1 development complete — Batch 2 list: messenger (Layer 2), test_report (Layer 2), test_report_profiler (Layer 3), api_spec_ingestor (Layer 3), dashboard (Layer 4), corp_messenger_gateway (Layer 5), corp_plm_gateway (Layer 5), customizations/issue_tracker Structure markers (small fix), customizations/checklists (drop-zone), customizations/test_report_parsers (drop-zone). **None of these are Ph-1-scope-cut** — all remain Ph-1 deliverables, just sequenced after Batch 1 dev for iteration efficiency. No ADRs warranted this session — iterative cadence is a project-workflow choice; customizations/rules content is idiomatic derivation from rule_engine + sister sharepoint_config pattern.

- 2026-06-10 Session 3 (Architecture phase continued) — **`workflow_engine/MODULE.md` Ph-1 initial draft** (~410 lines; NEW at `core/src/workflow_engine/`). Celery + Redis broker + Postgres result backend per `[D-022]`/`[D-043]`; 18 Ph-1 ActionKind task definitions mapped to ACTION_KIND_TO_TASK registry; TriggerDispatcher consuming `TriggerEvent` from 6 trigger sources (sp_alert / email_ingest / nsd_watch / plm_watch / scheduler_tick / state_change); Celery-beat schedule loader for periodic tickers (FR-23 / FR-26 / FR-55); hilda-beat singleton enforced by orchestrator; **4 queues by latency profile** (default / llm_calls / browser_automation / periodic); FR-31 sub-3 manual trigger handler bypassing rule_engine; FR-87 TPM-resolution round-trip worked example. Workload assignment clarified: workflow_engine code is entry point for **2 of the 4 [D-021] workloads** (`hilda-worker` + `hilda-beat`); `hilda-api` imports the Celery app to enqueue from corp-gateway POSTs but doesn't execute task bodies. Public surface: hilda_celery_app, TaskBinding, build_chain_from_rule_match, TriggerDispatcher.dispatch, BeatScheduleEntry, build_beat_schedule, reload_beat_schedule, handle_manual_trigger, WorkflowEngineConfig, polling/*.py (4 periodic tasks), trigger_sources/*.py (6 fire_from_* façades). 3 architect decisions captured inline as MODULE.md Key choices (no separate ADR): (1) 4 queues by latency profile; (2) hilda-beat singleton enforced by orchestrator; (3) 6 explicit trigger sources not generic event bus. **`tracker/MODULE.md` Ph-1 initial draft** (~510 lines; NEW at `core/src/tracker/`) heavily reviewed across ~12 iterations. 11-state delivery_state machine (Not Started + 8 active happy-path + Delayed + Blocked off-path); LEGAL_TRANSITIONS matrix with **23 legal transitions**; 8 transition guards per FR-7/FR-28 (transition_legal + OwnerClosed 2-condition + PMApproval gate via pm_approval_at field + carrier-upload + CLOSED requires TPM + Delayed/Blocked unconditional + OwnerClosed→UnderPMReview auto-advance + Rewind-from-Submitted requires TPM attribution); 4 cross-cutting orchestration actions consumed by workflow_engine task bodies: update_delivery_state, instantiate_default_workitem (FR-78), reassign_document_to_workitem (FR-83), propagate_tags_to_active_trackers (FR-82), apply_manual_tpm_override (FR-14); StateChangeDispatchSignal pattern breaks dependency cycle (tracker returns signal, workflow_engine task body fires StateChange trigger — keeps dependency one-way per `[D-022]`); 3 dependency-injection Protocols (StorageWriter / SpWriter / AuditWriter). **6 architect decisions captured inline as MODULE.md Key choices**: pure functional guards; dependency injection via Protocols; atomic state+SP+audit one-transaction; OWNER_CLOSED transient auto-advance; PM approval field on DeliveryItem (Option B; pm_approval_at + pm_approval_pm_id) with clearing discipline; rewind-from-SUBMITTED with TPM attribution (Option A for customer RFI). **2 architect decisions worth ADR capture** (selected at Step 3 triage): `[D-067]` customer RFI rewind path (SUBMITTED_TO_CUSTOMER → DOCUMENT_RECEIVED/OUTREACH_SENT with TPM attribution; TRK-E006); `[D-068]` PM approval recorded via DeliveryItem field (pm_approval_at; clearing discipline; rejected CommunicationLog query + implicit alternatives). Other tracker design refinements (matrix corrections + invariants): DELAYED/BLOCKED moved to reachable from OUTREACH_SENT onwards (not from OPEN); Confirmation items MUST have no_customer_upload=True; all Ph-1 CLOSED transitions are TPM-manual per FR-7+DEF-20 (per-item Mark-Closed + FR-64 batch); D-039 Steps 0+1 (Ph-1 deterministic hash dedup + slug match) fire on FR-83 reassignment with Ph-2 LLM Step 2/3 deferred (TRK-W007); apply_manual_tpm_override routing discipline (sub-trigger fields → rule_engine path; non-sub-trigger fields → FR-14 path); trigger_source 3-value semantic (automated / manual_tpm_override / tpm_button). 2 worked examples: OwnerStatusConfirmed "done" round-trip through OWNER_CLOSED transient + StateChange dispatch; FR-64 batch close milestone-level pseudocode + idempotency. Phase B Layer 2 cascade now: 5 modules done (email_service, customer_adapter, rule_engine, workflow_engine, tracker — tracker actually Layer 3 but drafted same session); 2 modules remain (messenger, test_report). Module count remaining drops 10 → 8 across all layers.

- 2026-06-10 Session 2 (Architecture phase resumed) — **`rule_engine/MODULE.md` initial Ph-1 draft** (~315 lines; NEW at `core/src/rule_engine/MODULE.md`). 8 sub-modules (`models.py` / `loader.py` / `resolver.py` / `evaluator.py` / `polling_schedule.py` / `orphan_audit.py` / `collision_audit.py` / `pause_state.py` + 2 CLIs). Public surface: TriggerKind (15 Ph-1 triggers), ActionKind (18 Ph-1 actions; Ph-2 deferred), RuleScope, RuleKind discriminator (TRIGGER_ACTION / POLLING_SCHEDULE), Rule (with kind discriminator + optional shape fields), PollingScheduleTier, EntityRef, RuleAction, TriggerEvent, RuleMatch, RuleSet, resolve_rules_for_entity, resolve_polling_schedule_for_item, RuleEngine.evaluate/explain, evaluate_polling_schedule, orphan + collision audits, PauseStateLookup Protocol, RuleEngineConfig. **4 architect decisions captured inline**: Q1 (Ph-1 action subset; Ph-2 → Deferred); Q2 (`[D-022]` pure evaluator boundary reaffirmed — no Celery / no SP write / no DB writes); Q3 (rule_engine owns Postgres `AutomationRuleOverride` orphan-audit per `[D-062]`); Q4 (**per-trigger ordered action lists; no priority / no score / no first-match — captured as `[D-066]`**). **2 model-shape decisions**: F1 absorbed via `Rule.kind` discriminator (TRIGGER_ACTION / POLLING_SCHEDULE) over splitting into two model classes (captured in MODULE.md Key choices, not separate ADR); F2 closed (sentinel `rule_id="__all_rules__"` for FR-31 sub-1 pause; ItemPauseRecord rejected — `AutomationRuleOverride` stays the single override table). **3 worked examples** grounding the abstract API: (1) `OwnerReassigned` single-rule-2-ordered-actions Global-only; (2) `LastContactThreshold` multi-rule additive composition (Customer overrides Global + adds new customer-specific rule); (3) `AttachmentReceived` + `polling_schedule` with FR-31 sub-2 Postgres tier override end-to-end. Each example walks YAML → resolve() → evaluate() → workflow_engine consumption + TPM SP UI options (FR-31 sub-1 / sub-2 / sub-3). Phase B Layer 2 cascade: rule_engine done; 3 modules remain in Layer 2 (workflow_engine, messenger, test_report). Module count remaining drops 11 → 10 across all layers.

- 2026-06-10 Session — customer_adapter/MODULE.md review pass (4 edits: FR-77 caller-resolves clarifier; SP UI FR-84 writeback invariant; selectors_version compatibility-pointer doc; selector-pack deploy-time refresh worked-example + CAD-W004) committed `b7d6edb` + pushed. **`[D-052]` ADR-body impl note 2026-06-07 appended** (dual→tri-backend: backend Literal expanded `["ollama_a4000","vllm_dgx","corp_llm"]`; hardware topology corrected — HILDA PC has no GPU; cross-refs `[D-068]` + `credential_service` SystemType split; chronology marker added to 2026-05-28b impl note) per `[D-002]` append-only. **regen-map run 2026-06-10** — 4 Structure sections populated with real code (`diagnostics`, `issue_tracker` core, `sharepoint_integration`, `template_schema`); 5 marked `[DRAFT]` (`credential_service`, `customer_adapter`, `email_service`, `llm`, `storage`); `MAP.md` wholly regenerated. Both committed `fd78e76` + pushed. Flagged: `customizations/issue_tracker/MODULE.md` missing Structure markers; `customizations/sharepoint_config/` NEW dir without MODULE.md. **`sharepoint_integration/MODULE.md` drift sweep** (uncommitted; 14 edits A1–A6 + B1–B4 + C1–C4): 8-list canonical entity set per `[D-051]`; `SpCrud.delete_item` + `SharePointListProvider.from_sp_fields` added to declared Public surface; `mock_server/` sub-module documented + `--serve --port` CLI mode; sample counts bumped 7→8; FR-84 outbound writeback invariant (SP→HILDA HTTP firewall-blocked; HILDA→SP REST sole writeback channel); FR-87 TPM resolution writeback path invariant (strict A→B→C ordering); column-map append-only invariant for 2026-06-08 cascade fields; SP Choice-field value sync added to Non-goals (SP UI engineer owns Choice values); SP-alert email channel added to Non-goals; Depended-on-by extended (`issue_tracker`, `customizations/issue_tracker`, indirect `customer_adapter`); two-halves-of-the-same-conversation positioning note for SP UI engineer collaboration; Rollback log block added. **HILDA_SP_Schema.xlsx Excel deliverable for SP UI engineer** (uncommitted, NEW at `docs/sp_ui_engineer/HILDA_SP_Schema.xlsx`) — 9 tabs (README + 8 SP lists per `[D-051]`), 112 rows covering Ph-1 + Ph-2 columns, 10-column-per-row layout (HILDA canonical → SP internal suggested → SP display → type → Choice values → Required → Phase → Default → Source FR/Decision → Notes); README documents the 3-tier name model (HILDA canonical / SP internal / SP display); 5 `???` cells flagged for HILDA-architect confirmation; covers Confirmation/TEST_TECH_WAIVER_REPORT/COMPLIANCE_CERTIFICATION_RELEASE_NOTES/Default ItemType + 11-value delivery_state (Not Started + 8 active + Delayed + Blocked) + PM/TPM/TeamLead/Admin roles. **Phase switch architecture → requirements** mid-session for FR-77 naming sweep evaluation; sweep dissolved into **no-op** under requirements-phase review (current variants "outermost source folder" / `<matched_source_folder>` / "outermost folder name relative to milestone root" / "outermost folder name inside the zip" are correctly specific per user — collapsing to generic `ingress_folder` would erase positional context).

- 2026-06-16 **Extensive FR review session — 19 FRs reviewed with cascade-drift fixes** (continuation of Ph-1 SP architecture cascade). FRs reviewed: FR-2 retrospective audit + supplementary-paragraph discipline established (M1-M4 retroactive misses caught); FR-3 → Ph-2 via DEF-22 (separate entry below); FR-5 (plm_id key locked to (device, milestone, owner); item_no spec; dashboard URL → <customer_id>/<item_id>); FR-6 (precedence ladder + milestone_completion_pct + no-op suppression); FR-7 (4th Closed path; Delayed/Blocked exit; CustomerJIRA-only → Confirmation; review_required defaults; doc_count=0 unified skip); FR-8 (PLM/outreach keys to (device, milestone, owner); per-channel polling activation; crash recovery); FR-9 (CustomerJIRA-only state machine); FR-10 (OutreachSent/DocumentReceived trigger; reminder_count + last_owner_response_at xlsx columns added; per-(device, milestone, TG, owner) escalation); FR-11 (Ph-1 cascade flow; multi-rung deadline; FR-10 combined-message rule); FR-12 (BATCH-id resolution; prior_delivery_state + manual_triage_required discipline; lenient sender match); FR-13 (NSD topology + path conventions + QF1-QF4 quoted-section catch; _staged_revision/_outbound/ underscore-prefix convention); FR-14 (Option B owner override → Ph-2; 13-fix comprehensive rewrite); FR-15 (timestamp + audit columns hub; FR-15-extended semantics labeled); FR-16 (parser firing post-classified-landing; FR-85 review-vs-pipeline invariant added); FR-17 (Ph-2 multi-revision rewrite with FR-66 cross-ref + I6 logical restructure); FR-46 (test_report final|interim with not_classified third value); FR-52 (sequential pipeline + symmetric M:M + DocumentIndexRow/DocumentItemAssociation split + (owner_corp_id, plm_id) fan-out key per user correction); FR-53 (review_status 4th value failed added; per-revision firing); FR-55 (milestone-level polling frequency); FR-56 (13-fix column model + 4 new buttons + delivery_state Writable=YES/TPM); FR-57 (PLM fan-out multi-scope behavior; download_url transient-NSD-location rationale; is_final in data shape); FR-58 (per-item_type behavioral guide); FR-59 (NSD path + is_final-aware Ph-2 + FR-47 picker + failure badges); FR-60 (review_status failed + classification not_classified + parser-failure display + FR-47 picker). Plus sweeps: xlsx row-number sweep across FRs; (owner × milestone) → (device, milestone, owner) sweep (11 instances); "PM dashboard" → "HILDA-rendered document section per [D-074]" sweep (6 high-impact HILDA-tab references). SP_UI_button_actions.md restructured to Part 1 (Write Actions; 3 new buttons: Clear Triage Flag + Confirm Carrier Acceptance + Resume from Delayed/Blocked) + Part 2 (Read Paths). SP_lists_authoritative.xlsx: 4 new columns (prior_delivery_state, reminder_count, last_owner_response_at, manual_triage_required) + delivery_state Writable flipped. ~30 commits to origin/main.
- 2026-06-16 **Ph-1 scope reduction — FR-3 (mid-milestone item management) deferred to Ph-2** via DEF-22. Cascade across requirements.md: FR-3 retagged `[Ph-2]` with Ph-1 capability-gap framing; FR-2 R&R lock revised (`setup_milestone` is sole row-creation path in Ph-1; mid-milestone on-demand add returns in Ph-2); FR-2 paragraph (c) clarified (per-milestone row set is static after `setup_milestone` in Ph-1); FR-8 mid-collection `ItemCreated` → `StartItemCollection` path tagged `[Ph-2]`; FR-39 TPM-adjustment path narrowed to FR-14 (field overrides) for Ph-1; FR-88 owner-change event semantics paragraph tagged `[Ph-2]` (the 3-field owner identity model itself stays Ph-1); Appendix A rule-trigger table rows 2 (`ItemCreated` mid-collection) and 3 (`OwnerReassigned`) phase column Ph-1 → Ph-2; phase-scope index row for FR-3 updated; DEF-22 entry added. **Ph-1 capability gap accepted**: if R&D owner transfers mid-milestone, row stays with original owner until Ph-2; TPM works around via out-of-band contact. Pushed to origin/main.
- 2026-06-15 **Ph-1 SP architecture cascade — 1 flat per-carrier list model** (after SP UI engineer disclosed his actual Ph-1 SP implementation differs from `[D-077]` v2 4-list normalized model). User confirmation: 1 list per customer (e.g., `Tasks_MMK`, `Tasks_VZW`) carrying every customer / device / (device, milestone) / milestone / TG / work-item field as columns duplicated by scope; HILDA reads via `$filter` on column values; milestone-scoped writes touch every row in the milestone with HILDA-side dedup per `[D-082]` cascade pattern; `assigned_pm_id` confirmed scoped per (device, milestone). Cascade edits: **requirements.md** — FR-2 (Ph-1 R&R lock + flat-list structure + full column duplication scope breakdown), FR-5 (Ph-1 uniqueness `(model, minorMilestone, item_no)` within `Deliverables_<customer_id>`; no SP Counter PK chain), FR-9 (assigned_pm_id source = work-item row column scoped per (device, milestone)), FR-13 (NSD path values read from work-item row directly), FR-40 (sharepoint_config canonical→SP-column mapping per-customer for the flat list; template.yaml structure unchanged), FR-84 (5-field routing → list select by `customer_id` + single `$filter` GET; milestone-scoped writes via N-row duplication + dedup; sentinel-row pattern dropped Ph-1), NFR-21 (read scope = N per-carrier lists with `$filter`; write scope = N-row duplication for milestone-scoped writes). **DECISIONS.md** — `[D-077]` 2026-06-15 impl note (Ph-1 flat per-carrier list simplification with full column-duplication inventory, trade-offs, Ph-2 normalization plan); `[D-073]` 2026-06-15 impl note (partial revert — Customers + Devices separate SP list provisioning moot for Ph-1; restored in Ph-2). **SP_UI_button_actions.md** — milestone-scoped buttons revised to N-row duplication writes; SP-alert configuration updated to per-customer `Deliverables_<customer_id>` lists. Pushed to origin/main.
- 2026-06-09 Same-day cascade follow-on review work (commits `50086b2`, `adf1759`, `bef99e0`, `65945e9`, `ad31919`, `65a53b7` — all pushed to origin/main): **email_service/MODULE.md** review pass (4 corrections + tg_resolver email-scope correction per `[D-060]` impl note 2026-06-09 + test-scenario rewrite to single-TG single-doc happy-path + over-routing threshold 10 per 5G/4G domain reality + Ph-1-first discipline shift + Test-scenario = initial validation NOT design scope clarifier). **diagnostics/MODULE.md** drift fixes (18→20 module prefix count; added CMG/CPG for corp-side gateway modules per SYSTEM.md §2.1). **credential_service/MODULE.md** — SystemType.LLM_GATEWAY split into 3 per `[D-052]` tri-backend (LLM_OLLAMA_A4000 / LLM_VLLM_DGX / LLM_CORP_LLM); per-backend .enc.env files; Purpose anchor list + Depended-on-by updated. **Locked conventions this session**: Ph-1-first discipline (active surface = Ph-1 only; Ph-2 → Deferred); Ph-2 gate enforcement (Claude flags Ph-2 dives going forward); Test-scenario = initial validation flow (full Ph-1 still in design scope). **No new ADRs this session** (all changes captured under existing `[D-053]` / `[D-055]` / `[D-060]` / `[D-052]` impl notes from earlier today).

- 2026-06-17 **FR cascade-drift review pass — 18 FRs reviewed (continuation from prior session post-compaction)**. Reviewed FR-18 (Iₙ run-id contract + carrier scope), FR-19 (4-pattern adapter discipline + CarrierAdapterError catalog), FR-20 (CSA-E000 pre-flight credential check + FR-19/FR-18/FR-63 alignment), FR-23 (3-tier inbound + outbound mailbox + bounce + per-env isolation), FR-24 ([HILDA:...] tag structure + routing precedence + FR-12 anomaly), FR-25 P1-P10 + I1-I7 (corp PLM gateway-mediated + JIRA individual credentials + P8 PLM closure final lock = Option B at FR-76 sweep), FR-26 (owner_corp_id key + FR-86 4-path landing + cadence cross-FR roster), FR-50 (messenger scope locked to FR-11 escalation + outbound-only + MSG-E001-E005 catalog), FR-29 (action-list comprehensive cascade-drift lock + FR-7 messenger→email + FR-66 STATUS Flag), FR-28 + FR-7 + FR-58 (CustomerJIRA-only OutreachSent path correction), FR-30 (Ph-1 override mechanism per FR-56 (a) + SIGHUP reload + polling_schedule canonical roster), FR-31 (entirely deferred to Ph-2 per architect lock; SP_UI_button_actions sync; Ph-1 TPM SP UI capability inventory locked), FR-51 (4 identity-model patterns + cascade to FR-25 P6 + FR-50), FR-39 (template path verification + SIGHUP workflow + R&R explicit), FR-40 (SP write target lock + TG-denormalized drift fix + path consistency). New error codes added: CSA-E000-E007, ITR-E002-E004, EML-E001-E002 + EML-W001-W009, MSG-E001-E005, STATUS-W002-W003. STATUS Flags updated: NFR-21 review per D-083; FR-66 version-selection mechanism vs FR-50 outbound-only; FR-83 reassignment-induced revert pattern. ~28 commits to origin/main.

- 2026-06-17/18 **SP architecture rewrite cascade — 5 canonical decisions ratified (D-083 through D-087); ~20+ FRs rewritten; global string sweeps + auto-memory rewrite**. SP UI engineer's revised schema (Google Sheets `1gqmD9QVLQjuJ08Wc7zc7KI3jKQhr9MjH`) pulled and integrated. **`[D-083]`** — 2-list SP architecture: per-customer `Deliverables_<customer_id>` Work Items + global `Milestones` list; existing global `Projects` list reused for `project_id → TPM` lookup; supersedes `[D-077]` flat-table. **`[D-084]`** — Form factor flag names `handset / tablet / wearable / ir / osmr / rmr / hmr_smr` (supersedes `[D-081]` `drr / ir_ffw_p1 / mr`). **`[D-085]`** — `Milestone.target_date` is sole authoritative deadline; `expected_completion_date` removed from Ph-1 schema; `[D-082]` cascade-dedup obsoleted for date propagation. **`[D-086]`** — All SP email / corp_id / owner_name columns are STR free-form text; no AD resolution / Person-Group / auto-derivation. **`[D-087]`** — `customer_delivery_credential_id` removed; FR-19/FR-51 per-(PM, carrier) sops files is canonical credential lookup. **FR rewrites**: FR-40 (template.yaml + 2-list architecture); FR-11 (target_date sole source); FR-25 (b) (customer_jira_url to template.yaml); FR-84 (per-list SP-alert routing — Source A Deliverables + Source B Milestones); FR-63/FR-64/FR-73 (milestone button single-row writes); FR-2/FR-6/FR-8 milestone-scope writes; FR-9 outreach (free-form text + FR-11 escalation + email-only); FR-56 (a) owner block + TG-denormalized fields. **Global sweeps**: `Tasks_<customer_id>` → `Deliverables_<customer_id>` (54 refs); `close_all_items_triggered_at` → `closed_all_items_triggered_at`; form factor flag rename; `expected_completion_date` cleanup sweep (16 refs across 7 FRs + SP_UI). **SP_UI_button_actions.md**: milestone button sections + Part 2 read paths rewritten for single-row Milestones writes + cross-list join discipline. **Auto-memory `project_sp_architecture_ph1.md`** fully rewritten: 2-list + Projects coupling; PM lookup chain; identity-model patterns; Ph-1 TPM SP UI capability inventory.

- 2026-06-19 **FR cascade-drift review pass — 16 FRs deep-scanned + D-088 ratified + xlsx + SP_UI_button_actions cascades**. Ratified `[D-088]`: `Projects.TPM` is SharePoint Person/Group column; HILDA extracts 3-tuple `(assigned_pm_id = TPM.User_name, pm_display_name = TPM.Name, pm_email = TPM.Work_email)`. **FRs deep-scanned**: FR-8 (25 findings), FR-6 (16), FR-3 (24), FR-7 (21), FR-9 (17), FR-11 (19), FR-84 (7+7 — full rewrite per architect screenshots + verification pass), FR-12 (18), FR-25 in full (12 + owner_corp_email correction), FR-40 (15 + YAML key reshape `<milestone_id>` → `<milestone_name>`), FR-56 (17), FR-28 (8), FR-14 (5 + customer_jira_url sweep), FR-26 (5), FR-13 (5 + Model → project_model cross-FR sweep). **Architect-direction locks** (mid-session): FR-56 (f) Refresh + FR-73 Download Package deferred to Ph-2; FR-66 corp messenger inbound locked OUT per FR-50 cascade; CustomerJIRA Ph-1/Ph-2 boundary (Ph-1 polling in HILDA Postgres only; Ph-2 adds SP write-back); owner_corp_email matches TPM.Work_email (corrected from owner_corp_usa_email; corp SP not USA-domain); FR-7 CorporateMessenger kept in 6-value enum for Ph-3+ but Ph-1/Ph-2 unused; customer_delivery_modality = Google Drive (Ph-1 lock); tg_name = 16-value globally fixed enum across all carriers; review_status = 4 values (pending/complete/not_required/failed); template.yaml YAML key reshape. **xlsx changes**: milestone_completion_pct PERCENTAGE on Milestones list; jira_open_ticket_count + jira_ticket_summary_json Ph-2 on Workitems list; TPM Data Type STR → Person/Group; new column H "LIST values (Choice enums)" with 10 LIST-type field enums; multiple Choice value locks. **SP_UI_button_actions.md**: Refresh + Download Package Ph-2 deferral + 2026-06-19 changelog entry. **Cross-FR sweeps**: milestone_id → milestone_name (template.yaml context only); Model → project_model SP-internal column name; FR-7 / FR-25 / FR-52 session finding-number anchors → stable phrasings; customer_jira_url stale references swept (FR-14 + NFR-21); ITR-E003 added to CredentialExpired trigger per FR-25 (b). ~48 commits pushed to origin/main.

- 2026-07-02 **End-of-window architectural design pass — SP alert reliability + 5-sync reconciliation (Path A) + Path A/B/C framing**. Milestone ADDED alert walkthrough confirmed HILDA has NO MilestoneTable + no `import_milestone_task` + no rule matches on `list_name=Milestones, sub_trigger=added` → HILDA acknowledges (dedup + audit) but writes nothing; Milestone metadata read via SpCrud on-demand at rule/task time. **SP alert reliability analysis**: at 250-concurrent-alert burst (5 carriers × 50 items setup deliverables), expect 3–15% alert loss due to SP internal alert-queue throttling + Exchange outbound-SMTP throttling + EWS pull batching + alert coalescing + body truncation + occasional spam-filter drops. **5-sync reconciliation design (Path A)** proposed with detailed review + field-name corrections: sync-1 delivery_item_count / sync-2 milestone-start-collection / sync-3 deliverable-approved (`pm_approval_at` + `pm_approval_pm_id`) / sync-4 milestone-submit-to-carrier / sync-5 milestone-close-all-items. Each is a Celery beat task (5-min default, 2-min for sync-3), invokes the same task body the email path invokes with `trigger_source="sync_backfill_*"`, terminates on convergence (no persistent stopped flag needed — task naturally no-ops when conditions aren't met). Trigger predicate refined: "**at least one** item still in the pre-transition state" (not "all") so partial-completion cases converge cleanly on subsequent ticks. **Path A / B / C architectural framing**: A = SP-authoritative + reconciliation (status quo + fix); B = hybrid (SP retained for carrier/project data which SP naturally owns; HILDA-native TPM UI for deliverables/milestones which HILDA fully owns semantically); C = full HILDA-native (carrier/project ingested from SP read-only). Recommendation: **A now to ship Ph-1, B post-stable-Ph-1 as the correct long-term shape**. No code — design captured for next session's cascade.

- 2026-07-02 **Template.yaml seed cascade + Deliverables-CHANGED null-guarded SP sync + customer_delivery_info denormalization + point-fixes (~1000 lines)**. Addresses SP alert body_kvs missing template-defined fields at Deliverables setup time (doc_count / target_folder / tracking_modality / milestone_gating / item_type / item_description / tg_path_id / item_path_id / form-factor flags + template-seeded no_customer_upload / force_tracking_enabled / review_required + customer_delivery_info). **Chunk 1**: new `core/src/template_schema/template_lookup.py` — process-local eager cache of all `customizations/template_schemas/*/template.yaml`; API `get_workitem(customer_id, device_id, milestone_id, item_no)` + `get_customer_delivery_info(customer_id)` + `get_delivery_path_template(customer_id)`. **Chunk 2**: `sp_alert_imports._build_delivery_item` merges template values (authoritative) + body_kvs (fallback + SP-dynamic fields) via type-aware helpers `_tmpl_bool` / `_tmpl_str` / `_tmpl_list`; falls back to body_kvs-only when template lookup misses. **Chunk 3**: new `scripts/backfill_static_fields_from_template.py` — one-shot idempotent null-guarded backfill for existing Postgres rows imported pre-cascade; per-customer scope + `--dry-run`. **Chunk 4**: new `SYNC_DELIVERABLE_FIELDS` ActionKind + `sync_deliverable_fields_task` + rule `sync_deliverable_fields_on_changed` — Deliverables CHANGED alerts apply null-guarded merge (int=0 / str=None|`""` / list=[] / bool=key-missing → skip; skip preserves current). **Follow-up (same day)**: `customer_delivery_info` denormalized from template.yaml root onto each item — root cause of CAD-E010 upload failures every submit_to_carrier run since June 24. **Also close-all-items cascade** — new `CLOSE_ALL_ITEMS` ActionKind + rule `close_all_items_on_milestone_closure_triggered` + task binding + parser `closed_all_items_triggered_at` promotion; task body (FR-64 (b) HILDA-owned per-item cascade, landed pre-window) was never wired to dispatcher; item_id lookup fix (was returning None because task only checked `delivery_item_id` attr while items expose `item_id`); idempotent-noop counted as success. **Point-fixes**: `kickoff_collection` eligibility excludes `item_type=Default` (defense-in-depth against SP UI engineer's setup script leaving `force_tracking_enabled=True` on SP row default per FR-81; Default WI was silently transitioning NS → OutreachSent in `__no_owner__` bucket, corrupting FR-78 hardcoded invariant); `rule_engine.evaluator._eval_leaf` no longer emits `RUL-W006` on missing field + `eq null` / `neq null` — missing IS semantically null; every kickoff/submit alert was warning `possible typo'd field name` on sibling trigger rules whose condition field wasn't in that particular alert (fail-closed preserved; only misleading warning suppressed). Commits: `8a60abb`, `5b09af4`, `2457af4`, `c093692`, `53e11ae`, `df0f7b3`.

- 2026-07-01 **JsonFileCredentialService + bootstrap adapter auto-discovery + Dashboard Ph-1 minimal + Submit-to-Carrier state-transition silent-failure fix**. **JsonFileCredentialService**: Ph-1 plaintext-JSON credentials (`config/customer_adapter.json` parallel to `sharepoint_integration.json` pattern) — no sops in Ph-1 for corp GDrive auth. **Bootstrap adapter auto-discovery**: `bootstrap_task_deps` auto-discovers per-customer adapter subclass via `HILDA_CUSTOMER_ID` env var (default `"MMK"`) + convention `customizations.customer_adapter.<customer_id>_adapter.ADAPTER_FACTORY(audit_writer=audit)`. Optional `bootstrap_directories(template)` call at boot pre-creates GDrive folders (default ON per architect 2026-07-01; `HILDA_SKIP_GDRIVE_DIRS_BOOTSTRAP=1` to opt out). sys.path guard so `customizations` package is importable from Celery worker context. Logger level fixes: root + `customizations.customer_adapter.*` promoted to INFO **before** the customer_adapter is built, so mmk_adapter's `DBG_MMK` factory trace lands in stdout regardless of Celery log-config timing. Diagnostic bootstrap-result logging elevated to WARNING level for the same reason. **Dashboard Ph-1**: TPM's "View Docs" button lands on a 3-column HTML table (#, Filename, humanized Doc Type) per architect lock 2026-07-01. `sp_id` column added to DeliveryItem (int, nullable); populated at import time via SP READ by natural key `(project_model, item_no)` after `create_delivery_item`. `ph1_minimal: bool = True` flag on `DashboardConfig` gates FR-60 review findings + FR-61 mediated download + FR-87 TPM buttons + revision-resolution + per-load SP READ blocks — Ph-2 flips one flag and templates re-hydrate the richer view. Dashboard auto-wires `PostgresStorage()` when uvicorn `--factory` calls `build_app()` with `storage=None`. `HILDA_STORAGE_DB_URL` env resolver falls back to `HILDA_DB_URL` (Docker Compose env). **Submit-to-Carrier state-transition silent-failure fix**: live smoke showed `uploaded_items=9` in task summary but all 9 items stayed in RFS in Postgres. Silent-failure chain: task wrote `carrier_upload_complete=True` to storage before transitioning; field is NOT a column on DeliveryItemTable and `update_delivery_item`'s hasattr guard silently skipped the write; Guard 4 read `getattr(item, "carrier_upload_complete", False)` = False; blocked the RFS → SubmittedToCustomer transition; `tracker.transitions.update_delivery_state` returned outcome=`guard_denied` without raising; task's `_transition_to_submitted` caught nothing then unconditionally bumped `uploaded_items++`. Fix: Guard 4 trusts `trigger_source='submit_to_carrier_task'` as authoritative evidence of upload completion; `_transition_to_submitted` returns bool; caller increments `uploaded_items` only on true success (else `partial_items`); new audit action `submit_to_carrier_transition_denied`. Commits: `e5e955b`, `9072194`, `87ee967`, `30f947f`, `a956dce`, `d7e21e5`, `4c1e062`, `3243c9f`, `184e82e`, `31d5e2a`, `c52777b`, `5cbc383`.

- 2026-06-30 **Submit-to-Carrier cascade — milestone-scoped upload orchestrator**. New `SUBMIT_TO_CARRIER` ActionKind + rule `submit_to_carrier_on_milestone_submission_triggered` + `submit_to_carrier_task`. Task iterates RFS items in the milestone, uploads classified documents per item via `customer_adapter.upload_attachment`, transitions each item RFS → SubmittedToCustomer on per-item all-files-success. `browser_automation` queue per [D-054] adapter throughput cap; `autoretry_for=(Exception,) + max_retries=2 + retry_backoff=180 + retry_backoff_max=300 + retry_jitter=True` = 3 total attempts at ~t=0, ~t=3min, ~t=8min. Parser `_MILESTONE_PROMOTE_FIELDS` tuple surfaces `milestone_collection_started_at` + `milestone_submission_triggered_at` from body_kvs to `field_deltas` when SP CHANGED alert omits Edited markers (architect Q3 lock 2026-06-30). Commits: `c818963`, `62a3222`.

- 2026-06-29 to 2026-06-30 **Inbound-attachment cascade hardening — 12 point-fixes surfaced by live smoke testing**. JIT back-fill of `item_description` / `tg_path_id` / `item_path_id` at attachment time (were NULL on import → FR-52 Step B1 matcher blind → everything landed in `_unrouted`); FR-52 Step B1 AND-of-OR substring semantics per FR-82 nested tag-set model; serialized attachment + owner_reply chain to eliminate doc_count-reach vs owner-intent-close race; `doc_id_slug` column widened 64 → 256 chars; STR-E001 vs STR-E003 error classification cascade; classified path composition fix (was creating file literally named `rev1` instead of writing into `rev1/` directory); per-customer doc_type rules loading per [D-091]; SP body_kvs parser handles dash-less Edited-value form; JIT doc_count back-fill never writes 0; DocumentIndexRow.milestone_id derived from batch context (was NULL for unrouted). Commits: `818efcd`, `c2b61e8`, `8240363`, `decc96b`, `02951ef`, `566c90b`, `4479a7c`, `7c4c62d`, `a4e375f`, `24eae60`, `2d66447`, `a201431`, `ef073dd`, `1fc4bd4`.

- 2026-06-28 to 2026-06-29 **PM approval Pattern A cascade**. `apply_pm_approval_task` lands as SP-authoritative mirror per [D-068]: SP UI engineer's Approve button atomically writes `delivery_state=ReadyForSubmission` + `pm_approval_at` + `pm_approval_pm_id`; HILDA reads via CHANGED alert and mirrors to Postgres without invoking guards (SP is authority). Dispatcher `_PM_APPROVAL_DELTA_FIELDS` refinement discriminates the sub-trigger; datetime coercion accepts ISO + SP `M/D/YYYY` forms; EntityRef.delivery_item_id synthesis path unblocked (was `None` on Deliverables CHANGED, blocking the whole dispatch). Owner-intent race resolution wired (`owner_intent_closed_at` cleared on OwnerClosed entry; new rule `reconcile_owner_intent_on_doc_count_reached`). ADR candidate for Pattern A doctrine — logged in Flags. Commits: `2ca7a2a`, `3874041`, `032ad19`, `3ffcddb`, `820764a`, `820ac33`, `24eae60`, `d9669c1`, `1034111`, `00dfba0`.

- 2026-06-28 PM (~14:00 → 16:00 PT; 2-3 commits ab9a2f9..[D-138-cascade]) **[D-137] tracker.transitions SP writeback fix + [D-138] DeliveryState value alignment to SP UI Choice column verbatim**. Architect mid-PM-approval design pass spotted: HILDA-driven state transitions (NS→Open→OutreachSent→OwnerClosed→UnderPMReview) were NOT reflecting back to SP Deliverables list. **Two compounded failures at transitions.py:335-348**: (A) dominant — `sp_scope = event_context.get('sp_scope')` was never set by dispatcher/kickoff-driven flows, so the entire SP-write branch silently skipped; (B) latent — even when sp_scope set, direct write passed HILDA's composite delivery_item_id to SP REST /items({Id}) which expects integer auto-counter Id (HTTP 400). **Fix commit `f9542fa`** (D-137 application): new `_sp_writeback_field_updates` helper derives ListScope from item.customer_id (no event_context dependency) + does the D-137 two-step natural-key → _sp_id lookup; best-effort per [D-118]. 3 new tracker tests cover the SP-writeback success/no-row-match/missing-customer-id branches. **Cascade trigger**: with D-137 fix landing real SP writes, architect immediately spotted that SP would reject most state values because of value-string drift — screenshot of SP UI Delivery State Choice column showed 11 values where multi-word values use PascalCase-NO-space (OutreachSent / OwnerClosed / UnderPMReview / DocumentReceived / ReadyForSubmission / SubmittedToCustomer), but HILDA's enum had space-bearing values ("Outreach Sent", "Owner Closed", etc.). **D-138 cascade**: 80 occurrences across 18 files updated via mechanical sed — enum, production code (6), tests (5), MODULE.md docs (4), customizations YAML (2), compact docs (2). **D-138 ADR ratified**; D-124's title-vs-Decision-block inconsistency formally captured (D-124's Decision block always showed no-space values; only the title said "PascalCase + spaces" — a typo that caused enum-file drift between ratification and 2026-06-28). 908/908 tests still passing post-cascade. **Unblocks PM approval design pass**: SP UI engineer's button visibility logic depends on delivery_state being current AND in the correct Choice value — both now true after D-137 + D-138. Test path on deploy box: pull + restart worker; reset items to OutreachSent (use new spelling); kickoff fresh; watch SP UI rows propagate through the cascade live.

- 2026-06-28 (00:22 → 11:16 PT; 15 commits e87a113..fed0b45) **Step 5 Phase A multi-owner outreach + Phase B owner-reply handler end-to-end live-validated on corp Exchange+SP**. **State transition fixes** (00:22 → 00:53): `e87a113` kickoff_collection NS→Open transition per item before dispatching ItemCreated (state machine required intermediate Open before OutreachSent); `a200578` PostgresStorage no-op `write_delivery_state` stub (was: AttributeError crashed every transition mid-cascade between send_initial_outreach audit + Open→OutreachSent); `a8dbb7e` tracker.transitions guards sp_writer.update_item on non-None scope + wraps in try/except — best-effort SP writeback doesn't roll back Postgres state change per [D-118]. **Step 5 Phase A multi-owner outreach** (06:32 → 07:29): `9538926` NEW `core/src/email_service/templates/outreach_table.j2` HTML template with editable status + owner_status_note cells + tagged `HILDA-BATCH-ID:` anchor for inbound correlation + ews_sender auto-detects HTML by leading `<` and wraps with exchangelib HTMLBody; `ddca41d` kickoff_collection MAJOR REWRITE — filter `force_tracking_enabled=true AND delivery_state="Not Started"` (idempotent re-click safe), group eligible items by owner via Path A SP batch-read, send ONE batch email per owner via new `_send_batch_outreach_email` helper, transition each item NS→Open→OutreachSent inline with shared batch_id (deterministic uuid5 over correlation_id+owner_key). **Phase B owner-reply handler** (09:32 → 11:16; 9 commits): `adf6630` NEW `core/src/email_service/inbound/body_parser_table.py` (BeautifulSoup HTML table extractor, structural header→column-index map, tolerant to email-client mangling, returns existing `StructuredReplyBlock` so consumers stay parser-agnostic) + NEW `core/src/workflow_engine/tasks/owner_reply.py` `apply_owner_reply_task` Celery task (BATCH-id lookup via audit trail, parser cascade table→text, per-row dispatch with note-only-on-Open path, audit-before-action invariant, sp_writer for note SP write-back) + email_polling.py classifier branch for OWNER_REPLY + bs4 added to requirements.txt + 12 unit tests + 7 stale-test repairs from Step 5 Phase A rewrites + 2 new pre-kickoff coverage tests (905/905 passing). **Phase B iterative live-test fixes**: `99e910b` EWS subject filter OR's `subject__contains="BATCH-"` (owner replies have subject "RE: [HILDA] Status request -- BATCH-<id>" which doesn't match SP-alert prefixes — were silently dropped at EWS query layer); `59eadf8` tasks/__init__.py imports owner_reply so worker registers `apply_owner_reply` task at boot (was: "Received unregistered task of type" error, Celery only registers via decorator at import time); `f0c9f99` `_lookup_batch_items` derives item_no from live delivery_item row instead of audit details (kickoff wasn't writing item_no to send_initial_outreach details — `batch_not_found` symptom); `53edffb` ews_receiver widens body_html acceptance via `_extract_body_html` helper (isinstance(HTMLBody) OR `<html|<body|<table|<div|<p|<br` markers — Outlook reply downgrades BodyType=Text while preserving HTML markup); `982003b` parse_table_block runs anchor regex on bs4-extracted text not raw HTML (Outlook inserts `<span mso-spacerun>&nbsp;</span><o:p>` between `HILDA-BATCH-ID:` label and `BATCH-<id>` token); `af756f8` parse_table_block drops literal anchor regex entirely, plain substring `if batch_id not in body: return None` (Outlook quoted-history content between label and token defeats any regex); `6c08425` WARN log when bs4 ImportError (root cause was bs4 not installed in container — `requirements.txt` updated but image not rebuilt; silent return None made symptom look identical to real parse failure for multiple debug rounds); `fed0b45` owner_reply `_write_note_only` two-step natural-key→_sp_id lookup before SP update (SP REST `/items({Id})` expects integer auto-counter Id not HILDA's composite slug — HTTP 400 on direct write). **Live-validation outcome**: Item 1 (Confirmation, owner replied "Closed") OutreachSent→OwnerClosed→auto-advance UnderPmReview ✓; Item 2 (non-Confirmation, owner replied "Open"+note) `owner_status_note` written to Postgres ✓; SP note write to be re-verified post-fed0b45 deploy. **MODULE.md touched this window**: `core/src/tracker/MODULE.md` only (Key choices update for [D-118] strict-boundary). **YAML edit**: `customizations/rules/global/automation_rules.yaml` removed erroneous `item_type != Confirmation` Confirmation-skip from `send_initial_outreach_on_collection_start` rule. **Container ops discovery**: bs4 missing in container surfaced the gap that current deploy workflow does manual `cp core/` + manual `pip install` against bind-mount; reboot would lose the manual install. Recommend image rebuild from `core/` + `requirements.txt`; bind-mount only `customizations/` + `config/`. Tests: 905/905 passing at HEAD.

- 2026-06-27 (07:30 → 22:00 PT; 22 commits 7bba72b..7553b51) **EWS poller in production + dispatcher refinement + Path A SP-read + 14 live-debug fixes against real corp Exchange + corp SP**. **Bootstrap consolidation**: `7bba72b` RuleSet.load import path fix (live diagnosis); `ba034d1` bootstrap explicit config_path for SharePoint; `265a3d3` SopsCredentialService for email_sender creds; `723f082` SOPS_AGE_KEY_FILE env honored; `2ae5927` tasks/__init__.py relative imports to avoid partial-init race. **EWS polling task ADDED** (`7632902`): periodic `poll_ews_inbox_task` + beat schedule replacing prior manual one-shot script; bridges async EwsReceiver.fetch_once → sync Celery via asyncio.run. **Storage engine fix** (`9476a60`): switched Postgres engine to NullPool to support sync-bridge usage (avoids cross-event-loop pool corruption). **Inbound audit observability gap closed** (`17616a1`): per-message CommunicationLog row written for every SP alert BEFORE dispatch — closes Step 1.5 debug where milestone-add alerts arrived + dispatched=1 every poll but communication_log stayed at 0 rows (no rule matched list_name=Milestones+added). **EWS adapter hardening**: `4856edf` server-side `is_read=False` filter + post-extraction mark-as-read for persistent progress tracking (instance-level `_last_fetched_at` watermark was unreliable across receiver instance churn); `dba6263` subject-prefix scoping (`sp_alert_subject_prefixes` tuple `("Milestones","Deliverables","Projects")`) for shared OMADM_BOT mailbox (must NOT mark non-HILDA mail as read). **Dispatcher sub_trigger refinement** (`dfa515e`): promotes raw `changed` → semantic `OwnerReassigned` / `DeadlineMoved` / `TagsModified` based on field_deltas; Item-snapshot enrichment sees final sub_trigger. **Collection-started gate** (`ce10f86`): `notify_new_owner_task` + `start_item_collection_task` both defer when item.delivery_state ∈ (None, "Not Started") — prevents pre-kickoff one-item desynchronization from sibling trackers; pairs cleanly with kickoff_collection sending initial outreach across ALL trackers when TPM clicks Start Collection. **Celery chain fix** (`971597c`): `.si()` immutable signatures in registry so chained task bodies don't get prev result prepended as arg0 (was: `TypeError takes 2 args got 3`). **start_item_collection gate ordering** (`f03d456`): gate FIRST, strict delivery_item_id check AFTER — accommodates SP-alert-driven OwnerReassigned events without delivery_item_id natural-key resolution. **Path A "SP-read at fire-time"** (`fe9dd16`): send_initial_outreach reads owner identity from SP at fire-time rather than HILDA's stored copy — mid-flight TPM owner edits in SP honored without HILDA-side replication; new `SpCrudWriter.get_items(canonical_filters)` helper. **list_provider placeholder substitution** (`af1af1e`): `<customer_id>` and `<device_id>` in list names resolved at scope-time (was: HTTP 400 from SP read on literal `Deliverables_<customer_id>`); `a41c1e5` omit `$select` on read path for resilience to column-map drift (was: HTTP 400 on stale `rules_paused` column not provisioned in MMK SP); `5067b6a` filter SP read by (customer, milestone, item_no) unique key; `d723434` add `project_model` (item.device_id) to SP-read filter. **FR-58 correction**: `170a51e` removed erroneous `item_type != Confirmation` skip from `send_initial_outreach_on_collection_start` rule — Confirmation items DO receive outreach per FR-58 (architect correction during live test). **State enum coercion** (`51dbb71`): tracker.transitions coerces stored varchar `item.delivery_state` → DeliveryState enum on read (was: `'str' object has no attribute 'value'` at transitions.py:273). **Import deliverable tracker fix** (`7553b51`): stamps customer_id + device_id on DeliveryItemBase during import (was: NULL columns broke downstream owner-edit cascade). Tests post-window: 879/879 passing.

- 2026-06-26 PM continuation (14:46 → 22:15 PT; 12 commits 69c1d68..39c345f) **[D-118] cascade Chunks 3+4+5 + workflow_engine ActionKind wire-ups + storage wireup Chunks 1-5 + bootstrap auto-construction**. **[D-118] cascade COMPLETE**: Chunk 3 (`69c1d68`) real `import_deliverable_tracker_task` body — parses SP ADDED alert body_kvs → DeliveryItemBase, resolves device_id, idempotency via `find_items_by_natural_key`; Chunk 4 (`5196a4a`) real `kickoff_collection_task` body — filters by `force_tracking_enabled=true`, fires ItemCreated per matching tracker via TriggerDispatcher; Chunk 5 (`4ed2e0e`) removed `sp_writer.create_item` from `instantiate_default_workitem` per architect direction — HILDA no longer writes Default WI to SP. **workflow_engine remaining ActionKind wire-ups**: `4d138f3` outreach recipient resolution storage-first per [D-080]; `74116dd` reminder_count + last_reminder_triggered_at increment in send_reminder_task; `dc338b6`+`bffde46` dispatcher enriches `event.derived_fields` from item snapshot + derives doc_count_reached for guard evaluation; `a11d914` queue_submission_task resolves target_folder + customer_delivery_info via storage chain (rule-review Chunk B); `6377106` bootstrap wires TaskDeps with dispatcher + email_sender. **Storage wireup strand**: Chunk 1 (`a38569b`) DeliveryItem CRUD ops + sync `PostgresStorage` wrapper via `run_async_sync`; Chunk 2 (`3844b81`) `PostgresAuditWriter` + `_sync_bridge` thread helper; Chunk 3 (`5341a12`) `SpCrudWriter` sync wrapper conforming to tracker.SpWriter Protocol; Chunk 4 (`3277f5c`) bootstrap auto-constructs PostgresStorage + PostgresAuditWriter from HILDA_STORAGE_DB_URL; Chunk 5 (`63d3ea6`) end-to-end smoke test `test_full_chain_import_then_kickoff` validating Chunks 1-4 in-process with sqlite. **Bootstrap auto-construction completion** (`39c345f`): SpCrudWriter auto-construction honoring JSON-driven sharepoint config. Tests post-window: 879/879 passing.

- 2026-06-26 PM ([D-118] strict-boundary cascade Chunks 1+2 of 5; 2 commits 62fa8ce..70b256a) **Foundational design pivot + 2/5 chunks of [D-118] strict-boundary cascade shipped**. **Architectural discovery**: HILDA's setup_milestone task does NOT exist (only 1 placeholder comment in `tracker/default_workitem.py:125`); `instantiate_default_workitem` calls `sp_writer.create_item` to write Default WI row to SP -- violates [D-118] SP UI engineer provisioning boundary. **Architect-direction decision 2026-06-26 PM**: SP UI engineer owns ALL SP row creation (Milestones + Deliverables + Default WI); HILDA's role becomes alert-driven listener -- import each Deliverable into HILDA local storage when SP fires ADDED alert; fire ItemCreated TriggerEvents when TPM clicks Start Collection. Cleaner separation of responsibility, aligns with [D-118] more strictly. Replaces partial-design where HILDA writes some SP rows (specifically Default WI per FR-78). **5-chunk implementation plan**: (1) `StorageWriter.create_delivery_item` Protocol method ✅; (2) new ActionKinds `IMPORT_DELIVERABLE_TRACKER` + `KICKOFF_COLLECTION` + stub task bindings ✅; (3) real `import_deliverable_tracker_task` body + tests (next session ~60 min); (4) real `kickoff_collection_task` body + tests (next session ~45 min); (5) remove `sp_writer.create_item` from `instantiate_default_workitem` (next session ~15 min). **Commits**: `62fa8ce` Chunk 1 -- StorageWriter.create_delivery_item Protocol method + Mock impl in test_tracker.py + test_workflow_engine_tasks.py (no concrete impl in core/src/storage/ -- mock-only at this layer); `70b256a` Chunk 2 -- ActionKind enum +2 members (18 -> 20 total), `core/src/workflow_engine/tasks/sp_alert_imports.py` NEW module with both task body stubs (raise NotImplementedError) + TaskBinding registrations at import time, test_workflow_engine_tasks.py adds test_sp_alert_imports_actions_registered + renames test_16_of_18_action_kinds_registered_now -> test_18_of_20_*, test_rule_engine.py + test_workflow_engine.py update count assertions 18 -> 20. **Other design work this session (no code yet)**: (a) rule walkthrough validated `send_initial_outreach_on_collection_start` -- found `template: initial_outreach_per_owner` param is dead code (composer_outreach.py:47-52 auto-selects `outreach_individual.j2` or `outreach_tg_alias.j2` based on tg_email_group_alias + batch size; `template:` param silently ignored); (b) authored `customizations/template_schemas/MMK/doc_type_filename_rules.yaml` LOCALLY on Linux box per Point 3 (not committed; covers test_report/tech_report/waiver/compliance_certification_release_notes with corp-specific patterns OTADM/MMS/RAN/Cloud/WEA/Motive/TechM/HWCert/SDoc/Qualification/Sustainability; (4G|5G)_LC variants classified as test_report (not tech_report) via positive match + negative lookahead in tech_report's TechM regex); (c) confirmed multi-device template.yaml support; (d) confirmed 1 owner across multiple TGs is supported in data model + BATCH-id is the disambiguator for inbound TG resolution. **Files touched**: 3 production code files (tracker/protocols.py, rule_engine/models.py, workflow_engine/tasks/sp_alert_imports.py) + 1 production module init (workflow_engine/tasks/__init__.py) + 4 test files. **D-135 candidate**: [D-118] strict-boundary cascade -- replaces partial-design where HILDA writes Default WI row to SP; SP UI engineer owns ALL SP row creation now. Architecturally substantial (affects module boundaries, public APIs, multi-week implementation cascade); candidate for ratification when all 5 chunks land + tests pass + production validation. **Test suite**: 824 -> 825 (+1 net for sp_alert_imports registration test). **DECISIONS.md unchanged this session** (D-135 deferred until cascade complete). **No MODULE.md edits** (workflow_engine + tracker + rule_engine MODULE.md narrative updates deferred to Chunk 5 + close-session). **No structural changes** (regen-map skipped). **No drift-check run** (recent dev-full pass + scoped changes).

- 2026-06-26 (Phase D2 EWS production validation + sp_alert_parser real-data hardening; 11 commits 1b9a00f..9b5e62e) **End-to-end production validation of D-132 EWS adapter against real corp Exchange + sp_alert_parser 6/6 SP alert scenarios validated against real corp SP data + 6 production hardening commits surfaced by real-data smoke tests**. **Phase D2 EWS production state**: Authenticated as SEA\OMADM_BOT service account against real corp Exchange (m.sea.samsung.com/EWS/Exchange.asmx); sops + age + bind-mount credential pipeline operational inside containers; exchangelib wire path validated; 20 messages fetched from omadm_bot mailbox. **6/6 SP alert scenarios validated**: Milestone ADD (6 keys, action=added), Milestone CHANGE (7 keys, action=changed, field_deltas={target_date}), Milestone DELETE (6 keys, action=deleted), Deliverable ADD (32 keys, action=added), Deliverable CHANGE (25 keys, action=changed, field_deltas={4 owner fields}), Deliverable DELETE (23 keys, action=deleted). **11 commits today**: `1b9a00f` deploy: comment out hilda-llm-gateway service for Ph-1 first deploy; `b6fedf6` email_service: D-132 EWS adapter (config + receiver + sender + mocks + ADR ratified + tests 798→818); `59d3774` deploy: install real sops Go binary (pip-`sops` was wrong package); `f98e139` deploy: bind-mount sops-encrypted credentials + age key via deploy/.env env-var parameterization (HILDA_AGE_KEY_DIR + HILDA_CREDENTIALS_DIR + SOPS_AGE_KEY_FILE); `8886c18` deploy: drop ${VAR:?error-msg} required-var syntax for podman-compose 1.0.6 compatibility (silently dropped bind-mounts); `9c3decc` ews_receiver: fix first-run fetch to include existing inbox messages (`since=now()` filter excluded everything); `65f679b` ews_receiver: fix AttributeError on msg.body_type using HTMLBody isinstance check (exchangelib API mismatch); `f32147d` requirements: add rapidfuzz (transitively required by email_service.inbound.attachment_router; missing from prior commit); `f3c5cc1` sp_alert_parser: add content-hash dedup Layer-2 (catches corp SP duplicate-sends with different Message-IDs but identical payload — surfaced 2x identical Milestone-add alerts within same second); `2f348d4` sp_alert_parser: refactor extract_routing_key to consume body_kvs dict (eliminates divergent MULTILINE regex that missed project_id on 32-key Deliverable bodies); `9b5e62e` sp_alert_parser: accept multi-space separator in body kv lines (corp SP CHANGE-alert format renders non-edited fields with 3-4 spaces between key + value, no colon). **Production discoveries from real corp data**: (a) corp Exchange has no IMAP/SMTP — only EWS via on-prem SOAP per Chaitanya's sample (validates D-132 design); (b) SEA\OMADM_BOT service account works via basic auth + NT-domain format (SEA\username) NOT UPN (username@domain) — personal account `tarasu` basic auth disabled by corp policy; (c) SP CHANGE alert bodies have multi-space separators (no colon) for non-edited fields, colon for Edited fields — D-128 cascade missed this nuance; (d) SP fires duplicate alert emails with DIFFERENT Message-IDs but identical payload — Layer-1 Message-ID dedup couldn't catch; Layer-2 content-hash dedup added; (e) Outlook redirect rule from human account to omadm_bot works server-side without Outlook client running, preserves original subject (no FW: prefix); (f) corp SP can ONLY send alerts to human accounts (not service accounts), so the redirect pattern is required architecturally — HILDA's classifier+parser are To-agnostic (only inspect subject + body), so the redirect pattern is transparent; (g) `body_type` attribute doesn't exist on exchangelib Message — must use isinstance(msg.body, HTMLBody); (h) podman-compose 1.0.6 silently drops bind-mounts that use ${VAR:?error-msg} required-var syntax; (i) Outlook plain-text conversion of SP HTML table cells uses 3-4 whitespace chars between key + value, NOT tabs; (j) rapidfuzz was missing from requirements.txt despite being a top-level import in email_service.inbound.attachment_router (Celery worker boot was clean because Celery uses lazy task registration; failure only surfaces when a task that touches the module executes). **6/6 production hardening tests added**: TestEwsReceiver (8), TestEwsSender (4), TestAdapterFactory (3), TestMockEws (3), TestConfig +2 (D-132); TestSpAlertParserRegression +2 (multi-space separator + single-space safety, 9b5e62e); +1 content-hash dedup test (f3c5cc1); +3 routing_key dict tests including 32-key Deliverable regression (2f348d4). Suite: 818 → **824** (+6 net). **DECISIONS.md**: 128 → **129** (D-132 ratified b6fedf6). **D-133 candidate flagged**: Layer-2 content-hash dedup for SpAlertParser (architect's call to log or skip; marginal — implementation is small, but architecturally interesting as the design pattern is reusable for other duplicate-source data quality issues). **D-128 amendment flagged**: real SP CHANGE body format (multi-space separator + dict-based routing_key extraction). **Phase D2 EWS = SUCCESS milestone**: HILDA's email_service → exchangelib → corp Exchange wire path proven; sp_alert_parser validated against real corp SP data across full action × list-type matrix.

- 2026-06-25 (continuation 3 — Phase D1 + Phase D2 first bring-up; 3 commits 56d5bf1..e81de0f + close-session SHA) **Phase D1 deploy/ scaffolding committed + Phase D2 first bring-up on corp Linux box (`omadm-HP-Z640-Workstation`) — 5/6 HILDA containers up + healthy**. **Commits**: `56d5bf1` deploy: Phase D1 scaffolding -- 4 Dockerfiles (hilda-api/worker/beat/llm-gateway on `python:3.11-slim-bookworm` per [D-130]) + 6-service compose v3.9 stack (runtime-interchangeable Podman/Docker) + sanitized .env.example + sops_decrypt.sh + deploy/MODULE.md (full ops runbook) + top-level .gitignore extended for `deploy/.env` + `config/*.production.json` + `*.sops.decrypted.*`; `e81de0f` dashboard: add /healthz liveness probe (Phase D2 fix) -- 11-line unauthenticated FastAPI route returning `{"status":"ok","service":"hilda-api"}`; required to flip hilda-api podman healthcheck UNHEALTHY → HEALTHY; tests unchanged 823/823. **Close-session commit also folds psycopg2-binary install into all 3 Dockerfiles** (architect committed same fix locally on Linux box but had not pushed to github; folded here for github integrity): Celery's `db+postgresql://` SQLAlchemy result backend defaults to psycopg2 driver (separate from psycopg3 for sync storage and asyncpg for async); without it, worker crashed at startup banner with `ModuleNotFoundError: No module named 'psycopg2'`. **Phase D2 corp Linux box state at end-of-session**: hilda-postgres (Up healthy) + hilda-redis (Up healthy) + hilda-api (Up HEALTHY -- /healthz returns 200) + hilda-beat (Up) + hilda-worker (Up) + hilda-llm-gateway (restart loop -- gateway_server.py Ph-1 stub missing; deferred Ph-1 next pass). **Corp deployment env validated**: Ubuntu 24.04 + Podman 4.9.3 + podman-compose 1.0.6 + Debian python:3.11-slim-bookworm + Chromium at `/usr/lib/chromium/chromium` per [D-130] + NTLM SP auth + corp firewall blocks SSH outbound port 22 (HTTPS git clone used) + Docker auth.docker.io blocked (Podman path successful per [D-129]). **Three bugs discovered + fixed during bring-up**: (1) psycopg2-binary missing from Dockerfiles (folded into close-session commit); (2) `config/dashboard.json` `"jinja_templates_dir": null` rejected by pydantic Path validation (fix: omit field; let code default `Path(__file__).parent/"templates"` win; LOCAL fix on Linux box; sanitized json.txt mockup needs same correction next session); (3) /healthz endpoint missing in dashboard (committed `e81de0f`). **Architectural artifacts captured for next-session**: (a) Chaitanya Kamsu's `ExchangeMailService` corp EWS sample (216 lines) confirms corp Exchange 2013 mail flow via `exchangelib` + `Configuration` + service account basic auth; Barracuda Message Archiver clarified as archive-only not primary mail server; **D-132 candidate flagged** for EWS adapter (replaces IMAP/SMTP per FR-23 for corp deployment); (b) corp reverse proxy IT ticket drafted: backend `105.52.91.33:8443`, restricted paths `/docs/*` + `/dl/*` + `/healthz` + `/static/*`, `X-Authenticated-User` header injection per [D-114]; (c) real corp SP URL `https://mobiledocs.sec.samsung.net/sp/tg/MNOCENTRAL` reachable from container (401 NTLM challenge confirmed). **Critical Point 3 enforcement gap surfaced**: architect modified tracked sanitized samples (`customer.yaml` + `MMK/template.yaml` + `ops_alerts/recipients.yaml` + `sharepoint_integration.json` + `storage.json`) with real corp content; STAYED local (no `git add .` slip) but underlying risk persists. **Next-session Point 3 refactor flagged TOP PRIORITY**: convert tracked samples to `.example` pattern + `.gitignore` non-`.example` real files (~8 files; ~30 min). **Phase D2 = SUCCESS milestone**: HILDA Ph-1 architecture running end-to-end on real corp Linux box. Tests: 823/823 unchanged. DECISIONS.md: 128 ADRs unchanged. ActionKinds wired: 16/18 unchanged. **D-131 ADR candidate (marginal)**: psycopg2-binary install requirement; architect's call to log or skip.

- 2026-06-25 (extraordinary 14-commit session continuation) **ops_alerts module dev complete + sp_alert_parser real-format cascade + workflow_engine 16/18 ActionKinds + drift-check dev-full + regen-map + Tier 3 .gitignore + MNO-* scrub + doc_type_filename_rules path migration + corp-env Podman validation (14 commits c909c18..7d05f78)**. **Commits**: `c909c18` D-126 cascade closes [D-116] D13 (customer_delivery_info per-row 9th arg; customer_delivery_modality moved per-item→per-customer; CAD-E010); `1690572` ops_alerts arch draft + D-127 ratified; `ca9893b` ops_alerts Ph-1 dev complete (8 files ~430 lines; 42 tests; OPS-EXXX prefix); `7bf9e4c` sp_alert_parser real-format cascade (subject regex `Milestones - <Title>` / `Deliverables_<MMK> - <Title>`; field-delta extraction from `Edited` markers; Message-ID LRU dedup; no-op-change drop; Projects Ph-2 silent drop; 14 new regression tests); `59dccf6` rules: 2 leftover DeliveryState α drifts per D-124; `d6a688d` customizations: MNO-* taxonomy scrub + `.gitignore` Point 3 enforcement; `a975703` doc_type_filename_rules.yaml path migration to template_schemas/<customer_id>/; `a7d84cb` workflow_engine 6 ActionKind wire-ups (SEND_INITIAL_OUTREACH / SEND_REMINDER / NOTIFY_NEW_OWNER / ESCALATE / START_ITEM_COLLECTION / QUEUE_SUBMISSION); `2cb20a1` drift-check dev-full -- 5 MODULE.md drifts resolved; `7d05f78` regen-map -- 18 Structure sections + MAP.md refreshed (messenger + ops_alerts added). **New ADRs ratified today**: D-123 (Dashboard URL + per-load SP READ + $expand), D-124 (DeliveryState (α) PascalCase+space lock), D-125 (Point 3 policy LOCAL corp YAML), D-126 (customer_delivery_info per-row + customer_delivery_modality per-customer + CAD-E010), D-127 (ops_alerts module). **Corp-env validation Linux box**: Ubuntu 24.04 + Podman 4.9.3 + podman-compose 1.0.6; docker.io registry accessible directly; corp SP TLS clean (401 NTLM challenge expected); multi-container compose stack (postgres + redis + service-name DNS + connectivity) ALL PASSED. **Box is READY for HILDA Phase D1 deployment**. Tests: 755 -> **823 (+68 net)**; DECISIONS.md: 123 -> **128**; ActionKinds wired: 10/18 -> **16/18**; ops_alerts is new module (15 dev-complete + ops_alerts; only #5 llm arch-only).

- 2026-06-26 **Dashboard FR-87 cascade + Gap 1+2+6+7+8 + DeliveryState (α) enum + customer.yaml rename + SpClient $expand (3 commits f8289f2..2f791d6)**. Yesterday's TOP PRIORITY (FR-87 correction cascade per [D-122] D-122) cleared + expanded scope to address 5 P0 gaps surfaced in yesterday's compliance review + architect's Q1-Q9 locks from live SP REST probe 2026-06-26. **Commits**: `f8289f2` Dashboard Gap 1+2+6+7+8 cascade -- URL reshape `/docs/<customer_id>/<sp_id>` (was single-segment `/docs/<delivery_item_id>`; would 404 against SP UI engineer's 2-segment link convention); 2 FR-87 POST endpoints (`POST /docs/.../resolve_reassign` step A + `POST /docs/.../resolve_doc_type` step B with [D-119] 4-value validation + storage update + SP audit writeback via SpCrud per [D-064]+[D-117] digest dance + 303 redirect; step C deferred Ph-2 per [D-039] Step 2 + architect direction); per-load SP READ via SpCrud per [D-074] (no caching Ph-1); FR-58 Confirmation detection authoritative via fresh sp_row["item_type"] (was heuristic "no docs -> Confirmation"); SpCrud wiring into build_app (production sp_crud + test mode mock_sp_rows fallback); DeliveryState enum (α) direction per architect lock 2026-06-26 -- enum value strings match SP display "Not Started"/"OutreachSent"/"OwnerClosed"/etc. PascalCase-with-spaces (same pattern as [D-094] SUPERSEDED 2026-06-23 item_type mixed-case); mock_customer.yaml `_x0020_` partial cleanup (5 explicitly-tested fields); composer_escalation.py DELETED per Q-M6 lock (vestigial; messenger owns escalation composition); FR-87 action handlers removed from email_service/sp_alert_parser/parser.py per [D-122] (FR-87 is HILDA-page direct POST not SP-alert mediated); test_dashboard.py rewrite 20 -> 37 tests +17; test_email_service.py drops 3 obsolete tests; 4 other test files updated for new DeliveryState strings; tests 741 -> 755 (+14 net). `057b33d` automation_rules.yaml orphan delivery_state cleanup per architect lock 2026-06-26 -- dropped OwnerResponseReceived from condition list (redundant with LastContactThreshold trigger); DELETED advance_to_ai_reviewed_on_pass rule (AI review tracks via item.review_status field, not delivery_state); dropped OutreachReminded/AIReviewed from condition `in [...]` lists; updated 4 condition lists from `in [..., orphan_value]` -> `eq "<single_value>"`; YAML rule count 25 -> 24; reminder cadence tracks via reminder_count field only (no intermediate state). `2f791d6` **4 architect-direction actions 2026-06-26**: (1) `git mv` mock_customer.yaml -> customer.yaml per architect Q1 lock (single file per deployment); Point 3 policy header added (production customer.yaml lives LOCALLY only per architect Linux box; this github copy is privacy-clean SAMPLE / RUNTIME PLACEHOLDER; corp-SP-derived mappings stay local); (2) SpClient.get_list_items + SpCrud.get_item `$expand` + `extra_select` kwarg support per architect Q9 lock -- for User-type Person/Group fields like Projects.TPM (HILDA fetches via `?$expand=TPM&$select=TPM/EMail,TPM/Title` returns nested `{"TPM": {"EMail": "abc@corp.com"}}` -> HILDA derives tpm_corp_id = email.split('@')[0] per [D-088] 3-tuple + corp PLM Q4 lock 2026-06-25); (3) customer_adapter MODULE.md flagged [D-116] D13 cascade follow-up per architect lock -- customer_delivery_info is per-row on Deliverables SP list NOT binding-baked customer-root; binding API needs 9th arg + per-customer subclass scaffold reads row-context not bakes-config; customer_delivery_credential_id REMOVED per [D-019] shared HILDA ops-team identity; (4) STATUS Flag accumulated 4 architect-discussion TODOs: HILDA OPS alert mechanism (ITR-W004 + FR-87 SP audit writeback) + [D-116] D13 cascade follow-up + SP UI engineer coordination items (ADD rules_paused / REMOVE expected_completion_date / CORRECT or REMOVE milestone_id) + Point 3 policy formalized. **Architect live SP REST probe 2026-06-26 confirmed**: pattern (a) URL fetch works; all HILDA custom columns bare snake_case (NOT _x0020_-encoded); 127/89/95 columns across Deliverables/Milestones/Projects; Projects.TPM User-type Person/Group field; SP UI engineer's URL convention confirms 2-segment `/docs/<customer_id>/<sp_id>` pattern; SP cookie capture None in this deployment (NTLM auth flows through requests.Session per-request anyway); pagination `$top + __next` continuation works (resolves [D-117] Q4 OPEN). **YESTERDAY'S STATUS FLAG TOP PRIORITY = CLEARED**: all 7 FR-87 cascade items verified via grep: (1) FR-87 rules removed from automation_rules.yaml ✓; (2) FR-87 handlers removed from sp_alert_parser/parser.py (only docstring comments documenting removal remain with [D-122] anchor) ✓; (3) email_service/MODULE.md FR-87 narrative dropped ✓; (4) 2 of 3 dashboard FR-87 POST endpoints added (step C Ph-2 per [D-039] Step 2) ✓; (5) dashboard/MODULE.md FR-87 POST routes documented ✓; (6) tests updated (23 FR-87 test references in test_dashboard.py) ✓; (7) D-122 ADR captured in yesterday's close-session ✓. **3 commits today; tests 741 -> 755 (+14); DECISIONS.md 123 ADRs unchanged today; module status 13/13 + messenger unchanged (only #5 llm remains arch-only).**

- 2026-06-25 (continuation 2) **13-module strict-order sweep CLOSED + messenger module + Tier 1 P0 YAML (6 commits ed36fff..ee01636)**. Marathon session that pushed the entire HILDA strict-order Module sweep to completion + landed messenger module (FR-10 escalation channel) + provisioned Tier 1 P0 YAML config files for end-to-end runtime against mock_customer. **Commits**: `ed36fff` email_service Module #12 arch revisit (13 drift items D1-D13 against locks since 2026-06-09: item_type case rename per [D-094] SUPERSEDED; owner_email → 4-field per [D-105]; customer_slug → customer_id per [D-091]; TGGroupBase DROPPED per [D-106]; customer_adapter API per [D-116]; DocType vs tpm_resolved_doc_type per [D-119]; TriggerDispatcher per [D-113]; ActionKinds SCREAMING_SNAKE_CASE; sp_alert_parser 3-list scope per Module #11 Q1; [D-117] SP NTLM ack; [D-118] SP UI engineer provisioning boundary; anchors refresh dropping [D-051]); `0dfb1d4` **email_service Ph-1 dev complete** -- greenfield 3338 lines across 27 files (8 source + 3 Jinja templates + 1 YAML + 1 test + diagnostic CLI). protocol.py (full Public surface with InboundMessage/InboundAttachment/EmailKind/StructuredReplyBlock/RoutedAttachment + AttachmentItemMatch FR-79 multi-match + ClassificationResolution + 3 Protocols); inbound/receiver.py (IMAP IDLE primary + poll fallback per FR-23 + [D-016]); inbound/classifier.py (SP_ALERT regex -> OWNER_REPLY BATCH-id -> OTHER); inbound/subject_parser.py FR-24; inbound/body_parser_structured.py (FR-12 path a with 4-field sender match per [D-105]); inbound/body_parser_freetext.py Ph-2 NotImplementedError stub; inbound/tg_resolver.py (email-channel only per [D-060] + [D-106]); **inbound/attachment_router.py (513 lines: full FR-52 5-step pipeline B1 substring -> B2 fuzzy rapidfuzz -> B3 FR-77 Type-2 folder routing -> B4 LLM ROUTE_ATTACHMENT -> B5 staged default; FR-85 2-step ladder A1 filename regex + A2 LLM CLASSIFY_DOC_TYPE Ph-1-next-pass stub-with-UNRESOLVED-fallback per llm phasing; FR-86 4-path storage matrix dispatch: classified / unrouted / staged_not_revision / staged_not_classified; Step 0 hash dedup; FR-79 multi-match; EML-W007 over-routing alert >10 matches per user domain reality)**; sp_alert_parser/parser.py (3-list scope per Module #11 Q1 lock 2026-06-25 with out-of-scope alerts silently dropped per [D-118]; FR-87 A/B/C handler dispatch with [D-119] 4-value validation -- **NOTE: FR-87 handler dispatch is INCORRECT per architect Q3 clarification 2026-06-25; FR-87 is HILDA-internal-page direct POST not SP-alert mediated; correction cascade pending next session**); outbound/sender.py SMTP wrapper with FR-42 CommunicationLog; outbound/composer_outreach.py FR-9 + composer_reminder.py FR-10 + composer_escalation.py (Ph-1 stub dict; VESTIGIAL per architect Q-M6 lock 2026-06-25 -- messenger owns composition; remove in follow-up); 3 Jinja2 templates + default_doc_type_rules.yaml universal fallback FR-85 Step 1; mocks + CLI --diagnostic/--once/--mock/--send. 46 new tests across 13 classes; full suite 574 -> 604 +30 (delta); unblocks workflow_engine ActionKinds SEND_INITIAL_OUTREACH + SEND_REMINDER + NOTIFY_NEW_OWNER + START_ITEM_COLLECTION; `5c1ab7e` **issue_tracker Module #13 arch revisit + Ph-1 dev complete -- closes 13-module sweep**. Architect's 5-API corp PLM spec from VS Code screenshot + Q1-Q7 lock 2026-06-25 (createPLM trigger on tracker creation/Start Collection per Q1; closePLM in workflow_engine.tasks/milestone.py FINAL_SWEEP per Q2; deadline-tiered polling per FR-23-style pattern per Q3; **tpm_corp_id from local part of Projects.TPM email per Q4** -- per-customer NOT shared HILDA ops-team identity per [D-019]; attribution param only not credential; corp_plm_gateway PC handles actual auth per FR-25 (a) no-credential pattern; retry + ITR-W004 opaque log + HILDA OPS alert TBD architect discussion per Q5; in-flight (plm_id, file_id) lock-guarded dict per Q6; document_id + file_id BOTH required for downloadFile per Q7). Module SPLIT into 2 Protocols: CorpPlmAdapter (5 methods matching createPLM/closePLM/getdocumentslist/downloadFile/uploadFile per architect spec; thin wrapper per [D-027] Teacher/Student with abstract _invoke_X methods raising ITR-E001 by default; concrete binding-call body filled in by Cline on Work PC); CustomerJiraAdapter (standard JIRA REST via httpx; Ph-1 INFORMATIONAL POLLING ONLY per [D-092]; Ph-2 SP write-back). Sub-module restructure: corp_plm/ + customer_jira/; new in_flight_tracker.py per Q6 (asyncio.Lock-guarded dict; Ph-2 Postgres-backed); new poller.py per FR-26 + FR-23-style deadline-tiered cadence; derive_tpm_corp_id helper per Q4; per-customer subclass scaffolds at customizations/issue_tracker/example_corp_plm_adapter.py + example_customer_jira_adapter.py with # TODO(cline) markers per [D-027]. Pragmatic Ph-1 deviation: legacy generic IssueTracker Protocol + jira_adapter.py + mock_adapter.py kept in place because customizations/issue_tracker/defecttrack_adapter.py + 8-test contract suite depend on it -- MODULE.md documents Ph-2 removal. 54 -> 103 issue_tracker tests +49; ITR-W003/W004/W005 added per [D-098] narrowing + Q5/Q6 + new in-flight; **D-120 candidate flagged**: corp PLM 5-API thin-wrapper + tpm_corp_id-as-attribution + in-flight tracking; `04d3e8d` **messenger module Ph-1 dev complete -- per Q-M1..M6 lock 2026-06-25 + sample sendMessage API from architect screenshot**. NEW module (NOT in 13-module strict-order sweep) -- owns corp messenger channel for FR-10 cross-channel escalation. Q-M1 sendMessage returns true when owner RECEIVES (stronger than gateway-accepted); Q-M2 4K message buffer + 3 messages/day per owner_corp_id enforced at messenger module level via CommunicationLog query; Q-M3 sender appears as "anonymous HILDA BOT" no TPM identity prepended; Q-M4 escalation trigger is rule-driven via AutomationRules + YAML config (rule_engine evaluates -> workflow_engine ESCALATE ActionKind -> messenger.send_escalation); Q-M5 CommunicationLog audit per FR-42 + no message_id thread continuity Ph-1; **Q-M6 messenger OWNS composition (Jinja2 templates) + send + daily-limit NOT email_service** (composer_escalation.py in email_service is vestigial). Sub-modules: protocol.py (MessengerAdapter Protocol + EscalationReason enum POST_REMINDER/DEADLINE_IMMINENT/MANUAL_TPM + SendResult); config.py (MessengerConfig 3-tier per [D-025]); composer.py (compose_escalation + Jinja2 + REQUIRED_TEMPLATE_VARS validation + 4K truncation + privacy assert); daily_limit.py (DailyLimitChecker -- blocks 4th send MSG-W001 + records FR-42); service.py (MessengerService orchestrator -- the ESCALATE ActionKind entry point); corp_messenger/adapter.py (CorpMessengerAdapter thin wrapper per [D-027] -- abstract _invoke_send_message raises MSG-E003 by default); templates/escalation.j2 (HILDA BOT signature); mocks/CLI/utils. customizations/messenger/example_corp_messenger_adapter.py scaffold with # TODO(cline) markers per [D-027]. 30 new tests; 711 -> 741 (+30). MSG-E001/E002/E003 + MSG-W001/W002/W003 registered. **D-121 candidate flagged**: messenger module ownership boundary + 4K/3-per-day + HILDA BOT anonymous + composition-in-messenger. Unblocks workflow_engine ESCALATE ActionKind (last cross-module dependency); `1caa106` **Tier 1 P0 YAML config -- mock_customer end-to-end enablement (3 files / 1123 lines)**. HILDA-side code complete (741 tests across 13 modules + customer_adapter + messenger) but had no config files to operate against. Created: customizations/template_schemas/mock_customer/template.yaml (619 lines -- canonical per-customer schema: 2 devices MODEL-A + MODEL-B / 2 milestones P1+P2 / 14 work items across mixed-case item_type per [D-094] SUPERSEDED 2026-06-23 / 3 owners with [D-105] 4-field / 4 TG groups GENERAL/SUSTAINABILITY/BLUETOOTH/PERFORMANCE with [D-106] denormalization / customer_delivery_info per FR-77 + [D-097] / form factor flags per [D-084] / item_description per FR-82 nested tag-set [D-082]); customizations/sharepoint_config/customers/mock_customer.yaml (186 lines -- 3 SP lists Deliverables_mock_customer + Milestones_mock_customer + Projects_mock_customer per Module #11 Q1 lock; ~50 cols matching xlsx authority per architect Q5 + SP _x0020_ encoding); customizations/rules/global/automation_rules.yaml (318 lines -- 22 trigger-action rules + 2 polling-schedule rules covering FR-9 outreach + FR-10 reminders + Q-M4 messenger escalation reminder_count>=2 + days_to_deadline<=3 + state machine transitions + FR-78 tracker-create + FR-64 [D-100] Close-All-Items HILDA-owned per-item cascade + FR-74 closure + FR-87 TPM resolution A->B->C strict ordering [**NOTE: FR-87 rules incorrectly placed here per architect Q3 lock 2026-06-25 -- FR-87 is HILDA-internal-page direct POST not rule-driven; 3 rules to REMOVE next session**] + FR-20 credential expiry + FR-23 + FR-26 deadline-tiered polling). RuleSet.load() resolves 28 distinct rule_ids in aggregate (22 new + 6 existing defaults.yaml). Architect-flagged 3 TODOs surfaced: (a) corp_id_list + tg_email_group_alias placement in template.yaml -- RESOLVED via ee01636 fix; (b) closed_all_items_triggered_at not truncated keep as-is; (c) FR-87 rule_engine sub-trigger framing -- CORRECTED to FR-87-is-direct-POST framing per Q3 (cascade pending next session); `ee01636` **template.yaml Q1 fix per architect 2026-06-25 clarification**: template.yaml IS canonical source-of-truth for every value HILDA tracks regardless of whether it lives in SP lists. Populated 7 fields across all 14 work items per-TG context-aware (tg_email_group_alias + tg_owner_name + tg_owner_corp_usa_email + tg_owner_corp_email + tg_owner_corp_id + corp_id_list + email_cc_list); 0 remaining null TG-related fields. Privacy preserved (mock-corp.com domain + john_smith / jane_doe / alex_admin synthetic identifiers). **Module status delta**: 13/13 strict-order modules now arch + dev complete (was 11/13 + 10/13); plus messenger module fully landed (was not started); customer_adapter from prior commit unchanged. **Only #5 llm remains arch-only** (CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT TaskKinds for Ph-1 next pass). **Tier inventory**: Tier 1 P0 YAML COMPLETE (this session); Tier 2 config/*.json operational tuning (defaults work; thin overrides pending); Tier 3 sops credentials (Cline / Work PC ownership per [D-027] + [D-038]); Tier 4 deploy/docker-compose.yml (pending); Tier 5 per-customer folder_routing.yaml + doc_type_filename_rules.yaml (optional Ph-1 -- universal default works). **Test suite 662 -> 741** (+79 net: email_service +46 + issue_tracker +49 + messenger +30 net of test-count update for new YAML files; 0 regressions). **DECISIONS.md 120 ADRs unchanged this session** (D-120/D-121/D-122 candidates flagged for next close-session ratification). **3 architecture decisions Q&A-locked during this session pending ratification**: D-120 corp PLM 5-API thin-wrapper + tpm_corp_id-as-attribution-not-credential + in-flight tracking + Q1-Q7 lock; D-121 messenger module ownership + 4K/3-per-day + anonymous HILDA BOT + composition-in-messenger lock (Q-M1..M6); **D-122 (REVISED per architect Q3 clarification 2026-06-25)** FR-87 step A/B/C resolution is HILDA-internal-page direct POST NOT SP-alert mediated; lives in dashboard module not email_service sp_alert_parser nor rule_engine triggers; correction cascade pending next session. **FR-87 correction cascade flagged TOP PRIORITY next session** (~1-2 hr): (1) remove 3 FR-87 rules from automation_rules.yaml; (2) remove FR-87 handlers from email_service/sp_alert_parser/parser.py; (3) update email_service/MODULE.md drop FR-87 references; (4) add 3 POST endpoints to dashboard/app.py + handlers (storage + SpCrud writeback per [D-064] + [D-117]); (5) update dashboard/MODULE.md; (6) update tests both modules; (7) capture D-122 ADR.

- 2026-06-25 (continuation) **Modules #10 + #11 Ph-1 dev complete + NTLM digest-dance lock (10 commits a833b85..857e14f)**. Major day: spans architecture revisits + dev landings + NTLM production-pattern integration + drift-check + ADR bulk-capture + 3 new ADR Q&A locks for next-session promotion. **Commits**: `a833b85` customer_adapter MODULE.md arch revisit (D1-D11); `14ef7d4` close-session 2026-06-24 + bulk-captured 21 D-DRAFT ADRs (D-094..D-115 ratified, append to DECISIONS.md); `b9657d2` rule_engine + workflow_engine D-112 PauseStateLookup cleanup (`pause_state.py` DELETED; 1 stale doc-comment fix in `__init__.py`; `dispatcher.py` docstring corrected); `b490c57` customer_adapter + credential_service D-116 binding API finalized + cascade (D12-D17 second pass: 8-arg `uploadAttachment(Model_No, milestone_name, source_dir, target_dir, filename, pm_id, pm_password, totp_code) -> bool` locked; B-α component-pass for Drive target side; CustomerCredential 3-tuple via credential_service.basic_totp auth_type + totp_seed field; pyotp TOTP per-call; CAD-W005 clock-skew warning; carrier_file_id/url Ph-2 forward-looking; session_manager.py + selector_loader.py + capability_flags.py DROPPED from Ph-1); `411ba31` drift-check design — 7 drifts resolved in requirements.md (DRIFT-1/2/3 Projects per-customer FR-2/40/77 per [D-104]; DRIFT-4 FR-19 thin-wrapper Ph-1 framing per [D-116]; DRIFT-5 ~50 occurrences item_type mixed-case Confirmation/Default PascalCase per [D-094] SUPERSEDED; DRIFT-6 FR-26 hash-match-dropped per [D-098]; DRIFT-7 FR-25 IssueTracker.getAttachmentHash removed); `9810e33` sharepoint_integration + sharepoint_config Module #11 arch revisit (12-item cascade D1-D12: HILDA SP scope CONTRACTED 8 → 3 lists per architect Q1 lock 2026-06-25 -- HILDA reads/writes only Milestones_<customer_id> + Projects_<customer_id> + Deliverables_<customer_id>, ignores TasksTemplate/Tasks/Trials/Activities/Email/CommunicationLog SP lists per Q1 + Q3 locks; per-customer SP list naming pattern <base>_<customer_id> per Q1 sample; slug→id rename throughout per [D-091]; field-name authority `docs/sp_ui_engineer/milestones_workitems_fields_values.xlsx` per Q5; SP REST URL pattern confirmed; browser-vs-server auth context; pagination Q4 OPEN; D-DRAFT-Y promoted to [D-106]; D-DRAFT-Z SUPERSEDED by Q1 + [D-104]; D-DRAFT-X retained pending for ADR ratification); `29d5e01` **customer_adapter Ph-1 dev complete per [D-116] thin-wrapper strategy** -- greenfield ~1527 lines across 8 new files (`protocol.py`/`google_drive_base.py`/`totp.py`/`config.py`/`mock_customer_adapter.py`/`diagnostics_cli.py`/`__init__.py` + `customizations/customer_adapter/example_adapter.py` per-customer subclass scaffold with `# TODO(cline)` markers per [D-027] Teacher/Student) + credential_service.Credential extended with `totp_seed` + `basic_totp` AuthType + diagnostics CAD prefix + CAD-E001..CAD-E009 + CAD-W001..CAD-W005 registered; 30 new tests (TestCarrierUploadResult/TestTotpGeneration/TestGoogleDriveBaseAdapter/TestMockCustomerAdapter/TestErrorCodes/TestConfig/TestProtocolContract/TestDiagnosticsCli); CLI smoke clean; full suite 574 → 604 (+30); 0 regressions; credential discipline asserted (test asserts pm_password + totp_seed absent from audit row); bounded error_detail tokens only; UNBLOCKS workflow_engine.tasks.submission.QUEUE_SUBMISSION; `b655fea` sharepoint_integration Ph-1 dev alignment refactor (slug→id rename per [D-091]; 3-entity validation in FileBasedListProvider rejects legacy 6 entities with SHP-E002; new `core/tests/fixtures/sharepoint_config/customers/test_customer.yaml` 3-list fixture with [D-105] 4-field owner + [D-106] denormalized TG + [D-108] rules_paused + [D-091] customer_id YAML key + field names tracking architect Q5 xlsx; 604 → 604 no regressions); `cb4e182` **sharepoint_integration NTLM digest-dance refactor per architect production-validated pattern 2026-06-25** -- replaces placeholder httpx-async client with `requests-ntlm` sync session + 3-step digest dance (GET cookie + POST /_api/contextinfo + cache FormDigestValue + 403-refresh-retry); new `core/src/sharepoint_integration/sp_session.py` (251 lines) `SpSession` class + `list_item_type()` helper for SP 2017 `__metadata.type` encoding (`_x005f_` for underscores + `_x0020_` for spaces); MERGE/DELETE pseudo-verb dispatch + X-RequestDigest + IF-MATCH:* + customer_id flowing through SpClient writes for `__metadata` composition; new `mock_server/client.py` `MockSpSession` runs REAL digest dance against mock endpoints; mock_server/app.py extended with /_api/contextinfo endpoint + Set-Cookie issuance + 403-on-missing-digest test path + /__force_403_next_writes__ knob; auth.py rewritten with thin HttpNtlmAuth wrapper; 12 new tests (4 list_item_type encoding + 3 SpSession digest-dance + end-to-end 403→refresh path); requests-ntlm added as new 3rd-party dep; per-Celery-task SpSession (no pool) per Q4; lazy digest acquisition + auto-regen on 403 per Q2; corp\<user> full literal per Q1; 604 → 616 (+12); 0 regressions; D-117 candidate FLAGGED in MODULE.md for ADR promotion; `857e14f` sharepoint_config example.yaml cascade to 3-list scope per Module #11 lock + [D-091] -- pre-cascade 7-list example.yaml was breaking FileBasedListProvider strict-validation post-Module #11; full rewrite to 3-list contract with documentation header + [D-104]/[D-105]/[D-106]/[D-108] anchors + removed entities footer; --mock smoke now emits lists_reachable=3. **Module status delta**: Module #10 customer_adapter went arch-only → arch ✓ + dev ✓; Module #11 sharepoint_integration + sharepoint_config went arch-stale → arch ✓ + dev ✓ (NTLM-locked). **11/13 modules arch complete; 10/13 fully through dev**. **Test suite 574 → 616** (+42 net: +30 customer_adapter + 12 SpSession). **DECISIONS.md 93 → 117 ADRs** (D-094..D-115 promoted in commit `14ef7d4` + D-112 in `b9657d2` + D-116 in `b490c57`). **3 architecture decisions Q&A-locked during the session pending close-session ratification**: D-117 SP 2017 NTLM digest-dance + MERGE pseudo-verb pattern bundle (Q-A/B/C/D answers captured: requests-ntlm + corp\<user> full literal + lazy digest + per-Celery-task session + __metadata wrapper + _x005f_/_x0020_ encoding + 403-refresh; async-native deferred Ph-3+); D-118 SP UI engineer manual provisioning (was D-DRAFT-X; HILDA does NOT call REST to create lists; SP UI engineer owns provisioning ceremony since SP-alert/workflows/custom-field-types can't be expressed via REST + "owner of SP module is SP UI engineer; HILDA uses SP services" + rejected alternative was HILDA-provisions-every-milestone-of-every-carrier-into-Milestones+Deliverables-rows on single-responsibility principle; customer onboarding 2-step coordination); D-119 tpm_resolved_doc_type is HILDA-managed 4-value Choice (DocType minus `unresolved`; lowercase snake_case not SP UI engineer mixed-case since HILDA owns full read+write lifecycle via FR-87 step B HILDA-rendered web page). **Privacy hygiene maintained throughout**: <customer_id> placeholder + test_customer synthetic identifier; no real carrier names; passwords + totp_seed + FormDigestValue + cookies never logged. **Open items for next session**: Module #12 (email_service) arch revisit + Module #13 (issue_tracker) arch revisit close out the 11→13 strict-order sweep; D-117/D-118/D-119 promoted in this close-session.

- 2026-06-25 **Module #10 (customer_adapter) arch revisit — 11 drift items + thin-wrapper strategy locked (commit `a833b85`)**. Architecture-only session (no dev). Drift cascade: D1 (carrier_slug → customer_id per [D-091]; carrier_credential_id → customer_credential_id throughout); D2 (`Fr52Config` typo → `CustomerAdapterConfig`); D3 (FR-68 PLM-Carrier hash sync DROPPED per architect direction 2026-06-20 — verification = upload-success markers only; new `CarrierCapabilityFlags.supports_upload_success_verification` Ph-1=True for Google Drive); D4 ([D-054] anchor narrowed to "individual files only, never zips"); D5 (FR-77 path composition per NFR-21 amendment §6 — `upload_attachment(target_folder=...)` receives FULLY-RESOLVED path composed by workflow_engine's submission task body); D6 (workflow_engine `tasks/submission.QUEUE_SUBMISSION` stub-pending acknowledgment per commit `96a498f`); D7 (credential_service per-customer scope per credential_service mini-cascade 2026-06-21 — `get_credential(pm_id, system_type, customer_id=...)`); D8 (dashboard integration — Module #9 surfaces upload status from CommunicationLog per `dc31949`); D9 (status header refresh); D10 ([D-091] anchor add); **D11 (NEW per user direction 2026-06-25) — thin-wrapper implementation strategy locked**: HILDA's `GoogleDriveBaseAdapter` is a Protocol-conformant thin wrapper around the user's existing Google Drive API binding (reused from pre-existing tooling). HILDA owns Protocol contract + `CarrierUploadResult` shape + CommunicationLog discipline + selector versioning + session pool. User binding owns actual upload call. Per `[D-027]` Teacher/Student: HILDA-side scaffold authored Personal PC; concrete API body filled in by Cline on Work PC (no proprietary API details on public github per NFR-2). **Boundary clarifications captured for the architect's lead**: (a) customer_adapter scope = outbound carrier submission ONLY (Google Drive Ph-1/Ph-2; web portal + JIRA Ph-3+); (b) corp PLM lives in `issue_tracker` Module #13 + `corp_plm_gateway` corp-side PC; (c) corp messenger lives in separate `messenger` module + `corp_messenger_gateway` corp-side PC; (d) customer JIRA lives in `issue_tracker` Module #13 (informational polling only Ph-1 per FR-25 (b)). **Q&A side-discussion**: workflow_engine pending dev surface mapped — 8/18 ActionKinds await downstream (SEND_REMINDER/SEND_INITIAL_OUTREACH/NOTIFY_NEW_OWNER ← email_service #12; ESCALATE ← messenger; START_ITEM_COLLECTION ← issue_tracker #13 + email_service #12; TRIGGER_PARSER ← test_report; TRIGGER_AI_REVIEW ← llm #5 Ph-1 next pass + runtime-dormant; QUEUE_SUBMISSION ← customer_adapter #10 once dev lands). Plus ~600-800 lines of trigger_sources / polling / beat_schedule / workflow_engine_cli still pending — most blocked on downstream module landings; 3 self-contained pieces buildable now (state_change_source + scheduler_tick_source + deadline_evaluator). **Module status**: 10/13 modules arch complete (+ customer_adapter). 8/13 fully through dev (unchanged this session).

- 2026-06-24 **Module #9 (dashboard) full Ph-1 dev — top priority delivered (commit `dc31949`)**. Focused single-commit session: 12 files (8 Python + 6 Jinja templates) + 20 pytest tests + 6 DSH error codes registered; 483/483 tests pass across full suite. Files: `app.py` (FastAPI app + 5 routes: GET `/docs/<id>` with HTML/JSON content negotiation + FR-58 Confirmation skip + FR-60 review findings with D7 cascade null placeholder; GET `/dl/<token>` with FR-61 mediated download + inline/attachment disposition policy + DSH-E002 friendly 410 page on expired token; POST `/milestone/<id>/refresh` with D3 cascade integration to workflow_engine.TriggerDispatcher + rate-limit + dedup + 503 fallback when dispatcher unavailable; GET `/milestone/<id>/refresh/status` polling endpoint; GET `/admin/overrides` Ph-1 empty per D1 cascade); `auth.py` (Kerberos/SPNEGO middleware stub per [D-006] -- reads trusted X-Authenticated-User from corp reverse proxy; mock_auth=True bypasses for tests + --serve --mock); `config.py` (DashboardConfig 3-tier precedence); `dashboard_cli.py` (--diagnostic + --serve --mock per [D-005]); `__init__.py` (clean exports); 6 Jinja templates (base + doc_section + doc_row + doc_row_staged with FR-87 step A/B/C button info per D4 cascade + token_expired + admin_overrides Ph-1 note). **Architectural decisions softly resolved** (per dashboard MODULE.md "Architectural decisions to lock" items 2 + 3): decision 2 = reverse-proxy header-trust (option (b)) -- proxy validates Kerberos + sets X-Authenticated-User; dashboard reads trusted header; production must enforce source-IP allowlist at proxy + network layer. Decision 3 = dashboard-local auth middleware (no separate core/src/auth/ module) -- deferred until FR-62 upload endpoint Ph-2 forces the split. **Dependencies added**: FastAPI 0.138 + Jinja2 3.1.6 + httpx 0.28 + httpx2. **Tests** (test_dashboard.py, 20 tests): TestGetDocumentSection (7) -- HTML default + JSON Accept + Confirmation FR-58 skip + llm_review_findings D7 placeholder + FR-87 staged button info + 401 unauth + proxy identity accepted; TestDownloadFile (3) -- valid token streams + xlsx attachment + DSH-E002 expired 410; TestMilestoneRefresh (5) -- 503 no dispatcher + dispatch via D3 cascade + rate-limit dedup + status endpoint + unknown milestone; TestAdminOverrides (2) -- Ph-1 empty + populated rendering; TestErrorCodes (1); TestConfig (2). CLI smoke: 9 FastAPI routes registered; expected_routes_present=true. **Session totals**: 1 commit, 1133 lines added, 20 new tests, 8/13 modules now fully through dev (diagnostics + credential_service + template_schema + storage + tracker + rule_engine + dashboard; workflow_engine substantially complete with foundation + 10/18 tasks + CLI; llm arch only).

- 2026-06-22 to 2026-06-23 **Module-by-module revisit + dev sweep — Modules 6-9 + privacy scrub + template_schema mini-cascade (22 commits, ~14000 lines, 463 tests pass full suite)**. Continuation from 2026-06-21 session. **llm Ph-1 phasing locked** (`5d8d86e`+`dce20c3`): CLASSIFY_DOC reframed Ph-1 → Ph-2; CLASSIFY_DOC_TYPE demoted Ph-1 first pass → Ph-1 next pass (filename regex sufficient for early-drop classification per architect direction 2026-06-22); Ph-1 first pass = ROUTE_ATTACHMENT only. **Privacy scrub 2026-06-23** (`abf4edf`): 8 files remediated — Samsung device codenames (SM-S901U → MODEL-A), Galaxy S22 → Mock Device A, Verizon/VZW → CARRIER1, TMO → CARRIER2, project_id 2479 → 1001; Option A scrub-forward (history retained); future content discipline locked: abstract placeholders for all spec examples + test fixtures. **template_schema mini-cascade 2026-06-23** (`649f64a`) — SP UI engineer enum lock: ItemType mixed-case (`Confirmation`/`Default` PascalCase per SP Choice column values; long-named categories `test_tech_waiver_report`/`compliance_certification_release_notes` snake_case — reverts 2026-06-20 all-lowercase rename for the two short labels); TrackingModality 5 → 6 values (adds `SP_UI = "SPUI"` — HILDA forward-looking Ph-1; SP UI engineer adds Ph-2); ingress_nsd Choice PascalCase `None`/`NSD1`/`NSD2`; review_status keeps 4-value with `failed` (SP UI engineer's 'Not Failed' is typo); tg_name keeps 17-value enum including MNO-CP (SP UI engineer has 16; Ph-2 add); customer_delivery_modality `GoogleDrive` confirmed. Architect principle: HILDA spec stays forward-looking — SP UI engineer omissions reconciled in their Ph-2 SP-side rollout. **Module #6 (tracker) full arch + Ph-1 dev** (`e5f186e`+`fa5166c`+`2377f28`): 13 drift items D1-D18 cascade + 11 files greenfield implementation + 53 pytest tests + 14 TRK error codes; Ph-1/Ph-2 extension points cleanly factored (FR-66 multi-revision; storage Protocol optional helpers for FR-85/FR-77/customer_adapter enqueue). **rules_paused: bool field added to DeliveryItemBase + DefaultWorkItemConfig + tracker default_workitem.py** — pre-req for rule_engine D5 (FR-31 sub-1 per-item pause mechanism via SP Choice(Yes/No) column). **Module #7 (rule_engine) full arch + Ph-1 dev refactor** (`20aa181`+`b6c13a6`): 14 drift items D1-D14 cascade with major Ph-1 scope reduction — (a) **AutomationRuleOverride Postgres-override consumption deferred to Ph-2** (early drop = single mock customer; no per-customer/device runtime tuning needed; YAML edit + service restart sufficient); (b) **SIGHUP YAML hot-reload deferred to Ph-2** (Ph-1 = startup-only load); (c) **per-customer + per-device YAML directories deferred to Ph-2** (Ph-1 reads only `customizations/rules/global/*.yaml`); (d) **FR-31 sub-1 per-item pause via `rules_paused` SP column** — RESOLVES D-DRAFT-2 (PauseStateLookup home TBD); PauseStateLookup Protocol DROPPED; (e) FR-31 sub-3 per-item manual trigger already covered by existing per-action button + HILDA-managed timestamp column pattern (Send Reminder canonical) — no new mechanism needed. Dev refactor: slug→id rename + Global-only resolver + evaluator takes `item_snapshot` kwarg; 84 pytest tests pass. **Module #8 (workflow_engine) full arch + foundation + 10/18 task bodies + diagnostics CLI** (`1e7e8a0`+`11f5e5d`+`96a498f`): 12 drift items D1-D12 cascade mirroring rule_engine Ph-1 narrowing; **TriggerDispatcher reads item snapshot from storage BEFORE rule_engine.evaluate(event, item_snapshot=item)** — D12 new flow; tasks/milestone.close_all_items NEW per FR-64 Option (b) HILDA-owned per-item cascade. Foundation: 6 files (celery_app + config + registry + dispatcher + manual_trigger + __init__) + 11 WFL error codes + 25 tests. Task bodies: 4 task files registering 10 ActionKinds (state.UPDATE_STATE + INSTANTIATE_DEFAULT_WORK_ITEM; milestone.4 tasks; routing_resolution.3 tasks; escalation.2 workflow-owned) + task_deps.py runtime DI + diagnostics_cli (`--diagnostic` + `--replay-event`); 19 additional tests. **Celery 5.6.3 added.** 8 ActionKinds pending downstream module landing. **Module #9 (dashboard) arch only** (`7dee1ed`): 8 drift items D1-D8 (AutomationRuleOverride admin view Ph-2 deferred; CONFIRMATION → "Confirmation" PascalCase; FR-56 integration with workflow_engine TriggerDispatcher; FR-87 step (A)(B)(C) clarification; llm_review_findings null in Ph-1 early drop). Dev pending — skeleton only. **Session totals**: 22 commits, ~14000 lines added, 463 tests pass full suite. Modules complete arch+dev: diagnostics, credential_service, template_schema (+ mini-cascade), storage, tracker, rule_engine (6/13). Module #5 (llm) arch only. Module #8 substantially dev (foundation + 10/18 tasks + CLI). Module #9 arch only. Modules #10-13 + dashboard dev pending.

- 2026-06-21 **Architecture-phase Module-by-module revisit + dev cascades — Modules 1-4 complete (10 commits)**. Per-module arch revisit + dev cascade applying recent FR cascade locks. **Modules completed**: (1) **diagnostics** — CSA prefix rename + STATUS meta-prefix + CMG/CPG additions + ITR-E003 reassign to "customer JIRA auth expired" per FR-25 (b) + ITR-E008 preserves old idempotency semantic + CRD-E005 added; PREFIX_REGISTRY 18 → 21 entries. (2) **credential_service** — D1-D3 drift fixes (per-(account,customer) customer JIRA per FR-25 (b) + per-customer Google Drive per FR-19/77 + corp PLM/messenger no-credential pattern per FR-25 (a) + FR-51 pattern (d)) + Q1 customer_id signature extension on get_credential; scope-aware SopsCredentialService.get_credential routing (SHARED / PER_CUSTOMER / PER_ACCOUNT_PER_CUSTOMER / NO_CREDENTIAL); customizations/credentials/MODULE.md NEW drop-zone; CredentialScope + SYSTEM_CRED_SCOPE + SYSTEM_SUBTREE maps added to protocol.py. (3) **template_schema** — D1-D14 drift fixes: NOT_STARTED verified in DeliveryState enum (11 values); 4-field owner identity per FR-88 + [D-080] + [D-086] (owner_corp_usa_email + owner_corp_email + owner_corp_id + owner_name; single owner_email removed); nested item_description list[list[str]] per FR-82 lock 2026-06-20; **TGGroupBase DROPPED** per [D-051] denormalization + architect lock 2026-06-21 (TG fields denormalized onto DeliveryItemBase); DefaultWorkItemConfig FR-78 hardcoded inventory expansion (tg_path_id="_unrouted", item_path_id=None, force_tracking_enabled=False, owner_*=None, tracking_modality=None, milestone_gating=True, no_customer_upload=True, review_required=False, doc_count=0); CustomerTemplateBase + customer_jira_url + customer_delivery_info + delivery_path_template (customer-level per FR-77 + NFR-21 §6); MilestoneBase + carrier + project_id + project_model per [D-088] + button-trigger timestamps (HILDA-managed runtime); DeviceBase.assigned_pm_id optional with [D-088] runtime-resolution note; path_slug → path_id rename throughout; review_status 4-value enum validator (pending/complete/not_required/failed); ingress_nsd Choice validator (none/nsd1/nsd2); 7-flag form-factor set per [D-084] (mr removed); TSC-W005..W008 codes added. **MMK template.yaml NEW** at customizations/template_schemas/MMK/template.yaml — 10 work-items + customer-level delivery_path_template (OEM_Folder1/OEM_Folder2/{project_model}/{milestone_name}); 10/10 roundtrip-validated through Pydantic. (4) **storage** — D1-D4 drift fixes: 4-field owner identity on DocumentItemAssociation + PLMFanOutTarget + reassign_document_to_workitem signature; **owner_corp_id is sole PLM grouping key per FR-5 + FR-8 step 2 + [D-035]** (architect lock 2026-06-21; was owner_email); NSDPath constructor param slug → id rename (customer_id/device_id/milestone_name/tg_path_id/item_path_id/doc_type/inferred_tg_path_id; doc_id_slug preserved); TGGroupBase references replaced with DeliveryItemBase TG-denormalized field references; "Default" → "default" item_type per lowercase_snake_case rename; ix_assoc_owner_corp_id index added for FR-79 PLM fan-out grouping. **D5 flagged for Module #7 (rule_engine) joint revisit**: AutomationRuleOverride does not support per-item scope (FR-31 sub-1 pause + sub-3 manual trigger). **Test results**: 252 tests pass across 5 modules (template_schema 61 + diagnostics 27 + credential_service 51 + storage 59 + issue_tracker 54). **MAP.md regenerated** with new module customizations/credentials + new [NEW] flag for customizations/template_schemas (template.yaml drop-zone needs MODULE.md next session). **Architect direction captured for next session**: LLM Ph-1 scope = CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT only (CLASSIFY_MESSAGE + REVIEW_DOCUMENT deferred to next pass; CLASSIFY_DOC = Ph-2 per [D-039] revision resolution); FR-52 5-step pipeline full implementation in email_service Module #12. **SP UI engineer note** (heads-up for Module #11 sharepoint_config): bool fields rendered as SP Choice (Yes/No) strings — translation layer concern (force_tracking_enabled / no_customer_upload / review_required / milestone_gating / manual_triage_required / folder_routing_enabled + 7 form-factor flags). **Commits**: 864041e (diagnostics arch) → 77020ed (credential_service arch) → fd61ec7 (credential_service dev) → 9c39a0e (template_schema arch + MMK template.yaml) → e3b0cb5 (template_schema dev) → d9f108b (storage arch) → 4029102 (storage dev) → 0301bcd (Projects-per-customer FR rewrite) → 17a8e54 (Projects-per-customer thorough sweep) → 40bbfd0 (MAP regen earlier). 4 confirmed-to-be-Ph-1-scope-only architectural deliverables with intentional Ph-2 hooks (additive fields marked `[Ph-2]`) per architect guardrails 2026-06-21.

- 2026-06-20 **Spec sweep + architect-direction locks** (continuation from 2026-06-19 close-session at 900K context). Deep-scans: 28 FRs/NFRs (FR-55/39/63/64 + NFR-21 full rewrite + NFR-1..NFR-20 sweep + FR-66/67/68/69/70/73/74/76/75/77/78/79/80/82/83/84-rescan/85/86/87/88). Major architect-direction locks captured: (a) `item_type` enum lowercase_snake_case rename (~190 sites + xlsx + example.yaml); (b) Ph-1 setup-window owner-editability (FR-88 + FR-2 + FR-25 (b) TSC-W006 runtime detection at Start Collection + FR-71 ODF write policy + FR-84 dispatch + FR-40 template.yaml excludes `owner_corp_email`); (c) FR-82 nested tag-set model (JSON list-of-lists + TSC-W007 subset-detection + TSC-W008 doc_count consistency); (d) `customer_delivery_info` → base-URL only + FR-69 `delivery_path_template`; (e) FR-68 hash-match logic dropped — `uploadAttachment` return + UI file-exists check sufficient; FR-19 cascade; (f) FR-81 option (a) refined for SP BOOL binary semantic (column-default = `true`); (g) FR-64 Close All Items Option (b) — HILDA-owned per-item cascade write (dropped "atomic multi-row" framing — SP-2017 only supports row-level atomicity); (h) `tpm_resolved_doc_type` STR→LIST Choice (defense-in-depth); (i) `corp_id_list` + `email_cc_list` semi-colon separator + `corp_id_list` xlsx row added; (j) MNO-CP as 17th `tg_name` value; (k) FR-86 path-name cascade applied to source-of-truth (`_staged_classification/` → `_staged_not_classified/`; `_unrouted/` placement); FR-84 5-field routing key legacy paragraphs removed; NFR-21 full rewrite per [D-082] cascade-dedup obsoletion; (l) SP_UI_button_actions.md: Start Collection enablement "In Progress" removed (structurally impossible state combo); Close All Items reframed per Option (b). **Files**: requirements.md (heavy); SP_lists_authoritative.xlsx (~12 rows updated/added); SP_UI_button_actions.md; example.yaml. **Architectural Q&A**: pm_approval_pm_id NOT pre-populated on owner edit; tpm_* fields Ph-1 vs Ph-2 mapping; default WI field defaults inventory; force_tracking_enabled SP BOOL binary; prior_delivery_state NULL default (not "none" enum); milestone.status enum (Not Started → In Progress → Delayed/Completed); who writes Closed (per-item Mark Closed = SP UI engineer; Close All Items per Option (b) = HILDA); View Documents URL construction. **Reviewed**: `milestones_workitems_fields_values.xlsx` (3 tabs Milestones/Deliverables/Projects) — surfaced 23 issues/gaps/conflicts (7 + 14 + 2); fixes flagged for SP UI engineer follow-up (see Flags). **~10 D-DRAFT candidates** awaiting formal ADR ratification (see Flags).

- 2026-06-18 **Post-cascade deep-scan verification — 5 FRs audited (FR-2/FR-3/FR-6/FR-8/FR-9) for residual cascade-drift after SP architecture rewrite**. FR-2 — 13 findings (architecture intro stale; assigned_pm_id denormalization; TG-level field drift; HILDA never creates rows paragraph; bootstrap reads wrong list; setup_milestone scope missing Milestones row; D-077/D-082 stale; PM lookup chain undocumented; ops-editable fields; project_id auto-generated framing; "a few others" vague; Projects list maintenance; "triple's SP rows" incomplete). FR-3 — 5 findings (Person/Group field stale; D-083 Ph-2 write scope; item removal with docs; owner-change vs delivery_state interaction; PM vs owner reassignment). FR-6 — 5 findings (SP UI rendering pattern; initial value ambiguity; daily tick vs polling_schedule conflation; default WI Delayed contribution; history stamp). FR-8 — 5 findings (owner_corp_id key sweep; corp PLM gateway authentication; Milestone.status N-row writeback; PM lookup chain; NSD polling condition wrong field). FR-9 — 10 findings (outreach sender; tg_owner_corp_email never existed; CustomerJIRA-only outreach corrected; CorporateMessenger as FR-11 not FR-10; default_cc_list source; BATCH-id scope owner_corp_id; PM dashboard → TPM dashboard; CorporatePLM/NSD outreach body TBD STATUS Flag; NFR-10 retry hardcoded numerics; history stamp). **Pattern observation acknowledged mid-session**: was clustering at 5 findings; commitment to enumerate without target cap going forward.

### 2026-07-08 → 2026-07-15 (16 commits, live corp Linux box hardening + serialization design pass)

Wave-based recap; commits `25bc746..9cf65ee`.

**Wave 1 — Reconciliation impl (D-142/D-143 landing).** `4b8f37d` seeded strand; `40c3e0c` landed meta-reconciler + 5 sync sub-tasks + Celery beat wiring + config-driven kill switch. Ph-1 posture: disabled during focused testing per architect; enabled later once reconciliation-active testing begins.

**Wave 2 — Live corp Linux box bugfixes surfaced during Ph-1 testing 2026-07-06 → 2026-07-08.** Four commits, each fixing a real defect uncovered by architect's 10-device × 11-item smoke tests:
  * `e8c1701` — `find_items_by_natural_key` now optionally scopes by `device_id`; import path passes it. Prior key `(customer, tg, item_no)` matched across 10 devices → 9 of 10 ADDED alerts dropped as duplicates → only 11 rows landed for 110 expected. Import + tag-propagation call sites both preserved (tag-propagation still spans devices intentionally).
  * `a03cdcf` — device_id scoping added to `kickoff_collection_task`, `submit_to_carrier_task`, and `close_all_items_task` event_context resolution. Prior behavior: TPM clicking Start Collection / Submit-to-Customer / Close-All-Items on ONE device transitioned items across ALL devices in the milestone. All 3 tasks now filter by `event_context.device_id` (dispatcher populates from EntityRef).
  * `c09cbd5` — `inbound_attachment.py` inline OutreachSent → DocumentReceived call passed `update_delivery_state` without `params={}`. Signature required it → TypeError → outer try/except swallowed → `events_fired=0` → items skipped DocumentReceived and jumped straight to OwnerClosed (state machine legal edge) via owner-reply cascade. Fix: pass `params={}` and reorder args to canonical signature.
  * `215a8e0` — `attachment_router.py` Step 0 conflated file-bytes dedup with item-association dedup. Same regulatory PDF (same file_hash) arriving for items on multiple devices within the same milestone: first arrival routed; all subsequent devices got `matches=()` because Step 0 returned early. Fix: split into (0a) bytes-existence check + (0b) always-route + per-item association filter using new `document_ops.item_has_association(hash, item_id)` helper. FR-79 same-milestone invariant on `add_document_item_association` still holds. Storage exports gain `item_has_association` + `get_local_nsd_path_for_file_hash` (Postgres sync wrappers on PostgresStorage; async wrapper on `_AsyncStorageShim`).

**Wave 3 — Ph-1 UX polish.**
  * `747a9a1` — Ph-1 view-docs branch rewrite to use async `get_documents_for_item` + `list_associations_for_item` + `make_download_token`; template gets a 4th column with per-doc "Download" button linking to `/dl/<token>`. Outreach subject enriched: `[HILDA] <customer_id> / <device_id> / <milestone_id> -- Status request -- BATCH-<xxx>`. Outreach table headers switched to human-readable labels (Item No / Item Title / Current Status / Owner Comment). `body_parser_table._HEADER_ALIASES` extended with the 4 friendly aliases; legacy snake_case aliases retained for in-flight batches.
  * `b7ea8de` — follow-up: propagate `customer_id / device_id / milestone_id` per-row into `item_dicts` in `kickoff_collection_task` (subject enrichment reads from `items[0]` in `_send_batch_outreach_email`).

**Wave 4 — Serialization cascade (2026-07-15 architect design pass).** Three-phase design to eliminate SP alert ordering races between Setup Deliverables / Start Tracking / PM Approval / Submit-to-Customer. All three phases land HILDA-driven state writebacks so SP UI can gate button visibility on delivery_state being visible.
  * `a29859c` — **Setup Deliverables serialization**: `import_deliverable_tracker_task` after item create now transitions Not Started → Open via `update_delivery_state`, writing to both Postgres AND SP. Applies to ALL item types (Confirmation + Default WI included per architect ask). `kickoff_collection_task` eligibility filter widened from `delivery_state == "Not Started"` to `delivery_state in ("Not Started", "Open")` so items already advanced by import are still picked up. SP UI engineer to gate Start Tracking button on all items being Open. Prospective ADR: D-144.
  * `339bf6a` — **Phase A: PM Approval RFS writeback + Confirmation 2-hop close**. `apply_pm_approval_task` now mirrors pm_approval fields then calls `update_delivery_state(target=RFS)` (writes to both Postgres and SP via `_sp_writeback_field_updates`). For Confirmation items: additional hop `RFS → CLOSED` with `trigger_source='tpm_button'` (satisfies DEF-20). Backward-compat with SP legacy 3-field atomic write: no_op_idempotent handles it. `QueueSubmission` action removed from `advance_to_ready_for_submission_on_pm_approval` rule (task binding + enum + tests intact for future rules); carrier upload is now solely TPM-triggered via milestone-level Submit-to-Customer. Prospective ADR: D-145.
  * `ca1f108` — **Phase B: Default WI auto-close sweep**. `state_machine.LEGAL_TRANSITIONS[OPEN]` gains CLOSED as legal target (Default WI shortcut; skips artificial intermediate states). Guard 5 (DEF-20 TPM-attribution required for CLOSED) gets a carve-out: `item_type=='Default' AND from_state==OPEN AND trigger_source=='automated'` accepted. `apply_pm_approval_task` after per-item transitions land runs a sweep over the (customer, device, milestone) scope: if all non-Default items in {RFS, SubmittedToCustomer, Closed}, transition Default WI Open → Closed via automated trigger with `rule_id='default_wi_auto_close_on_all_ready'`. Ph-2 gate: skip auto-close and log `default_wi_close_deferred_ph2_pending` if Default WI has any classified attachments (documents pending routing / doc_type / revision resolution). Prospective ADR: D-146.
  * `e33e2cf` — **Phase C: scheduled TPM DRR closure email**. Beat task in new `core/src/workflow_engine/tasks/tpm_notification.py` runs every 300s; iterates customer configs, reads Milestones list from SP, per-row: parses target_date, classifies window vs `America/New_York` now (target_date-1 at 00:00 AND target_date at 00:00 with configurable window_minutes), audit-log idempotency guard, ops-alert on missed window or missing target_date. Excel builder in new `core/src/email_service/outbound/drr_report_excel.py` using openpyxl (columns: Item No / Item Title / Open-or-Closed Status / Owner Comment; "Closed" = `delivery_state in {Closed, ReadyForSubmission, SubmittedToCustomer}`). HTML body in new `core/src/email_service/templates/tpm_drr_closure.j2` (summary table Open/Closed/Completion% + per-TG pending table filtered to rows with count>0). EmailSender protocol + SmtpSender + EwsSender extended with optional `attachments=[(filename, bytes, mime)]` parameter (backward-compatible default None). Config in new `config/tpm_notification.json` + parser `core/src/workflow_engine/tpm_notification_config.py` with kill switch. TPM email + name resolved from Projects_<customer_id> list keyed on `project_model=device_id`. Prospective ADR: D-147.

**Wave 5 — SP onboarding tools for DRR milestone.** Architect prepping DRR milestone with ~85 items in `Deliverables_Template` list (vs P1's 10-item test milestone).
  * `0fccc44` — `scripts/list_sp_columns.py`: standalone helper that bypasses HILDA's 3-list entity abstraction and talks to `SpSession` directly with any list name. Prints two tables (user-facing vs system/hidden fields) with InternalName / Title / Type / Required / ReadOnly / Hidden. Read-only single GET on `_api/web/lists/getbytitle('<name>')/fields?$top=500`.
  * `1073584` — sys.path fix: prepends `/app` to `sys.path` so the script runs regardless of CWD (podman exec drops user in `/` by default; `PYTHONPATH=/app` workaround still valid).
  * `9cf65ee` — `scripts/create_sp_rows_from_excel.py`: bulk-create SP list rows from an Excel sheet. Row 1 = internal column names; rows 2+ = values. Coerces empty cells to skip (SP applies default), datetimes to ISO, else pass-through. Safety rails: refuses to write to non-*Template* list without `--force`; `--dry-run` prints payloads without hitting SP; `--start-row` / `--limit` for partial retries.

**Architectural discussions (no code this session)**:
  * SP echo alert defense-in-depth trace across parser + dispatcher + rule + task-whitelist for `delivery_state`, `PmApproved`, `ReadyForSubmission→Closed` echoes; confirmed silent drop via `sync_deliverable_fields_on_changed`'s whitelist filter (delivery_state not in `_STR_FIELDS ∪ _BOOL_FIELDS ∪ int/list bucket`).
  * Race between per-item PM approval mirrors and milestone-level Submit-to-Customer (5 items uploaded / 5 skipped in architect's live test) — root cause: SP fires PM approvals as N per-item alerts; `submit_to_carrier_task` is milestone-scoped and one-shot; if TPM clicks Submit-to-Customer before all mirrors land, subset stays at UnderPMReview and gets skipped. Serialization Phase A/B fixes the underlying signal: SP UI can now gate Submit button visibility on delivery_state=RFS being visible.
  * Reaffirmed Pattern A for Ph-1; Path B (HILDA-native TPM UI for deliverables/milestones) still deferred to post-Ph-1 per [D-143].
  * QueueSubmission double-upload risk vs milestone-level SubmitToCarrier: full regression scan → task/binding/enum/tests untouched by rule removal; only the YAML action line removed.
  * Container stale-code diagnosis: hilda-api's compose config on architect's Linux box was missing `../core:/app/core:ro,Z` bind mount; container had baked-in code from 2026-06-25. Fixed by architect editing compose + recreate.
  * Dashboard `ph1_minimal` config gap: config file was missing the flag, code default is True, but Pydantic BaseModel `extra='forbid'` rejected our attempted add — turned out to be an even earlier stale-code case (container's config.py didn't yet have the ph1_minimal field). Root cause: same missing bind mount.
  * SP password rotation: creds live in `~/hilda/config/sharepoint_integration.json` locally (not committed; corp-only). Bind mount RO + container restart picks up new values.
  * Windows Trust Zone / Restricted Sites blocking `ms-excel:ofv|u|\\ip\path.xlsx` — corp GPO forces UNC paths into Restricted zone regardless of user Local Intranet additions ("some settings are managed by your system administrator"). Suggested Excel Trusted Locations bypass (`Trust Center → Trusted Locations → Allow Trusted Locations on my network` + add UNC path); if GPO also locks Trusted Locations, escalate to IT for a GPO exception.
  * Discussion of browser view+edit paths for corp docs: Microsoft Office for the Web (WOPI Host required + partner program access → closed), Office Online Server / Web Apps Server on-prem (Microsoft product, legacy, per-user CAL), OnlyOffice Document Server (open-source WOPI-compatible, self-hostable in Podman container, ~200 lines HILDA-side WOPI Host), Collabora Online (same but LibreOffice-based), server-side PDF conversion via LibreOffice headless (view-only, simplest, ~1 hour). No code committed; awaiting architect decision.

## In progress

- ~~**5-sync reconciliation cascade (Path A) — DESIGNED 2026-07-02; awaiting implementation.**~~ *— CLOSED 2026-07-15: impl landed in commit `40c3e0c` (meta-reconciler + 5 sync sub-tasks); wired into Celery beat schedule with 300s interval; config in `config/reconcile.json` supports enabled/disabled per sync-type and elapsed_threshold_sec tuning. Ph-1 posture: `enabled: false` in config (kill switch) during focused live-box testing per architect ask; can be flipped to `enabled: true` once reconciliation-active testing begins. Trigger scope + retry-limit questions from prior close-session both answered by architect and reflected in impl (SP-driven scope; bounded retries deferred to Ph-2 when reconciliation is actually running).*

- ~~**Corp-box customer_delivery_info deploy + submit_to_carrier live retest.**~~ *— CLOSED 2026-07-08 by architect live-run. Submit-to-Carrier fired for SM-F918U P1 milestone; uploaded 5 items and skipped 5 due to race with per-item PM-approval mirror (root cause: SP fires PM approvals as N per-item alerts, submit-to-carrier is one milestone-level task; if TPM clicks Submit-to-Customer before all mirrors land, subset stays at UnderPMReview and gets skipped). Architect retest with fresh click after all mirrors settled: 10/10 uploaded. Underlying `5b09af4` CAD-E010 fix confirmed correct; race is a separate architectural concern addressed via the 2026-07-15 Phase A/B/C serialization cascade (HILDA now drives delivery_state writeback to SP so button visibility can gate on RFS state).*

- ~~**Post-cascade deep-scan verification**~~ *— CLOSED 2026-06-20: ALL remaining FRs/NFRs deep-scanned this session — FR-55, FR-39, FR-63, FR-64, NFR-21 (full rewrite), NFR-1..NFR-20 sweep, FR-66, FR-67, FR-68 (architect direction — hash-match dropped), FR-19 cascade, FR-69, FR-70, FR-73 (Ph-2 per [D-089]), FR-74, FR-76, FR-75, FR-77, FR-78, FR-79, FR-80, FR-82, FR-83, FR-84 re-scan, FR-85, FR-86, FR-87, FR-88. Sweep complete; spec is internally consistent across the post-[D-083]/[D-085]/[D-088]/[D-091] cascade.*

- ~~**SP UI requirements Ph-1 narrowing (NEW 2026-06-09; started, paused)**~~ *— CLOSED 2026-06-09: 11-group Ph-1 narrowing landed in `sharepoint/REQUIREMENTS.md` (commit `726da9d`). 3 new TPM-resolution buttons (§4.9 / §4.10 / §4.11) + new §7.4 SP-alert action-verb conventions + 5 new DeliveryItems fields + 3 new TGGroups fields + §12 open items refreshed (#7 RESOLVED; #8/#9/#10 NEW).*

- **Requirements.md user-led review continuation** — review pass closed for the FRs touched in 2026-06-05 session; future review passes may cover other FRs (FR-1 through FR-50 range; NFR section; Appendix A) when user resumes. No commitment on cadence; resume as needed.

- ~~**Architecture-phase Module rollbacks — partially complete (status 2026-06-07; updated 2026-06-09)**~~ *— CLOSED 2026-06-10: all 5 modules done. (1) `template_schema` ✓ `93e0aec` + `aefd8b7`; (2) `storage` ✓ `93e0aec` + `c02fff4` + cascade Group 2; (3) `llm` ✓ `054bcfa` + cascade Group 3; (4) `email_service` ✓ cascade Group 4; (5) `customer_adapter/MODULE.md` initial Ph-1 draft committed `f3804b8` 2026-06-09 + review-pass refinement `b7d6edb` 2026-06-10. Phase B cascade chain complete against the corrected `[D-053]` model locked 2026-06-08. Appendix refreshes in `requirements.md` (Appendix B FRs-requiring-LLM-calls + Appendix C phase scope index) remain queued — moved to Next.*

- **`[D-053]` reversal + doc_type classification redesign — CLOSED 2026-06-08.** All 3 design questions resolved through 13+ turn iterative refinement; corrected model captured in FR-7 + FR-85 + FR-86 + FR-87 + amendments to FR-16/FR-52/FR-53/FR-78/FR-83 (see Done 2026-06-08). Affected MODULE.md edits in `template_schema`, `storage`, `llm` move to a new In-progress entry below for the cascade work.

- ~~**MODULE.md cascade after `[D-053]` reversal close (NEW 2026-06-08)**~~ *— CLOSED 2026-06-09: cascade Groups 1 (template_schema in `aefd8b7`) + 2 (storage) + 3 (llm) + 4 (email_service) all complete and pending commit this close-session. `customer_adapter/MODULE.md` is NOT in scope of this cascade entry — it remains a NEW draft per the Module rollbacks entry below.*

- **MODULE.md cascade after `[D-053]` reversal close (NEW 2026-06-08; architecture phase when /switch-phase architecture resumes)** — 3 already-committed MODULE.md files need re-cascade against the corrected requirements model (locked 2026-06-08): **(1) `template_schema/MODULE.md`** — `ItemType` enum collapse from previous multi-value to 4 values (`Confirmation`, `TEST_TECH_WAIVER_REPORT`, `COMPLIANCE_CERTIFICATION_RELEASE_NOTES`, `Default`); `DocType` enum 4-value → 5-value (`compliance_certification_release_notes` rename of the former `default`; add `unresolved`); strike "1:1 derivation from item.item_type" docstring; add alignment invariant docstring per FR-86; restore `CLASSIFY_DOC_TYPE` TaskKind reference. **(2) `storage/MODULE.md`** — `NSDPath.internal_default_workitem()` signature add `inferred_tg_name` parameter (per FR-86 corrected path); add 2 new NSDPath helpers (`internal_staged_classification()`, `internal_staged_revision_for_classified()`); `DocumentIndexRow.doc_type` becomes `Optional[DocType]` allowing UNRESOLVED state (or sentinel value); document the FR-86 storage matrix in DocumentIndexRow docstring; add `STR-W007` (unrecognized doc_type) if needed. **(3) `llm/MODULE.md`** — restore `CLASSIFY_DOC_TYPE` TaskKind (un-revert the 2026-05-28b removal); add new ClassifyDocTypeInput/Output schemas with restricted candidate set `{test_report, tech_report, waiver}` per FR-85 Step 2; revert the "Not a doc_type classifier" Non-goal; tentative-assignments table 4 → 5 rows.

### 2026-05-28 requirements update — CLOSED (artifacts landed)

Closed at commit (this commit): all locked decisions from 8-item user-delivered requirements update materialized as durable artifacts. `requirements.md` gained 11 FR amendments (FR-7, FR-13, FR-17, FR-18, FR-19, FR-26, FR-52, FR-55, FR-62, FR-68, FR-72) + 7 new FRs (FR-77 through FR-83). `DECISIONS.md` gained 2 new ADRs (`[D-053]` routing model + default work-item + multi-item + ItemType.DEFAULT; `[D-054]` customer-portal destination + symmetric `no_customer_upload` + Google-Drive-only adapter) + 4 impl notes (on `[D-013]` dual-NSD, `[D-039]` item-routing-precedes-D-039, `[D-050]` simplified 4-area structure, `[D-052]` CLASSIFY_DOC_TYPE removed 5→4 TaskKinds). Phase switched back to architecture for Module rollbacks (see In progress).

### Requirements-phase working notes (2026-05-28; superseded by durable artifacts above)

> The detailed working notes below served as a recovery checkpoint during the section-by-section requirements review. All locked decisions are now durable in `requirements.md` (FR amendments + FR-77..83) and `DECISIONS.md` (D-053, D-054, impl notes on D-013/D-039/D-050/D-052). This subsection retained for audit trail; reference the durable artifacts for current spec.

**Section 1 — Doc types (locked):**
- `DocType` enum stays 4-valued: `{test_report, tech_report, waiver, default}`. Compliance docs, certification docs, release notes all fold into `default`.
- FR-16 (rule-based parser) → `test_report` only.
- FR-46 (final/interim classifier) → `test_report` only.
- FR-53 (LLM checklist review) → `{test_report, tech_report, waiver}` only.
- `default` doc_type triggers no downstream processing (no parser, no classifier, no review).
- `ItemType.DEFAULT` added (7 enum values now). ~~1:1 `item_type → expected_doc_type` derivation locks doc_type from item routing — no runtime doc_type LLM call needed.~~ **(2026-06-07 — REVERTED: `ItemType.DEFAULT` addition remains valid; the 1:1 derivation framing is incorrect — a single non-Confirmation, non-Default work item CAN receive any of `{test_report, tech_report, waiver}`, requiring per-document doc_type classification at runtime. See In progress "[D-053] reversal + doc_type classification redesign".)**
- ~~**`CLASSIFY_DOC_TYPE` TaskKind REMOVED from llm module** (5 → 4 Ph-1 TaskKinds). Requires `llm/MODULE.md` + `[D-052]` impl note rollback when returning to architecture phase.~~ **(2026-06-07 — SCHEDULED FOR RE-REVERSAL: `CLASSIFY_DOC_TYPE` (or equivalent classification mechanism) needs restoration; pending requirements-phase decision on mechanism. See In progress.)**

**Section 2 — Zip handling + PLM/Carrier upload (locked):**
- Zip files are archive-only — live in NSD `<tg>/_zip_audit/` (TG-level subdirectory); never uploaded to PLM or carrier portal.
- Version tree operates on individual files only — zip-level versioning irrelevant; D-039 + `[D-048]` apply per-individual-file.
- PLM uploads receive **only individual files** (Email/NSD-source: HILDA extracts + uploads; PLM-source: HILDA reads, no re-upload).
- Carrier portal receives **only individual files**, `is_final=true` after FR-18 dispatch; same for all ingress sources.
- PLM-source extracted files are written to HILDA NSD internal classified tree for audit consistency (`<tg>/<item>/<doc_type>/<doc_id_slug>/revN/`).
- `_zip_audit/<zip_filename>.zip` + `_zip_audit/<zip_filename>/<extracted-tree>/` co-located.
- New hash sync invariant: extends FR-68 — every file uploaded to carrier portal must hash-match a file present on PLM (top-level OR inside a PLM-resident zip). Non-match → `ITR-W003` PM-dashboard flag.

**Section 3 — Dual-NSD topology (locked):**
- NSD1 = bidirectional (HILDA-owned `internal/` tree + ingress); NSD2 = ingress-only.
- Both SMB-mounted on HILDA PC simultaneously; same `hilda-svc` AD service account.
- NSD2-sourced docs → NSD1's `internal/` tree (cross-drive write).
- NSD2 retention: original files stay on NSD2 forever; no archiving.
- Polling: both NSDs polled identically (same `polling_schedule`; no per-drive override).
- Resolution: FR-13 amendment + `[D-013]` impl note 2026-05-28; no new ADR.

**Section 6 partial — NSD ingestion structure overhaul (locked items):**
- No more per-item NSD ingress folders. Flat structure: `\\NSD_X\<carrier>\<device>\<milestone>\<Folder-1..n>\`.
- Folder name no longer authoritative for item routing.
- FR-52 routing pipeline (5-step ladder):
  1. **Strict substring** — `item_description` is semicolon-separated tag list; ALL tags must appear in filename.
  2. **Fuzzy match** — rapidfuzz(filename, item_name); top scorer above threshold wins. (`item_description` repurposed; no separate `routing_tags` field; SP column addition avoided.)
  3. **Source-folder → work-item** template (NSD-only; per-TG opt-in via `folder_routing_enabled = True`).
  4. **LLM `ROUTE_ATTACHMENT`** — first-page excerpt.
  5. **Staged → associated with milestone's default work-item** (FR-52 step 4); PM/TPM triage; parent NSD path tracked via new `DocumentIndexRow.unrouted_source_path: str | None`.
- TG-level routing strategy: TG-1 style (NSD1 ingress + no folder→work-item template) vs TG-2 style (NSD2 ingress + folder→work-item template); each TG declares its own ingress NSD + folder-routing opt-in.
- Default work-item: every milestone auto-includes one; `item_type = Default`; doesn't gate submission (per item 8).
- Multi-item routing (one doc → many items): adopt many-to-many join table `DocumentItemAssociation(document_index_row_id, delivery_item_id, association_kind: Literal["primary", "secondary"])` for clean per-association metadata.
- New `TGGroupBase` fields: `ingress_nsd: Literal["NSD1", "NSD2"]`, `folder_routing_enabled: bool`.
- Consolidated `folder_routing.yaml` per customer template, per TG: `<tg>/<source-folder-path>/{work_item, target_folder}` — both fields optional.

**Section 6 partial — Other (locked):**
- 6.2.4 fuzzy folder match: substring containment (case sensitivity TBD); longest-substring-wins on multi-match conflict.
- 6.2.5.5: source-folder template applies only to NSDx ingress (not Email/PLM).
- Item description format: semicolon-separated tag substrings; Pydantic validation regex; emit `TSC-W003` on non-tag format at template load; existing prose-description templates need cleanup migration.

**Section 7 partial — Email/PLM ingress (locked):**
- Zips extracted recursively; each extracted file → FR-52 routing.
- FR-52 step 3 (source-folder template) does NOT apply to zip-extracted files (no NSD source folder context); step 3 is NSD-direct only.
- 7.1.3.1 per-work-item → target-folder mapping: subsumed into consolidated `folder_routing.yaml` Option (b).
- 7.1.3.2: items in `staged/` block portal upload until TPM resolves.

**Section 8 — TPM manual handling (locked):**
- Default work-item does NOT gate `ReadyForSubmission` or carrier dispatch.
- TPM downloads via existing FR-57 download endpoint (`https://hilda.corp/dl/<scoped_token>`); manually uploads to carrier portal as needed (legacy/uncaptured deliverables path).

**Sections pending review:**
- Section 4: no-customer-upload property (granularity, state semantics, TPM download flow)
- Section 5: no-tracking TG + sub-item-only tracking (G-11/G-12/G-13 unresolved; FR-62 SP-UI upload extension question)

**ADR plan (revised after Section 3 disposition):**
- ~~`[D-053]` Dual-NSD topology~~ — superseded by `[D-013]` impl note 2026-05-28 (no new ADR).
- **`[D-053]` candidate**: Default work-item entity + multi-item routing + ItemType.DEFAULT + item homogeneity constraint (Section 6).
- **`[D-054]` candidate**: Per-TG `tracking_enabled` + `customer_upload_enabled` flags + customer-portal-as-carrier-submission-destination (Section 2+4+5).
- Disposition pending Section 4–5 close.

**FR amendments queued (drafts not yet written):**
- FR-13, FR-17, FR-18, FR-19, FR-52, FR-55, FR-68 amendments.
- FR-58 new (target-folder mapping).
- 3+ new FRs (default work-item, multi-item association, per-TG flags).

**Module rollbacks queued (apply after requirements-phase exit):**
- `llm/MODULE.md` — `CLASSIFY_DOC_TYPE` TaskKind removed; tentative-assignments table 5→4 rows; `[D-052]` impl note revised.
- `template_schema/MODULE.md` — `ItemType.DEFAULT` added; `item_description` semantic shift documented; `DocType` enum stays 4-valued (no compliance/cert/release_notes); new `TGGroupBase` fields.
- `email_service/MODULE.md` — FR-52 pipeline rewrite (5-step ladder); routing-tags semantic clarification.
- `storage/MODULE.md` — multi-item association table; `unrouted_source_path` field on `DocumentIndexRow`.
- `customer_adapter/MODULE.md` (not yet drafted) — Google Drive only Ph-1/Ph-2 in `customizations/`.
- `[D-050]` impl note — 5-area NSD structure simplified to `_zip_audit/` + classified + staged + outbound.

**Customer-adapter placement decided**: all flavors in `customizations/customer_adapter/<carrier_slug>_adapter.py`; Ph-1/Ph-2 = Google Drive only; Ph-3+ adds web portal + JIRA customer-portal. Protocol stays in `core/src/customer_adapter/protocol.py`.

## Next

- **Module architecture — Batch 1 complete; iterative dev↔arch cadence locked 2026-06-10 session 4**. Batch 1 design done (7 modules + 2 drop-zones; all Ph-1): `diagnostics`, `template_schema`, `storage`, `credential_service`, `sharepoint_integration`, `issue_tracker`, `llm` (Layer 0/1 fully done); `email_service`, `customer_adapter`, `rule_engine`, `workflow_engine` (Layer 2 designed); `tracker` (Layer 3 designed early); `customizations/sharepoint_config`, `customizations/rules` (drop-zone MODULE.md done). **Development of Batch 1 begins next.** **Batch 2 architecture revisit list** (will resume after Batch 1 development complete; none of these are Ph-1-scope-cut): `messenger` (Layer 2; Slack + proprietary adapter via api_spec_ingestor per `[D-016]`), `test_report` (Layer 2; FR-16/46; required when first customer with TEST_TECH_WAIVER_REPORT items onboards), `test_report_profiler` (Layer 3; producer of test_report's per-customer parsers per `[D-011]`; requires generic-vs-per-customer parser strategy flag resolution first), `api_spec_ingestor` (Layer 3; generates corp PLM adapter + proprietary messenger adapter per `[D-003]`/`[D-016]`), `dashboard` (Layer 4; PM/ops triage surfaces), `corp_messenger_gateway` (Layer 5 corp-side), `corp_plm_gateway` (Layer 5 corp-side), `customizations/issue_tracker` Structure markers (5-minute fix), `customizations/checklists` MODULE.md (drop-zone), `customizations/test_report_parsers` MODULE.md (drop-zone). `template_schema_ingestor` (Layer 3) stays Ph-2-only per DEF-15. Total HILDA-PC modules = 18; plus 2 corp-side gateway modules (`corp_messenger_gateway`, `corp_plm_gateway`) = 20 modules total. Next candidates by dependency order: Layer 2 (`workflow_engine`, `email_service` including new `sp_alert_parser` sub-module, `messenger`, `customer_adapter`, `rule_engine`, `test_report`) → Layer 3 (`tracker`, `template_schema_ingestor`, `api_spec_ingestor`, `test_report_profiler`) → Layer 4 (`dashboard`) → Layer 5 (corp-side gateways).
- **Dashboard module — promoted Batch 2 → Batch 1 architecture queue 2026-06-12** (was deferred to Batch 2 per STATUS Next 2026-06-10; promoted because the Variant A decision today makes dashboard's server-side HTML rendering a Ph-1 dependency for FR-57 / FR-59 / FR-60 / FR-61). Drafting next architecture session. Scope: HTTP entry point for HILDA (FastAPI app); `GET /docs/<delivery_item_id>` server-side HTML render (Jinja2 recommended); `GET /dl/<scoped_token>` download endpoint; `POST /milestone/<id>/refresh` per FR-56; `GET /admin/overrides` per FR-31; auth via Windows Integrated (Kerberos / SPNEGO) through corp reverse proxy. Decisions to lock in MODULE.md draft: HTML rendering engine (Jinja2 vs alternative); reverse-proxy identity-forwarding mechanism; content negotiation HTML/JSON; token-expiry UX (recommend auto-redirect); cross-cutting `core/src/auth/` MODULE.md candidate (Kerberos middleware shared by dashboard + future HTTP endpoints).
- **requirements.md FR-61 text reword (queued 2026-06-12)** — FR-61 prose currently says "resolves to the NSD classified path (`<doc_type_slug>/<doc_id_slug>/revN/` per `[D-041]`)" — predates the 2026-06-08 FR-86 cascade landing of the 4-path NSD model (classified / staged_classification / staged_revision / unrouted). Should reword to "resolves to the file's current NSD location per `DocumentItemAssociation.local_nsd_path` regardless of which of the 4 FR-86 path types it currently occupies; the download mechanism is path-type agnostic." Storage MODULE.md `make_download_token` docstring already correct; only requirements.md text lags. Trivial; batch with next requirements pass.
- **storage MODULE.md — token-stability Invariant for `make_download_token` (queued 2026-06-12)** — drafted but not applied this session: download tokens encode `(file_hash, delivery_item_id)`; HILDA resolves NSD path at request time via `local_nsd_path` lookup. Stable across FR-87 step (B) doc_type resolution and step (C) revision resolution; invalidated by FR-83 work-item reassignment (deletes the underlying association) and by 300s TTL expiry. STR-E007 raised on expired / association-orphaned tokens. Apply at next storage MODULE.md edit pass.
- **3 remaining follow-ups from the 2026-05-24 architecture review** (3 of the original 6 resolved via 2026-05-24 impl notes + D-047): (a) **IMAP IDLE / EWS streaming confirmation** from corp Exchange admin — covered as a separate Next item below; (b) **`MilestoneGating` + `ItemNumber`-stability schema decisions** with SP UI engineer — covered as a separate Next item below; (c) **`corp_messenger_gateway` + `corp_plm_gateway` MODULE.md drafts** — Layer 5 in the per-layer plan. *(Resolved on 2026-05-24: D-024 CI/CD reconciliation = impl note 2026-05-24 ✓; SP-alert email channel ADR = `[D-047]` captured 2026-05-24 ✓; `[D-021]` / `[D-026]` 3-host revision = impl notes 2026-05-24 ✓.)*
- **Resolve D-042 (Open design question)**: customer template storage location and TPM authoring path; currently customizations/template_schemas/ per FR-39/40/41 but full TPM authoring workflow + SP-side wiring still TBD.
- **Confirm with SP UI engineer** (cross-team architecture-phase action items): (1) ~~`ItemNumber` stability~~ *— RESOLVED 2026-06-12: `DeliveryItemBase.item_no` declared immutable per item lifetime in `template_schema/MODULE.md` Invariants; SP UI reorder is a `sort_order` concern, not `item_no`; load-bearing for FR-77 + `storage.TGFolderRoutingRow` referential integrity (storage/MODULE.md cross-reference Invariant 2026-06-12).* (2) SP alert "Send Alerts for These Changes" setting must be configured to "Anything changes" (so all PM/TPM field edits fire alerts — required for FR-14 correctness). (3) Confirm SP-side list-field naming aligns with HILDA's canonical schema (or document the name-mapping translation layer needed in `sp_alert_parser`).
- **Confirm with corp Exchange admin**: IMAP IDLE (or EWS streaming notifications) is permitted from HILDA PC for the dedicated mailbox. If yes → IDLE primary, latency ~1–2 s. If no → short-interval polling fallback (5–10 s). Either way, FR-23 deadline-tiered schedule is the third-tier fallback only. (SYSTEM.md Open Q #12.)
- **Architecture-phase choice for Test Report Profiler (`[D-011]`)**: PDF text-extraction path (`pdfplumber` / `pypdf` / `pymupdf`) and legacy `doc` handler (`antiword` / LibreOffice headless). Decide before `test_report_profiler` MODULE.md.
- **Ratify or supersede the reference-imported sub-conventions in `structure-conventions.md`** during architecture: (a) cross-tier `core ↔ customizations` deps allowed in both directions — still pending explicit ratification ADR; (b) the two-tier `config/global.json` + `config/<module>.json` model + env-var override pattern added 2026-05-24 — anchored by `[D-025]` impl note 2026-05-26 (scope expanded from `sharepoint_config/` to all `customizations/` sub-directories); explicit ratification ADR still pending.
- **Wire NTLM/Kerberos auth handlers against a real SP 2017 instance** (deferred until corp AD lab access). Current handlers raise SHP-E004 with detail; Ph-1/Ph-2 dev path uses NoAuthHandler against the mock server.
- **Code sync before next development trip**: `core/src/template_schema/enums.py` (rename BINARY → CONFIRMATION, add TGNameRegistry) and `models.py` (remove DeliverableBase per D-028; update DeliveryItemBase, MilestoneBase, CustomerTemplateBase per D-028 + round-2 field additions + 2026-05-24 schema additions — TG-group fields, doc_count, review_required, etc.). **2026-05-26 update**: add `TGGroup` Pydantic model per `[D-051]` (one row per `(milestone_id, tg_name)` — `tg_owner_name`, `tg_owner_email`, `email_group_alias` nullable, `corp_id_list` JSON nullable, `default_cc_list` JSON nullable); update `tg_groups.yaml` schema per `[D-049]` (ODF source data).
- **Decide SP UI engineer feedback path**: original Next item (TPMs run `sharepoint_integration_cli --serve` against mock SP) is partially superseded — SP UI engineer is now prototyping on the actual corp SP (live email samples sent to HILDA team). Decide whether the mock-SP harness is still needed for TPM UX feedback, or only for HILDA-side automated tests.
- **Propagation checklist for FR/NFR changes** (new 2026-05-26): whenever `requirements.md` FRs or NFRs are added or modified, sweep derivative docs for stale references before close-session. Derivative docs currently in scope: `HILDA_Design.md` (data model + workflow stages + architecture sections), `SYSTEM.md` (communication channels + persistence + open questions), `sharepoint/REQUIREMENTS.md` (SP list columns + alert config + open items), `structure-conventions.md` (customizations/ layout), per-module `MODULE.md` files for any module affected by the FR. Pattern: open the FR/NFR text; grep derivative docs for related terms; update or flag. Apply as part of close-session daily.
- **Deferred ADR candidates from 2026-05-26 forward-drift scan** (renumbered 2026-05-28 — `D-052` was taken by dual-backend LLM ADR landed 2026-05-28): **D-053** async carrier-package assembly with progress feedback (FR-73) — will land naturally at `customer_adapter/MODULE.md` draft (Layer 3); **D-054** SharePoint UI boundary (SP UI engineer ownership vs HILDA team) — anchored implicitly in `structure-conventions.md` and `sharepoint/REQUIREMENTS.md`; standalone ADR deferred unless drift-check flags it later. Track here so the candidates don't get lost during MODULE.md drafting.

- **5-sync reconciliation cascade (Path A) implementation** — see In progress entry. Awaiting architect's answer on the two open questions (trigger scope; retry limits).

- **Path B (hybrid HILDA-native TPM UI) architectural evaluation** — post-Ph-1-stable. Proposal: SP retained for carrier/project data (SP-authoritative per existing convention; correct home); HILDA-native web UI for deliverables/milestones (HILDA is the sole user of this data; eliminates SP alert reliability + reconciliation + denormalization bugs entirely). Existing `hilda-api` FastAPI container already serves Ph-1 dashboard — extend with `POST /milestone/<id>/setup` + `GET /admin/milestones/<customer>` + a modern SPA (React/Svelte/HTMX — architect choice). Estimated: 2–3 weeks. Decision trigger: after Ph-1 stable on 1 carrier + reconciliation running + real usage data on SP alert loss rate.

- **Deploy submit_to_carrier live retest + delivery_item_count reconciliation cross-check** — after `5b09af4` deployed + backfill run + successful Submit-to-Carrier. Compare `SELECT COUNT(*) FROM delivery_items WHERE milestone_id='P1'` (Postgres) vs SP Deliverables_MMK P1 count. Should be equal (or off by exactly 1 for Default WI depending on how default is counted).

## Flags

- **[NEW 2026-07-02] Path A vs Path B/C architectural decision open** — see 2026-07-02 Done entry. Ph-1 will ship on Path A (SP-authoritative + reconciliation). Path B (hybrid HILDA-native TPM UI for deliverables/milestones) is recommended for post-Ph-1. Decision trigger: after Ph-1 stable + reconciliation running + real usage data on SP alert loss rate. Not blocking Ph-1 ship.

- **[NEW 2026-07-02] 5-sync reconciliation trigger-scope + retry-limit policy open** — see In progress. Two questions block coding: (a) sync-1 scope (Postgres-guard vs SP-driven — recommended SP-driven so we catch the "every ADDED alert lost" case); (b) sync-4/5 retry limits per item/milestone with ops_alerts escalation on limit. Answered by architect → codes cleanly.

- **[NEW 2026-07-02] ADR candidates from 51-commit window** — prospective ADRs: (a) **Pattern A doctrine** (SP-authoritative state-mirror with atomic multi-field writes — apply_pm_approval_task is the canonical case; Guard 4's trust of `trigger_source='submit_to_carrier_task'` is the same doctrine); (b) **template.yaml as authoritative source** for structural DeliveryItem fields at import + Deliverables-CHANGED null-guarded merge for TPM edits; (c) **SP alert lossiness + email-plus-reconciliation-backstop pattern** (formal recognition that SP alerts are lossy; email is fast-path signal, reconciliation is authoritative backstop); (d) **Ph-1 Dashboard `ph1_minimal` flag** as the Ph-1/Ph-2 UI-scope boundary (single-flag gate for FR-60/61/87 blocks; template-native, no code fork). Deferred to next close-session's Pass-1 triage — this session's triage below determines which get formal ADR IDs.

- **[NEW 2026-07-02] MODULE.md full-file curation cascade needed post-D-139..D-143** — drift-check dev-full 2026-07-02 resolved 8 drifts by adding targeted rollback-log entries pointing at the ADRs. Full curation pass (Public surface + Invariants + Sub-modules + Depends on sections) for the 8 affected modules (`template_schema`, `workflow_engine`, `rule_engine`, `credential_service`, `storage`, `tracker`, `email_service`, `customer_adapter`) still deferred. Each MODULE.md's rollback-log entry names the specific sections that need curation. Estimated: 1–2 hours dedicated session.

- **[NEW 2026-07-02] doc_count consistency warnings should self-resolve post-backfill** — architect saw `WARNING doc_count consistency violation - item 'MMK-SM-S671U1-P1-N' has doc_count=0 but len(item_description)=1 per FR-82 architect lock 2026-06-20` on every live smoke. Root cause was import task never populating `doc_count` (template merge cascade fixes going forward; backfill fixes existing rows). Verify after backfill run: `SELECT item_id, doc_count, item_description FROM delivery_items WHERE milestone_id='P1'` — all deliverable items should have `doc_count = len(item_description)`. If violations persist, root-cause is elsewhere.

- **NEXT SESSION PRIORITIES 2026-06-28 (Phase B live-validation complete; D-DRAFT capture + container build hygiene + Phase B remainders)** — Phase B owner-reply handler PROVEN end-to-end on corp Exchange+SP. Top priorities for next session: **(a) D-DRAFT capture (3 ADRs queued)**: (1) **D-135** [D-118] strict-boundary cascade ratification (HILDA → alert-driven listener; SP UI engineer owns ALL SP row creation; HILDA never writes Milestones/Deliverables/Default-WI rows; rationale fully in commit messages 62fa8ce..4ed2e0e + Done entry 2026-06-26 PM); (2) **D-136** owner-reply parser anchor = substring batch_id check (NOT literal `HILDA-BATCH-ID:` regex on raw HTML; Outlook reply rendering inserts arbitrary inline markup between label and token; safety preserved by `_find_hilda_table_rows` requiring item_no+status headers; two prior regex attempts failed in live test commits 982003b → af756f8); (3) **D-137** HILDA→SP field-write pattern = two-step natural-key→_sp_id lookup (SP REST `/items({Id})` expects integer auto-counter Id, not HILDA's composite `<customer>-<device>-<milestone>-<item_no>`; `get_items(canonical_filters)` returns rows with `_sp_id`; pattern established in `owner_reply._write_note_only`, fed0b45; same pattern applies to tracker.transitions SP writeback if ever upgraded from best-effort). **(b) Re-verify Phase B SP note write** after `fed0b45` SP-ID lookup deploys (mark reply unread → watch `apply_owner_reply_task` audit `sp_written:true` flag → check SP UI item P1-2 `owner_status_note` cell shows "Working with sustainability team"). **(c) Phase B remainders**: (1) populate `delivery_item.owner_corp_usa_email` Postgres column from SP at kickoff time (currently `sender_match=mismatch` in audit because Postgres column is empty even though SP has the value; kickoff Path A reads from SP but doesn't write back to local row); (2) Confirmation-item template variant for FR-25(b) JIRA-modality body (Yes/No semantics rather than Open/Closed/Blocked/Delayed). **(d) Container build hygiene (architect-raised 2026-06-28)**: current deploy uses bind-mount for `customizations/` + `config/` (correct, environment-specific) but manual `cp` of `core/` + manual `pip install` for requirements.txt deltas (fragile — bs4 install would be lost on reboot). Recommend proper image build from `core/` + `requirements.txt`; bind-mount only customizations+config. If architect shares Dockerfile, spot-check for `pip install -r requirements.txt`. **(e) Step 5.5 attachment handling**: FR-52 routing, FR-86 storage matrix, FR-16 parser for `test_report` doc_type (next major work item after Phase B fully verified). **(f) Step 6**: View Documents from HILDA dashboard tab. **(g) Deferred Ph-1 follow-ups still open**: state_transition_log table + proper `write_delivery_state` implementation (currently no-op stub on PostgresStorage); HILDA → SP `delivery_state` writeback (currently best-effort via tracker.transitions with try/except); Confirmation-item outreach template variant per FR-25(b); delivery_item_id natural-key resolution in dispatcher for SP-alert OwnerReassigned events. **(h) MODULE.md updates pending**: workflow_engine/MODULE.md (Sub-modules tree should add `owner_reply.py`); email_service/MODULE.md (Sub-modules tree should add `body_parser_table.py` + `outreach_table.j2` template); both Public surface sections should reflect Phase B exports `apply_owner_reply_task` + `parse_table_block`. Est. 30-45 min. **(i) Carry-forward from prior 2026-06-26 PM Flag (still unmet)**: Point 3 refactor — convert 8 tracked sanitized samples to `.example` pattern (~30 min). Standing risk that careless `git commit -am` leaks corp content; no incident this window but the pattern remains fragile.

- ~~**NEXT SESSION PRIORITIES 2026-06-26 PM EVENING ([D-118] strict-boundary cascade Chunks 1+2/5 shipped; 3 chunks remain)**~~ *— CLOSED 2026-06-28: all 5 chunks shipped (3=`69c1d68`, 4=`5196a4a`, 5=`4ed2e0e`); architect-driven YAML rules added on Linux box; end-to-end test PASSED with live validation on corp Exchange+SP; MODULE.md updates pending (tracker/MODULE.md done; workflow_engine + email_service Sub-modules trees still need owner_reply.py + body_parser_table.py + outreach_table.j2 additions — carried forward to 2026-06-28 Flag above point (h)); D-135 ratification carried forward to 2026-06-28 Flag above point (a). Original Flag text retained below for archaeology.* HILDA Ph-1 architecture refinement in progress. Top priorities for next session: **(a) Continue [D-118] cascade — Chunk 3**: Build real `import_deliverable_tracker_task` body in `core/src/workflow_engine/tasks/sp_alert_imports.py`. Parses SP ADDED alert body_kvs into a DeliveryItemBase Pydantic model (need to map 25+ canonical fields per [D-105]/[D-106]), resolves device_id from body project_model or template lookup, calls `storage.create_delivery_item` with idempotency check via `find_items_by_natural_key`. Audit-log via AuditWriter. Tests: 4-6 cases (happy path, idempotent re-import, missing required field, race condition + concurrent create). Est. 60 min. **(b) Continue [D-118] cascade — Chunk 4**: Build real `kickoff_collection_task` body in same file. Reads `storage.list_items_for_milestone(milestone_id)` for the milestone, filters by `force_tracking_enabled=true AND item_type != "Confirmation"`, fires `ItemCreated` TriggerEvent per matching tracker via `TriggerDispatcher.dispatch`. Tests: 3-4 cases (happy path with 6 email-tracking items, empty milestone, all-confirmation milestone). Est. 45 min. **(c) Continue [D-118] cascade — Chunk 5**: Remove `sp_writer.create_item` call from `tracker/default_workitem.py:127` -- HILDA no longer writes Default WI row to SP (SP UI engineer creates it via "Setup Deliverables" button per architect direction 2026-06-26 PM). Update related tests in test_tracker.py. Update tracker/MODULE.md Key choices to document the boundary. Est. 15 min. **(d) Architect-driven YAML edits (Linux box)**: Add 2 new rules to `customizations/rules/global/automation_rules.yaml` LOCAL on Linux box: (i) `import_deliverable_tracker_on_sp_add` with trigger=ItemModified + condition list_name=Deliverables + action_type=added; (ii) `kickoff_collection_on_milestone_started` with trigger=ItemModified + condition list_name=Milestones + action_type=changed + field_deltas contains_key=milestone_collection_started_at. Both fire the corresponding new ActionKinds. Est. 15 min. **(e) End-to-end test (Linux box)**: With Chunks 3+4+5 + new rules: TPM clicks "Setup Milestone" in SP UI → SP fires ADDED alert → HILDA logs (no action); TPM clicks "Setup Deliverables" → SP UI engineer creates 10 + 1 Default WI rows → SP fires 11 ADDED alerts → HILDA's import_deliverable_tracker_task creates 11 trackers in storage; TPM clicks "Start Collection" → SP fires CHANGED alert with milestone_collection_started_at delta → HILDA's kickoff_collection_task fires ItemCreated for 6 email-tracking items → send_initial_outreach_on_collection_start rule matches → outreach emails sent to owners. Validates full FR-2/FR-8/FR-9/FR-78 chain end-to-end. Est. 30-60 min depending on errors surfaced. **(f) D-135 ratification**: After all 5 chunks land + end-to-end test passes + production telemetry validates, capture [D-118] strict-boundary cascade as new ADR (D-135). Substantial architectural decision worth its own entry; rationale fully captured in commit messages + this STATUS Done entry + architect-direction quotes 2026-06-26 PM ("SP UI engineer is the single owner of SP rows"). Est. 30 min in next-next close-session. **(g) MODULE.md updates**: Once cascade completes, update workflow_engine/MODULE.md (Public surface +2 task bindings; Sub-modules tree adds sp_alert_imports.py), tracker/MODULE.md (Key choices: HILDA no longer writes Default WI to SP per [D-118] strict-boundary), email_service/MODULE.md (still pending from prior close-session — Layer-2 content-hash dedup + multi-space body separator narrative). Est. 60 min. **(h) Carry-forward from prior 2026-06-26 morning Flag (still TOP PRIORITY)**: Point 3 refactor — convert 8 tracked sanitized samples to `.example` pattern. Architect's mods today STAYED local but underlying risk persists. ~30 min refactor + 5-10 min loader test. **(i) Other carry-forwards**: credential_service age_key_path mismatch, EwsConfig auth_type ntlm extension, TriggerDispatcher cold-start enrichment, Corp IT tickets pending (reverse-proxy registration + NSD1 mount), SP UI engineer coordination items (expected_completion_date removal), `template:` param dead-code cleanup in send_initial_outreach_on_collection_start rule.

- **NEXT SESSION PRIORITIES 2026-06-26 EVENING (Phase D2 EWS production validation + sp_alert_parser real-data hardening — both COMPLETE; carry-forwards from prior 2026-06-25 evening Flag)** — HILDA Ph-1 architecture VALIDATED end-to-end on real corp Exchange + real corp SP data. Carry-forwards in priority order: **(a) STILL TOP PRIORITY: Point 3 refactor** — convert 8 tracked sanitized samples to `.example` pattern + `.gitignore` non-`.example` real files. Today's session ran without any Point 3 incident but the underlying risk persists. ~30 min refactor + 5-10 min loader test. **(b) D-133 candidate triage**: Layer-2 content-hash dedup for SpAlertParser (real-format-driven enhancement; architect's call to log or skip — implementation is small but pattern is reusable; rationale fully captured in commit f3c5cc1 + smoke test output). **(c) D-128 amendment candidate**: real SP CHANGE body format (multi-space separator + dict-based routing_key extraction). Architect's call to amend D-128 narrative OR mint as separate ADR. **(d) MODULE.md updates pending**: email_service/MODULE.md should add (i) Layer-2 content-hash dedup to Key choices, (ii) multi-space body separator to Key choices, (iii) D-132 EWS adapter narrative update (already partially done at b6fedf6 but Sub-modules tree could be more explicit), (iv) move "Health endpoint implementation" out of Deferred (delivered). **(e) credential_service default age_key_path mismatch**: code default is `/etc/hilda/age.key` (single file) but deploy/MODULE.md runbook + production bind-mount target is `/etc/hilda/age-key/keys.txt` (directory + file). Change code default to match runbook; saves explicit age_key_path passing in future smoke tests. **(f) EwsConfig.auth_type Literal extension**: add `"ntlm"` to allow personal-account testing (Ph-1 next pass). **(g) TriggerDispatcher cold-start enrichment for SP-alert TriggerEvents**: CHANGE alerts contain only edited deltas; for rows HILDA hasn't seen yet (cold-start), dispatcher must fall back to `sharepoint_integration.SpClient.get_list_items(...)` per [D-117]. Wiring this into workflow_engine.TriggerDispatcher dispatch path completes the SP-alert→rule_engine integration. Also document `MILESTONE_NOT_FOUND` error path. **(h) Corp IT tickets still pending** (architect-driven, asynchronous): reverse-proxy registration (backend `105.52.91.33:8443`, paths `/docs/*`+`/dl/*`+`/healthz`+`/static/*`, `X-Authenticated-User` per [D-114]); real NSD1 SMB/CIFS mount for `storage.json` production switch. EWS service account already provisioned per today's validation (omadm_bot working). **(i) Phase D2 cleanup on Linux box**: remove leftover `mpose-test_postgres_1` + `mpose-test_redis_1` + `mpose-test_ping_1` containers from early 2026-06-25 smoke test (cosmetic). **(j) sp_alert_parser Ph-2 enhancements**: dedup persistence to Redis or Postgres for cross-restart + multi-replica tolerance; streaming notifications from EWS (lower latency than polling); username_format discovery in EwsConfig (NT-domain vs UPN). **(k) SP UI engineer coordination items** (already in prior STATUS Flag — REINFORCED today after observation in real Deliverable CHANGE body): `expected_completion_date` still in Deliverables body_kvs per [D-085] removal direction; SP UI engineer needs to drop column from list. **(l) D-131 ADR (marginal; still architect's call to log or skip)**: psycopg2-binary install requirement; commit b6fedf6 already fixed in tree.

- ~~**NEXT SESSION PRIORITIES 2026-06-25 EVENING (Phase D2 first bring-up SUCCESS; carry-forwards)**~~ *— PARTIALLY CLOSED 2026-06-26: (b) D-132 EWS adapter DELIVERED (commit b6fedf6) + production-validated end-to-end against real corp Exchange (6/6 SP alert scenarios); (c) llm-gateway commented out per commit 1b9a00f. Items (a), (d), (e), (f), (g), (h), (i) carried forward to 2026-06-26 EVENING Flag below.* Original priority list retained for archaeology: HILDA Ph-1 architecture validated end-to-end on corp Linux box (5/6 containers healthy; only hilda-llm-gateway in restart loop). Carry-forwards in priority order: **(a) TOP PRIORITY: Point 3 refactor** — convert 8 tracked sanitized samples to `.example` pattern + `.gitignore` non-`.example` real files (`customer.yaml` + `MMK/template.yaml` + `mock_customer/template.yaml` + `ops_alerts/recipients.yaml` + `sharepoint_integration.json` + `storage.json` + `workflow_engine.json` + `dashboard.json`); requires runtime loader update to fall back to `.example` when real file absent; ~30 min refactor + 5-10 min loader test. Removes the standing risk that future careless `git commit -am` could leak corp content. Architect's modifications today STAYED local but the pattern is fragile. **(b) D-132 EWS adapter for email_service** — corp Exchange 2013 has no IMAP/SMTP enabled; architect's colleague Chaitanya Kamsu provided 216-line `ExchangeMailService` sample using `exchangelib` + `Configuration` + service account basic auth. Implement `EwsReceiver` + `EwsSender` adapter classes matching existing `EmailReceiver` + `EmailSender` Protocol; extend `EmailServiceConfig` with `EwsConfig` nested + `mode` discriminator; tests via `exchangelib` mocks; MODULE.md narrative update; ratify D-132. ~3-4 hours focused work on personal laptop (HILDA-architectural code; no corp content). **(c) llm-gateway gateway_server.py Ph-1 stub** — currently in restart loop on Linux box; either implement Ph-1 mock LLM backend (returns canned responses on `:8080`) or comment `hilda-llm-gateway` service out of `deploy/docker-compose.yml` for Ph-1 first deploy. ~30 lines for stub OR 1-line compose comment-out. **(d) sanitized json.txt mockup correction** — the `dashboard.json` mockup provided earlier had `"jinja_templates_dir": null` which fails pydantic Path validation. Omit the field entirely (let code default win). Apply same correction to any other sanitized mockups that surface during D-132 work. **(e) Corp IT tickets pending** (architect-driven, asynchronous): reverse-proxy registration (backend `105.52.91.33:8443`, paths `/docs/*` + `/dl/*` + `/healthz` + `/static/*`, `X-Authenticated-User` per [D-114]); EWS service account / shared mailbox provisioning for D-132 (`hilda-noreply@corp`); eventually real NSD1 SMB/CIFS mount for `storage.json` production switch from named-volume sandbox. **(f) D-131 ADR (marginal; architect's call to log or skip)** — psycopg2-binary install in HILDA Dockerfiles. Rationale: SQLAlchemy `postgresql://` dialect defaults to psycopg2 (not psycopg3 nor asyncpg) for Celery's database result backend; alternatives considered: switch Celery to redis result backend (would lose audit trail in postgres), drop SQLAlchemy result backend entirely (Celery has no built-in alternative for `db+postgresql`), monkey-patch dialect (fragile). Marginal because the fix is a 3-line Dockerfile change; not architectural beyond "Celery + SQLAlchemy + postgres = need psycopg2". **(g) Update sanitized `.env.example` with EWS variables** — once D-132 lands, deprecate IMAP/SMTP env vars in favor of `EWS_SERVER` + `EWS_SERVICE_ACCOUNT_EMAIL` + `EWS_PRIMARY_SMTP_ADDRESS` per Chaitanya sample. **(h) Phase D2 cleanup on Linux box** — remove leftover `mpose-test_postgres_1` + `mpose-test_redis_1` + `mpose-test_ping_1` containers from early 2026-06-25 smoke test (cosmetic). **(i) Carry-forward from prior 2026-06-25 Flag (now folded)**: wire `ops_alerts.emit_alert(...)` call sites across 10 modules (1-line each); Module #5 llm Ph-1 next pass (CLASSIFY_DOC_TYPE + CLASSIFY_MESSAGE + REVIEW_DOCUMENT runtime-dormant; TRIGGER_PARSER + TRIGGER_AI_REVIEW ActionKinds 2/18 remaining); Project File Structure in MAP.md whole-repo walk; customizations/issue_tracker/MODULE.md STRUCTURE markers.

- **ACCUMULATED ARCHITECT-DISCUSSION TODOs 2026-06-26** (separate sessions; not blocking; in priority order): (1) **HILDA OPS alert mechanism** — multiple signal sites need a destination: ITR-W004 (PLM N-retries-exhausted per issue_tracker Q5 lock 2026-06-25), FR-87 SP audit writeback silent failures (best-effort try/except per [D-064]+[D-117] secondary channel pattern), future operational signals likely to surface. Likely outcomes: messenger HILDA BOT identity to ops_corp_id; OR dedicated dashboard /admin/alerts page; OR both. Will produce shared HILDA-OPS alert channel design. (2) **[D-116] D13 cascade follow-up CLOSED 2026-06-26 evening per D-126** — Q1 (binding 9th arg customer_delivery_info) + Q2 (customer_delivery_modality moved DeliveryItemBase → CustomerTemplateBase per-customer; no_customer_upload sole upload gate per FR-80) + Q3 (CAD-E010 strict missing-customer_delivery_info validation) all applied. Tests 755/755. **MMK template.yaml local-paste pending** per [D-125] Point 3: 10 per-item modality lines to delete + 1 top-level `customer_delivery_modality: GoogleDrive` to add; chat-generated diff shipped to architect for local Linux box. (3) **SP UI engineer coordination items** (architect informing them; HILDA waits for confirmation): (a) ADD `rules_paused: Choice(Yes/No)` column to Deliverables_<customer_id> per [D-108] (HILDA reads but column not provisioned yet); (b) REMOVE `expected_completion_date` from Deliverables (vestigial per [D-085] -- Milestone.target_date is sole authoritative deadline); (c) CORRECT or REMOVE `milestone_id` column on Deliverables (integer; not canonical per [D-090] -- milestone_name is the authoritative key). HILDA waits for SP UI engineer to apply before customer.yaml mappings reach production. (4) **Point 3 policy formalized 2026-06-26**: corp-SP-derived YAML mappings stay LOCAL (architect maintains on Linux deployment box); public github gets only sanitized placeholder + sample example.yaml. Github commit at 2026-06-26 renamed mock_customer.yaml -> customer.yaml + added Point 3 policy header note. Production customer.yaml generated in chat content for architect to paste locally.

- ~~**NEXT SESSION PRIORITIES 2026-06-25 (extraordinary close-session end-of-day)**~~ *— CLOSED 2026-06-25 evening: Phase D1 deploy/ scaffolding DELIVERED (commit 56d5bf1) + Phase D2 first bring-up on corp Linux box SUCCEEDED (5/6 containers up + healthy). Other queued items (ops_alerts emit_alert call sites + Module #5 llm Ph-1 next pass + Project File Structure in MAP.md + customizations/issue_tracker STRUCTURE markers) carried forward to 2026-06-25 evening Flag below.* Original priority list retained for archaeology: All HILDA Ph-1 code work essentially DONE: 15/16 modules dev-complete + ops_alerts + 16/18 ActionKinds wired + sp_alert_parser real-format + 5 ADRs ratified + drift-check dev-full clean + regen-map applied + corp-env Podman validation PASSED. **Suggested next session priority A: Phase D1 deploy/ scaffolding** — Dockerfile.hilda-api / Dockerfile.hilda-worker / Dockerfile.hilda-beat / Dockerfile.hilda-llm-gateway + deploy/docker-compose.yml + deploy/.env.example + deploy/sops_decrypt.sh + deploy/MODULE.md (sanitized samples for github; real `.env` + sops keys stay LOCAL per Point 3 [D-125]). After Phase D1 lands, Phase D2 = architect-led integration session on Linux box: `git pull` + place LOCAL files + `podman-compose up -d`. **Other queued non-blocking items**: (a) wire `ops_alerts.emit_alert(...)` call sites across 10 modules (1-line each at known failure sites -- issue_tracker ITR-W004 first, then CAD-EXXX/SHP-EXXX/WFE-EXXX); (b) Module #5 llm Ph-1 next pass (CLASSIFY_DOC_TYPE + CLASSIFY_MESSAGE + REVIEW_DOCUMENT) -- runtime-dormant in early drop per architect 2026-06-22 lock, so TRIGGER_PARSER + TRIGGER_AI_REVIEW ActionKinds (2/18 remaining) stay unwired; (c) Project File Structure section in MAP.md (deferred from today's regen-map -- whole-repo walk); (d) customizations/issue_tracker/MODULE.md needs STRUCTURE markers inserted for next regen-map pass.

- **NEXT SESSION PRIORITIES 2026-06-26 (close-session end-of-day)** — Yesterday's TOP PRIORITY FR-87 correction cascade DELIVERED ✓ + dashboard Gap 1+2+6+7+8 + DeliveryState (α) enum + customer.yaml rename + SpClient $expand. No blocking carry-forwards. Pick-from menu (in priority order; all non-blocking): (a) **Module #5 (llm) Ph-1 dev** — only remaining module without Ph-1 dev; would land CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT TaskKind implementations + prompt templates + dispatch per llm/MODULE.md Ph-1 first pass + [D-052] tri-backend. (b) **workflow_engine remaining 6 ActionKind wire-ups** — SEND_REMINDER + SEND_INITIAL_OUTREACH + NOTIFY_NEW_OWNER + ESCALATE + START_ITEM_COLLECTION + QUEUE_SUBMISSION. All downstream adapters now landed; these are 10-20 line task body fills each. (c) **`/drift-check dev-full`** — substantial code added this week + DeliveryState enum value change touches many sites; verify code matches MODULE.md contracts module-by-module. (d) **Full `regen-map`** — substantial code added (email_service / issue_tracker / messenger / dashboard / customer_adapter); MODULE.md Structure sections [DRAFT]; MAP.md needs regeneration. (e) **Tier 2 config/*.json operational tuning** — 7 modules need optional override files; defaults work for dev. (f) **Tier 4 deploy/docker-compose.yml** — hilda-api + hilda-worker + hilda-beat + Postgres + Redis + NSD mounts per [D-021] deployment topology. + messenger module + Tier 1 P0 YAML config delivered. Remaining priorities, in order: (a) **TOP PRIORITY: FR-87 correction cascade per architect Q3 lock 2026-06-25** -- FR-87 step A/B/C resolution is HILDA-internal-page direct POST NOT SP-alert mediated; lives in dashboard module not email_service sp_alert_parser nor rule_engine triggers. ~1-2 hr focused cascade: (1) remove 3 FR-87 rules from customizations/rules/global/automation_rules.yaml; (2) remove FR-87 handlers (tpm_reassign_to_workitem / tpm_resolve_doc_type / tpm_resolve_revision) from core/src/email_service/sp_alert_parser/parser.py; (3) update core/src/email_service/MODULE.md drop FR-87 references in sp_alert_parser narrative; (4) add 3 POST endpoints to core/src/dashboard/app.py + handlers (storage + SpCrud writeback per [D-064] + [D-117]); (5) update core/src/dashboard/MODULE.md document FR-87 button POST routes; (6) update tests both modules; (7) capture D-122 (revised) ADR. (b) **D-120 + D-121 + D-122 ADR promotion** -- 3 architecture decisions Q&A-locked this session pending close-session ratification: corp PLM 5-API thin-wrapper (D-120); messenger module ownership boundary (D-121); FR-87 direct-POST clarification (D-122). (c) **email_service vestigial cleanup** -- delete core/src/email_service/outbound/composer_escalation.py per Q-M6 lock (messenger owns composition). (d) **Module #5 (llm) Ph-1 dev** -- only remaining module without Ph-1 dev; would land CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT TaskKind implementations + prompt templates + dispatch (per llm/MODULE.md Ph-1 first pass scope per [D-052] tri-backend). (e) **workflow_engine remaining ActionKind wire-up** -- 6 of 8 pending unblocked after this session (TRIGGER_PARSER still needs test_report module Ph-1 dev which is out of HILDA-architect scope); the remaining 6 (SEND_REMINDER + SEND_INITIAL_OUTREACH + NOTIFY_NEW_OWNER + ESCALATE + START_ITEM_COLLECTION + QUEUE_SUBMISSION) wire-up to landed adapter modules. (f) **`/drift-check dev-full`** -- substantial code added this session (email_service 27 files + issue_tracker restructure + messenger 16 files); verify code matches MODULE.md contracts module-by-module. (g) **Full `regen-map`** -- substantial code added; MAP.md per-module Structure section regen + Project File Structure walk pending; STRUCTURE block in email_service / issue_tracker / messenger MODULE.mds currently [DRAFT]. (h) **Tier 2 config/*.json operational tuning** -- defaults work for dev; thin override files for production tuning. (i) **Tier 4 deploy/docker-compose.yml** -- hilda-api + hilda-worker + hilda-beat + Postgres + Redis + NSD mounts per [D-021] deployment topology. (j) **Q5 HILDA OPS alert mechanism** -- TODO architect discussion for ITR-W004 (PLM N-retries-exhausted notification mechanism). (customer_adapter Ph-1 dev + sharepoint_integration Ph-1 dev with NTLM digest-dance lock). D-117/D-118/D-119 PROMOTED to DECISIONS.md in this close-session. Remaining priorities, in order: (a) **Module #12 (email_service) arch revisit** — strict-order sweep #12 → #13; covers `sp_alert_parser` sub-module per [D-047] SP-alert channel + IMAP IDLE primary + polling fallback per FR-23 + outreach + reminders + escalations + FR-12 reply parsing + the 5 outreach/sp_alert ActionKinds for workflow_engine. (b) **Module #13 (issue_tracker) arch revisit** — closes the 13-module sweep; covers corp PLM polling via corp_plm_gateway + customer JIRA polling (Ph-1 informational only per [D-092]) + FR-26 PLM upload + FR-68 upload-success verification per [D-098] narrowing + 1 ActionKind for workflow_engine. (c) **workflow_engine remaining task body completions** — once #12 + #13 land, 8 pending ActionKinds + 6 trigger_sources + 4 polling tasks + beat_schedule unblock incrementally. (d) **`/drift-check dev-full`** — multiple layers touched (design + implementation) across customer_adapter + sharepoint_integration + credential_service this session; verify code matches MODULE.md contracts module-by-module. (e) **Full regen-map** — substantial code added (customer_adapter 8 new files + sharepoint_integration sp_session.py + mock_server/client.py); MAP.md status markers + per-module Structure section regen + Project File Structure walk pending. (f) **Module #5 (llm) Ph-1 dev** — arch-only currently; dev would land CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT TaskKind implementations + prompt templates + dispatch (per llm/MODULE.md Ph-1 first pass scope per [D-052] tri-backend). (g) **example.yaml minor follow-up audit** — 2026-06-25 rewrite was end-of-session; verify field-name accuracy against `docs/sp_ui_engineer/milestones_workitems_fields_values.xlsx` worksheets in a quick pass.

- **SP architecture lock per `[D-083]` (2026-06-17)** — Ph-1 SP model is 2 lists: per-customer `Deliverables_<customer_id>` Work Items list + global `Milestones` list; existing global `Projects` list reused for `project_id → TPM` lookup. Supersedes prior `[D-077]` flat-table framing. Ph-2 normalization plan deferred until production telemetry indicates whether further structural changes are needed.

- **SP_lists_authoritative.xlsx replacement (new 2026-06-17; architecture phase)** — current xlsx in repo reflects prior `[D-077]` flat-table architecture; SP UI engineer's revised schema (per `[D-083]` Google Sheets at `1gqmD9QVLQjuJ08Wc7zc7KI3jKQhr9MjH`) is the new canonical structure. **Action**: download the Google Sheet as xlsx, commit to `docs/sp_ui_engineer/SP_lists_authoritative.xlsx` (replacing existing file); audit column mappings against the FR-40 schema; verify SP-internal truncated column names captured in `customizations/sharepoint_config/customers/<customer_id>.yaml` mapping per `[D-065]`.

- ~~**SP UI engineer alignment on Project list columns (new 2026-06-17)**~~ *— CLOSED 2026-06-19 via `[D-088]`: `Projects.TPM` is SP Person/Group; HILDA extracts 3-tuple `(assigned_pm_id, pm_display_name, pm_email)` from sub-fields. Architect-confirmed `project_id` lookup is sufficient (Projects row carries `project_id` + `project_model` + `TPM`).*

- **FR-9 CorporatePLM + NSD outreach body format (new 2026-06-17; surfaced during FR-9 deep-scan)** — FR-9 specifies that CorporatePLM and NetworkSharedDrive modalities receive email outreach informing the owner of the expected delivery path (PLM issue URL or NSD shared drive path). The exact email body template / structure for these two modalities is currently TBD ('outbound format definition pending architecture phase' per FR-9). **Action**: define template body for each modality during architecture phase — must incorporate FR-24 `[HILDA:...]` subject tag prefix + FR-23 outbound mailbox identity + signature contract + FR-9 BATCH-id reply block conventions. Likely lives in `core/src/email_service/templates/<modality>.html.j2` + `.txt.j2` (Jinja2 templates) per email_service MODULE.md. Verify consistency with Email-modality template (path (b)) + CustomerJIRA-modality template (per FR-25 (b) JIRA URL phrasing). Cross-ref FR-9 + FR-23 + FR-24.

- ~~**NFR-21 review per `[D-083]` (new 2026-06-17; surfaced during FR-2 deep-scan)**~~ *— CLOSED 2026-06-20: NFR-21 fully rewritten this session. Dropped N-row cascade + [D-082] cascade-dedup framing; replaced with 3-list architecture per [D-083] (`Deliverables_<customer_id>` + `Milestones` + `Projects`) + Milestones single-row write per [D-085] + Projects.TPM 3-tuple lookup per [D-088]. STATUS-W004 + STATUS-W011 (sp-service-account-unset) inventoried in FR-84 self-loop section.*

- **`milestones_workitems_fields_values.xlsx` corrections needed (new 2026-06-20; surfaced during architect review)** — 23 issues/gaps/conflicts identified across 3 tabs (Milestones, Deliverables, Projects). **Deliverables high-priority**: (a) `doc_count = 0.0` on all 11 rows but should equal `len(tag_catalog[item_no])` per TSC-W008 (fix per-row); (b) `review_required = false` on test_tech_waiver_report items — per FR-58 should default `true`; (c) `list_documents_url_prefix` uses backslashes (invalid URL syntax) — must be forward slashes; (d) default WI `tg_name = 'null'` — must be `'_unrouted'` per FR-78; (e) R3 `item_description = 'Sustainability'` not JSON-serialized (should be `'[["Sustainability"]]'` per FR-82); (f) R11 `item_description = '[[LTE","OMADM"]]'` invalid JSON syntax. **Deliverables medium**: per_item_rule_overrides column doesn't match FR-31 Postgres architecture; ingress_folder/target_folder columns not in FR-2 line 46 inventory. **Milestones**: R6 Title="P1" non-standard vs LE-N convention; R19 duplicate TODO row; M3 confirm 32-char truncation Notes. **Projects**: missing `project_id` lookup key per [D-088]. **Action**: SP UI engineer to apply corrections; verify TSC-W007/W008 after fixes; once aligned, derive template.yaml from xlsx per architect direction. Full review available in 2026-06-20 session transcript.

- **PRIORITY next session — Module #5 (llm) arch revisit (new 2026-06-21 end-of-session)** — llm/MODULE.md is ~90% aligned with architect direction confirmed this session. Two narrative-only drift items to apply: **L1** — CLASSIFY_DOC TaskKind reframe from Ph-1 active → Ph-2 deferred (architect direction earlier this session: "CLASSIFY_DOC ([D-039] Tier-2) — Ph-2 (ignore)"); current MODULE.md line 64 lists it as Ph-1 TaskKind, line 68-70 has Ph-2 deferred (DRAFT_CUSTOMER_REPLY + SUMMARIZE_STATUS) — move CLASSIFY_DOC to the deferred block. **L2** — Ph-1 phasing not documented: first-pass active = CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT only; next-pass active = CLASSIFY_MESSAGE + REVIEW_DOCUMENT (Protocol declared but no implementation). Update Status header + Ph-1 scope narrative to surface this phasing. **Aligned (no edit needed)**: TaskKind Protocol declares all 5 TaskKinds (correct per Guardrail #3 forward-compat contract tests); ROUTE_ATTACHMENT as FR-52 step 4 of 5; restricted candidate set per FR-85; tri-backend topology; two-hop separation. **After arch edits, switch to dev for CLASSIFY_DOC_TYPE + ROUTE_ATTACHMENT TaskKind full implementations** (prompt templates + schemas + dispatch). FR-52 5-step pipeline (substring → fuzzy → folder routing → LLM ROUTE_ATTACHMENT → default WI fallback) implementation lives in email_service Module #12 driver.

- **PRIORITY next session — focused FR rewrite before architecture phase / YAML authoring (new 2026-06-21)** — Projects-per-customer architectural correction was locked in NFR-21 amendment §(1)-(6) on 2026-06-21, but ~7 load-bearing FRs still carry stale "global Projects" framing that would mis-inform YAML authoring + MODULE.md design-phase work. **Scope**: NFR-21 body text cleanup (line 483 mid-text still says "global Projects list reused as-is" — contradicts amendment); FR-2 lines 36/46/48/50/63; FR-9 sender attribution; FR-11 PM identification; FR-25 (b) CustomerJIRA; FR-77 portal routing; FR-84 Source A/B routing. Estimated 30-45 min focused work. **Then** `/switch-phase architecture` + start YAML authoring (`customizations/sharepoint_config/customers/<customer_id>.yaml`, template.yaml derived from `milestones_workitems_fields_values.xlsx`, `folder_routing.yaml`). Also: capture **D-104 candidate** (Projects per-customer + Milestones global asymmetry) as part of next-session ADR triage with the 10 already-flagged D-DRAFTs.

- **xlsx follow-up edits (2026-06-21)** — applied 23 corrections + 5 actor-attribution fixes + default WI revert (item_path_id NULL + force_tracking_enabled false) to `milestones_workitems_fields_values.xlsx`. Remaining: SP UI engineer to (a) confirm 32-char truncation columns; (b) provision SP columns with Required=No for nullable INTEGER (`tpm_reassignment_target_item_id`); (c) verify column data types match Notes (DateTime/STR/LIST/BOOL/INTEGER) before deriving template.yaml.

- **Storage D5 — FR-31 per-item scope architecture gap (new 2026-06-21; surfaced during storage MODULE.md revisit)** — `AutomationRuleOverride` currently supports Scope ∈ {Device, Customer, Global}; FR-31 sub-1 (per-item pause/resume) and sub-3 (per-item manual trigger) need per-item override OR a separate per-item table. Cross-module concern: storage + rule_engine joint design needed. **Action**: surface during Module #7 (rule_engine) revisit; options: (a) extend Scope enum to include "Item" with scope_id=delivery_item_id; (b) new per-item table `AutomationRuleItemOverride` keyed by (delivery_item_id, rule_id); (c) rework FR-31 sub-1 as a separate pause-flag column on Deliverables_<customer_id> rather than via the AutomationRuleOverride table. Recommend (b) — separates concerns cleanly + avoids overloading the Scope enum + per-item pause is fundamentally different from per-item parameter override.

- ~~**D-DRAFT candidates 2026-06-20/21 (~13 ADRs pending ratification)**~~ *— CLOSED 2026-06-25: 21 of 23 candidates bulk-captured via Option D (research-and-draft agent) + appended to DECISIONS.md as D-094..D-111 + D-113 + D-114 + D-115. **D-112 RESOLVED 2026-06-25** — architect confirmed interpretation (a) (clean up code per Done entry); `core/src/rule_engine/pause_state.py` DELETED + `__init__.py` stale comment removed + `workflow_engine/dispatcher.py` docstring corrected ("Ph-2 forward-looking only" claim removed); 109 rule_engine + workflow_engine tests pass post-cleanup; D-112 ADR promoted to DECISIONS.md (now 116 ADRs). **D-116 RESOLVED 2026-06-25** — architect provided final 8-arg binding API `uploadAttachment(Model_No, milestone_name, source_dir, target_dir, filename, pm_id, pm_password, totp_code) -> bool` + locked decisions: (A) `customer_id` implicit via per-customer subclass; (B-α) HILDA passes identifier components for Drive target side, binding composes internally; binding auto-creates target_dir; TOTP/MFA via 3-tuple `(user_id, password, totp_seed)` in credential_service + pyotp generation per upload; Ph-1 bool-only return (carrier_file_id + carrier_file_url deferred Ph-2 per Google Drive's 5-pattern URL fragmentation). Cascade applied to: customer_adapter/MODULE.md (D12-D17 second pass — drops `session_manager.py` + `selector_loader.py` + `capability_flags.py` from Ph-1 Sub-modules; updates CustomerAdapter Protocol signature; updates CarrierUploadResult Ph-1 shape; reverses D5 target-side; adds CAD-W005 clock-skew + pyotp dep) + credential_service/MODULE.md (`auth_type="basic_totp"` + `totp_seed` field). D-116 promoted to DECISIONS.md as Ratified (now 117 ADRs). Temp file `D-DRAFT-batch-2026-06-25.md` DELETED — D-DRAFT backlog fully drained. Original Flag text preserved below for archaeology.* **D-DRAFT candidates 2026-06-20/21 (~13 ADRs pending ratification)** — EXPANDED 2026-06-21 with 3 additional candidates from Module-by-module revisit session: D-104 (Projects per-customer per architect lock 2026-06-21; supersedes [D-083] paragraph framing Projects as global; supersedes [D-088] Projects.TPM lookup pattern → per-customer scoped); D-105 (4-field owner identity with `owner_corp_id` as PLM grouping key per FR-5 + FR-8 step 2 + [D-035]; supersedes single owner_email field across DeliveryItemBase + DocumentItemAssociation + PLMFanOutTarget); D-106 (TGGroupBase Pydantic model DROPPED per [D-051] denormalization architect lock 2026-06-21; TG fields denormalized onto DeliveryItemBase); D-107 (credential_service scope-aware routing with CredentialScope enum {SHARED, PER_CUSTOMER, PER_ACCOUNT_PER_CUSTOMER, NO_CREDENTIAL} + SYSTEM_CRED_SCOPE map + SYSTEM_SUBTREE map; supersedes flat sops layout for corp PLM/messenger no-credential pattern per FR-25 (a) + FR-51 pattern (d)). **drift-check design 2026-06-21 verified**: D-094..D-107 are the SINGLE drift surfaced by design-mode check; all other FR↔MODULE.md alignments are clean post-cascade. **Action**: batch-capture all 13 ADRs in dedicated triage session (high-confidence rationale is in STATUS.md Done entries 2026-06-20 + 2026-06-21 + commit messages; ratification is documentation hygiene). Original 10 candidates: D-094 `item_type` lowercase_snake_case enum rename; D-095 Ph-1 setup-window owner-editability + FR-40 owner_corp_email exclusion + TSC-W006 runtime at Start Collection + FR-71 ODF write policy; D-096 FR-82 nested tag-set model (JSON list-of-lists + TSC-W007 subset + TSC-W008 doc_count); D-097 customer_delivery_info base-URL + FR-69 delivery_path_template; D-098 FR-68 hash-match logic dropped (uploadAttachment return + UI file-exists check sufficient); D-099 FR-81 option (a) — force_tracking_enabled sole per-item field + SP BOOL binary semantic with column-default = true; D-100 FR-64 Option (b) — HILDA-owned per-item cascade for Close All Items; D-101 tpm_resolved_doc_type STR→LIST Choice conversion (defense-in-depth); D-102 corp_id_list + email_cc_list semi-colon separator convention; D-103 SP_UI Start Collection enablement narrowed (In Progress removed) + Close All Items single trigger + HILDA cascade. **Original 2026-06-20 entry retained below for context**: ~10 ADRs flagged from spec sweep day. — architect-direction locks applied this session that warrant formal ADR capture in DECISIONS.md: D-094 `item_type` lowercase_snake_case enum rename; D-095 Ph-1 setup-window owner-editability + FR-40 owner_corp_email exclusion + TSC-W006 runtime at Start Collection + FR-71 ODF write policy; D-096 FR-82 nested tag-set model (JSON list-of-lists + TSC-W007 subset + TSC-W008 doc_count); D-097 customer_delivery_info base-URL + FR-69 delivery_path_template; D-098 FR-68 hash-match logic dropped (uploadAttachment return + UI file-exists check sufficient); D-099 FR-81 option (a) — force_tracking_enabled sole per-item field + SP BOOL binary semantic with column-default = true; D-100 FR-64 Option (b) — HILDA-owned per-item cascade for Close All Items; D-101 tpm_resolved_doc_type STR→LIST Choice conversion (defense-in-depth); D-102 corp_id_list + email_cc_list semi-colon separator convention; D-103 SP_UI Start Collection enablement narrowed (In Progress removed) + Close All Items single trigger + HILDA cascade. **Action**: formal ADR capture deferred to a future close-session triage (this session focused on spec edits at architect's direction; not enough decision-template context per ADR for batch ratification at close-time).

- **template_schema/models.py + sharepoint_config rename cascade per `[D-084]` (2026-06-17)** — Form factor flag names update: `drr` → `ir`, `ir_ffw_p1` → `osmr`, `mr` → `rmr` (sheet names supersede prior `[D-081]` placeholder names). Cascade: `core/src/template_schema/models.py` `DeliveryItemBase` Pydantic schema + `customizations/sharepoint_config/customers/<customer_id>.yaml` example mappings + `core/tests/test_template_schema.py`. Architecture-phase task.

- **drift-check: 2 unresolved from 2026-06-16 requirements run** — deferred per user to upcoming FR-specific comprehensive reviews: (a) **DRIFT-2**: FR-79 PLM fan-out idiom uses `(owner_email × plm_id)` instead of `(owner_corp_id, plm_id)` per FR-52 I10 lock — covered in FR-79 review; ~~(b) **DRIFT-3**: FR-64 SP write target uses `Milestones.closed_all_items_triggered_at` instead of N-row write to `Deliverables_<customer_id>` per Ph-1 flat-table `[D-077]` — covered in FR-64 review;~~ *(CLOSED 2026-06-17 — resolved in FR-64 review: N-row write to `Deliverables_<customer_id>` per `[D-082]` cascade-dedup; Milestones SP list remains read-only per `[D-077]`)*; (c) **DRIFT-5**: FR-74 collection-closure final sweep references "all `inbound/` folders" instead of milestone NSD ingress folder per FR-13 lock — covered in FR-74 review.

- **Cross-NSD-share write performance (new 2026-06-16; architecture phase)** — per FR-13 NSD topology, NSD2-sourced documents are written cross-share to NSD1 `internal/` tree. Same-share writes (NSD1 ingress → NSD1 internal) are faster. If cross-share write throughput becomes a hot spot, a future optimization could mirror NSD2 ingress to NSD1 staging first so internal-tree write is same-share. Surface during architecture-phase NSD performance assessment.

- ~~**D-DRAFT-Z `customer.yaml` schema + FileBasedListProvider extension pending (new 2026-06-12)**~~ *— SUPERSEDED 2026-06-13/14 + CLOSED 2026-06-15. D-DRAFT-Z v1 (customer.yaml + Milestone denormalization) was REVERSED via v2 rewrite + 2 amendments (14b/14c) promoted as canonical `[D-077]` on 2026-06-15. Final architecture: customer.yaml DROPPED entirely; HILDA's runtime SP coupling EXPANDED from 2 lists to 4 lists (Customers + Devices + Milestones + DeliveryItems read scope); HILDA reads customer_id + customer_jira_url from Customers SP row, device_id + assigned_pm_id from Devices SP row at SP-alert receive (per FR-84 resolution path); SP UI engineer's `setup_milestone` task is sole row-creation path; HILDA NEVER creates SP rows. `FileBasedListProvider` extension no longer needed for customer/device data; still needed for `template.yaml` (per FR-40 single-template structure). See `[D-077]` + impl notes in canonical DECISIONS.md.*

- **template_schema/models.py + sharepoint_config/MODULE.md schema propagation pending (updated 2026-06-15; supersedes 2026-06-12 framing)** — canonical decisions landed (`[D-080]` owner email split + `[D-081]` form factor 5→7) but `core/src/template_schema/models.py` `DeliveryItemBase` + `TGGroupBase` Pydantic schemas have NOT yet been updated to reflect them. Required edits: (a) **`[D-080]` owner email split** — remove `owner_email`, add `owner_corp_usa_email: str | None = None` + `owner_corp_email: str | None = None` per FR-88; same split on TGGroupBase (`tg_owner_email` → `tg_owner_corp_usa_email` + `tg_owner_corp_email`); (b) **`[D-084]` 7 form factor flags** on DeliveryItemBase (supersedes `[D-081]`) — canonical names: `handset`, `tablet`, `wearable`, `ir`, `osmr`, `rmr`, `hmr_smr` (all `bool = False`); (c) **2026-06-14 slug→id rename** — `path_slug` → `item_path_id`, `email_group_alias` → `tg_email_group_alias`, `tg_path_slug` → `tg_path_id`. Cascade: same renames in `customizations/sharepoint_config/customers/example.yaml` example schema + update `core/tests/test_template_schema.py` for the new owner field set + form factor flags. **NEW additions from this session's FR cascade**: (d) **`TSC-W005` TG-equality validator** on `DeliveryItemBase` (TG-field equality across siblings sharing same `tg_name` per FR-40 denormalization discipline); (e) **`EML-W008` error code** registration in `email_service/error_codes` (Model cross-validation mismatch in sp_alert_parser per FR-84 / `[D-077]`); (f) `customizations/sharepoint_config/customers/example.yaml` cascade (D-077 reversal — drop Milestones-tab denormalized customer_id/device_id rows; D-073 impl note — add Customers/Devices SP column mappings for HILDA-readable fields). Order (A) architecture-phase batch.

- **Reverse-proxy registration binding contract for dashboard (new 2026-06-12)** — surfaced during dashboard-v1 strand architecture-review planning; user described: HILDA's `dashboard` module registers its `ip:addr/port/full http path` with corp reverse-proxy infrastructure via some binding contract; once registered, the reverse-proxy admin manually brings the proxy down momentarily and back up. **Open architectural questions for dashboard architecture review pass**: (1) **Reverse-proxy product** — NGINX? Apache? Custom corp software? F5 BIG-IP? (binding-contract format depends on this). (2) **Registration mechanism format** — config-file edit + reload, admin UI submission, REST/gRPC API call, ticket-based request to infra team? (3) **Registration cadence** — one-time at deploy, or on every dashboard restart? (4) **Health-check probe** — does the proxy probe backend health? At what path / interval / expected response? (Ph-1 dashboard likely needs `GET /health` endpoint if yes.) **Implications for dashboard/MODULE.md** (capture during architecture review): (a) `DashboardConfig` adds `external_url_prefix: str` (the path prefix at which proxy exposes dashboard, e.g. `/` or `/dashboard/` — used by dashboard to generate absolute download-token URLs that match proxy-exposed paths); (b) NEW Invariant "Reverse-proxy registration is an external infrastructure dependency. Dashboard does NOT auto-register, does NOT call the proxy at runtime, does NOT detect proxy restart events. Changes to dashboard's binding values require a coordinated ops ceremony (admin updates registration + restarts proxy)."; (c) NEW Non-goal "NOT a reverse-proxy registration agent — registration protocol is proxy-product-specific; deployment ops owns the ceremony"; (d) NEW Deferred "Auto-registration / health-check integration IF corp infra surfaces a registration API"; (e) NEW Deferred "GET /health endpoint to be added Ph-1 when reverse-proxy admin specifies probe contract"; (f) NEW open architectural decision item 7 in MODULE.md (adds to the 6 already listed): **URL-prefix model** — `/` (dashboard owns entire hilda-proxy.corp namespace) vs `/dashboard/` or `/hilda/` (path-prefix; allows other HILDA services to share proxy origin). Likely decision-worthy as D-XXX — locks the URL namespace for all HILDA HTTP surfaces going forward. **Owner**: user (HILDA architect) — gather (1)/(2)/(3)/(4) answers from corp infra team OR the reverse-proxy admin BEFORE the dashboard architecture-review pass; alternatively, dashboard architecture review can lock the binding-contract model abstractly + defer the proxy-product-specific detail.

- **setup_milestone task handoff sequencing for SP UI engineer sync (new 2026-06-15)** — FR-2 R&R lock (per `[D-077]` final amendment) makes SP UI engineer's TPM-triggered `setup_milestone` task the SOLE SP row-creation path. Open questions for SP UI engineer sync at architecture phase: (1) sequence with FR-8 Start Collection (distinct actions: setup_milestone = creation; Start Collection = state machine activation); (2) confirm per-(customer_id, device_id, milestone_id) triple granularity (one milestone at a time, not all-milestones-per-device) per "work-items per carrier don't change per device" rule (FR-40); (3) confirm idempotency invariant — re-running `setup_milestone` on already-onboarded triple is no-op for existing rows; HILDA never sees partial state per atomic 4-list commit boundary; (4) clarify template.yaml line 28 minor ambiguity (a milestone block appears nested inside the second device block in user's late-session template.yaml file — confirm or correct that milestones live at customer level only). Per FR-2 customer-onboarding flow + `[D-077]` impl note 2026-06-14 R&R lock. Captured in FR-2 + SP_UI_button_actions.md Start Collection section.

- ~~**Drift-check stale (new 2026-06-15)** — `Last drift-check: 2026-06-10` (5 days old)...~~ *(CLOSED 2026-06-16: `/drift-check requirements` run — 3 drifts resolved + 3 deferred to upcoming FR reviews per Flag above. Design-layer drift-check still pending — recommend `/drift-check design` next session per close-session nudge.)*

- **HILDA SIGHUP handler implementation confirmation (new 2026-06-15; arch-phase)** — FR-2 (b) Template.yaml change semantics assumes HILDA implements Unix `SIGHUP` signal handling for ops-triggered template.yaml reload. Architecture-phase confirmation needed: does HILDA process actually handle SIGHUP, or is the reload mechanism CLI command / HTTP admin endpoint / file watcher? If SIGHUP is not implemented, replace literal "SIGHUP" with the actual mechanism in FR-2 + relevant cascade locations. Surface during HILDA process module architecture review.

- **Customer offboarding + device end-of-life lifecycle (new 2026-06-15; Ph-3+ scope)** — FR-2 covers customer onboarding + device launch + per-milestone progression but does NOT cover offboarding (customer no longer tracked) or device retirement (end-of-life). Ph-1/Ph-2 operational answer: TPM marks all DIs as `Closed` via FR-64; storage cleanup per FR-76 removes NSD data; SP rows preserved for audit. Ph-3+: formal lifecycle deferral (template.yaml entry removal vs. preservation; SP row archival vs. deletion; new `DEF-XX` candidate). Track as Deferred when requirements pass for the Deferred section is opened.

- **FR review continuation — deeper-scan approach (new 2026-06-15)** — yesterday's FR-2/FR-3/FR-6/FR-8/FR-9/FR-13 reviews used a 2-cap-per-category approach (find ~2 issues per category and stop). Subsequent user clarification: the correct approach is "find ALL issues; surface TOP 2 recommendations per category" — meaning deeper scan, broader inventory, focused recommendations. **Action**: re-review FR-2/FR-3/FR-6/FR-8/FR-9/FR-13 with the corrected deeper-scan approach; continue forward with new FRs at the new depth. Working on `main` directly (no strand needed — llm-v1 and rule-engine-v1 strands are downstream-blocked on requirements review; no parallel-edit risk).

- **FR-83 reassignment-induced revert pattern (new 2026-06-17; surfaced during FR-63 review)** — FR-62 lock established: PM/TPM upload from `SubmittedToCustomer` reverts item to `UnderPMReview`; from `ReadyForSubmission` also reverts to `UnderPMReview` (clearing `pm_approval_at` + `pm_approval_pm_id` via SP-side write per `[D-068]`). Parallel pattern should apply to FR-83 work-item reassignment: when TPM reassigns a doc from default work-item (or from one real item to another) to a target real item that is already in `{ReadyForSubmission, SubmittedToCustomer}`, the target should revert to `UnderPMReview` (new doc invalidates prior PM approval / prior submission). FR-83 doesn't currently spell this out. **Action**: add explicit revert-on-reassignment paragraph during FR-83 review (mirror FR-62 revert pattern + `[D-068]` clearing discipline); verify FR-87 step (A) (TPM-driven reassignment) and `[D-039]`-driven downstream reassignment flows both honor the revert.

- **Ph-1 TPM SP UI capability inventory (new 2026-06-17; locked during FR-31 review)** — FR-31 entirely deferred to Ph-2 (rule control panel removed from Ph-1 scope alongside FR-3 / DEF-22 owner change + FR-14 Option B owner override + FR-56 (a) per_item_rule_overrides Ph-2 deferrals). **Ph-1 TPM SP UI capabilities**: **(a) Per-row cell edits** (per FR-14 lock): `delivery_state` (limited to 2 use cases — Delayed/Blocked entry + erroneous-transition rollback per FR-56 (a) lock); `review_required`; `email_cc_list`; `comment`; `tracking_modality`; `manual_triage_required` (clear only). **(b) Per-row buttons**: Approve / Send Reminder / Mark Closed / Clear Triage Flag / Confirm Carrier Acceptance / Resume from Delayed-Blocked. **(c) Per-row links** (navigation): View Documents → HILDA tab per `[D-074]`; View in PLM. **(d) Milestone-level buttons**: Start Collection / Submit to Carrier / Close All Items / Refresh / Download Package. **NOT in Ph-1 (Ph-2 deferred)**: Upload Document (FR-62 Ph-2); owner field edits (FR-3 / DEF-22 + FR-14 Option B Ph-2); per_item_rule_overrides (FR-56 (a) lock 2026-06-12 Ph-2); Pause/Resume Item Rules + Pause All-Resume All + Trigger Action dropdown (FR-31 Ph-2 per architect lock 2026-06-17). **In Ph-1**, TPM rule pause / customize / manual-trigger requests go to HILDA ops via ticket; ops edit `customizations/rules/<customer_id>/per_item_overrides.yaml` (YAML drop-zone + SIGHUP reload per FR-30 I1) OR invoke FR-29 actions via HILDA admin CLI / direct rule_engine call (out-of-band). Cascade impact: SP_UI_button_actions.md sections Pause Item Rules / Resume Item Rules / Pause All-Resume All / Trigger Action retagged `[Ph-2]`; SP_lists_authoritative.xlsx columns `rules_paused_at`, `manual_action_triggered_at`, `manual_action_kind` are Ph-2 (verify xlsx tagging during architecture phase).

- ~~**`customizations/template_schemas/` path verification (new 2026-06-17; surfaced during FR-30 review; EXTENDED during FR-39 review 2026-06-17)** — FR-30 + FR-39 both cite template schema YAMLs at `customizations/template_schemas/`. Per `[D-077]` final amendment: customer.yaml DROPPED entirely; surviving template.yaml lives somewhere per FR-40.~~ *(CLOSED 2026-06-17 during FR-40 review — FR-40 is the authority; template.yaml lives at `customizations/template_schemas/<customer_id>/template.yaml` (per-customer file, NOT single multi-customer). FR-30 + FR-39 references are correct as-is; future cleanup can remove FR-39's overly-cautious "verification pending" wording.)*

- ~~**FR-66 version-selection mechanism vs FR-50 outbound-only lock (new 2026-06-17; surfaced during FR-29 review)**~~ *— CLOSED 2026-06-19 per architect direction: corp messenger inbound mechanism locked OUT (per FR-50 + FR-66 cascade lock 2026-06-19); replacement mechanism deferred to Ph-2 architecture phase. Candidate channels: (a) email reply with structured selection block parsed per FR-12; (b) SP UI form (Ph-2 owner SP write permissions); (c) HILDA-rendered tab per `[D-074]`. Architect to pick (a)/(b)/(c) or alternative at Ph-2 architecture review.*

- **FR-76 PLM sweep step (new 2026-06-17; surfaced during FR-25 review)** — FR-25 P8 final lock 2026-06-17 (Option B): single PLM closure trigger at FR-76 MilestoneStorageCleanup. FR-76 must perform a PLM sweep BEFORE NSD cleanup: enumerate all PLM issues created by HILDA for `(device_id, milestone_id, *)` tuples and close via `updateIssueStatus(plm_id, status='Closed', metadata={...constant per adapter config...})`. Sweep failures log `ITR-E004` to `HildaOpsAlert` (retryable=true) but do NOT block NSD cleanup. **Action**: explicit PLM sweep paragraph during FR-76 review — add ITR-E004 error code; clarify sweep-then-delete ordering; reference per-customer adapter config constant metadata fields.

- ~~**SP_UI_button_actions.md sync for CustomerJIRA ticket-count display (new 2026-06-17; surfaced during FR-25 review)**~~ *— PARTIALLY CLOSED 2026-06-19: `jira_open_ticket_count` + `jira_ticket_summary_json` columns added to `SP_lists_authoritative.xlsx` Workitems list as `[Ph-2]`; FR-25 (b) + FR-9 + FR-12 cascade locked Ph-1 vs Ph-2 boundary (Ph-1 polling results in HILDA Postgres + CommunicationLog only — no SP write-back; Ph-2 adds SP write-back). SP_UI_button_actions.md Part 2 (Read Paths) for these columns + badge UI choice still pending — Ph-2 work; defer to Ph-2 architecture review.*

- **dashboard module Variant A architecture deferred items (new 2026-06-12)** — surfaced during FR-57 / FR-59 / FR-60 / FR-61 review under cross-origin-XHR-blocked corp policy. To address during dashboard MODULE.md draft: (a) HTML rendering engine choice (Jinja2 vs alternative — recommend Jinja2 for Ph-1; FastAPI-native, server-side only); (b) Reverse-proxy identity-forwarding mechanism (Kerberos re-forward via Negotiate vs proxy-validates + sets `X-Authenticated-User` header with strict source-IP allowlist on dashboard — depends on what corp's reverse-proxy product supports); (c) `/docs/<di_id>` response: HTML only vs HTML + JSON via `Accept` content-negotiation (recommend HTML default; JSON if Accept negotiated for HILDA-internal admin tools); (d) Token-expiry UX (TPM lands on expired-token page: auto-redirect back to `/docs/<di_id>` for fresh tokens vs "click here to refresh" link — recommend auto-redirect); (e) Cross-cutting `core/src/auth/` MODULE.md candidate (Kerberos / SPNEGO middleware shared by dashboard + future HTTP endpoints — recommend separate MODULE.md, not dashboard-local); (f) Reverse-proxy origin + CORS allowlist convention documented even if Ph-1 has no JSON consumers (anti-pattern-break prevention).

- ~~**requirements.md naming sweep — `target_folder` → `ingress_folder` for inbound side (queued 2026-06-07; FR-77 / FR-52)**~~ *— CLOSED 2026-06-10 as no-op via requirements-phase evaluation. Premise misread on close inspection: `target_folder` is consistently used only for OUTBOUND (carrier-side) in `requirements.md`. The INBOUND side has multiple descriptive variants ("outermost source folder (NSD or zip-internal)", "outermost folder name relative to milestone root", "outermost folder name inside the zip", "outermost folder name relative to fully-extracted zip root", `<matched_source_folder>` in path templates) — each correctly specific to its narrative context per user feedback. Collapsing to generic `ingress_folder` would dilute positional context. `template_schema/MODULE.md` alignment unaffected (its `FolderRoutingEntry.ingress_folder` / `DeliveryItemBase.ingress_folder` / `DeliveryItemBase.target_folder` field names remain canonical — they're code-level field names, distinct from prose terminology in requirements.md).*

- **Storage module debt — `DocumentIndexRow.inferred_tg_name` (new 2026-06-07; FR-78 / FR-83 / [D-053])** — Default work-item is per-MILESTONE (not per-TG); decided during template_schema/MODULE.md rollback. TG of an inbound document IS knowable from the channel (NSD ingress folder via TGGroupBase.ingress_nsd; email sender via email_group_alias / owner_email lookup; PLM-id via DeliveryItemBase.plm_id reverse-lookup). When FR-52 routing pipeline cannot resolve a specific work-item, document lands on the milestone's default work-item BUT the document record must carry the resolved TG. **Action**: add `inferred_tg_name: str | None` to `DocumentIndexRow` (plus reverse-lookup helper) during `storage/MODULE.md` rollback. Consumed by FR-83 TPM-manual reassignment to shortlist candidate work-items within the resolved TG.

- **MAP.md broadly stale (2026-05-24; refreshed 2026-05-26)**: `template_schema` module Purpose line still reads "Device / Milestone / Deliverable / DeliveryItem" — Deliverable level removed by D-028. Many other modules have touched fields since the last regen — `sharepoint_integration`, `issue_tracker`, `diagnostics`, **`storage`** (new MODULE.md draft 2026-05-26 — adds new module-row to MAP.md). Run regen-map before next architecture-phase MODULE.md drafting batch.

- `structure-conventions.md` references `the reference implementation` as the live reference for layout details (cross-tier deps, config rules, Protocol-boundary pattern). If the reference implementation's conventions evolve, HILDA's `structure-conventions.md` does NOT auto-track — it's a one-time import, captured in D-001's "Why". Re-verify alignment when reviewing the conventions file.

- **[BACKLOGGED 2026-05-06 by `[D-024]`; D-024 reconciliation resolved 2026-05-24]** **CI/CD tool selection — pending corp-ops consultation.** Pipeline shape decided in `[D-024]`. Open: (a) CI runner — GitHub Actions self-hosted / GitLab CI / Jenkins / corp-specific; (b) image registry — Harbor / Artifactory / Nexus / corp-specific; (c) GitOps tool — ArgoCD / Flux / none (CI-driven `helm upgrade`); (d) environment topology. **Update 2026-05-26**: D-024 reconciliation completed via impl note 2026-05-24 (Helm pipeline pinned to Ph-3+; Ph-1/Ph-2 = basic CI + manual deploy via `deploy/scripts/deploy.sh` + sops per `[D-038]`). Tool-bound choices (a-d) **remain backlogged** pending corp-ops consultation — they apply to the Ph-3+ Helm-CD landing, not the Ph-1/Ph-2 manual deploy path which is operator-driven.

- ~~**[DRIFT-2 skipped 2026-05-14]**~~ *(resolved 2026-05-14: Ph-1 = corp PLM adapter via API Spec Ingestor; Ph-2 = CustomerJIRA adapter. DEF-6 promoted to Ph-1; D-003 impl note updated.)*

- **Per-TaskKind LLM backend A/B test gate (new 2026-05-28; updated 2026-06-07 for tri-backend per `[D-052]` Consequences)** — **4 Ph-1 TaskKinds** (was 5; `CLASSIFY_DOC_TYPE` removed 2026-05-28b per `[D-053]` — doc_type now derived 1:1 from `item.item_type`) require measured-quality A/B testing on representative real fixtures before `task_backend_map` / `task_model_map` are locked for production. TaskKinds: `ROUTE_ATTACHMENT` (FR-52 step 4), `CLASSIFY_DOC` (D-039 Step 2), `REVIEW_DOCUMENT` (FR-53), `CLASSIFY_MESSAGE` (FR-12 path c). **Three backends to test** (updated 2026-06-07): `ollama_a4000` (Ollama on RTX A4000, 16 GB GDDR6, ~448 GB/s — fastest single-stream tokens/sec for ≤14 GB models) vs `vllm_dgx` (vLLM on DGX Spark, 128 GB unified LPDDR5X, ~273 GB/s — supports models that don't fit on A4000 + continuous batching at concurrency, but per-token throughput LOWER than A4000 for fits-on-A4000 models due to bandwidth) vs `corp_llm` (corp on-prem chat + agentic APIs, rate-limited). **Model catalog**: same Ollama-format models (`qwen3:8b-q4_k_m`, `gemma3:12b`) served on both `ollama_a4000` (Ollama) and `vllm_dgx` (vLLM with HF weights) for direct comparability. If small-model A/B results unsatisfactory, larger models (`gemma3:27b`, `qwen2.5:32b`, `llama3.1:70b`) added to `vllm_dgx` catalog (DGX Spark capacity allows; A4000 cannot fit). Tentative defaults: all 4 on `ollama_a4000` until A/B run completes. **Priority**: `REVIEW_DOCUMENT` A/B run is highest-value (user observation: corp LLM empirically weak at test-case enumeration; FR-53 task shape is checklist-against-content, untested at HILDA scale). **Capacity-vs-bandwidth caveat (clarified 2026-06-07)**: DGX Spark's 128 GB unified RAM gives capacity for large models the A4000 cannot fit, but the LPDDR5X bandwidth (~273 GB/s) is LOWER than A4000's GDDR6 (~448 GB/s). For autoregressive inference of fits-on-A4000 models, A4000 typically wins per-token throughput; DGX Spark wins on capacity + vLLM continuous-batching concurrency. A/B results per TaskKind capture these tradeoffs empirically. **No automatic spillover** between backends per `[D-052]` — silent fallback would risk degraded quality without a gate; spillover surfaces as `LLG-W006`. Tracked owner: TBD.

- ~~**`[D-053]` impl note 2026-05-28b "1:1 derivation of doc_type" is incorrect (NEW 2026-06-07; HIGH priority)**~~ *— CLOSED 2026-06-08 via requirements-phase redesign session (Done 2026-06-08 entry). The corrected model is captured in FR-7 + FR-85 + FR-86 + FR-87 + amendments to FR-16/FR-52/FR-53/FR-78/FR-83. All 3 design questions resolved. MODULE.md cascade work moved to In-progress.*

- ~~**`[D-053]` ADR body needs impl note appended (NEW 2026-06-08)**~~ *— CLOSED 2026-06-08 at close-session triage. Impl note appended to `[D-053]` in DECISIONS.md capturing the full corrected model (4-value ItemType + 5-value DocType + 2-step ladder + 4-path matrix + alignment invariant + `[D-039]` skip rule + SP UI A→B→C). Per `[D-002]` append-only — original Decision/Why/Consequences bodies preserved verbatim.*

- ~~**`[D-060]` ADR body needs impl note appended (NEW 2026-06-08)**~~ *— CLOSED 2026-06-08 at close-session triage. Impl note appended to `[D-060]` in DECISIONS.md documenting the `inferred_tg_name`-in-unrouted-path extension. Original Decision/Why/Consequences preserved.*

- **Error code IDs ratification at architecture phase (new 2026-06-19)** — `STATUS-W005..W011`, `ITR-E005`, `EML-W012` minted as candidates during this session's deep-scans. Final IDs ratified at architecture phase per error-code catalog policy.

- **FR-84 sp_alert_parser regex finalization from raw .eml samples (new 2026-06-19; architecture/implementation phase)** — FR-84 envelope + body regex tolerantly specified; concrete contract requires raw .eml samples (ADD + EDIT + DELETE × Source A + Source B). Derive precise patterns at implementation; prefer `text/html` part of `multipart/alternative` for edit-diff marker detection.

- **FR-84 HILDA SP service account identity registration (new 2026-06-19; ops deployment)** — `hilda_config.yaml` field `sp_service_account.actor_corp_id` must be populated before production cutover; unset → self-loop suppression disabled. STATUS-W011 (candidate) logged at startup if unset.

- **FR-84 Source A + Source B Id field surfacing (new 2026-06-19; SP UI engineer effort)** — Tier 1 routing key activation requires SP UI engineer to surface the SP auto-Counter `Id` field in alert body. Current placeholder `IDShadow` is ignored by parser. Tier 2 4-field natural key is the active fallback.

- **FR-84 Source A + Source B DELETE event format (new 2026-06-19)** — Format spec pending architect-provided sample. Source A DELETE is Ph-2 per FR-3; Source B DELETE scope TBD.

- **FR-9 email template registry (new 2026-06-19; architecture phase)** — Email body templates spec'd to live at `customizations/email_templates/<customer_id>/<event_type>.j2` (Jinja2 per FR-40 customization pattern). Standardized rendering context: PM identity 3-tuple per `[D-088]`, owner identity per FR-88 preference rule, BATCH-id, PLM URL, NSD path placeholders. Concrete template format + rendering pipeline deferred to architecture.

- **FR-11 Re-arm Postgres table+schema (new 2026-06-19; architecture phase per `[D-071]`)** — Per-item last-DeadlineProximity-rung state stored in HILDA Postgres; concrete table + schema deferred. Also: FR-11 SP-side escalation tracking columns (Ph-2 if needed; current model is HILDA-Postgres-only).

- **FR-8 PLM issue payload template (new 2026-06-19; architecture phase)** — Subject / body / labels / owner-identity-field templated from `customizations/issue_tracker/<corp_plm_slug>_adapter_config.yaml`; concrete payload schema deferred.

- **`[D-035]` PLM grouping key audit vs FR-88 lock (new 2026-06-19)** — Verify `[D-035]` reads `(device, milestone, owner_corp_id)` as PLM grouping key (not stale shape); add impl note if `[D-035]` predates the FR-88 owner_corp_id lock.

- **milestone_gating rename in template_schema/MODULE.md + downstream code (new 2026-06-19; dev phase / regen-map drift)** — `is_milestone_gating` → `milestone_gating` rename applied to requirements.md + SP_UI_button_actions.md + DECISIONS.md D-078 impl note 2026-06-19. Downstream code (`core/src/template_schema/models.py` Pydantic field; tests; SQL; dashboard) still uses old name; will surface at next regen-map / dev-phase sweep.

- **FR-12 (c.1)/(c.2) threshold defaults (new 2026-06-19; architecture phase)** — Default thresholds 0.85 (c.1 rule-based) / 0.75 (c.2 LLM) tentative; per-customer overrides at `customizations/rules/<customer_id>/inbound_thresholds.yaml`. Architecture-phase ratification.

- **FR-12 inbound parsing error codes catalog (new 2026-06-19; architecture phase)** — BATCH-id-not-resolvable / sender-mismatch-hard-reject / threshold-undershoot codes pending.

- **NFR-21 deep-scan needed (new 2026-06-19; flagged during FR-13 + FR-14 sweeps)** — NFR-21 area at line 665 of requirements.md still has D-083-era stale references: 5-field routing key with `MinorMilestone` / `ProjectID` naming; "milestone-level column writes to every row in the milestone — N rows × 1 column write" cascade; `[D-082]` cascade-dedup pattern. customer_jira_url + Model SP-name issues swept this session but other items remain. Deep-scan deferred to next session.

- **FR-55 deep-scan needed (new 2026-06-19)** — Twin of FR-26 NSD polling; same milestone_name sweep + target_date `[D-085]` anchor + FR-86 underscore-prefix consistency. Quick deep-scan; deferred to next session.

- ~~**customer_delivery_modality enum lock**~~ *— CLOSED 2026-06-19: locked to `Google Drive` (1 value Ph-1; Ph-3+ may expand) per FR-40 lock.*

- ~~**review_status enum lock**~~ *— CLOSED 2026-06-19: locked to 4 values `pending`, `complete`, `not_required`, `failed` per FR-53 / FR-62 / FR-87 lifecycle.*

- ~~**tg_name enum lock**~~ *— CLOSED 2026-06-19: locked to 16 globally fixed values across all carriers per FR-40.*

- ~~**TPM Data Type STR → Person/Group on xlsx**~~ *— CLOSED 2026-06-19: applied via `[D-088]` ratification.*

- ~~**expected_completion_date column removal**~~ *— CLOSED 2026-06-19: architect removed from xlsx.*

- ~~**item_description long-field handling**~~ *— CLOSED 2026-06-19: comma-separated tags per user clarification; line-by-line regex sufficient — not architectural concern.*

- ~~**`[D-052]` ADR body needs impl note appended (new 2026-06-07)**~~ *— CLOSED 2026-06-10 via commit `fd78e76`. Impl note 2026-06-07 appended (dual→tri-backend; BackendConfig.name Literal expanded; hardware topology corrected; cross-refs `[D-068]` + `credential_service` SystemType split). Plus chronology marker added to 2026-05-28b impl note pointing to the 2026-06-08 `CLASSIFY_DOC_TYPE` restoration so 2026-05-28b doesn't read stale in isolation.*

- ~~**5 `???` cells in `docs/sp_ui_engineer/HILDA_SP_Schema.xlsx` awaiting HILDA-architect confirmation (new 2026-06-10)**~~ *— CLOSED 2026-06-12: (a) CommunicationLog `milestone_id` / `tg_name` / `credential_id` Ph-1 (already on `CommunicationLogRow` per storage/MODULE.md); `source_system` DROPPED (channel column already covers the use case); (b) `milestone_gating` → `is_milestone_gating` (template_schema/MODULE.md Invariant 2026-06-12); (c) `closed_all_items_triggered_at` confirmed SP-side-only (no HILDA Postgres mirror; SP is system-of-record); (d) `folder_routing_template_id` DROPPED (templates not reused across TGs per user 2026-06-12). Excel + customizations/sharepoint_config/MODULE.md edits applied; user to update HILDA_SP_Schema.xlsx in next workbook revision.*

- **`customizations/issue_tracker/MODULE.md` missing Structure markers (new 2026-06-10)** — Flagged by regen-map 2026-06-10. File lacks `<!-- BEGIN:STRUCTURE -->` / `<!-- END:STRUCTURE -->` markers, so regen-map skipped Structure regeneration per skill spec. `defecttrack_adapter.py` has substantive public surface (`DefectTrackConfig`, `DefectTrackAdapter`, `make_adapter`) currently unreflected in MODULE.md. **Action**: add markers in a hand-edit so future regen-map passes include this module.

- **`customizations/sharepoint_config/` NEW dir without MODULE.md (new 2026-06-10)** — Flagged by regen-map 2026-06-10. Top-level customizations dir per structure-conventions but holds only `__init__.py` + `customers/example.yaml` (data drop-zone, no code). Two options: (a) add a stub MODULE.md documenting the per-customer SP-config drop-zone convention (data files only; no Python code); (b) carve out config-only dirs in `structure-conventions.md` so they're exempt from "every top-level customizations dir is a module." (a) is the cleaner long-term path since this is a contribution surface for ops + SP UI engineer.

- **template_schema TSC-W003 `item_description` tag-list field_validator not yet implemented (new 2026-06-10 — template-schema-v2-rewrite strand landed)** — Per template_schema/MODULE.md Invariants line 420: `item_description` should have a Pydantic `@field_validator` that splits on comma, strips whitespace, and checks each tag against a customer's tag catalog; unknown tags raise `TSC-W003` warning. The TSC-W003 error code IS registered in `core/src/template_schema/error_codes.py`, but the validator itself is NOT yet on `DeliveryItemBase`. **Why deferred**: (1) MODULE.md references "TagCatalogEntry registry" but no `TagCatalogRegistry: set[str]` exists in `core/src/template_schema/registry.py` — the registry mechanism may be forward-looking; (2) no current Python consumer of `item_description` tag-list validation — the consumer is `rule_engine`'s `PropagateTagsToActiveTrackers` action. **Trigger**: when `rule_engine` Phase 2 dev needs to consume tag lists, OR when `TagCatalogRegistry` architecture is finalized in MODULE.md. **Effort**: ~30 min once architecture clear.

- **`core/tests/test_mock_server.py` broken due to Starlette/httpx2 dep mismatch (new 2026-06-10)** — `pytest` against `test_mock_server.py` fails with `ModuleNotFoundError: No module named 'httpx2'` because Starlette's `testclient` deprecated `httpx` and requires `httpx2`. Not caused by template_schema work. **Fix**: `pip install httpx2` + update `requirements.txt` to include `httpx2` (or pin Starlette/FastAPI to older versions that still accept plain httpx). **Effort**: ~10 min. Surfaces in any pytest run across `core/tests/` without `--ignore=core/tests/test_mock_server.py`.

- **Batch 2 architecture-phase revisit after Batch 1 dev complete (new 2026-06-10 session 4)** — 10 modules in Batch 2 revisit list (see Next section). Trigger to resume: Batch 1 development phase completion. No urgency until then. Sub-flags:
  - **test_report module revisit trigger**: customer onboarding with TEST_TECH_WAIVER_REPORT items (when `review_required=True` becomes relevant). Currently `review_required=False` for all items per user deployment 2026-06-10 — workflow_engine `TRIGGER_PARSER` + `TRIGGER_AI_REVIEW` task bodies stub as no-op or NotImplementedError until test_report module lands.
  - **api_spec_ingestor module revisit trigger**: messenger OR corp PLM adapter generation needed. Until then, customer JIRA adapter (FR-25) hand-written remains the only IssueTracker adapter.
  - **`customizations/issue_tracker/MODULE.md` Structure markers** (5-min fix per regen-map 2026-06-10) — defer to Batch 2 revisit.

- **template_schema needs `pm_approval_at` + `pm_approval_pm_id` fields on DeliveryItemBase per `[D-068]` (new 2026-06-10 session 3)** — Option B PM approval storage decision requires 2 new fields on DeliveryItem. **Action**: add to `core/src/template_schema/MODULE.md` Public surface + enums + Pydantic model on next template_schema review pass.

- ~~**HILDA_SP_Schema.xlsx + customizations/sharepoint_config need 2 new DeliveryItems columns per `[D-068]` (new 2026-06-10 session 3)**~~ *— FULLY CLOSED 2026-06-12: customizations/sharepoint_config/MODULE.md example schema gained `pm_approval_at` (DateTime) + `pm_approval_pm_id` (User type) mappings under `delivery_items:`. HILDA_SP_Schema.xlsx DeliveryItems tab includes both fields. **Write owner clarified via `[D-068]` impl note 2026-06-12** — SP UI engineer's web part code writes both fields atomically when TPM clicks Approve (with `delivery_state = ReadyForSubmission`); HILDA reads from SP, doesn't write. SP-alert subscription on these field changes propagates to HILDA per `[D-047]`.*

- **workflow_engine MODULE.md follow-up tasks (new 2026-06-10 session 3)** — Add `milestone.close_all_items` Celery task definition (FR-64 batch close path) + update `tasks/state.py` documentation: PMApproval action's UPDATE_STATE task body must set `pm_approval_at = now()` + `pm_approval_pm_id` BEFORE invoking tracker.update_delivery_state. Per `[D-068]` clearing discipline.

- **email_service sp_alert_parser routing-discipline table (new 2026-06-10 session 3)** — `email_service/MODULE.md` `sp_alert_parser` documentation should include explicit routing table: sub-trigger fields (owner_email/expected_completion_date/item_description) → rule_engine path via ItemModified.<sub_trigger>; all other TPM-edited fields → FR-14 manual override path via workflow_engine.apply_manual_tpm_override. Per tracker MODULE.md Invariant 2026-06-10.

- **template_schema Confirmation+no_customer_upload validator (new 2026-06-10 session 3)** — Confirmation items (`ItemType.Confirmation`) MUST have `no_customer_upload=True` per tracker invariant. Add TSC-W003 warning + Pydantic validator at template load time in `core/src/template_schema/`.

- ~~**Excel workbook + customizations/sharepoint_config gap follow-up (new 2026-06-10 session 3)**~~ *— CLOSED 2026-06-12: `owner_corp_id` added on Users (canonical per-person); `user_corp_messenger_handle` DROPPED (corp messenger uses `owner_corp_id` directly per user 2026-06-12); `tg_owner_name` / `tg_owner_email` / `default_cc_list` denormalized onto DeliveryItems per D-DRAFT-Y → D-073 (was originally Ph-1 TGGroups before TGGroups list was dropped); `corp_id_list` deferred to Ph-2; SP system column reuse discipline (Created / Modified / Author / Editor not duplicated) added as Invariant in customizations/sharepoint_config/MODULE.md.*

- **SP UI in-focus refresh strategy (new 2026-06-10; FR-87 / `[D-064]` / sharepoint/REQUIREMENTS.md §8.1)** — SP UI engineer confirmed 2026-06-10 that no unconditional polling is needed while browser view is in focus; **focus-aware refresh** is the model. On tab/window focus-gain → re-fetch list deltas. In-focus refresh strategy (continuous interval / interaction-triggered / hybrid) is the SP UI engineer's choice. Track here in case the in-focus strategy choice has implications for FR-87 TPM-resolution UX (round-trip: SP-UI-button → SP-field-write → SP-alert email → HILDA processing → SP REST writeback → visible to TPM only on next in-focus refresh; total latency may matter for TPM UX). Revisit during SP UI engineer's prototype review.

- **`delivery_state` 11-value framing in MODULE.md test scenarios (optional, low-priority; new 2026-06-10)** — `customer_adapter/MODULE.md`, `email_service/MODULE.md`, `sharepoint_integration/MODULE.md` test-scenario sections describe an "8-state machine (Open → ... → Closed)" — this is the **happy-path traversal**, correct as far as it goes, but the canonical `delivery_state` enum is 11-value (`Not Started` + 8 active + `Delayed` + `Blocked`). `Not Started` = template-loaded by TPM in SP UI but tracking not active; `Open` = tracking started by HILDA. **Action**: consider a small follow-up pass to clarify "happy-path traversal of 8 active states within the 11-state `delivery_state` enum" in those test-scenario sections. Not strictly drift — the existing prose is technically accurate.

- **Generic-vs-per-customer test report parser strategy (new 2026-05-28; affects `[D-011]`, FR-16, FR-46)** — Current `[D-011]` scope assumes a per-customer rule-based parser generated by the Test Report Profiler from a historical corpus. User observation (2026-05-28): real-world test reports are heterogeneous across (a) file types (`xls`, `xlsx`, `pdf`, `doc`, `docx`), (b) content formats (tables, embedded images, multi-tab xlsx with or without summary tabs), and (c) structure adherence (~90% follow some template; ~10% are arbitrary feature reports, e.g. battery tests, with no standard `testcase_id` / `testcase_description` / `test_result` / `comment` columns). Building per-file-type rule-based parsers handles (a) and the structured 90% — but **fails on the unstructured 10%** and adds N parsers to maintain per file type per customer. Design principle to preserve: **HILDA must not require custom per-document logic — extraction should work generically across documents regardless of internal structure.** Likely path: LLM-augmented FR-16/FR-46 — rule-based parser for the structured majority, LLM fallback (a new `EXTRACT_TEST_CASES` TaskKind in `core/src/llm/`) for arbitrary-format reports. Revisit `[D-011]` during architecture phase before `test_report_profiler/MODULE.md` is drafted; may produce a new ADR superseding or extending `[D-011]`. Note in `llm/MODULE.md` Non-goals 2026-05-28 cross-references this Flag.
- **Staged-document fallback ladder — requirements-phase follow-up (raised 2026-05-28)**: Current FR-52 behaviour on Tier-2 LLM ambiguity is straight-to-`staged/` + PM dashboard flag. User proposed a 3-step fallback ladder before PM triage: **(a)** attempt routing to a target folder path (HILDA-PC local or customer-mapped) via a customizations-folder lookup — it's acceptable for a document to land in a folder without item-level routing being resolved; **(b)** if folder resolution also fails, reach out to the owner to determine the work item; **(c)** if owner does not respond after repeated attempts or nearing deadline, escalate visibility to TPM. This changes FR-52's failure-mode semantics + adds a new customizations surface (`customizations/folder_routing/`?) + introduces an owner-outreach branch that today lives only in FR-9/FR-10. **Action**: switch to requirements phase to walk through this carefully; expected outputs: FR-52 amendment + new `[D-XXX]` ADR + new MODULE.md surface on `email_service` `attachment_router.py` for the fallback ladder. Not blocking the 2026-05-28 80+-test-report use case (test scenario currently goes straight to `staged_pm` per current FR-52).

- **`D-052` ADR-number collision resolved 2026-05-28** — STATUS.md had reserved `D-052` and `D-053` as placeholders for deferred carrier-package-assembly (FR-73) and SP UI boundary ADRs from the 2026-05-26 forward-drift scan. Dual-backend LLM landed as `D-052` on 2026-05-28; placeholder candidates renumbered to `D-053` (carrier-package) and `D-054` (SP UI boundary) in this STATUS.md Next entry. No outstanding ADR-numbering conflicts.

- **Customer adapter browser-automation implementation (new 2026-06-05; FR-19 + `[D-054]` impl note)** — Google Drive REST API is unavailable per corp/carrier policy; FR-19 implementation uses `selenium`/`playwright` browser automation against headless Chromium. **Architecture-phase items**: (a) **selector-versioning strategy** — Google Drive UI changes will break selectors; need a deployment-time refresh mechanism (where do versioned selectors live? `customizations/customer_adapter/<carrier_slug>_adapter_config.yaml` per FR-19, but the refresh-on-UI-change procedure needs ops runbook); (b) **headless Chromium as hard ops dependency** — installed in `hilda-worker` container image OR pre-installed on HILDA PC; deploy procedure must include Chromium provisioning + version pinning; (c) **PM session management** — long-lived browser sessions per PM × carrier; session-cookie storage in `credential_service` (sops-encrypted); session-refresh / login-flow automation per carrier; (d) **Capability flags** — `getStatus` and `postComment` may be unavailable per carrier; the `CustomerAdapter` Protocol surface must accept `NotImplementedError` from these methods gracefully (e.g., dashboard surfaces "status not available for this carrier" rather than treating it as an error). To resolve when `customer_adapter/MODULE.md` is drafted (Phase B Module rollback queue).

- ~~**[DRIFT-3 skipped 2026-05-14]**~~ *(resolved 2026-05-14: D-035 impl note appended — inbound/ + revisions/ + timestamp/version-prefix convention superseded by doc_type_slug/doc_id_slug/revN/ sequential numbering per FR-13.)*

- **[BACKLOGGED 2026-05-13]** **Outbound notification format for CorporatePLM and NetworkSharedDrive modalities — TBD.** FR-9 notes "outbound format TBD architecture phase": when an owner's primary channel is PLM or shared drive, HILDA must inform them of the delivery path (PLM issue reference or shared drive path) at tracker creation and on reminders. Decide during architecture when PLM adapter and storage MODULE.md are drafted.

- **[BACKLOGGED 2026-04-30; refined 2026-05-04]** **Eval-data channel — multiple dev-time eval/correction surfaces** — PM corrections flow across multiple surfaces: test report review, tech report review, email parsing, message classification, customer-response drafts. How do these corrections feed back to improve checklists / prompts? No explicit pipeline in the design doc. Each surface will need its own correction schema and eval data path. Revisit when AI/LLM modules are designed in architecture phase.

- ~~**[BACKLOGGED 2026-04-30]** **Template Schema Ingestor input format**~~ — *resolved 2026-05-04 by `[D-018]`: three modes (schema-file YAML / row-offset / infer); `CustomerSchema` is the common output contract.*

- **[NEW 2026-05-21]** **D-042 (Open design question) — customer template storage location and TPM authoring path** is open. Captures the unresolved question of where customer templates live (currently `customizations/template_schemas/` per FR-39/40/41) and how TPMs author them end-to-end (Excel import path is deferred per DEF-15; SP UI authoring path TBD). Resolve before `template_schema_ingestor` MODULE.md and before any customer-onboarding runbook.

- **[NEW 2026-05-24; updated 2026-05-26] Architecture-phase follow-up ADRs surfaced during HILDA_Design.md / PROJECT.md / SYSTEM.md / structure-conventions.md review pass.** Status of the 6 items:
  - ~~(1) `[D-024]` reconciliation pinning Helm to Ph-3+~~ — **RESOLVED 2026-05-24** via impl note appended to D-024.
  - ~~(2) New ADR for SP-alert email channel + `sp_alert_parser` sub-module~~ — **RESOLVED 2026-05-24** as `[D-047]` captured.
  - ~~(3) `[D-021]` / `[D-026]` revision documenting actual 3-host HILDA-owned deployment surface~~ — **RESOLVED 2026-05-24** via impl notes appended to D-021 + D-026.
  - **(4) IMAP IDLE / EWS streaming confirmation from corp Exchange admin** — still pending; tracked as a separate Next item.
  - ~~(5) `MilestoneGating` + `ItemNumber`-stability schema decisions~~ — **RESOLVED 2026-06-12**: `MilestoneGating` → `is_milestone_gating: bool` field on `DeliveryItemBase` (template_schema/MODULE.md, renamed from `milestone_gating` 2026-06-12); `item_no` immutability declared as Invariant on `DeliveryItemBase` (template_schema/MODULE.md 2026-06-12, referential-integrity load-bearing for FR-77 + storage.TGFolderRoutingRow). Both per SP UI engineer 2026-06-10 review absorption.
  - **(6) `corp_messenger_gateway` + `corp_plm_gateway` MODULE.md drafts** — still pending; will land at Layer 5 per the per-layer MODULE.md plan in Next.

- **[NEW 2026-05-24] SP-side configuration requirement** — SharePoint list alerts must be configured to "Send Alerts for These Changes: **Anything changes**" (not specific columns), otherwise TPM direct field edits do not fire alert emails to HILDA and FR-14 manual overrides will silently miss. To be captured in the deployment runbook.

- **[NEW 2026-05-24] STATUS.md upkeep policy** — confirmed: user will invoke `close-session` daily going forward to keep `Done` current. Until that cadence is established, manual `Done` appends will continue at the end of any working session where meaningful work landed.

- **[NEW 2026-05-26] Derivative-doc drift risk** — `drift-check` skill currently verifies `requirements.md` ↔ `DECISIONS.md` ↔ `MODULE.md` but does **not** check derivative docs like `sharepoint/REQUIREMENTS.md`, future ops runbooks, or per-module READMEs. Evidence: 2026-05-26 TG-group fields gap (`tg_owner_*`, `email_group_alias`, `corp_id_list` added to FR-2 + HILDA_Design.md §3.4 by 2026-05-24 but silently missed `sharepoint/REQUIREMENTS.md` in the 2026-05-26 initial commit `f8e9345`; surfaced by user review, fixed in commit `1c102a9`). **Mitigation**: propagation checklist added to Next section (above); apply as part of close-session daily. **Architecture-phase consideration**: expand `drift-check` to cover derivative docs as a Ph-1 task; treat each derivative doc as a "view" over `requirements.md` + `DECISIONS.md` with explicit cross-reference index (`docs/compact/derivative-docs.md` — new file, lists which derivative docs reference which FR/NFR/D-XXX). To be drafted during architecture phase.

- **[NEW 2026-05-26] D-048..D-051 are first-touch designs (forward-drift scan output)** — the 4 new ADRs captured 2026-05-26 (commit `4ae4f71`) were drafted cold from `requirements.md` FRs without an in-flight module design pass: **D-048** (FR-66 version-selection workflow), **D-049** (FR-71 ODF + TG-group field model), **D-050** (FR-72 ZIP ingestion + 5-area NSD structure), **D-051** (TGGroups SP list normalization). These are expected to be **revisited and possibly refined** when their corresponding MODULE.md drafts land (`messenger/MODULE.md` for D-048; `workflow_engine/MODULE.md` + ODF rule for D-049; `email_service/MODULE.md` + `storage/MODULE.md` extension for D-050; `tracker/MODULE.md` + SP list provisioning for D-051). Treat current versions as initial-design baselines subject to MODULE.md-driven refinement. The risk is the same as all forward-drift ADRs created without grounding in concrete module code — rationale may not survive contact with implementation reality. Mitigation: re-evaluate each as part of the corresponding MODULE.md drafting session.

- **[NEW 2026-07-15] SP UI engineer coordination items from serialization cascade** — three items HILDA has done its part on; SP UI engineer needs the corresponding button-visibility gates: (a) Start Tracking button visible only when all items are at Open state (was Not Started); (b) Submit-to-Customer button visible only when all items are at ReadyForSubmission (was: immediately after PM Approve click). Without SP UI gates, races the serialization was designed to prevent can still happen if TPM clicks buttons out-of-order. Communicated per architect ask 2026-07-15.

- **[NEW 2026-07-15] TPM notification retest pending** — Phase C beat task landed but hasn't fired against a real Milestones row with target_date set. First real fire will be at 00:00 US/Eastern on some milestone's target_date-1. To smoke test earlier: (a) set config `window_minutes` to 1440 + `strict_only: false` + `beat_interval_seconds: 60`, (b) set an SP milestone's target_date to today (Eastern), (c) watch worker logs for `tpm_notification_sent`. Restore config after test.

- **[NEW 2026-07-15] Browser doc view+edit path decision open** — architect exploring (a) SharePoint upload + Office Online (requires OWAS/OOS on corp SP 2017 — IT to confirm), (b) OnlyOffice self-hosted container + WOPI Host on HILDA (~1-2 days work), (c) PDF conversion via LibreOffice headless (view-only, ~1 hour, ships fast). No decision yet; captured here so it's not lost. Related side-quest: Windows Trust Zone GPO exceptions for network-share `ms-excel:` handler — corp IT scope.

- **[NEW 2026-07-15] Deliverables_Template bulk create — script ready; awaiting Excel from architect**. `scripts/create_sp_rows_from_excel.py` supports dry-run + safety rail + per-row error resilience + `--start-row` retry. Architect to send Excel with ~85 rows + column headers matching `list_sp_columns.py` output. Estimated 5-15 min live run after dry-run validation.
